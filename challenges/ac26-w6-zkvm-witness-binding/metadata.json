{
  "id": "ac26-w6-zkvm-witness-binding",
  "name": "証明は valid だった。 ただし、 別の口座についての証明だった",
  "category": "Challenge",
  "status": "draft",
  "visibility": "public",
  "track": {
    "id": "advanced-cryptography-2026",
    "order": 650,
    "chapter": "Week 6 / Programmable Cryptography Stack Design"
  },
  "courseAlignment": {
    "courseId": "advanced-cryptography-program",
    "edition": "2026",
    "week": 6,
    "role": "assignment-companion",
    "spoilerPolicy": "independent-reimplementation",
    "sources": [
      {
        "repository": "zk-tokyo/advanced-cryptography-2026",
        "ref": "a3aa4b56fa88fbe803b57d320fbc87c1a203b480",
        "path": "week6/README.md",
        "kind": "lecture"
      },
      {
        "repository": "zk-tokyo/advanced-cryptography-2026",
        "ref": "a3aa4b56fa88fbe803b57d320fbc87c1a203b480",
        "path": "week6/problems/zkvm-exploit/README.md",
        "kind": "assignment"
      }
    ]
  },
  "difficulty": 5,
  "estimatedDuration": "60-90 分",
  "shortDescription": "zkVM が証明するのは 「そのプログラムが走った」 ことだけである。 **どの**プログラムか、 **どの**入力についてか、 **何を**主張しているのかは、 guest が bytes で言い切るしかない。 その contract — public statement・private witness・public journal — を書く。",
  "description": "Week 6 の 5 問目、 そして `ac26-w6-zkvm-exploit-predicate` の続きである。 前問は **exact exploit predicate** を決めた。 この問題はその predicate を、 zkVM guest として成立する **public / private input contract** に仕上げる。\n\n暗号の側は最初から scope 外である。 proof は 1 つも生成しない。 やるのは、 proof system が**何も証明してくれない 2 つの半分**を自分で塞ぐことだけである。\n\n```text\npublic statement   その proof が何についての主張なのか\npublic journal     その run が公開したもの。 永久に、 全員に\n```\n\n暗号が保証するのは 「ある program の ある run に対応する journal である」 ことだけで、 **どの** program か、 **どの** 入力か、 **どの** claim かは何ひとつ言っていない。 それを言うのは guest の仕事で、 言い方を間違えれば完全に valid な proof が別物の証拠になる。\n\n## 前問から移動した 2 つ\n\n口座 (`price` / `spent` / `budget`) は target spec に焼き込まれた定数ではなく **public input** になった。 同じ compiled guest が世界中の口座について主張を出せるので、 「どの口座の話か」 を決めるのは bind された statement だけである。\n\n整数意味論も public input になった。 `semantics` profile は幅**と**桁溢れ時の挙動を名指し、 同じ image が profile 違いで別の machine になる。\n\n```text\nwrapping     2 ** width で法をとる — exploit が存在する唯一の machine\nsaturating   最大値で頭打ちになる\nchecked      trap して run が止まる\n```\n\nexploit が存在するのは 3 つのうち 1 つだけである。 どれで走ったかを言わない journal は、 読み手が勝手に想定した machine についての proof である。\n\n## この問題の中心にある 1 組\n\nfixtures は毎 seed、 **本物の口座 2 つ**を引く。 どちらにも本物の exploit がある。 長さ prefix の無い encoder に通すと、 2 つは同じ bytes になる — `\"53\" + \"7\"` と `\"5\" + \"37\"` は同じ 3 文字だからである。\n\nこれは malformed な statement がすり抜ける話ではない。 **片方についての valid な proof が、 もう片方についての valid な proof になる**という話で、 その間ずっと暗号は 1 ミリも壊れていない。 canonical serialization を 「書式の作法」 だと思っている guest が落ちるのはここである。\n\n## 採点の設計\n\nこの問題は 55 個の壊れた guest を同梱していて、 そのうち **42 個は 「happy path が verify し、 別 program に差し出した receipt を拒否する」 という誰でも書く 2 問に正しく答える**。 `make reference-test` が毎回この数を測り直す。 その 2 つは問題文にそのまま書いてあるので誰も発見する必要がない。 残りが checkpoint になっているのはそのためである。",
  "instructions": "## 前提 — 中学の数学から\n\n- **学校で習ったこと**: 「区切りが無いと読み方が決まらない」こと。\"ab\" と \"c\" をつないだものと、\"a\" と \"bc\" をつないだものは、どちらも \"abc\" で**見分けがつきません**。先に長さを書く約束にすると \"2ab1c\" と \"1a2bc\" になり、別物になります。この「1 つの中身には 1 通りの書き方しかない」書き方を**正準符号化**と呼びます。Week 3 の Schnorr でも同じ理由で出ました。\n- **バイト列**: コンピュータは文字も数も 0〜255 の数の並びに直して扱います。その並びがバイト列で、Python では `b\"...\"` と書きます。文字を数の並びに直す約束のひとつが UTF-8 です。\n- **1 桁の例(この問題の中心)**: プリペイド式の口座を「1 個の値段 price」「すでに使った額 spent」「使ってよい上限 budget」の 3 つの数で表します。口座 A =(53, 7, 272)、口座 B =(5, 37, 272)。3 つを何も挟まず書き並べると A は `537272`、B も `537272` — **同じ並び**です。桁のずれがちょうど打ち消し合っています。長さを先に書けば `2`+`53`, `1`+`7`, `3`+`272` と `1`+`5`, `2`+`37`, `3`+`272` で、もう混ざりません。この 53 や 5 は説明用の数で、あなたの 2 つの口座は「証拠を確認」が出します。\n- **言葉**: zkVM = プログラムを走らせて、「たしかにこのプログラムを走らせた」と後から他人に示せる仕組み(仮想機械 = 本物の CPU の代わりにソフトで作った計算機)。guest = その中で走らせる側のプログラム。statement(主張)= 「どのプログラムの、どの入力についての話か」を書いた**公開**データ。この問題では domain / guestVersion / imageDigest / semantics / claim / params の 6 項目。witness = 主張が本当だと言える具体的な値。**他人には見せません**。journal = 走り終わった guest が公開する記録。誰でも読めて、あとから消せません。image = 実際に走るプログラムの中身(バイト列)と、ビルド道具がその横に書き残したラベル。digest = バイト列を決まった手順で短い文字列に潰したもの。同じバイト列なら必ず同じ digest になります(潰す関数 `commit()` は用意されています)。receipt = journal を 1 つ包んだもので、「これがその主張の証拠です」と言って差し出されます。ビット = 0 か 1 の 1 桁のこと。n ビットの数は 0 から「2 を n 回掛けた数 − 1」までを持てます (例: 4 ビットなら 0〜15 で、はみ出したら 16 で割ったあまりを取ります)。semantics = 数を何ビットまで持てるか + 収まらないときどうするか(`wrapping` = あまりを取る / `saturating` = 最大値で止まる / `checked` = そこで停止)の組。checkpoint = 採点される小問。この問題には 8 つあります。\n- **この問題の位置づけ**: Week 6 の 5 問目、前問 `ac26-w6-zkvm-exploit-predicate`(何をもって攻撃成立と呼ぶかを決めた)の続きです。ここでは証明を 1 つも作りません。暗号がやってくれる部分は範囲外で、**暗号が何も言ってくれない残りの半分**だけを書きます。\n\n## はじめに\n\nタイトルの「口座」は、上の 1 桁の例に出たプリペイド口座のことです。あなたはこの口座について「上限を超えた買い方が存在する」と主張する側の担当で、その主張を zkVM に載せる直前にいます。\n\nzkVM が保証してくれるのは「そのプログラムが走って、この journal が出た」までです。**どの**プログラムか、**どの**口座についてか、**何を**主張しているのかは、暗号は一言も言っていません。それを言うのは guest の仕事です。言い方が甘いと、口座 A のために作られた、どこも壊れていない正しい証明が、そのまま口座 B の証明として通ります。偽造は 1 つも起きていません。「どっちの口座の話か」をバイト列が言い切っていなかっただけです。こうして、**誰も触っていない口座が上限を超えている「証拠」**が手に入ります。\n\nだから最初にやるのは、主張を 1 通りのバイト列に固定することです。ここが二重に読めると、その先で何を書いても二重に読めます。あとの 7 つはそこから順に積み上がります。\n\n## 最初の一手\n\nParticipant Portal(この問題文が表示されているサイト)で **「起動」** を押すと、あなた専用の実行環境(コンテナ)が立ち上がり、問題文と同じ画面に **問題エディタ** が出ます。\n\nエディタに入っているファイルは **`guest.py` 1 つだけ**で、書く言語は **Python** です。7 つの関数の枠(空の本体と説明文)が最初から入っていて、新しくファイルを作る必要はありません。関数ごとの引数・返り値・例外を出す条件は、その関数の先頭の説明文(docstring)に詳しく書いてあります。**この問題で Rust は書きません**(講義側の課題は Rust ですが、ここで決めるのは言語によらない入出力の約束の形です)。\n\n**「証拠を確認」** を押すと、この起動のためだけに作られた値が表示されます。ボタンの中で `show.py` が走るので、自分で何かを打ち込む必要はありません。出るのは次の 9 つです。\n\n1. **あなたの statement** — 6 項目を、正準符号化が出すべき順に並べたもの。\n2. **あなたの machine** — 何ビットか、あまりを取る境目(modulus)、持てる最大値(max)、収まらないときの挙動(overflow)。あわせて checkpoint が選びうるビット幅の一覧(**8・16・32・64 はありません**)と overflow の 3 通り。\n3. **口座と、それを使うプログラム** — price / spent / budget の値、4 行のプログラム、破ってはいけない条件式(はみ出しの無い普通の整数で書いたもの)。\n4. **1 つの符号化器が区別できない 2 つの statement** — あなたの口座 A と B、それぞれの 3 つの数を何も挟まず並べた結果、「別の statement か?」と「同じバイト列か?」の答え合わせ、区別しなければならない statement の総数。\n5. **image と、その隣の 4 つ** — imageId、sourcePath、buildId、プログラムの手数と中身、そして 1 か所だけ違う 4 つ(`rebuilt` = 作り直した / `restamped` = 日付だけ押し直した / `renamed` = ファイル名だけ変えた / `relabelled` = ラベルだけ書き換えた)。**どれが同じプログラムかは checkpoint なので出ません。**\n6. **入力の 2 つの扉** — `env` の呼び出しのうち、どれが記録(transcript)に残り、どれが残らないか。\n7. **journal に書く項目** — 5 つの項目名と、公開してよい測定値の名前。\n8. **差し出される receipt の id 一覧** — 15 個。どれを受け取ってよいかは checkpoint なので出ません。\n9. **監査する run の id 一覧** — 10 個(`d0`〜`d9`)。値が外に出る 6 つの経路(channel)の名前と、公開してよい名前の一覧も出ます。\n\n**まず 4 番を見てください。**「別の statement か?」が `True`、「同じバイト列か?」も `True` になっています。この 1 行を自分の目で見ることが、1 番の checkpoint が書式の作法ではなく仕様である理由です。数はどれも起動するたびに変わるので、他の人の値を写しても合いません。\n\n**「公開テストを実行」** を押すと、形だけの検査(バイト列が返るか、辞書のキーを入れた順番を変えても同じバイト列になるか、run と journal が決められた項目を持つか、自分で封をした receipt が自分の statement に対して通るか)を手元で試せます。中身の正しさはここでは分かりません。 エディタを最初の状態に戻したくなったら **「初期状態に戻す」** を押します。\n\n## 答えるもの — checkpoint は 8 つ、この順で\n\n8 つとも**コードの checkpoint**で、答えを直接書き込む欄はありません。手順は「エディタに書く → その checkpoint の **「提出 (+点数)」** を押す」だけで、押した時点の `guest.py` 全体がそのまま採点へ送られます — **同じ 1 ファイルを 8 通りの角度から採点します**。不正解でも出し直せます(1 回まちがえるごとに 15 点引かれますが、合計が 0 点未満になることはありません)。checkpoint は独立なので、1 つ詰まっても他は取れます。\n\n1. **encoding** — `encode_statement(statement) -> bytes`。statement 1 つを、1 通りにしか読めないバイト列にします。長さの付け方・整数の幅・並び順に使う定数は `participant/lab.py` にあり、採点側と同じものを読みます。`participant/lab.py` そのものはエディタに出ませんが、`guest.py` 冒頭の説明文にある `from participant.lab import (…)` の行を自分のコードに書き写せば、定数も道具もそのまま使えます (各定数の意味は `encode_statement` の docstring にあります)。statement でないものが来たら `ValueError` を出します (`raise ValueError(\"理由\")` と 1 行書けば出せます)。\n2. **identity** — `image_digest(image) -> str`。「どのプログラムの話か」を表す名前を 1 つ返します。image の 4 つの項目のうち実際に走るのは 1 つで、残りはビルド道具の覚え書きです。何を材料に潰すかを決めるのがこの checkpoint で、5 番の 4 つの兄弟は、決め方をまちがえたときに何が取り違えられるかを見る材料です。\n3. **ingestion** — `guest_input(env, statement, witness) -> None`(返り値なし)。`env` に、guest が仕事をするのに必要なものを全部載せ、なおかつ記録を他人に読まれても witness が分からない状態にします。扉の使い分けは 6 番に出ています。引っ張る向きが逆の 2 つを同時に満たします。\n4. **reexec** — `run_guest(image, env) -> dict`。statement が名指したプログラムを、statement が名指した machine の上で自分で走らせ、決められた 7 項目の辞書を返します。host = guest の外側で zkVM を動かし、guest に入力を渡す側のこと。この問題では、証明を通したがっている当事者でもあります。host が「この run はこうなったはずだ」と添えてくる値は**入力であって答えではありません** — host は証明される側の当事者です。\n5. **journal** — `seal_journal(statement, run) -> dict`。公開する記録を 5 項目ちょうどで作ります。1 番の符号化がここで使われます。測定値を 1 つ載せますが、載せてよいかどうかの基準は「読み手が公開情報からすでに計算できるか」です。「小さいから」でも「ただの数字だから」でもありません。\n6. **replay** — `accept_receipt(receipt, statement) -> bool`。差し出された receipt がその statement の証拠になっているかを返します。8 番の 15 個が実際に渡ってきます。**この関数は例外を投げません** — 壊れた receipt が来ても、判断を返すのが仕事です。\n7. **privacy** — `leak_report(disclosure, statement, image) -> tuple`。1 回の run が外に出したもののうち公開の約束から外れているものを、`(経路名, 名前)` の組にして重複なく並べ替えて返します。9 番の 10 個すべてに対して呼ばれます。10 個は全部同じ主張を出し、全部その主張は正しい — 違うのは**何をついでに見せてしまったか**だけです。\n8. **transfer** — いまの `guest.py` がそのまま、**見たことのないビット幅・口座・プログラム・protocol (= この問題全体の約束事) の版**で 1〜7 をもう一度全部走ります。画面に出た数値を書き写した実装はここで落ちます。\n\n## ゴール\n\nstatement が 1 通りのバイト列になり、プログラムの名前が実際に走る中身へ結びつき、witness が記録に出ず、host の言い分を使わずに走らせ直せて、journal が余計なことを言わず、別物に差し出された receipt を断り、10 個の run の漏れを過不足なく挙げられて、それが見たことのない値でも成立したら終わりです。\n",
  "writeup": "## 暗号が証明しないもの\n\nzkVM の proof が言うのは 「ある program の ある run があって、 この journal を出した」 までです。 **どの** program か、 **どの** 入力についてか、 **何を** 主張しているのかは、 guest が bytes で言い切るしかありません。 この問題の 8 checkpoint はすべてその 「言い切り」 の話で、 暗号の話は 1 つもありません。\n\n## 中心にあるのは 1 組の口座です\n\nfixtures は毎 seed、 本物の口座を 2 つ引きます。 どちらにも本物の exploit があります。 長さ prefix の無い encoder に通すと 2 つは同じ bytes になります。\n\n```text\nleft   price=53 spent=7  budget=272   ->  \"53\" \"7\"  \"272\"\nright  price=5  spent=37 budget=272   ->  \"5\"  \"37\" \"272\"\n```\n\n連結すると両方 `537272` です。 だから `left` について sealed された receipt が `right` に対して verify します。 **誰も触っていない口座が予算超過している証拠**が、 何も forge せずに手に入るということです。\n\n`naive_encode` は藁人形ではありません。 field 順は固定、 全 field 在り、 何も落ちていません。 それでも encoding ではないのは、 field の境界が出力の中に無いからです。 長さ prefix はそこにあります。\n\nそして分けなければならないのは、 この 1 組だけではありません。 `domain` だけが違う 2 つ、 `guestVersion` だけが違う 2 つも別の statement です。 この 2 つは 「計算に影響しないから」 という理由で真っ先に落とされます。\n\n## digest は 「走る bytes」 についてです\n\nimage record には toolchain が書いた label が 2 つ (`sourcePath` / `imageId`) と、 実際に走る `body` があります。 4 つの兄弟のうち 2 つは base と同じ program で、 2 つは違います。\n\n```text\nrebuilt     同じ source path、 比較演算子が 1 つ違う、 stamp も新しい  -> 別の program\nrestamped   step は同じ、 build stamp だけ違う                        -> 別の image\nrenamed     同じ bytes、 別の path                                    -> 同じ program\nrelabelled  同じ bytes、 別の imageId                                 -> 同じ program\n```\n\n`sourcePath` を digest する guest は `rebuilt` を base だと言います。 その 2 つは 「合計がちょうど budget に着地する注文」 について答えが違い、 それは攻撃者が選ぶ注文そのものです。 `imageId` を digest する guest は `relabelled` を別物だと言い、 何の問題も無い proof が誰にも理由の分からない形で拒否されます。\n\n`restamped` だけが判断です。 proving system が実際にそうしているのと同じに倒します — **観測可能な変化が無いことは、 監査の対象であって入力ではない**ので、 再ビルドは別の image です。\n\n## host の言い分は入力であって答えではありません\n\n`env.hints()` には host 自身の 「この run はこうなったはずだ」 が入っています。 採点で渡される hint は自信満々で、 詳細で、 全部間違っています。 host は証明される側の当事者です。 hint を採る guest は速くて、 ほとんどの場合正しくて、 何も証明していません。\n\n同じ理屈で、 statement が名指していない image を渡されたら**1 step も走らせる前に**拒否します。 走らせてから 「どの program だったか」 を報告した run は、 文脈を外して引用できる run です。\n\n## journal に入れてよい測定値の条件\n\n1 つだけあります。 **読み手がすでに計算できるもの**であることです。 「小さいから」 でも 「ただの数字だから」 でもありません。\n\nwitness とともに変わる cycle 数は測定値ではなく、 解像度を落とした witness です。 そして journal は run より長生きします — debug の都合で足して release で外す、 という扱いができる唯一できない場所です。\n\n## journal の field は、 読むものではなく照合するものです\n\njournal には statement の digest のほかに `imageDigest` と `guestVersion` が入っています。 これは statement を持っていない読み手のための便宜で、 **証拠ではありません**。 書いておくのは無料で、 信じるのは高くつきます。\n\n採点では、 誰も replay していない receipt の journal 側だけを後から書き換えたものが渡されます。 digest は合っていて、 journal は statement と違うことを言っています。 **verifier が読む journal field は、 攻撃者が書く journal field です。**\n\nそして `claimResult` が `False` の receipt は、 何も証明していない run についての正しい journal です。 それを claim の証拠として受理するのが、 いちばん静かな入口です。\n\n## 承認された名前は承認ではありません\n\nprivacy audit の policy は 2 段です。 1 段目は `PUBLIC_NAMES` — 出してよい名前の一覧です。 2 段目は、 **承認された名前が承認された値を運んでいるか**です。\n\n`spent` は公開してよい名前です。 しかし machine 自身の total が `spent` という名前を着て出てくるのは別の開示で、 price は公開かつ可逆なので、 それは 1 回の modular inverse を残しただけの quantity です。 名前しか見ない scan はここで何も見つけません。\n\n10 個の run のうち 2 個は何も漏らしていません。 そのうち 1 個は**うるさい** — 全 channel を数字で埋めます。 その 2 つに leak を報告するのは、 残り 8 つを見逃すのと同じだけ間違いです。 **つねに何かを見つける監査は、 読んでいるのではありません。**\n\n## 実測\n\nこの問題は 55 個の壊れた guest を同梱していて、 そのうち **42 個は 「happy path が verify し、 別 program に差し出した receipt を拒否する」 に正しく答えます**。 `make reference-test` が毎回測り直します。 その 2 つは問題文にそのまま書いてあるカテゴリで、 誰も発見する必要がありません。\n\n## 監査が証明できることと、 できないこと\n\n証明できるのは、 同梱された 55 個の欠陥をこの 8 checkpoint が捕まえること、 そして reference が全部を通ることです。 証明できないのは 「この contract に他の穴が無い」 ことです。\n\n## toy と production の差\n\n幅は 7〜13 bit、 口座は 1 つ、 program は 4 step、 receipt には seal がありません。 seal の検証は**まさに暗号がすでにやってくれる部分**なので scope 外で、 seal が無ければ意味を持たない binding のほうが scope 内です。 本物の zkVM では statement は program の ELF digest と public input の列で、 journal は proving system が commit する出力です。 主張しているのは 「binding は exact な contract として書ける」 ことであって、 「書いたものが完全である」 ことではありません。\n\n## 対象外\n\n実際の zkVM proof 生成と receipt verification、 production の binary reproducibility system、 特定 zkVM の zero-knowledge 性、 remote attestation。",
  "tags": [
    "advanced-cryptography-2026",
    "week6",
    "zkvm",
    "proof-of-exploit",
    "public-input",
    "canonical-serialization",
    "zero-knowledge",
    "local-play",
    "container"
  ],
  "exposedPorts": [
    {
      "port": 18117,
      "name": "Portal editor API + /verify (loopback only)"
    }
  ],
  "learningGoals": [
    "public statement と private witness を明示的な型として分離できる",
    "canonical serialization の必要性を、 実際に衝突する 2 つの statement で示せる",
    "field 順・長さ prefix・整数幅・endianness・domain 分離を固定した serializer を書ける",
    "target identity を source path 文字列ではなく実行される bytes へ bind できる",
    "witness を public argument / 環境変数 / log へ出さずに guest へ渡せる",
    "host が供給した結果を信じず、 guest 内で target execution を再計算できる",
    "statement が名指していない image を、 1 step も走らせる前に拒否できる",
    "wrapping / saturating / checked を statement が名指す profile として扱える",
    "statement digest・target digest・claim result・guest version を 1 つの journal へ commit できる",
    "public measurement の条件を 「読み手がすでに計算できること」 として言える",
    "receipt が別 target / 別 claim / 別 semantics / 別 protocol version へ replay されるのを拒否できる",
    "journal の便宜 field を証拠として読まず、 commitment と照合できる",
    "witness が journal・stdout・stderr・error・trace・一時ファイルへ漏れていないか監査できる",
    "承認された名前が承認されていない値を運んでいる開示を検出できる"
  ],
  "runtime": {
    "provider": "docker",
    "engine": "compose",
    "entry": "local/docker-compose.yml",
    "verifyUrl": "http://127.0.0.1:18117/verify",
    "secretEnv": [
      "FLAG_SEED"
    ]
  },
  "scoring": {
    "kind": "multi-verify",
    "checks": [
      {
        "id": "encoding",
        "input": "multiline",
        "label": "同じ statement は同じ bytes、 違う statement は違う bytes",
        "points": 45,
        "wrongAnswerPenalty": 15,
        "hints": [
          {
            "id": "encoding-hint-1",
            "content": "各 field を `<長さ><中身>` で出します。 長さは `LENGTH_PREFIX_BYTES` bytes を `BYTE_ORDER` で、 文字列は UTF-8、 整数は `INTEGER_BYTES` bytes 固定幅です。 field の順は `STATEMENT_FIELDS` から読みます — 呼び出し側の dict の挿入順ではありません。 `params` はそれ自体を 1 つの枠に入れ、 中は名前と値の組を `PARAM_NAMES` 順に並べます。 外枠が無いと、 3 つの parameter を持つ statement と、 2 つ持って次の field が長い statement の境目が出力の中に存在しません。 固定幅なのは `1` と `01` が同じ数だからで、 固定順なのは等しい dict が同じ statement だからです。",
            "penalty": 20
          }
        ]
      },
      {
        "id": "identity",
        "input": "multiline",
        "label": "どのプログラムの話かを、 走る bytes で名指す",
        "points": 30,
        "wrongAnswerPenalty": 15,
        "hints": [
          {
            "id": "identity-hint-1",
            "content": "image record の 4 つの field のうち、 実際に走るのは 1 つだけです。 残りは build system が書いた覚え書きです。 `sibling_images` の 4 つは 2 つが同じ program で 2 つが違い、 間違え方ごとに違う組を取り違えます — `sourcePath` を digest すると再ビルドが base に見え、 `imageId` を digest すると同じ bytes の複製が別物に見えます。 `restamped` は判断で、 proving system と同じに倒します。 それから `decode_program` が拒否する body に digest を出さないこと: digest は 「この bytes が走った」 という約束で、 走れない bytes にその約束はできません。",
            "penalty": 14
          }
        ]
      },
      {
        "id": "ingestion",
        "input": "multiline",
        "label": "witness を通す扉は 1 つだけ",
        "points": 35,
        "wrongAnswerPenalty": 15,
        "hints": [
          {
            "id": "ingestion-hint-1",
            "content": "同時に成り立たせるものが 2 つあり、 引っ張る向きが逆です。 guest には statement 全体と witness 全体が要ります — public input はちょうど `STATEMENT_FIELDS`、 少なければ verifier は再構成できない claim を持たされ、 多ければ誰も合意していない public input です。 一方で `env.transcript()` は、 witness を知ってはいけない人に読まれても平気でなければなりません。 witness の 3 つの部分は 3 つとも公開できず、 理由がそれぞれ違います: `quantity` は exploit そのもの、 `aux` は price が可逆なので modular inverse 1 回分手前の exploit、 `search` は exploit とその近傍です。 「再現用に」 という note が最後に効きます。",
            "penalty": 16
          }
        ]
      },
      {
        "id": "reexec",
        "input": "multiline",
        "label": "host の言い分は入力であって答えではない",
        "points": 45,
        "wrongAnswerPenalty": 15,
        "hints": [
          {
            "id": "reexec-hint-1",
            "content": "`env.hints()` には host 自身の 「この run はこうなったはずだ」 が入っていて、 採点で渡されるものは全部間違っています。 host は証明される側です。 それから statement が名指していない image を渡されたら、 **1 step も走らせる前に** `ValueError` です。 算術は `SEMANTICS[statement[\"semantics\"]]` からだけ読みます — 幅は 8・16・32・64 のどれでもなく、 checkpoint ごとに変わり、 `overflow` も 3 通りあります。 `wrapping` は法をとって溢れた箇所を記録、 `saturating` は max で頭打ち (何も 0 の向こうへ回らないので何も wrap していません)、 `checked` は止まって `trapped`、 step 数はそこまでです。 `violated` は**値**についての判定で、 machine がどの register に入れていたかは無関係です。",
            "penalty": 20
          }
        ]
      },
      {
        "id": "journal",
        "input": "multiline",
        "label": "公開してよいのは、 読者がすでに計算できるものだけ",
        "points": 35,
        "wrongAnswerPenalty": 15,
        "hints": [
          {
            "id": "journal-hint-1",
            "content": "`statementDigest` は checkpoint 1 の出番です — `encode_statement(statement)` を `STATEMENT_COMMITMENT_DOMAIN` で commit します。 statement の言うことは全部この 1 field の中にあり、 だから次の checkpoint が可能になります。 残りの 3 field は statement を持っていない読み手への便宜で、 証拠ではありません。 測定値は 1 つだけで、 条件は 「読み手が公開 image からすでに計算できること」 です — 「小さいこと」 でも 「ただの数字であること」 でもありません。 そして journal に入らないものが 3 つあります: witness、 実行 trace、 探索が試した値。 どれも 「debug のうちだけ足しておく」 ができません。 journal は run より長生きします。",
            "penalty": 16
          }
        ]
      },
      {
        "id": "replay",
        "input": "multiline",
        "label": "その receipt は、 その statement の証拠か",
        "points": 50,
        "wrongAnswerPenalty": 15,
        "hints": [
          {
            "id": "replay-hint-1",
            "content": "seal の検証は暗号がやってくれる部分で、 やってくれないのは 「その seal が、 いま隣に置かれている statement の話ではない」 ことに気付くことです。 statement 全体への commitment 1 つを照合すれば、 別 target・別 claim・別整数幅・別 overflow 挙動・別 protocol version・別 guest build の 6 つが同じ 1 行で落ちます — 「面白い field」 だけに commit していると落ちません。 journal 側の便宜 field は**照合する**もので、 読むものではありません: digest は合っているのに journal が statement と違うことを言っている receipt が来ます。 `claimResult` が `False` の receipt は、 何も証明していない run についての正しい journal です。 そしてこの関数は例外を投げません — 投げた時点で、 呼び出し側に返すべき判断を例外処理に変えています。",
            "penalty": 24
          }
        ]
      },
      {
        "id": "privacy",
        "input": "multiline",
        "label": "承認された名前は、 承認ではない",
        "points": 35,
        "wrongAnswerPenalty": 15,
        "hints": [
          {
            "id": "privacy-hint-1",
            "content": "policy は 2 段です。 1 段目は `PUBLIC_NAMES` — 出してよい名前です。 2 段目は、 承認された名前が承認された値を運んでいるか: `PARAM_NAMES` の名前はその parameter 自身の公開値だけ、 `MEASUREMENT_NAMES` の名前は公開 image が含意する step 数だけを運べます。 名前しか見ない scan が見つけられない run が 2 つあります。 `values` の中の dict は、 その中の名前も公開しています。 label と message は自由文で policy の対象外です — 名前は policy を書ける単位で、 整形済みの文字列はそうではないからです。 10 個のうち 2 個は何も漏らしておらず、 そのうち 1 個は全 channel を数字で埋めます。 その 2 つへの報告は、 残り 8 つの見逃しと同じだけ間違いです。",
            "penalty": 16
          }
        ]
      },
      {
        "id": "transfer",
        "input": "multiline",
        "label": "見たことのない target・claim・protocol version で",
        "points": 25,
        "wrongAnswerPenalty": 15
      }
    ]
  },
  "i18n": {
    "en": {
      "name": "The proof was valid. It was a proof about a different account",
      "shortDescription": "A zkVM proves that a program ran, and nothing else. Which program, about which inputs, asserting what — the guest has to say all of that out loud, in bytes. Write the contract: public statement, private witness, public journal.",
      "description": "Week 6's fifth problem, and the sequel to `ac26-w6-zkvm-exploit-predicate`. That one settled the **exact exploit predicate**. This one turns that predicate into a **public / private input contract** a zkVM guest can actually stand on.\n\nThe cryptography is out of scope from the start. No proof is generated. The whole job is the two halves the proof system proves **nothing** about:\n\n```text\nthe public statement   what the proof is a claim about\nthe public journal     what the run published, forever, to everyone\n```\n\nWhat the cryptography guarantees is that the journal corresponds to some run of some program. It does not say **which** program, **which** inputs, or **which** claim. Saying that is the guest's job, and saying it badly makes a perfectly valid proof into evidence for something else.\n\n## Two things moved over from the last problem\n\nThe account (`price` / `spent` / `budget`) is no longer a constant baked into a target spec — it is **public input**. The same compiled guest proves claims about every account there is, so the only thing that says which account a proof is about is the statement it was bound to.\n\nThe integer semantics moved too. A `semantics` profile names the width **and** what the hardware does on overflow, so the same image under two profiles is two different machines:\n\n```text\nwrapping     reduced modulo 2 ** width — the only machine where the exploit exists\nsaturating   clamped at the largest value the machine can hold\nchecked      the machine traps and the run stops\n```\n\nThe exploit exists on exactly one of the three. A journal that does not say which one it ran under is a proof about whichever machine the reader assumes.\n\n## The pair at the centre of it\n\nEvery seed draws **two real accounts**. Both have real exploits. Run their statements through an encoder with no length prefixes and the two produce the same bytes — `\"53\" + \"7\"` and `\"5\" + \"37\"` are the same three characters.\n\nThis is not a malformed statement slipping through. It is that a **valid proof about one account is a valid proof about the other**, with nothing in the cryptography broken while it happens. A guest that treats canonical serialization as a formatting convention fails exactly here.\n\n## On scoring\n\nThis problem ships 55 deliberately broken guests, and **42 of them still answer the two questions anybody writes first** — does the happy path produce a receipt that verifies, and is a receipt offered against a different program refused. `make reference-test` re-measures that count on every run. Both are stated outright in the problem text and nobody has to discover them. The rest is why there are checkpoints.",
      "instructions": "## Before you start — from school math\n\n- **What you already know**: without separators, a reading is not determined. \"ab\" joined to \"c\" and \"a\" joined to \"bc\" both give \"abc\" — **indistinguishable**. Write the lengths first and they become \"2ab1c\" and \"1a2bc\", two different strings. That \"one content, exactly one spelling\" rule is called **canonical serialization**, and Week 3's Schnorr needed it for the same reason.\n- **Bytes**: a computer stores text and numbers alike as a sequence of numbers from 0 to 255. That sequence is a byte string, written `b\"...\"` in Python. UTF-8 is one of the agreements for turning text into such a sequence.\n- **A one-digit example (the centre of this problem)**: a prepaid account is three numbers — the unit `price`, the amount already spent (`spent`), and the ceiling (`budget`). Account A = (53, 7, 272) and account B = (5, 37, 272). Write the three out with nothing between them and A gives `537272` — and so does B. **The same string.** The digit shifts cancel exactly. Put the lengths in front and you get `2`+`53`, `1`+`7`, `3`+`272` against `1`+`5`, `2`+`37`, `3`+`272`, which can no longer be confused. The 53 and the 5 are illustration; your own two accounts come from **Inspect evidence**.\n- **Words**: zkVM = a machine that runs a program so that \"this program really did run\" can be shown to someone afterwards (a virtual machine = a computer built in software instead of hardware). guest = the program that runs inside it. statement = the **public** data saying which program, on which input, this is about — here six fields: domain / guestVersion / imageDigest / semantics / claim / params. witness = the concrete values that make the claim true; **never shown to anyone**. journal = what the finished guest publishes: anyone can read it and it cannot be taken back. image = the bytes of the program that actually runs, plus the labels a build tool wrote down beside them. digest = a byte string crushed into a short string by a fixed procedure, so the same bytes always give the same digest (the crushing function `commit()` is supplied). bit = a single 0-or-1 digit; an n-bit number holds 0 through \"2 multiplied n times, minus 1\" (4 bits: 0 through 15; anything past that wraps to the remainder after dividing by 16). receipt = one journal, wrapped up and offered with the words \"here is the evidence for that claim\". semantics = how many bits a number gets, plus what happens when a result does not fit (`wrapping` = take the remainder, `saturating` = stop at the largest value, `checked` = halt there). checkpoint = one small graded question; this problem has eight.\n- **Where this problem sits**: Week 6's fifth problem and the sequel to `ac26-w6-zkvm-exploit-predicate`, which settled what counts as an exploit. No proof is generated here. The part cryptography handles is out of scope, and you write **the half cryptography says nothing about**.\n\n## The idea\n\nThe \"account\" in the title is the prepaid account from the one-digit example above. You are the party claiming that this account admits a purchase past its ceiling, and you are one step away from putting that claim into a zkVM.\n\nWhat the zkVM guarantees is \"that program ran and produced this journal\", and no more. **Which** program, about **which** account, asserting **what** — the cryptography says none of it. Saying it is the guest's job. Say it loosely and a perfectly sound proof built for account A passes as a proof about account B. Nothing was forged; the bytes simply never said which account they were about. What you are left holding is **evidence that an account nobody has touched is over its ceiling**.\n\nSo the first thing to do is pin the statement to exactly one byte string. If that reading is ambiguous, everything written on top of it is ambiguous too. The other seven build up from there.\n\n## First move\n\nIn Participant Portal (the site showing this statement), select **起動 (Start)**. Your own container starts and the **problem editor** appears on the same page as the statement.\n\nThe editor holds **one file, `guest.py`**, and the language is **Python**. Seven function stubs (an empty body and a docstring each) are already there; you never create a file. Each function's arguments, return shape and the conditions for raising are documented in its own docstring. **You do not write Rust here** — the course assignment is in Rust, but what this problem fixes is the shape of the input/output contract, which is the same in any language.\n\nSelect **Inspect evidence** to see the values built for this start only. The button runs `show.py`, so there is nothing to type. It prints nine blocks:\n\n1. **Your statement** — the six fields in the order a canonical encoding must emit them.\n2. **Your machine** — the width in bits, the modulus (where a result wraps), the largest value it holds (max), and the overflow behaviour. Plus the list of widths a checkpoint may draw from (**8, 16, 32 and 64 are not among them**) and the three overflow behaviours.\n3. **The account and the program that spends it** — price / spent / budget, the four-line program, and the property that must not be broken, written over ordinary integers where nothing overflows.\n4. **Two statements one encoder cannot tell apart** — your accounts A and B, each one's three numbers run together with nothing between them, the answers to \"different statement?\" and \"same bytes?\", and the size of the statement family your encoder has to separate.\n5. **The image and the four beside it** — imageId, sourcePath, buildId, the program's step count and steps, and four siblings each differing in exactly one way (`rebuilt` = built again from source / `restamped` = only the date re-stamped / `renamed` = only the file name changed / `relabelled` = only the label edited). **Which of them are the same program is a checkpoint and is not printed.**\n6. **The two doors for input** — which `env` calls are kept in the transcript and which are not.\n7. **What a journal carries** — the five field names and the name of the measurement it may publish.\n8. **The ids of the receipts you will be offered** — fifteen of them. Which may be accepted is a checkpoint and is not printed.\n9. **The ids of the runs you will audit** — ten (`d0`–`d9`), with the names of the six channels a value can escape through and the list of names a run may disclose.\n\n**Look at block 4 first.** \"Different statement?\" says `True` and \"same bytes?\" also says `True`. Seeing that one line yourself is the reason checkpoint 1 is a specification and not a formatting chore. Every number changes per start, so copying someone else's values will not match.\n\n**Run public tests** checks shapes only: that an encoding returns bytes, that it is the same whichever order the dict's keys were inserted in, that a run and a journal carry their declared fields, and that a receipt you sealed yourself verifies against the statement you sealed it under. It says nothing about whether the content is right. To put the editor back the way it started, select **Restore starter**.\n\n## What you answer — eight checkpoints, in this order\n\nAll eight are **code checkpoints**; there is no answer field to type into. The whole flow is: write in the editor, then press that checkpoint's **Submit (+points)**. Whatever is in `guest.py` at that moment is sent for grading — **one file graded from eight angles**. A wrong answer can be resubmitted; each wrong attempt costs 15 points and the total never falls below zero. The checkpoints are independent, so being stuck on one does not block the rest.\n\n1. **encoding** — `encode_statement(statement) -> bytes`. Turn one statement into bytes that admit exactly one reading. The constants for length prefixes, integer width and field order live in `participant/lab.py`, so you and the grader read the same numbers. `participant/lab.py` itself does not appear in the editor, but copying the `from participant.lab import (…)` line from `guest.py`'s opening docstring into your code gives you every constant and helper (what each means is in `encode_statement`'s docstring). Raise `ValueError` for anything that is not a statement (one line: `raise ValueError(\"reason\")`).\n2. **identity** — `image_digest(image) -> str`. Return one name for \"which program this is about\". Of the image's four fields, one is what actually runs and the rest are what a build tool wrote down. Deciding what to crush is the checkpoint; the four siblings in block 5 are there to show what a wrong decision mistakes for what.\n3. **ingestion** — `guest_input(env, statement, witness) -> None` (returns nothing). Load `env` with everything the guest needs to do its work while leaving the transcript safe to be read by someone who must not learn the witness. Which door is which is in block 6. Two requirements pull in opposite directions and both have to hold.\n4. **reexec** — `run_guest(image, env) -> dict`. Run the program the statement names, on the machine the statement names, yourself, and return the fixed seven-field record. The host is the side running the zkVM around the guest and feeding it inputs — here, also the party that wants the proof to pass. Whatever the host attaches as \"here is what this run produced\" is **an input, not the answer** — the host is the party being proved about.\n5. **journal** — `seal_journal(statement, run) -> dict`. Build the published record as exactly five fields. Checkpoint 1's encoding is used here. One measurement goes in, and the test for it is \"could a reader already compute this from public information\" — not \"is it small\" and not \"is it just a number\".\n6. **replay** — `accept_receipt(receipt, statement) -> bool`. Decide whether the receipt offered is evidence for that statement. The fifteen ids from block 8 actually arrive. **This function never raises** — even a malformed receipt gets a decision, because a decision is what it owes its caller.\n7. **privacy** — `leak_report(disclosure, statement, image) -> tuple`. Return every `(channel, name)` pair one run put in front of someone outside the policy, sorted and without duplicates. It is called on all ten runs from block 9. All ten published the same claim and all ten are correct — what differs is **what else they let slip**.\n8. **transfer** — your `guest.py` runs all of 1–7 again on **a width, an account, a program and a protocol (= this problem's overall ruleset) version you have not seen**. An implementation that copied the numbers off the screen fails here.\n\n## Goal\n\nDone means: the statement has exactly one byte string, a program's name is bound to the bytes that run, the witness never reaches the transcript, the run is recomputed without the host's account of it, the journal says nothing extra, a receipt offered against something else is refused, all ten runs are audited with neither a miss nor a false alarm — and all of it still holds on values you have never seen.\n",
      "writeup": "## What the cryptography does not prove\n\nA zkVM proof says that some run of some program produced this journal. **Which** program, about **which** inputs, asserting **what** — the guest has to say all of that out loud, in bytes. All eight checkpoints are about that saying, and none of them is about cryptography.\n\n## The pair at the centre of it\n\nEvery seed draws two real accounts. Both have real exploits. Run them through an encoder with no length prefixes and they are the same bytes:\n\n```text\nleft   price=53 spent=7  budget=272   ->  \"53\" \"7\"  \"272\"\nright  price=5  spent=37 budget=272   ->  \"5\"  \"37\" \"272\"\n```\n\nBoth concatenate to `537272`. So a receipt sealed under `left` verifies against `right`, and what you have is **evidence that an account nobody has touched is over its budget**, forged out of nothing.\n\n`naive_encode` is not a straw man. The field order is fixed, every field is present, nothing is dropped. It is still not an encoding, because the boundary between one field and the next is not in the output. That is what the length prefix is.\n\nAnd that pair is not the only one that has to stay apart. Two statements differing only in `domain`, and two differing only in `guestVersion`, are different statements — and those are the two people drop first, because they \"do not affect the computation\".\n\n## A digest is about the bytes that run\n\nAn image record carries two labels a toolchain wrote (`sourcePath`, `imageId`) next to the `body` that actually executes. Of the four siblings, two are the same program as the base and two are not:\n\n```text\nrebuilt     same source path, one comparison changed, a new stamp  -> a different program\nrestamped   the same steps, a different build stamp                -> a different image\nrenamed     the same bytes, another path                           -> the same program\nrelabelled  the same bytes, another image id                       -> the same program\n```\n\nA digest over `sourcePath` calls the rebuild the base image. Those two disagree about every order whose total lands exactly on the budget, which is the order an attacker picks. A digest over `imageId` calls the relabelled copy a different program, and a perfectly good proof is refused for a reason nobody can find.\n\n`restamped` is the judgement call. Settle it the way proving systems do: **\"nothing observable changed\" is the claim under audit rather than an input to it**, so a rebuild is a different image.\n\n## The host's account is an input, not the answer\n\n`env.hints()` carries the host's own account of what the run produced, and on the runs the checker builds it is confident, detailed and wrong. The host is the party being proved about. A guest that takes the hint is fast, is right almost always, and proves nothing at all.\n\nSame reasoning: an image the statement does not name is refused **before a single step runs**. Executing first and reporting which program it was afterwards produces a run somebody can quote out of context.\n\n## What a public measurement has to pass\n\nOne test: **could a reader already compute it?** Not \"is it small\", and not \"is it just a number\".\n\nA cycle count that varies with the witness is not a measurement, it is the witness at lower resolution. And a journal outlives its run, which makes it the one artifact where \"add it for debugging and take it out before release\" is not available.\n\n## A journal's own fields are checked, not read\n\nBesides the statement's digest, a journal carries `imageDigest` and `guestVersion`. Those are a convenience for a reader who has the journal and not the statement, and they are **not the evidence**. Writing them down costs nothing; believing them costs everything.\n\nThe checkpoint hands in receipts nobody replayed whose journal was edited after sealing: the digest still matches, and the journal now says something the statement does not. **A journal field a verifier reads is a journal field an attacker writes.**\n\nAnd a receipt whose `claimResult` is `False` is a correct journal about a run that proves nothing. Accepting it as evidence for the claim is the quietest way in of all.\n\n## An approved name is not an approval\n\nThe privacy policy has two halves. The first is `PUBLIC_NAMES`, the names a run may disclose. The second is whether an approved name is carrying an approved **value**.\n\n`spent` is on the list. The machine's own total wearing the name `spent` is a different disclosure — the price is public and invertible, so it is the quantity with one modular inverse left to apply. A scan that only looks at names finds nothing there.\n\nTwo of the ten runs disclose nothing, and one of those two is **loud**: it fills every channel with numbers. Reporting a leak against either is exactly as wrong as missing one against the other eight. **An audit that always finds something has not read anything.**\n\n## Measured\n\nThis problem ships 55 deliberately broken guests, and **42 of them still get the easy two right** — the happy path verifies, and a receipt offered against a different program is refused. `make reference-test` re-measures it on every run. Both categories are written out in the problem text; nobody has to discover them.\n\n## What the suite proves, and what it does not\n\nIt proves that these eight checkpoints catch the 55 defects shipped with the problem, and that the reference clears all of them. It does **not** prove that the contract has no other hole.\n\n## Toy versus production\n\nThe width is seven to thirteen bits, there is one account, the program is four steps, and a receipt carries no seal. Verifying a seal is precisely the part cryptography already does, so it is out of scope — and the binding it is useless without is not. In a real zkVM the statement is a program's ELF digest and a list of public inputs, and the journal is the output the proving system commits to. The claim here is that the binding can be written as an exact contract, not that the one written here is complete.\n\n## Not in scope\n\nActual zkVM proof generation and receipt verification, production binary reproducibility systems, the zero-knowledge property of a specific zkVM, and remote attestation.",
      "learningGoals": [
        "Separate the public statement from the private witness as explicit types",
        "Demonstrate why canonical serialization is needed, with two statements that actually collide",
        "Fix field order, length prefixes, integer width, endianness and domain separation in one serializer",
        "Bind target identity to the bytes that execute rather than to a source path string",
        "Hand a witness to the guest without exposing it through arguments, environment or logs",
        "Recompute the target execution inside the guest instead of trusting a host-supplied result",
        "Refuse an image the statement does not name before a single step runs",
        "Treat wrapping, saturating and checked as a profile the statement names",
        "Commit the statement digest, target digest, claim result and guest version into one journal",
        "State the condition for a public measurement as \"a reader could already compute it\"",
        "Refuse a receipt replayed against another target, claim, semantics or protocol version",
        "Check a journal's convenience fields against the commitment rather than reading them as evidence",
        "Audit the journal, stdout, stderr, error, trace and temporary artifacts for witness disclosure",
        "Detect a disclosure where an approved name carries a value it was never approved for"
      ],
      "checks": [
        {
          "id": "encoding",
          "label": "Same statement, same bytes; different statement, different bytes",
          "hints": [
            {
              "id": "encoding-hint-1",
              "content": "Every field is emitted as `<length><payload>`, the length being `LENGTH_PREFIX_BYTES` bytes in `BYTE_ORDER`; text becomes UTF-8 and an integer becomes `INTEGER_BYTES` bytes at a fixed width. The field order comes from `STATEMENT_FIELDS`, not from the order the caller's dict was built in. `params` is its own framed block, each `PARAM_NAMES` entry as a name and then a value. Without that outer frame, a statement with three parameters and one with two followed by a longer next field are not separated by anything in the output. Fixed width because `1` and `01` are the same number; fixed order because two dicts that compare equal are one statement."
            }
          ]
        },
        {
          "id": "identity",
          "label": "Name the program by the bytes that run",
          "hints": [
            {
              "id": "identity-hint-1",
              "content": "Of the four fields on an image record, one is what actually executes. The rest are what a build system wrote down about it. Two of the four siblings are the same program as the base and two are not, and every way of getting this wrong gets a different pair wrong: a digest over `sourcePath` says the rebuild is the base image, and one over `imageId` says the relabelled copy is a different program. `restamped` is the judgement call — settle it the way proving systems do. And do not hand out a digest for a body `decode_program` refuses: a digest is a promise that the bytes under it are the bytes that ran, and bytes nothing can run have no such promise to make."
            }
          ]
        },
        {
          "id": "ingestion",
          "label": "The witness goes through one door",
          "hints": [
            {
              "id": "ingestion-hint-1",
              "content": "Two things have to be true at once and they pull in opposite directions. The guest needs the whole statement and the whole witness — the public inputs have to be exactly `STATEMENT_FIELDS`, since fewer leaves a verifier holding a claim it cannot reconstruct and more is a public input nobody agreed to. And `env.transcript()` has to survive being read by somebody who should not learn the witness. None of the witness's three parts is publishable, and each for a different reason: `quantity` is the exploit, `aux` is the exploit with one modular inverse still to apply because the price is public and invertible, and `search` is the exploit surrounded by its neighbours. The note written \"so the run can be reproduced\" is the one that gets you."
            }
          ]
        },
        {
          "id": "reexec",
          "label": "The host's account is an input, not the answer",
          "hints": [
            {
              "id": "reexec-hint-1",
              "content": "`env.hints()` carries the host's own account of what this run produced, and on the runs the checker builds every field of it is wrong. The host is the party being proved about. Refuse — `ValueError` — **before a single step runs** if the image handed over is not the one the statement names. The arithmetic comes out of `SEMANTICS[statement[\"semantics\"]]` and nowhere else: the width is not 8, 16, 32 or 64, it changes between checkpoints, and `overflow` has three values. `wrapping` reduces and records which site had to be reduced; `saturating` clamps at max, so nothing carried around zero and nothing wrapped; `checked` stops, with `trapped` set and however many steps had run. `violated` is a statement about **value** — which register the machine kept it in is no part of it."
            }
          ]
        },
        {
          "id": "journal",
          "label": "Publish only what a reader could already compute",
          "hints": [
            {
              "id": "journal-hint-1",
              "content": "`statementDigest` is where checkpoint 1 gets used: commit to `encode_statement(statement)` under `STATEMENT_COMMITMENT_DOMAIN`. Everything the statement says is inside that one field, which is what makes the next checkpoint possible. The three fields after it are a convenience for a reader who has the journal and not the statement, and they are not the evidence. There is one measurement and its test is \"could a reader already compute it from the public image\" — not \"is it small\" and not \"is it just a number\". Three things are never in a journal: the witness, the execution trace, and what the search tried. None of them is a debugging convenience that can be removed before release, because a journal outlives the run."
            }
          ]
        },
        {
          "id": "replay",
          "label": "Is this receipt evidence for this statement?",
          "hints": [
            {
              "id": "replay-hint-1",
              "content": "Verifying a seal is the part cryptography already does. What it does not do is notice that a perfectly valid seal is being shown to you next to a statement it was never about. Checking one commitment to the **whole** statement refuses six things on one line — another target, another claim, another integer width, another overflow behaviour, another protocol version, another guest build — and committing to only the interesting fields refuses none of them. The journal's own fields are **checked**, not read: receipts arrive whose digest matches and whose journal says something the statement does not. A receipt whose `claimResult` is `False` is a correct journal about a run that proves nothing. And this function never raises — raising turns a decision it owed its caller into an exception."
            }
          ]
        },
        {
          "id": "privacy",
          "label": "An approved name is not an approval",
          "hints": [
            {
              "id": "privacy-hint-1",
              "content": "The policy has two halves. The first is `PUBLIC_NAMES`, the names a run may disclose. The second is whether an approved name carries an approved value: a `PARAM_NAMES` name may carry that parameter's own public value and nothing else, and a `MEASUREMENT_NAMES` name may carry only what the public image already implies. Two of the runs are invisible to a scan that only reads names. A `values` entry that is itself a dict has published the names inside it too. Labels and messages are free text and are not policed, because a name is something a policy can be written against and a formatted string is not. Two of the ten disclose nothing, and one of those two fills every channel with numbers — reporting against either is as wrong as missing one against the other eight."
            }
          ]
        },
        {
          "id": "transfer",
          "label": "A target, a claim and a protocol version you have not seen"
        }
      ]
    }
  }
}
