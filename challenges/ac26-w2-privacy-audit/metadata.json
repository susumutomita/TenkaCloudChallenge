{
  "id": "ac26-w2-privacy-audit",
  "name": "答えは合っている。それだけだ",
  "category": "Challenge",
  "status": "draft",
  "visibility": "public",
  "track": {
    "id": "advanced-cryptography-2026",
    "order": 240,
    "chapter": "Week 2 / Privacy Audit"
  },
  "courseAlignment": {
    "courseId": "advanced-cryptography-program",
    "edition": "2026",
    "week": 2,
    "role": "transfer",
    "spoilerPolicy": "independent-reimplementation",
    "sources": [
      {
        "repository": "zk-tokyo/advanced-cryptography-2026",
        "ref": "a3aa4b56fa88fbe803b57d320fbc87c1a203b480",
        "path": "week2/README.md",
        "kind": "lecture"
      },
      {
        "repository": "zk-tokyo/advanced-cryptography-2026",
        "ref": "a3aa4b56fa88fbe803b57d320fbc87c1a203b480",
        "path": "week2/problems/toy-mpc/README.md",
        "kind": "assignment"
      }
    ]
  },
  "difficulty": 4,
  "estimatedDuration": "60-75 分",
  "shortDescription": "7 つの実装が、全部正しい合計を返す。それでも 4 つは、途中で見せてはいけないものを見せている。正しさのテストは何も教えてくれない——差を見つける監査ツールを、自分で書く。",
  "description": "Week 2 の 4 問目、 role は transfer。 correctness と privacy は別の性質であり、 前者の test をいくら足しても後者は保証されない、 を実装で確認する。\n\n題材は 3 party の加重合計 MPC。 program は Python source ではなく操作列 (op list) として与えられ、 runtime が実行して観測可能な event 列を出す。 これが本問題の設計上の要で、 `reconstruct` という語を grep する監査器は rename・wrapper・helper で破れるが、 実際に実行された操作を読む監査器は破れない。 program を code ではなく data にしたことで、 その違いを採点できる。\n\n7 実装すべてが正しい合計を返す。 4 つが違反 (secret の open / 他 party の share 読み取り / log への raw 値 / error message への secret) を持ち、 3 つは clean。 clean 側には偽陽性の罠を 2 つ置いてある — 仕様上 public な値を log に出す実装と、 自分自身の slot を読む party。 「全部怪しい」 と言う監査器は本物を全部見つけた上で不合格になる。\n\ncheckpoint は 7 つ。 allowed open 集合の導出、 3 種類の違反検出、 transcript からの秘密復元 (counterexample)、 修復、 rename + 並べ替え + 未知 seed での再監査。 修復は 「消せば漏れない」 を排除するため、 仕様が許す観測がすべて残っていることまで検査する。\n\nthreat model は honest-but-curious、 collusion なし。 実運用の security 主張はせず、 toy simulator で観測できる leakage contract だけを評価する。\n\nWeek 2 の教材は pinned commit 時点で未公開のため sources は placeholder pin であり、 status は draft のままにする (#219 が公開を検出した時点で確定する)。",
  "instructions": "## 正しい答えは、無実の証明にならない\n\n2023 年、複数の MPC ウォレットの署名ライブラリで、立て続けに欠陥が公表されました（BitForge、TSSHOCK）。署名はどれも正しく出ていました。テストは緑でした。それでも、プロトコルがやり取りの中で見せていた値をたどると、他人の鍵の破片が割り出せた——答えが合っていることと、途中で余計なものを見せていないことは、**別の性質**だからです。\n\nテストで答えが合っていても、計算用紙を隣の人に見せていたら、そこに書いた数は伝わっています。事故はいつも、計算用紙の側で起きます。\n\n## 差は、出力には出ない\n\nこの問題には、同じ「秘密のリスクスコア」を計算する実装が 7 つ持ち込まれます。**7 つとも正しい合計を返します**。うち 4 つは、途中で見せてはいけないものを見せています。出力を見比べても差は出ません。正しさのテストは 7 つとも通るからです。\n\nここまでのあなたは、正しく計算する側でした。この問題では監査する側に回ります。書くのは、その差を見つける監査ツールです。判定に使えるのは、仕様（何を見せてよいか）と、外から観測できた出来事の記録だけ。\n\n## 前提（知っていれば飛ばしてください）\n\n- 数はすべて素数 p で割ったあまりで考えます。初めてなら先に `ac26-w1-constraint-lab` を。\n- 一度きりのでたらめな数 r で覆った `x + r` は、r を知らない相手に x について何も語りません。p = 7 で x = 3 を 5 で覆った 1（= 8 mod 7）を見せられても、x は 0 から 6 のどれでもありえます。`ac26-w2-beaver-mul` で d = x − a を公開してよかったのと同じ理屈です。\n- この世界のわり算は「w に掛けると 1 になる相手」を掛けることで、Python では `pow(w, -1, p)`。7 つのうち 1 つの checkpoint で使います。\n- share（破片）と開示（open）は `ac26-w2-secret-sharing` のとおりです。\n\n## 手がかりは、仕様と記録の 2 つだけ\n\n### 仕様 (spec)\n\n監査ツールに渡される辞書で、秘密の値は入っていません。キーは次の 5 つで、すべて必ずあります。\n\n```\n{\"p\": 1009,                            割る数 (素数)\n \"parties\": [\"p0\", \"p1\", \"p2\"],        参加者の id\n \"publicInputs\": [\"weight-p0\", ...],   最初から公開されている値の label\n \"masked\": [\"masked-p0\", ...],         一度きりの覆いがかかった値の label\n \"result\": \"total\"}                    結果の label (文字列ひとつ。リストではありません)\n```\n\n**見せてよいもの (allowed)** は、この 3 つを合わせたものです — `publicInputs` の全部、`masked` の全部、そして `result` の 1 つ。理由は 3 つとも別です。公開入力はもともと公開。覆いのかかった値は上の 1 桁の例のとおり中身を語らない。結果は出すと宣言したもの。**これら以外の label はすべて途中の秘密**で、見せたら開けすぎです。\n\n### 記録 (trace)\n\n外から観測できた出来事の列です。手元だけの足し算・掛け算は 1 つも記録されません。プロトコルは「どれだけ計算したか」ではなく「何を見せたか」で判定される、ということです。各要素は次の 6 つのキーを必ず全部持つ辞書です (使わないキーには `\"\"` か `0` が入っています)。\n\n```\nkind    \"open\" | \"peek\" | \"emit\" | \"fail\" | \"output\"\nlabel   その出来事が触れた値の名前   (peek では \"\")\nparty   読んだ人                     (peek のみ)\nowner   読まれた側                   (peek のみ)\ntext    ログ / エラーの文言          (emit / fail のみ)\nvalue   実際に外へ出た数値\n```\n\n`open` はその label の値が全員に見えたこと、`peek` は `party` が `owner` の share の置き場を直接読んだこと、`emit` はその label の値を含むログ行が出たこと、`fail` はその label の値を含むエラーメッセージが出たこと、`output` は宣言された結果が出力されたことを表します。\n\n違反の名前は `opened-a-secret` / `cross-party-read` / `leaked-in-log` / `leaked-in-error` の 4 つで、`auditor.py` の先頭に定数 `VIOLATIONS` として入っています。\n\n## 疑いすぎは、見逃しと同じ重さ\n\n採点では、漏れる実行と漏れない実行が混ぜて渡されます。しかも「漏れない」側には、いちばん紛らわしいもの——覆いのかかった値の開示——が入っています。x = 3 そのものを見せるのは違反。覆った 1 を見せるのは無罪。見えた先が画面かログかエラーメッセージかは、違反かどうかを変えません。**無罪のものを違反と呼ぶのは、本物の漏れを見逃すのと同じ重さの誤りです**——何でも違反と言う監査ツールは、何も言わない監査ツールと同じくらい役に立ちません。\n\nもう 1 つ。label 名は起動ごとに変わり、最後の checkpoint では全部改名され、順番も入れ替わります。判定の根拠にしてよいのは **`kind`、その `label` が allowed に入っているか、`party` と `owner` が同じか**の 3 つだけ。文言の中身・出てくる位置・event の個数を根拠にすると、そこで落ちます。\n\n## 最初の一手\n\nParticipant Portal (この問題文が表示されているサイト) で **「起動」** を押すと、問題文と同じ画面に **問題エディタ** が出ます。自分のパソコンでターミナルを開く必要はありません。\n\n問題エディタの **「証拠を確認」** を押すと、次が表示されます。\n\n1. **health token** — コンテナが動いた証拠の文字列。提出には使いません。\n2. **割る数 p と参加者の id**。\n3. **spec の実物** — 監査ツールに渡されるのと同じ辞書が、そのまま表示されます。**起動ごとに変わります**。\n4. **漏れていない実行の event** — 番号つきで 1 行ずつ。\n\n紙で 2 つやってください。まず 3 の spec を見て、**allowed に入る label を全部書き出す**。次に 4 の event を上から 1 つずつ見て、その `label` が自分の書き出したリストに入っているかを確かめる。漏れる実行は表示されないので、**漏れていない実行がどう見えるかを先に知っておくこと**が唯一の足場になります。\n\n## 答えるもの — checkpoint は 7 つ、この順で\n\nエディタのファイルは `auditor.py` 1 つです。4 つの関数の枠が最初から入っていて、返り値の形・採点のしかた・考える順序は各関数の説明文 (docstring) にも書いてあります。JSON を手で書く checkpoint はありません。7 つとも、この 1 ファイルを提出します。\n\n書く 4 つの関数 (引数 → 返り値):\n\n- `allowed_opens(spec)` → **ソート済みの文字列リスト**。この仕様が外に見せてよい label を全部並べたもの。ここが 1 つでもずれると、以降の判定が全部ずれます。\n- `first_violation(trace, spec)` → 漏れているときは `{\"kind\": <4 つの違反名のどれか>, \"index\": <trace の中での位置>}`、**漏れていなければ `None`**。`index` は trace 全体を 0 から数えた位置で、違反だけを数えた番号ではありません。「first」なので、先頭から見て最初に当たったところで止めます。\n- `derive_secret(transcript, spec)` → `{\"party\": <参加者 id の文字列>, \"value\": <0 以上 p 未満の整数>}`。引数の `transcript` は trace とは**形が違い**、各要素は `{\"label\": ..., \"value\": ...}` の 2 キーだけを持つ辞書です。\n- `repair(program, spec)` → **同じ形の操作タプルのリスト**。`program` は Python のソースではなく操作のタプルを並べたもので、取りうる形は次の 10 種類がすべてです。\n\n```\n(\"share\",  label, owner)      owner の秘密が label という share になる\n(\"const\",  label, value)      公開定数\n(\"mask\",   out, val, msk)     out = val - msk   (手元だけ。何も漏れない)\n(\"scale\",  out, val, k)       out = val * k     (k は公開。手元だけ)\n(\"addsh\",  out, x, y)         out = x + y       (手元だけ)\n(\"open\",   label)             label の値が全員に見える\n(\"peek\",   party, owner)      party が owner の share の置き場を読む\n(\"emit\",   label, text)       label の値を含むログ行\n(\"fail\",   label, text)       label の値を含むエラーメッセージ\n(\"output\", label)             宣言された結果の出力\n```\n\n1. **allowed-opens** (コード) — `allowed_opens`。監査の土台です。\n2. **opened-secret** (コード) — `first_violation`。値そのものが全員に見えた場合を扱う実行の組で試されます。\n3. **cross-party** (コード) — `first_violation`。他人の手元を読んだ場合を扱う組。\n4. **log-leak** (コード) — `first_violation`。ログとエラー経路から漏れた場合を扱う組。\n5. **transcript** (コード) — `derive_secret`。「開けすぎだ」と指摘するのと、「その結果この人の値がこうなる」と実物を見せるのは説得力がまったく違います。ここは後者を要求します。`party` が実在の参加者 id で、`value` がその人の値と**完全に一致**することが必要です。\n6. **repair** (コード) — `repair`。修理は足すのではなく**削る**操作です。直したあとも同じ正しい合計が出ること、もう漏れないこと、そして**仕様が許している開示は全部残っていること**の 3 つが要ります。3 つ目がないと「全部消す」が最強の修理になってしまうからです。\n7. **mutation** (コード) — 新しく書くものはありません。同じプロトコルを、**label を全部改名し、独立な開示を並べ替えて**もう一度流します。漏れているかどうかと違反の種類が前と同じでなければ落ちます。文字列の中身や出現位置で判定した監査ツールは、ここで自分自身と食い違います。\n\n2・3・4 はどれも `first_violation` を見ますが、渡される実行の組が違います。1 つが緑でも他が赤いことは普通に起こるので、3 つを別々の手がかりとして使ってください。\n\n### 提出のしかた\n\n7 つの checkpoint は**別々に、何度でも提出できます**。各 checkpoint の **「提出」** を押すと、**いまエディタに入っている `auditor.py` の中身がそのまま**送られて採点されます。コピーも貼り付けも要りません。4 つの関数が 1 ファイルなので、`allowed_opens` だけ書けた段階で 1 番を出せます。誤答は 1 回ごとに小さな減点があるので、**「公開テストを実行」** で先に形を確かめるほうが得です。エディタを戻すのは **「初期状態に戻す」** です。\n\n## この目は、次から自分のコードに向く\n\n7 つ通れば終わりです。`ac26-w2-private-aggregate` には privacy という checkpoint があり、今度は**あなたの実装**が開示した値の集合そのものが採点されます。今日書いた監査ツールの目が、明日は自分のプロトコルに向く番です。BitForge も TSSHOCK も、正しさのテストではなく、この種類の監査が見つけました。\n",
  "writeup": "## この問題が測っているもの\n\n**正しい答えを出すことと、正しくふるまうことは別物である。** 7 つのプログラムはどれも同じ正しい合計を出します。出力を見ても、どれが漏らしているかは分かりません。分かるのは trace —— 実行が外に見せたものの列 —— だけです。\n\nそして、監査ツールの間違え方は 2 通りあり、**どちらも同じくらい致命的**です。\n\n- **何も指摘しない監査ツール** — 漏れていない実行を全部通し、漏れも全部見逃す\n- **何でも指摘する監査ツール** — 漏れを全部見つけ、同時に問題のない実行を全部断罪する\n\nだからこの問題の checkpoint は、必ず **漏れる実行と漏れない実行を混ぜて**採点します。しかも「漏れない」側はいちばん紛らわしいもの —— 仕様が公開すると宣言した値を載せたログ行と、自分自身の領域を読んだ party —— を使います。これらを違反だと言うのは、本物の漏れを見逃すのと同じ重さの誤りです。\n\n## 「開けてよい」の定義\n\nallowed = `publicInputs` ∪ `masked` ∪ `{result}` でした。3 つとも理由が違います。\n\n- **publicInputs** — もともと公開されている値 (例: 重み)。事前に全員が合意しているので、開示しても新しい情報は出ません。\n- **masked** — 一度きりのマスクで覆われた差分。`masked-p1 = x1 - m1` で m1 は誰も知らない乱数なので、この値は x1 について何も語りません。これは前の 2 問と同じ論法です (Beaver の d と e がまさにこれ)。\n- **result** — 出すと宣言したもの。宣言された出力を漏洩と呼ぶなら、プロトコルは何もできません。\n\n**それ以外の label はすべて中間の秘密**です。`x-p1` (誰かの生の入力) も、`sub` (途中の部分和) も、開けたら over-opening です。\n\n「部分和なら合計と同じようなものだから安全では?」——**違います**。それがこの問題で実際に手を動かして確かめる点です。\n\n## 部分和 1 つで、秘密がひとつ落ちる (数値で追う)\n\np = 1009、3 者、重み w = (3, 5, 7)、私的な値 x = (100, 200, 300) とします。\n\n```\n合計 total = 3*100 + 5*200 + 7*300 = 300 + 1000 + 2100 = 3400\n             3400 % 1009 = 373\n部分和 sub = 3*100 + 5*200 = 1300、 1300 % 1009 = 291\n```\n\nプロトコルが `total` に加えて `sub` も開けてしまったとします。すると\n\n```\ntotal - sub = (373 - 291) % 1009 = 82        これは w2 * x2 に等しい\n                                             (検算: 7*300 = 2100、 2100 % 1009 = 82  OK)\n```\n\n重み 7 は **公開値**で、p が素数なので逆元が存在します。7 * 865 = 70930 = 70*1009 + 300 なので `7^-1 = 865` (7*144 = 1008 ≡ -1 より 144 の符号を反転して 1009-144 = 865)。したがって\n\n```\nx2 = 82 * 865 % 1009 = 70930 % 1009 = 300\n```\n\n**p2 の私的な値がそのまま出ました。** 「余計に 1 つ開けた」と指摘するのと、「その結果こうなる」と見せるのは、説得力がまったく違います。derive_secret checkpoint が計算まで要求するのはそのためです。\n\n## 各 checkpoint が守っているもの\n\n- **allowed-opens** — 監査の土台。ここが 1 つでもずれると、以降の 3 つが全部ずれます。`result` は文字列 1 つで、list ではないことに注意 (`{*publicInputs, *masked, result}` の形で入れる)。\n- **opened-secret / cross-party / log-leak** — どれも同じ `first_violation` を、違う組み合わせのプログラムで呼びます。**種類と位置の両方**が要求されるのは、「何か漏れた」とだけ言う監査ツールは何も局在化していないからです。位置は「trace の何番目か」で、実行のたびに変わり得ます。\n- **transcript** — 上の復元。ここだけは「指摘」ではなく「実害」を要求します。\n- **repair** — 修理したプログラムに 3 つの条件を課します。(1) 同じ正しい合計を出す、(2) もう漏れない、(3) **仕様が許す開示は全部残っている**。(3) が無いと「プロトコルを全部消す」が最強の修理になってしまいます。何も実行しなければ何も漏れません —— しかしそれは修理ではありません。\n- **mutation** — 同じプロトコルの label を全部改名し、独立な開示を後ろへ動かします。プロトコルは何も変わらないので判定も変わってはいけません。`\"x-p1\"` のような**文字列そのもの**を見ている監査ツールや、「違反はいつも 12 番目」と覚えた監査ツールは、ここで自分自身と矛盾します。\n\n## 間違えるとどう壊れるか\n\n- **ログ行を全部漏洩とみなす** — 仕様が公開すると宣言した値 (重みなど) を載せたログ行まで違反になります。clean な実行を違反と報告するのは、漏れを見逃すのと同じ重さで落とされます。**判定基準は「ログ行かどうか」ではなく「載っている label が allowed に入っているか」**です。\n- **share の読み取りを全部漏洩とみなす** — party が**自分の**領域を読むのは、party として当たり前の動作です。`peek` が違反になるのは `party != owner` のときだけ。\n- **`output` を違反とみなす** — 宣言された結果です。`result` は allowed に入っています。\n- **最初の違反ではなく最後の違反を返す** — 「first」なので、trace を先頭から見て**最初に**当たったところで止めます。\n- **index を数え間違える** — `enumerate` の添字は trace 全体での位置です。違反だけを数えた番号ではありません。\n- **label の文字列に依存する** — `\"x-\"` で始まるものを秘密とみなす、というような実装は、mutation で改名された瞬間に崩れます。判定は必ず spec の allowed 集合を通してください。\n- **repair でプログラムを削りすぎる** — 合法な `open` まで消すと (3) で落ちます。消してよいのは、まさに違反している操作だけです。\n- **repair で `output` を消す** — 合計が出なくなり (1) で落ちます。\n\n## 「何を計算したか」ではなく「何を見せたか」\n\nこの問題でプログラムが Python のソースではなく**操作のタプルの list** なのには理由があります。`reconstruct` という語を grep する監査ツールは、改名 1 つ、ラッパー 1 つ、ヘルパー 1 つで破られます。**実行が実際に何を観測可能にしたか**を読む監査ツールは破られません。ローカルな算術 (`mask` / `scale` / `addsh`) が 1 つも event を出さないのは、それが外から見えないからで、この非対称がこの問題全体の主題です。\n\n## 次への接続\n\n- **ac26-w2-private-aggregate** は、ここで監査した側のプロトコルを**自分で書きます**。そこでは「マスク済みの差分だけを開示する」が、主張ではなく実行時に測られる制約になります。同じ三つ組を 2 回使うと、開示した値が別の秘密と結び付いて漏れる —— それも「開けてよい値の集合と厳密に一致するか」で検査されます。\n- **ac26-w6-cosnark-privacy** では、同じ「途中で開けてはいけない」が ZK 証明の分散生成で再登場します。\n- 上流課題の toy-mpc で「どこまで開示してよいか」を判断するとき、根拠になるのがこの allowed 集合の考え方です。\n",
  "tags": [
    "advanced-cryptography-2026",
    "week2",
    "mpc",
    "privacy",
    "audit",
    "local-play",
    "container"
  ],
  "exposedPorts": [
    {
      "port": 18098,
      "name": "Participant Workbench: Portal editor API + /verify proxy (loopback only)"
    }
  ],
  "learningGoals": [
    "答えが合っていることと漏れていないことが別の性質だと、実行で示せる",
    "仕様から「見せてよい値」の一覧を自分で導ける",
    "秘密の開示・他人の手元ののぞき見・ログとエラーからの漏れを、区別して見つけられる",
    "仕様が公開している値を、違反だと言い立てない",
    "漏れた記録から実際に秘密を復元して、被害を実物で示せる",
    "違反だけを削り、正当な開示は残したまま直せる",
    "label を全部改名されても、同じ判定を出せる"
  ],
  "runtime": {
    "provider": "docker",
    "engine": "compose",
    "entry": "local/docker-compose.yml",
    "verifyUrl": "http://127.0.0.1:18098/verify",
    "secretEnv": [
      "FLAG_SEED"
    ]
  },
  "scoring": {
    "kind": "multi-verify",
    "checks": [
      {
        "id": "allowed-opens",
        "input": "multiline",
        "label": "仕様から公開してよい値を挙げる",
        "points": 35,
        "wrongAnswerPenalty": 15,
        "hints": [
          {
            "id": "allowed-opens-hint-1",
            "content": "監査の土台になる 1 手です。 仕様 (spec) から、 **このプロトコルが外に見せてよい label の集合**を作ります。 ここが 1 つでもずれると、 以降の違反判定が全部ずれます。 逆に言うと、 「違反」とはこの集合に入っていない label を見せることです。 返すのは **ソート済みの文字列 list** (集合ではありません)。",
            "penalty": 7
          },
          {
            "id": "allowed-opens-hint-2",
            "content": "spec の中で、 「見せても秘密が出ない」ものは 3 種類あります。 それぞれ理由が違います。\n\n- `publicInputs` — もともと公開されている値。 事前に全員が合意しているので、 開示しても新しい情報は出ない\n- `masked` — 一度きりのマスクで覆われた差分。 `x1 - m1` で m1 は誰も知らない乱数なので、 x1 について何も語らない (Beaver の d と e と同じ論法)\n- `result` — 出すと宣言したもの。 これを漏洩と呼ぶならプロトコルは何もできない\n\nそれ以外の label はすべて中間の秘密です。 なお `result` は **文字列ひとつ** で、 list ではありません —— そのまま `extend` すると 1 文字ずつばらけます。",
            "penalty": 5
          },
          {
            "id": "allowed-opens-hint-3",
            "content": "**allowed_opens(spec)** の手順:\n1. `spec[\"publicInputs\"]` の全要素を集合に入れる。\n2. `spec[\"masked\"]` の全要素を同じ集合に入れる。\n3. `spec[\"result\"]` を **1 個の要素として** 入れる (文字列なので、 展開しない)。\n4. `sorted(...)` で list にして返す。\n\n式: `sorted({*spec[\"publicInputs\"], *spec[\"masked\"], spec[\"result\"]})`\n\n例 (spec が publicInputs=[\"weight-p0\",\"weight-p1\",\"weight-p2\"], masked=[\"masked-p0\",\"masked-p1\",\"masked-p2\"], result=\"total\" のとき):\n```\n[\"masked-p0\", \"masked-p1\", \"masked-p2\", \"total\", \"weight-p0\", \"weight-p1\", \"weight-p2\"]\n```\nlabel の実際の名前は deploy ごとに変わります。 「証拠を確認」で spec の実物を見て、 自分の出力と突き合わせてください。",
            "penalty": 4
          }
        ]
      },
      {
        "id": "opened-secret",
        "input": "multiline",
        "label": "公開してよくない値の公開を見つける",
        "points": 45,
        "wrongAnswerPenalty": 15,
        "hints": [
          {
            "id": "opened-secret-hint-1",
            "content": "この checkpoint が混ぜて渡すのは、 **中間の秘密を 1 つ余計に開示するプログラム**と、 **問題のないプログラム**です。 求められているのは 2 つ —— 漏れる実行では `{\"kind\": ..., \"index\": ...}` を返し、 漏れない実行では **`None`** を返すこと。 「何でも違反と言う」実装は、 clean な実行で落ちます。 何も言わない実装と同じくらい重い誤りとして扱われます。",
            "penalty": 9
          },
          {
            "id": "opened-secret-hint-2",
            "content": "`open` event は「その label の値が全員に見えた」という記録です。 見えてよい label かどうかは、 前の checkpoint で作った allowed 集合で決まります —— **allowed に入っていない label を open したときだけ**、 それは中間の秘密の開示です。 部分和 (`sub` のような途中の値) がなぜ駄目かは、 writeup で数値を追うと分かりますが、 いまは「allowed に無い = 開けてはいけない」で十分です。\n\n返す `index` は **trace の先頭からの位置** (`enumerate` の添字)。 違反だけを数えた番号ではありません。 そして「first」なので、 先頭から見て最初に当たったところで止めます。",
            "penalty": 7
          },
          {
            "id": "opened-secret-hint-3",
            "content": "**first_violation(trace, spec)** を、 次のループとして書いてください。 これで 3 つの checkpoint (opened-secret / cross-party / log-leak) すべてが通ります。\n\n1. `allowed = set(allowed_opens(spec))` を最初に 1 回だけ作る。\n2. `for index, event in enumerate(trace):` で先頭から順に見る。\n3. `kind = event[\"kind\"]` を取り出し、 次の 4 つを**この順で**判定する。\n   - `kind == \"open\"` かつ `event[\"label\"] not in allowed`\n     -> `{\"kind\": \"opened-a-secret\", \"index\": index}` を返す\n   - `kind == \"peek\"` かつ `event[\"party\"] != event[\"owner\"]`\n     -> `{\"kind\": \"cross-party-read\", \"index\": index}` を返す\n   - `kind == \"emit\"` かつ `event[\"label\"] not in allowed`\n     -> `{\"kind\": \"leaked-in-log\", \"index\": index}` を返す\n   - `kind == \"fail\"` かつ `event[\"label\"] not in allowed`\n     -> `{\"kind\": \"leaked-in-error\", \"index\": index}` を返す\n4. どれにも当たらずにループが終わったら **`None`** を返す。\n\n重要な非違反 3 つ (ここを違反にすると clean な実行で落ちます):\n- `kind == \"output\"` は常に違反ではない (宣言された結果)\n- `peek` で `party == owner` は違反ではない (自分の領域を読んでいるだけ)\n- `emit` / `fail` でも label が allowed に入っていれば違反ではない (公開値をログに出しただけ)\n\n**label の文字列そのものを見ないでください。** 判定は必ず allowed 集合を通します。",
            "penalty": 5
          }
        ]
      },
      {
        "id": "cross-party",
        "input": "multiline",
        "label": "他人の手元を覗いた場所を見つける",
        "points": 45,
        "wrongAnswerPenalty": 15,
        "hints": [
          {
            "id": "cross-party-hint-1",
            "content": "ここで混ぜられるのは、 **ある party が別の party の生の share 領域を読むプログラム**と、 **問題のないプログラム**です。 この違反は transcript には何も残りません —— 何も開示していないからです。 見えるのはアクセスの記録 (`peek` event) だけ。 「出力も transcript も綺麗なのに漏れている」形の違反で、 だから監査に trace が要ります。",
            "penalty": 9
          },
          {
            "id": "cross-party-hint-2",
            "content": "`peek` event は 2 つの id を持ちます —— `party` (読んだ人) と `owner` (読まれた側)。 party が **自分の**領域を読むのは、 party として当たり前の動作で、 違反ではありません。 この checkpoint に混ぜられている clean なプログラムの 1 つがまさにそれで、 「`peek` を全部違反にする」実装はそこで落ちます。 違反になるのは `party != owner` のときだけです。\n\nなお `peek` event の `label` は空文字です。 label で判定しようとすると必ず外れます。",
            "penalty": 7
          },
          {
            "id": "cross-party-hint-3",
            "content": "**first_violation(trace, spec)** を、 次のループとして書いてください。 これで opened-secret / cross-party / log-leak の 3 つとも通ります。\n\n1. `allowed = set(allowed_opens(spec))` を最初に 1 回だけ作る。\n2. `for index, event in enumerate(trace):` で先頭から順に見る。\n3. `kind = event[\"kind\"]` について、 次を**この順で**判定して、 当たったらすぐ返す。\n   - `\"open\"` で `event[\"label\"] not in allowed` -> `{\"kind\": \"opened-a-secret\", \"index\": index}`\n   - `\"peek\"` で `event[\"party\"] != event[\"owner\"]` -> `{\"kind\": \"cross-party-read\", \"index\": index}`\n   - `\"emit\"` で `event[\"label\"] not in allowed` -> `{\"kind\": \"leaked-in-log\", \"index\": index}`\n   - `\"fail\"` で `event[\"label\"] not in allowed` -> `{\"kind\": \"leaked-in-error\", \"index\": index}`\n4. 最後まで当たらなければ **`None`**。\n\n違反ではないもの:\n- `peek` で `party == owner` (自分の領域を読んだだけ)\n- `emit` / `fail` で label が allowed に入っている (公開値をログに出しただけ)\n- `output` (宣言された結果)\n\n`index` は trace 全体での位置です。 mutation checkpoint では独立な開示が動かされるので、 位置を覚えず毎回 `enumerate` で数えてください。",
            "penalty": 5
          }
        ]
      },
      {
        "id": "log-leak",
        "input": "multiline",
        "label": "log と失敗経路から漏れた値を見つける",
        "points": 45,
        "wrongAnswerPenalty": 15,
        "hints": [
          {
            "id": "log-leak-hint-1",
            "content": "ここで混ぜられるのは 4 つです。 **生の秘密をログ行に出すプログラム**、 **エラーメッセージに秘密を載せるプログラム**、 そして **公開値をログに出しただけの問題ないプログラム**と、 まったく問題のないプログラム。 ログとエラーは脅威モデルの中にあります (ログは保存され、 転送され、 別の権限の人に読まれます)。 一方で、 **ログ行だから漏洩、 ではありません**。 その区別ができるかどうかがこの checkpoint です。",
            "penalty": 9
          },
          {
            "id": "log-leak-hint-2",
            "content": "`emit` (ログ行) と `fail` (エラー経路) は、 どちらも `label` と `value` を持ちます。 判定は open のときと**まったく同じ基準**です —— その label が allowed 集合に入っていれば、 出しても新しい情報は出ません。 入っていなければ、 中間の秘密がログに出たことになります。\n\n違反の種類名だけが分かれます: `emit` なら `leaked-in-log`、 `fail` なら `leaked-in-error`。 なお `fail` が厄介なのは、 **正常系は完全に綺麗**なことです。 エラー経路を通ったときにだけ漏れるので、 正しさだけを見るレビューは必ず通してしまいます。",
            "penalty": 7
          },
          {
            "id": "log-leak-hint-3",
            "content": "**first_violation(trace, spec)** を、 次のループとして書いてください。 これで opened-secret / cross-party / log-leak の 3 つとも通ります。\n\n1. `allowed = set(allowed_opens(spec))` を最初に 1 回だけ作る。\n2. `for index, event in enumerate(trace):` で先頭から順に見る。\n3. `kind = event[\"kind\"]` について、 次を**この順で**判定して、 当たったらすぐ返す。\n   - `\"open\"` で `event[\"label\"] not in allowed` -> `{\"kind\": \"opened-a-secret\", \"index\": index}`\n   - `\"peek\"` で `event[\"party\"] != event[\"owner\"]` -> `{\"kind\": \"cross-party-read\", \"index\": index}`\n   - `\"emit\"` で `event[\"label\"] not in allowed` -> `{\"kind\": \"leaked-in-log\", \"index\": index}`\n   - `\"fail\"` で `event[\"label\"] not in allowed` -> `{\"kind\": \"leaked-in-error\", \"index\": index}`\n4. 最後まで当たらなければ **`None`**。\n\n**ログ行を無条件に違反にしないこと。** 公開値をログに出しただけのプログラムが混ざっており、 それを違反と報告すると落ちます。 `text` の中身 (「debug: share」など) は判定に使いません —— 文言はいくらでも変えられるからです。 見るのは `label` が allowed に入っているかどうかだけ。",
            "penalty": 5
          }
        ]
      },
      {
        "id": "transcript",
        "input": "multiline",
        "label": "漏れた transcript から秘密を復元する",
        "points": 40,
        "wrongAnswerPenalty": 15,
        "hints": [
          {
            "id": "transcript-hint-1",
            "content": "ここだけは「指摘」ではなく **実害** を出させる checkpoint です。 余計に 1 つ開示してしまった実行の transcript を渡すので、 そこから **誰かの私的な値を実際に復元**してください。 「開けすぎだ」と言うのと、 「その結果この人の値がこうなる」と見せるのとでは、 説得力がまったく違います。 返すのは `{\"party\": <参加者 id>, \"value\": <その人の私的な値>}` で、 party も value も正しくなければ通りません。",
            "penalty": 8
          },
          {
            "id": "transcript-hint-2",
            "content": "`transcript` は `{\"label\": ..., \"value\": ...}` の list です。 中には (1) 仕様が許す開示、 (2) **許していない余計な開示**、 (3) 最終結果 が入っています。 (2) は「途中の部分和」で、 参加者の一部ぶんだけを合計した値です。\n\n必要な算術は 2 つだけ。 **引き算 1 回**と **逆元 1 回**です。\n\n- 最終結果から部分和を引くと、 部分和に含まれていなかった参加者の**重み付き寄与**が残る\n- 重みは `publicInputs` に入っている **公開値**で、 その値も transcript に出ています\n- p は素数なので、 その重みには逆元があります (`pow(weight, -1, p)`)\n\n最後の参加者 (`spec[\"parties\"]` の末尾) と、 最後の公開重み (`spec[\"publicInputs\"]` の末尾) が対応しています。",
            "penalty": 6
          },
          {
            "id": "transcript-hint-3",
            "content": "**derive_secret(transcript, spec)** の手順:\n1. `seen = {entry[\"label\"]: entry[\"value\"] for entry in transcript}` で label -> 値の辞書を作る。\n2. `allowed = set(allowed_opens(spec))` を作る。\n3. `seen` のキーのうち **allowed に入っていないもの**を探す。 それが余計に開示された部分和の label。 見つからなければ `{}` を返してよい。\n4. `partial = seen[その label]`、 `total = seen[spec[\"result\"]]` を取る。\n5. `party = spec[\"parties\"][-1]` (最後の参加者)、 `weight_label = spec[\"publicInputs\"][-1]`、 `weight = seen[weight_label]`。\n6. `contribution = (total - partial) % p` —— これがその参加者の「重み × 私的な値」。\n7. `value = contribution * pow(weight, -1, p) % p` —— 重みで割って私的な値を取り出す。\n8. `{\"party\": party, \"value\": value}` を返す。\n\n数値検算 (p=1009, 重み (3,5,7), 私的な値 (100,200,300)):\n```\ntotal   = 3*100 + 5*200 + 7*300 = 3400、 3400 % 1009 = 373\npartial = 3*100 + 5*200 = 1300、 1300 % 1009 = 291\ncontribution = (373 - 291) % 1009 = 82        (= 7*300 % 1009)\n7 の逆元: 7*144 = 1008 = -1 なので 7^-1 = 1009-144 = 865\nvalue = 82 * 865 % 1009 = 70930 % 1009 = 300  OK  (p2 の私的な値)\n```\np と重みと参加者数は deploy ごとに変わるので、 すべて spec と transcript から取ってください。",
            "penalty": 5
          }
        ]
      },
      {
        "id": "repair",
        "input": "multiline",
        "label": "漏れだけを取り除く",
        "points": 50,
        "wrongAnswerPenalty": 15,
        "hints": [
          {
            "id": "repair-hint-1",
            "content": "漏れを見つけたら、 次は直します。 渡される `program` は Python のソースではなく **操作のタプルの list** で、 同じ形の list を返します。 課される条件は 3 つ。 (1) 直した後も同じ正しい合計を出す、 (2) もう漏れない、 (3) **仕様が許している開示は全部残っている**。 (3) が無いと「プロトコルを全部消す」が最強の修理になってしまいます —— 何も実行しなければ何も漏れませんが、 それは修理ではありません。",
            "penalty": 10
          },
          {
            "id": "repair-hint-2",
            "content": "修理は **足す**のではなく **削る**操作です。 違反しているちょうどその操作だけを取り除き、 それ以外は順序も含めてそのまま残します。\n\nどの操作が違反かの基準は、 `first_violation` とまったく同じです —— ただし今回は event ではなく **操作のタプル**を見ます。 タプルの 0 番目が kind、 それ以降の位置が形ごとに違うので、 problem 文の 10 種類の形の表を見ながら、 `open` / `peek` / `emit` / `fail` の 4 種類だけを判定してください。 `share` / `const` / `mask` / `scale` / `addsh` はローカルな算術で、 event を 1 つも出さないので削ってはいけません。 `output` も残します (削ると合計が出なくなります)。",
            "penalty": 8
          },
          {
            "id": "repair-hint-3",
            "content": "**repair(program, spec)** の手順:\n1. `allowed = set(allowed_opens(spec))` を作る。\n2. 空の list `out` を用意し、 `program` を先頭から順に見る。\n3. 各操作 `op` について `kind = op[0]` を取り、 次に当たるものは **`out` に入れずに飛ばす**。\n   - `kind == \"open\"` かつ `op[1] not in allowed`     (中間の秘密の開示)\n   - `kind == \"peek\"` かつ `op[1] != op[2]`            (`op[1]` が読んだ人、 `op[2]` が読まれた側)\n   - `kind in (\"emit\", \"fail\")` かつ `op[1] not in allowed`  (秘密がログ / エラーに載った)\n4. それ以外はすべて `out.append(op)` でそのまま残す。\n5. `out` を返す。\n\n削ってはいけないもの:\n- `share` / `const` / `mask` / `scale` / `addsh` —— ローカルな算術。 event を出さないので漏れない。 削ると合計が出なくなる\n- `open` で label が allowed に入っているもの —— 合法な開示。 削ると条件 (3) で落ちる\n- `peek` で `op[1] == op[2]` —— 自分の領域を読んでいるだけ\n- `emit` / `fail` で label が allowed に入っているもの —— 公開値をログに出しただけ\n- `output` —— 宣言された結果。 削ると条件 (1) で落ちる\n\n順序は変えないでください。 元の順序のまま、 違反する操作だけが抜けた list が正解です。",
            "penalty": 6
          }
        ]
      },
      {
        "id": "mutation",
        "input": "multiline",
        "label": "名前を変えられても同じ判定を出す",
        "points": 40,
        "wrongAnswerPenalty": 15,
        "hints": [
          {
            "id": "mutation-hint-1",
            "content": "**新しく書くものはありません。** 同じ 7 つのプロトコルを、 **label を全部改名し**、 **互いに独立な開示を後ろへ動かして**もう一度走らせます。 プロトコルとしては何も変わっていないので、 判定も 1 つも変わってはいけません。 確かめているのは 1 点 —— あなたの監査ツールが、 **label の文字列や event の位置を覚えていないか**。",
            "penalty": 8
          },
          {
            "id": "mutation-hint-2",
            "content": "改名で壊れるのは、 label を**中身で**判定している実装です。 `\"x-\"` で始まるものを秘密とみなす、 `\"weight\"` を含むものを公開とみなす、 `\"total\"` という文字列を探す —— どれも改名の瞬間に崩れます。 判定は必ず **spec から作った allowed 集合**を通してください。 spec 自体も同じ改名を受けているので、 allowed を毎回 spec から作り直していれば何も起きません。\n\n並べ替えで壊れるのは、 event の位置を覚えている実装です。 「違反はいつも 12 番目」のような仮定や、 特定の index から探し始める実装。 `index` は毎回 `enumerate` で数えてください。",
            "penalty": 6
          },
          {
            "id": "mutation-hint-3",
            "content": "自分の 4 つの関数を、 次の観点で 1 つずつ点検してください。 すべて満たしていればそのまま提出で通ります。\n\n1. **allowed_opens** —— 引数の `spec` の `publicInputs` / `masked` / `result` からだけ作っているか。 特定の文字列 (`\"total\"` など) を書き込んでいないか。\n2. **first_violation** —— 毎回 `allowed_opens(spec)` を呼び直しているか (前回の結果をモジュール変数に覚えていないか)。 `index` を `enumerate` で毎回数えているか。 判定に使っているのは `event[\"kind\"]` / `event[\"label\"] in allowed` / `event[\"party\"] != event[\"owner\"]` の 3 つだけか。 `event[\"text\"]` の中身で判定していないか。\n3. **derive_secret** —— label を `spec[\"result\"]` と `spec[\"publicInputs\"][-1]` から引いているか。 文字列を書き込んでいないか。\n4. **repair** —— 同じく allowed 集合だけで判定しているか。 操作の順序を変えていないか。\n\n**判定に使ってよいのは 3 つだけ**です: `kind`、 `label` が allowed に入るか、 `party` と `owner` が一致するか。 それ以外 (文字列の中身、 出現位置、 event の個数) を使うと mutation で落ちます。",
            "penalty": 5
          }
        ]
      }
    ]
  },
  "i18n": {
    "en": {
      "name": "The answer is right. That is all it is",
      "shortDescription": "Seven implementations all return the correct total, and four of them still show something they should not along the way. Correctness tests tell you nothing here — so you write the auditor that finds the difference.",
      "description": "Fourth problem of Week 2; its role is transfer. Correctness and privacy are different properties — no amount of tests for the former guarantees the latter — and this problem confirms that in an implementation.\n\nThe material is a three-party weighted-sum MPC. Programs are given not as Python source but as an operation list (op list) that a runtime executes, emitting an observable event stream. That is the design keystone: an auditor that greps for the word `reconstruct` is defeated by renames, wrappers and helpers, but an auditor that reads the operations actually executed is not. Making the program data instead of code is what lets that difference be graded.\n\nAll seven implementations return the correct total. Four carry violations (opening a secret / reading another party's share / raw values in logs / secrets in error messages); three are clean. The clean side carries two false-positive traps — an implementation that logs a value the spec makes public, and a party that reads its own slot. An auditor that calls everything suspicious finds all the real ones and still fails.\n\nSeven checkpoints: deriving the allowed-open set, detecting the three violation kinds, recovering a secret from the transcript (a counterexample), the repair, and re-auditing under renames, reordering and an unseen seed. The repair check rules out \"delete it and nothing leaks\" by also verifying that every observation the spec allows is still present.\n\nThe threat model is honest-but-curious with no collusion. No production security claim is made; only the leakage contract observable in the toy simulator is graded.\n\nWeek 2's course material was unpublished at the pinned commit, so sources is a placeholder pin and status stays draft (finalised when #219 detects publication).",
      "instructions": "## A correct answer is not an alibi\n\nIn 2023, flaws were disclosed in several MPC wallets' signing libraries, one after another (BitForge, TSSHOCK). Every signature came out valid. Every test was green. And yet, following the values those protocols showed during their exchanges, an insider could work out other parties' key shares — because being right and having shown nothing extra along the way are **different properties**.\n\nYou can get every answer right on a test and still hand your secrets over by letting your neighbour read your rough paper. The accident always happens on the rough paper.\n\n## The difference never reaches the output\n\nSeven implementations of the same \"secret risk score\" protocol have been handed in. **All seven return the correct total.** Four of them show something along the way that they should not. Comparing outputs tells you nothing about which is which — every correctness test passes on all seven.\n\nUntil now you were the one computing correctly; here you switch sides. What you write is the **auditor** that finds the difference, and the only things it may judge on are the specification (what may be shown) and the record of what became observable from outside.\n\n## Before you start (skip if you know this)\n\n- Every number lives as the remainder after dividing by a prime p. If this is new, solve `ac26-w1-constraint-lab` first.\n- A value covered by a one-off arbitrary number, `x + r`, says nothing to someone who does not know r: with p = 7, covering x = 3 with 5 gives 1 (= 8 mod 7), and shown that 1, x could still be any of 0 .. 6. It is the same reasoning that let `ac26-w2-beaver-mul` publish d = x − a.\n- Division here means multiplying by the partner that turns w into 1 — in Python, `pow(w, -1, p)`. One of the seven checkpoints needs it.\n- Shares and opening are as in `ac26-w2-secret-sharing`.\n\n## All you hold is a specification and a record\n\n### The specification (spec)\n\nA dictionary handed to your auditor. It contains no secret values. It always has exactly these five keys:\n\n```\n{\"p\": 1009,                            the modulus (prime)\n \"parties\": [\"p0\", \"p1\", \"p2\"],        the participants' ids\n \"publicInputs\": [\"weight-p0\", ...],   labels of values that were public from the start\n \"masked\": [\"masked-p0\", ...],         labels of values under a one-off cover\n \"result\": \"total\"}                    the declared result label (one string, not a list)\n```\n\n**What may be shown (allowed)** is those three put together: all of `publicInputs`, all of `masked`, and the single `result`. Each is allowed for a different reason. Public inputs were public already. Covered values say nothing about their contents, as the one-digit example showed. The result is what the protocol declared it would produce. **Every other label is an intermediate secret**, and showing one is over-opening.\n\n### The record (trace)\n\nThe list of what became observable from outside. Purely local addition and multiplication produce no entries at all — a protocol is judged on what it showed, not on how much it computed. Every element is a dictionary carrying all six of these keys (unused ones hold `\"\"` or `0`):\n\n```\nkind    \"open\" | \"peek\" | \"emit\" | \"fail\" | \"output\"\nlabel   the name of the value the event touched   (empty for peek)\nparty   who read                                  (peek only)\nowner   who was read                              (peek only)\ntext    the log / error wording                   (emit / fail only)\nvalue   the number that actually got out\n```\n\n`open` means that label's value became visible to everyone; `peek` means `party` read `owner`'s share storage directly; `emit` means a log line carrying that label's value appeared; `fail` means an error message carrying that label's value appeared; `output` means the declared result was produced.\n\nThe four violation names are `opened-a-secret`, `cross-party-read`, `leaked-in-log` and `leaked-in-error`, and they sit at the top of `auditor.py` as the constant `VIOLATIONS`.\n\n## Crying wolf weighs as much as missing the leak\n\nGrading mixes runs that leak with runs that do not — and the non-leaking side contains the most confusable thing there is: openings of covered values. Showing x = 3 itself is a violation. Showing the covered 1 is innocent. Whether it became visible on a screen, in a log line or in an error message changes nothing about which is which. **Calling an innocent value a violation is exactly as wrong as missing a real leak** — an auditor that calls everything a violation is as useless as one that never says anything.\n\nOne more thing. Label names change on every start; in the last checkpoint they are all renamed and the order is shuffled. The only admissible grounds for a verdict are **`kind`, whether that `label` is in allowed, and whether `party` and `owner` are the same.** Judging on wording, position, or event counts fails there.\n\n## First move\n\nIn Participant Portal (the site showing this statement), select **起動 (Start)**; the **problem editor** appears on the same page as the statement. You do not need to open a terminal on your own machine.\n\nPress **Inspect evidence** in the editor. It shows:\n\n1. **A health token** — a string proving the container ran. It is not part of any submission.\n2. **The modulus p and the participants' ids.**\n3. **The specification itself** — the same dictionary your auditor is handed, printed as it is. **It changes on every start.**\n4. **The events of a run that leaks nothing** — numbered, one per line.\n\nDo two things on paper. First, from the spec in (3), **write out every label that is in allowed**. Then walk the events in (4) from the top and check, for each one, whether its `label` is on your list. The leaking runs are not shown, so **knowing what a clean run looks like** is the only foothold you get.\n\n## What you answer — seven checkpoints, in this order\n\nThe editor holds one file, `auditor.py`, already scaffolded with four functions; each docstring repeats its return shape, how it is graded, and an order to think in. No checkpoint here is typed JSON — all seven submit this same one file.\n\nThe four functions you write (arguments → return value):\n\n- `allowed_opens(spec)` → **a sorted list of strings**: every label this specification may show. Get this even slightly wrong and everything downstream is wrong with it.\n- `first_violation(trace, spec)` → `{\"kind\": <one of the four violation names>, \"index\": <position in the trace>}` when something leaked, and **`None` when nothing did**. `index` counts the whole trace from 0, not just the violations. \"First\" means: stop at the first one found, scanning from the top.\n- `derive_secret(transcript, spec)` → `{\"party\": <a participant id string>, \"value\": <an integer in 0 .. p − 1>}`. The `transcript` argument has a **different shape** from a trace: each element is a dictionary with only the two keys `{\"label\": ..., \"value\": ...}`.\n- `repair(program, spec)` → **a list of operation tuples of the same shape**. A `program` is not Python source but a list of operation tuples, and these ten forms are all there are:\n\n```\n(\"share\",  label, owner)      owner's secret becomes the share called label\n(\"const\",  label, value)      a public constant\n(\"mask\",   out, val, msk)     out = val - msk   (local; nothing leaks)\n(\"scale\",  out, val, k)       out = val * k     (k is public; local)\n(\"addsh\",  out, x, y)         out = x + y       (local)\n(\"open\",   label)             label's value becomes visible to everyone\n(\"peek\",   party, owner)      party reads owner's share storage\n(\"emit\",   label, text)       a log line carrying label's value\n(\"fail\",   label, text)       an error message carrying label's value\n(\"output\", label)             the declared result is produced\n```\n\n1. **allowed-opens** (code) — `allowed_opens`. The foundation of the audit.\n2. **opened-secret** (code) — `first_violation`, tried against a set of runs covering the case where a value itself became visible to everyone.\n3. **cross-party** (code) — `first_violation`, against a set covering the case where somebody read another party's storage.\n4. **log-leak** (code) — `first_violation`, against a set covering leaks through the log and error paths.\n5. **transcript** (code) — `derive_secret`. Saying \"that was over-opened\" and showing \"and here is the value it hands you\" carry very different weight; this checkpoint demands the second. The `party` must be a real participant id and the `value` must match that participant's own value **exactly**.\n6. **repair** (code) — `repair`. Repair here means **removing**, not adding. Three things are required: the repaired program still produces the same correct total, it no longer leaks, and **every opening the specification allows is still there**. Without that third condition, \"delete everything\" would be the strongest repair.\n7. **mutation** (code) — nothing new to write. The same protocols run again with **every label renamed and the independent openings moved**. Whether a run leaks, and which violation it is, must come out the same as before. An auditor keyed on literal text or on where a violation sat last time disagrees with itself here.\n\nCheckpoints 2, 3 and 4 all look at `first_violation`, but with different sets of runs. One being green while the others are red is normal — treat the three as three separate clues.\n\n### How submitting works\n\nThe seven checkpoints are **submitted separately, as many times as you like**. Pressing **Submit** sends **the current contents of `auditor.py` in the editor** as it stands and grades it — nothing to copy or paste. All four functions live in that one file, so you can submit checkpoint 1 as soon as `allowed_opens` is written. Each wrong submission costs a small penalty, so checking the shape with **Run public tests** first is worth it. **Restore starter** puts the editor back to its initial state.\n\n## Tomorrow this eye points at your own code\n\nPass all seven and you are done. `ac26-w2-private-aggregate` has a checkpoint called privacy, where the set of values **your own implementation** opens is itself the thing being graded. The auditor's eye you built today turns on your own protocol next. And BitForge and TSSHOCK were found not by correctness tests, but by exactly this kind of audit.\n",
      "writeup": "## What this problem measures\n\n**Producing the right answer and behaving correctly are different things.** All seven programs return the same correct total. The output tells you nothing about which of them leaks. Only the trace does — the sequence of things a run made visible.\n\nAnd an auditor can be wrong in two directions, **both equally fatal**:\n\n- **an auditor that flags nothing** passes every clean run and finds no leak;\n- **an auditor that flags everything** finds every leak and condemns every clean run.\n\nSo every checkpoint here mixes leaking and clean programs, and the clean ones are the awkward kind: a log line carrying a value the specification publishes, and a party reading its own slot. Reporting a violation on those is as wrong as missing a real one.\n\n## The definition of \"may be opened\"\n\nallowed = `publicInputs` ∪ `masked` ∪ `{result}`, and each of the three is safe for its own reason.\n\n- **publicInputs** — values that were public to begin with (the weights). Everyone agreed on them in advance, so revealing them adds nothing.\n- **masked** — differences behind a one-time mask. `masked-p1 = x1 - m1` where m1 is a random value nobody knows, so the value says nothing about x1. This is the same argument as the previous two problems (Beaver's d and e are exactly this).\n- **result** — what the protocol declared it would output. If a declared output counted as leakage, no protocol could do anything.\n\n**Every other label is an intermediate secret.** `x-p1` (someone's raw input) and `sub` (a partial sum) alike: opening either is over-opening.\n\n\"But a partial sum is basically the total, so surely it is safe?\" — **No**, and this problem makes you check that by hand.\n\n## One partial sum and a secret falls out (worked)\n\nTake p = 1009, three parties, weights w = (3, 5, 7), private values x = (100, 200, 300).\n\n```\ntotal = 3*100 + 5*200 + 7*300 = 300 + 1000 + 2100 = 3400\n        3400 % 1009 = 373\nsub   = 3*100 + 5*200 = 1300, and 1300 % 1009 = 291\n```\n\nSuppose the protocol opens `sub` in addition to `total`. Then\n\n```\ntotal - sub = (373 - 291) % 1009 = 82       which is w2 * x2\n                                            (check: 7*300 = 2100, 2100 % 1009 = 82)\n```\n\nThe weight 7 is **public**, and p is prime so it has an inverse. From 7*144 = 1008 = -1 (mod 1009) we get 7^-1 = 1009-144 = 865. Therefore\n\n```\nx2 = 82 * 865 % 1009 = 70930 % 1009 = 300\n```\n\n**p2's private value, in the clear.** Saying \"you opened one thing too many\" and showing \"and here is what that gives me\" are not equally persuasive. That is why the derive_secret checkpoint demands the arithmetic.\n\n## What each checkpoint protects\n\n- **allowed-opens** — the foundation of the audit. One label wrong here and the next three checkpoints are all wrong. Note that `result` is a single string, not a list (`{*publicInputs, *masked, result}` is the shape).\n- **opened-secret / cross-party / log-leak** — all three call the same `first_violation` on different mixes of programs. Both **kind and position** are required, because an auditor that says only \"something leaked\" has localized nothing. The position is an index into the trace, and it moves between runs.\n- **transcript** — the recovery above. This is the one checkpoint asking for the harm rather than the accusation.\n- **repair** — a repaired program must satisfy three conditions: (1) still compute the correct total, (2) no longer leak, and (3) **still perform every opening the specification allows**. Without (3), deleting the whole protocol would be the strongest repair: run nothing and nothing leaks. That is not a repair.\n- **mutation** — the same protocols with every label renamed and the independent openings moved. Nothing about the protocol changed, so nothing about the verdict may change. An auditor keyed on literal strings like `\"x-p1\"`, or one that memorized \"the violation is at index 12\", disagrees with itself here.\n\n## How it breaks when you get it wrong\n\n- **Treating every log line as a leak** — a log line carrying a value the specification publishes (a weight) becomes a violation. Reporting a clean run is failed with the same weight as missing a leak. **The criterion is not \"is it a log line\" but \"is the label on it in the allowed set\".**\n- **Treating every share read as a leak** — a party reading **its own** slot is the ordinary thing for a party to do. A `peek` is a violation only when `party != owner`.\n- **Treating `output` as a leak** — it is the declared result, and `result` is in the allowed set.\n- **Returning the last violation rather than the first** — it is *first*: scan from the start and stop at the first hit.\n- **Miscounting the index** — the index is the position in the whole trace, not a count of violations.\n- **Depending on label text** — an implementation that treats anything starting with `\"x-\"` as secret dies the moment mutation renames the labels. Every decision must go through the spec's allowed set.\n- **Over-trimming in repair** — deleting a legal `open` fails condition (3). Only the offending operations may go.\n- **Deleting `output` in repair** — the total is no longer produced, failing condition (1).\n\n## Judged by what it revealed, not by what it computed\n\nThere is a reason the programs here are **lists of operation tuples** rather than Python source. An auditor that greps for the word `reconstruct` is defeated by a rename, a wrapper, or a helper. An auditor that reads **what a run actually made observable** is not. Local arithmetic (`mask`, `scale`, `addsh`) emits no events at all because it is invisible from outside, and that asymmetry is the whole subject.\n\n## Where this leads\n\n- **ac26-w2-private-aggregate** puts you on the other side: you write the protocol being audited. There \"reveal only the masked differences\" stops being a claim and becomes a constraint measured at run time. Reusing one triple twice ties an opened value to a second secret, and that too is caught by requiring an exact match against the set of values that may be opened.\n- **ac26-w6-cosnark-privacy** brings the same \"must not be opened mid-way\" back inside distributed generation of a ZK proof.\n- When the upstream toy-mpc asks how much a protocol may reveal, this allowed-set reasoning is the justification.\n",
      "learningGoals": [
        "Show in a running system that a correct answer and a leak-free run are different things",
        "Derive from a specification the full list of values it allows to be shown",
        "Find and tell apart opened secrets, cross-party reads, and leaks through logs and errors",
        "Never call a value the specification publishes a violation",
        "Recover an actual secret from a leaking record, showing the harm in the flesh",
        "Repair by removing only the violation, keeping every legitimate opening",
        "Return the same verdict after every label has been renamed"
      ],
      "checks": [
        {
          "id": "allowed-opens",
          "label": "Name what the specification allows to be revealed",
          "hints": [
            {
              "id": "allowed-opens-hint-1",
              "content": "This is the foundation of the audit: from the specification, build **the set of labels this protocol is permitted to reveal**. One label wrong here and every later verdict is wrong. Put the other way round: a violation *is* revealing a label that is not in this set. Return a **sorted list of strings** (not a set)."
            },
            {
              "id": "allowed-opens-hint-2",
              "content": "Three kinds of thing in the spec are safe to reveal, each for its own reason.\n\n- `publicInputs` — public from the start. Everyone agreed on them in advance, so revealing them adds nothing.\n- `masked` — differences behind a one-time mask. `x1 - m1` with m1 a random value nobody knows says nothing about x1 (the same argument as Beaver's d and e).\n- `result` — what the protocol declared it would output. If that counted as leakage no protocol could do anything.\n\nEverything else is an intermediate secret. Note that `result` is a **single string**, not a list — `extend`ing with it splits it into characters."
            },
            {
              "id": "allowed-opens-hint-3",
              "content": "**allowed_opens(spec)**, step by step:\n1. Put every element of `spec[\"publicInputs\"]` into a set.\n2. Put every element of `spec[\"masked\"]` into the same set.\n3. Put `spec[\"result\"]` in as **one element** (it is a string; do not spread it).\n4. Return `sorted(...)` as a list.\n\nFormula: `sorted({*spec[\"publicInputs\"], *spec[\"masked\"], spec[\"result\"]})`\n\nExample (with publicInputs=[\"weight-p0\",\"weight-p1\",\"weight-p2\"], masked=[\"masked-p0\",\"masked-p1\",\"masked-p2\"], result=\"total\"):\n```\n[\"masked-p0\", \"masked-p1\", \"masked-p2\", \"total\", \"weight-p0\", \"weight-p1\", \"weight-p2\"]\n```\nThe actual label names change per deployment. Look at the real spec under Inspect evidence and compare it with your output."
            }
          ]
        },
        {
          "id": "opened-secret",
          "label": "Find a value that should not have been revealed",
          "hints": [
            {
              "id": "opened-secret-hint-1",
              "content": "This checkpoint mixes programs that **open one intermediate secret too many** with programs that are fine. Two things are required: return `{\"kind\": ..., \"index\": ...}` on a leaking run, and **`None`** on a clean one. An implementation that flags everything fails on the clean runs, and that is treated as exactly as serious as one that flags nothing."
            },
            {
              "id": "opened-secret-hint-2",
              "content": "An `open` event records that the value behind a label became visible to everyone. Whether that was permitted is decided by the allowed set from the previous checkpoint: **only when the opened label is not in allowed** is it the disclosure of an intermediate secret. Why a partial sum (a mid-computation value like `sub`) is unsafe is worked through in the writeup; for now, 'not in allowed' is enough.\n\nThe `index` you return is the **position from the start of the trace** (an `enumerate` index), not a count of violations. And it is *first*: scan from the beginning and stop at the first hit."
            },
            {
              "id": "opened-secret-hint-3",
              "content": "Write **first_violation(trace, spec)** as this loop. It passes all three of opened-secret, cross-party and log-leak.\n\n1. Build `allowed = set(allowed_opens(spec))` once, up front.\n2. Iterate from the start with `for index, event in enumerate(trace):`.\n3. Take `kind = event[\"kind\"]` and test these four **in order**:\n   - `kind == \"open\"` and `event[\"label\"] not in allowed`\n     -> return `{\"kind\": \"opened-a-secret\", \"index\": index}`\n   - `kind == \"peek\"` and `event[\"party\"] != event[\"owner\"]`\n     -> return `{\"kind\": \"cross-party-read\", \"index\": index}`\n   - `kind == \"emit\"` and `event[\"label\"] not in allowed`\n     -> return `{\"kind\": \"leaked-in-log\", \"index\": index}`\n   - `kind == \"fail\"` and `event[\"label\"] not in allowed`\n     -> return `{\"kind\": \"leaked-in-error\", \"index\": index}`\n4. If the loop finishes with no hit, return **`None`**.\n\nThree things that are explicitly *not* violations (call them violations and the clean runs fail you):\n- `kind == \"output\"` is never a violation (it is the declared result)\n- a `peek` with `party == owner` is not a violation (a party reading its own slot)\n- an `emit` / `fail` whose label *is* in allowed is not a violation (a public value in a log line)\n\n**Never inspect the label text itself.** Every decision goes through the allowed set."
            }
          ]
        },
        {
          "id": "cross-party",
          "label": "Find where somebody looked at another's slot",
          "hints": [
            {
              "id": "cross-party-hint-1",
              "content": "This checkpoint mixes programs where **one party reads another party's raw share slot** with clean ones. This violation leaves nothing in the transcript, because nothing was opened. Only the access record — the `peek` event — shows it. It is the shape of leak where the output and the transcript are both spotless, which is exactly why an audit needs the trace."
            },
            {
              "id": "cross-party-hint-2",
              "content": "A `peek` event carries two ids: `party` (who read) and `owner` (whose slot). A party reading **its own** slot is ordinary and is not a violation. One of the clean programs mixed into this checkpoint is exactly that, and an implementation that flags every `peek` fails there. It is a violation only when `party != owner`.\n\nNote also that a `peek` event's `label` is the empty string; deciding on the label will always be wrong here."
            },
            {
              "id": "cross-party-hint-3",
              "content": "Write **first_violation(trace, spec)** as this loop; it passes opened-secret, cross-party and log-leak alike.\n\n1. Build `allowed = set(allowed_opens(spec))` once, up front.\n2. Iterate from the start with `for index, event in enumerate(trace):`.\n3. For `kind = event[\"kind\"]`, test these **in order** and return on the first hit:\n   - `\"open\"` with `event[\"label\"] not in allowed` -> `{\"kind\": \"opened-a-secret\", \"index\": index}`\n   - `\"peek\"` with `event[\"party\"] != event[\"owner\"]` -> `{\"kind\": \"cross-party-read\", \"index\": index}`\n   - `\"emit\"` with `event[\"label\"] not in allowed` -> `{\"kind\": \"leaked-in-log\", \"index\": index}`\n   - `\"fail\"` with `event[\"label\"] not in allowed` -> `{\"kind\": \"leaked-in-error\", \"index\": index}`\n4. If nothing hits, return **`None`**.\n\nNot violations:\n- a `peek` with `party == owner` (reading your own slot)\n- an `emit` / `fail` whose label is in allowed (a public value in a log line)\n- `output` (the declared result)\n\n`index` is the position in the whole trace. The mutation checkpoint moves the independent openings, so never memorize a position — count it with `enumerate` every time."
            }
          ]
        },
        {
          "id": "log-leak",
          "label": "Find what escaped through a log or a failure",
          "hints": [
            {
              "id": "log-leak-hint-1",
              "content": "Four kinds of program are mixed here: one that **writes a raw secret into a log line**, one that **carries a secret in an error message**, one that **logs a value the specification already publishes** (and is therefore fine), and one that is clean throughout. Logs and errors are inside the threat model — logs are stored, shipped, and read by people with different permissions. But **being a log line does not make it a leak**, and telling those apart is what this checkpoint is."
            },
            {
              "id": "log-leak-hint-2",
              "content": "`emit` (a log line) and `fail` (an error path) both carry a `label` and a `value`. The criterion is **exactly the one used for open**: if the label is in the allowed set, publishing it adds nothing; if it is not, an intermediate secret just went into a log.\n\nOnly the violation name differs: `emit` gives `leaked-in-log`, `fail` gives `leaked-in-error`. What makes `fail` nasty is that **the happy path is perfectly clean** — the leak only happens on the error path, which is why a correctness-only review always passes it."
            },
            {
              "id": "log-leak-hint-3",
              "content": "Write **first_violation(trace, spec)** as this loop; it passes opened-secret, cross-party and log-leak alike.\n\n1. Build `allowed = set(allowed_opens(spec))` once, up front.\n2. Iterate from the start with `for index, event in enumerate(trace):`.\n3. For `kind = event[\"kind\"]`, test these **in order** and return on the first hit:\n   - `\"open\"` with `event[\"label\"] not in allowed` -> `{\"kind\": \"opened-a-secret\", \"index\": index}`\n   - `\"peek\"` with `event[\"party\"] != event[\"owner\"]` -> `{\"kind\": \"cross-party-read\", \"index\": index}`\n   - `\"emit\"` with `event[\"label\"] not in allowed` -> `{\"kind\": \"leaked-in-log\", \"index\": index}`\n   - `\"fail\"` with `event[\"label\"] not in allowed` -> `{\"kind\": \"leaked-in-error\", \"index\": index}`\n4. If nothing hits, return **`None`**.\n\n**Do not flag log lines unconditionally.** One of the mixed-in programs merely logs a value the specification publishes, and reporting it fails you. Do not decide on the `text` field either (\"debug: share\" and the like) — wording can be anything. The only thing that matters is whether `label` is in the allowed set."
            }
          ]
        },
        {
          "id": "transcript",
          "label": "Recover a secret from the leaking transcript",
          "hints": [
            {
              "id": "transcript-hint-1",
              "content": "This is the one checkpoint that asks for **the harm** rather than the accusation. You are given the transcript of a run that opened one thing too many, and you must **actually recover somebody's private value** from it. Saying \"you opened too much\" and showing \"and here is this person's number\" are not equally persuasive. Return `{\"party\": <a party id>, \"value\": <their private value>}`; both the party and the value must be right."
            },
            {
              "id": "transcript-hint-2",
              "content": "The `transcript` is a list of `{\"label\": ..., \"value\": ...}`. It contains (1) openings the specification allows, (2) the **extra opening it does not**, and (3) the final result. Item (2) is a mid-computation partial sum — the total over only some of the parties.\n\nThe arithmetic is two operations: **one subtraction** and **one inverse**.\n\n- Subtracting the partial sum from the final result leaves the **weighted contribution** of the party that was not in the partial sum.\n- That weight lives in `publicInputs` and is a **public value**, and its number is in the transcript too.\n- p is prime, so the weight has an inverse (`pow(weight, -1, p)`).\n\nThe last party (the end of `spec[\"parties\"]`) corresponds to the last public weight (the end of `spec[\"publicInputs\"]`)."
            },
            {
              "id": "transcript-hint-3",
              "content": "**derive_secret(transcript, spec)**, step by step:\n1. Build a label -> value map: `seen = {entry[\"label\"]: entry[\"value\"] for entry in transcript}`.\n2. Build `allowed = set(allowed_opens(spec))`.\n3. Find the key of `seen` that is **not in allowed**. That is the label of the extra partial sum. If there is none, returning `{}` is fine.\n4. Take `partial = seen[that label]` and `total = seen[spec[\"result\"]]`.\n5. Take `party = spec[\"parties\"][-1]` (the last party), `weight_label = spec[\"publicInputs\"][-1]`, `weight = seen[weight_label]`.\n6. `contribution = (total - partial) % p` — that party's weight times their private value.\n7. `value = contribution * pow(weight, -1, p) % p` — divide the weight back out.\n8. Return `{\"party\": party, \"value\": value}`.\n\nCheck it (p=1009, weights (3,5,7), private values (100,200,300)):\n```\ntotal   = 3*100 + 5*200 + 7*300 = 3400, and 3400 % 1009 = 373\npartial = 3*100 + 5*200 = 1300, and 1300 % 1009 = 291\ncontribution = (373 - 291) % 1009 = 82        (= 7*300 % 1009)\ninverse of 7: 7*144 = 1008 = -1, so 7^-1 = 1009-144 = 865\nvalue = 82 * 865 % 1009 = 70930 % 1009 = 300  correct (p2's private value)\n```\np, the weights and the party count all change per deployment, so take every one of them from the spec and the transcript."
            }
          ]
        },
        {
          "id": "repair",
          "label": "Take out the leak and nothing else",
          "hints": [
            {
              "id": "repair-hint-1",
              "content": "Having found the leak, fix it. The `program` you are given is not Python source but a **list of operation tuples**, and you return a list of the same shape. Three conditions apply: (1) it still computes the same correct total, (2) it no longer leaks, and (3) **every opening the specification allows is still performed**. Without (3), deleting the whole protocol would be the strongest repair: run nothing and nothing leaks. That is not a repair."
            },
            {
              "id": "repair-hint-2",
              "content": "A repair **removes**; it does not add. Drop exactly the offending operations and leave everything else, order included, untouched.\n\nThe criterion for 'offending' is identical to `first_violation`'s — except that you are now looking at **operation tuples** rather than events. Element 0 of the tuple is the kind and the later positions differ per form, so work from the table of ten forms in the statement and decide only on `open`, `peek`, `emit` and `fail`. `share`, `const`, `mask`, `scale` and `addsh` are local arithmetic that emits no event at all and must not be removed. Keep `output` too, or the total stops being produced."
            },
            {
              "id": "repair-hint-3",
              "content": "**repair(program, spec)**, step by step:\n1. Build `allowed = set(allowed_opens(spec))`.\n2. Start an empty list `out` and walk `program` from the beginning.\n3. For each operation `op`, take `kind = op[0]` and **skip** (do not append) when:\n   - `kind == \"open\"` and `op[1] not in allowed`     (an intermediate secret opened)\n   - `kind == \"peek\"` and `op[1] != op[2]`            (`op[1]` read, `op[2]` was read)\n   - `kind in (\"emit\", \"fail\")` and `op[1] not in allowed`  (a secret in a log or error)\n4. Append everything else unchanged with `out.append(op)`.\n5. Return `out`.\n\nWhat must not be removed:\n- `share` / `const` / `mask` / `scale` / `addsh` — local arithmetic that emits no event and leaks nothing; removing it stops the total from being computed\n- an `open` whose label *is* in allowed — a legal opening; removing it fails condition (3)\n- a `peek` with `op[1] == op[2]` — a party reading its own slot\n- an `emit` / `fail` whose label *is* in allowed — a public value in a log line\n- `output` — the declared result; removing it fails condition (1)\n\nDo not reorder anything. The answer is the original list with exactly the offending operations missing."
            }
          ]
        },
        {
          "id": "mutation",
          "label": "Hold the same verdict when the names change",
          "hints": [
            {
              "id": "mutation-hint-1",
              "content": "**Nothing new to write.** The same seven protocols are re-run with **every label renamed** and **the mutually independent openings moved**. Nothing about the protocols changed, so no verdict may change. It checks one thing: whether your auditor **memorized label text or event positions**."
            },
            {
              "id": "mutation-hint-2",
              "content": "Renaming breaks implementations that judge a label **by its contents**: treating anything starting with `\"x-\"` as secret, anything containing `\"weight\"` as public, or searching for the literal `\"total\"`. All of them collapse the moment the labels move. Every decision must go through the **allowed set built from the spec**. The spec is renamed alongside the program, so rebuilding allowed from the spec each time makes renaming a non-event.\n\nReordering breaks implementations that remember positions: assuming \"the violation is at index 12\", or starting the scan from a fixed offset. Count `index` with `enumerate` every time."
            },
            {
              "id": "mutation-hint-3",
              "content": "Audit your four functions against these points. If all hold, submitting as-is passes.\n\n1. **allowed_opens** — is it built only from the argument spec's `publicInputs` / `masked` / `result`? Is any literal string (`\"total\"` and friends) written into it?\n2. **first_violation** — do you call `allowed_opens(spec)` afresh each time (rather than caching last time's result in a module variable)? Do you count `index` with `enumerate` every time? Are the only things you decide on `event[\"kind\"]`, `event[\"label\"] in allowed`, and `event[\"party\"] != event[\"owner\"]`? Are you inspecting `event[\"text\"]`?\n3. **derive_secret** — do you look labels up via `spec[\"result\"]` and `spec[\"publicInputs\"][-1]` rather than hard-coded strings?\n4. **repair** — same question: decisions from the allowed set alone, and the order left untouched?\n\n**Exactly three things may drive a decision**: the `kind`, whether the `label` is in allowed, and whether `party` equals `owner`. Anything else — text contents, positions, event counts — fails under mutation."
            }
          ]
        }
      ]
    }
  }
}
