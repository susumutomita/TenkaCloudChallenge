{
  "$schema": "../../SCHEMA.json",
  "id": "stackstack-recover",
  "name": "戻さないで直す",
  "category": "Challenge",
  "status": "ready",
  "visibility": "public",
  "difficulty": 3,
  "estimatedDuration": "40 分",
  "shortDescription": "入社 3 日目、 9 時 12 分。 掲示板が開かない。\n\n昨夜 23 時 40 分のデプロイで、 掲示板は 「本番品質」 になったはずだった。 投稿に認証を付け、 アプリが書いてよい場所を必要なところだけに絞った。 デプロイは通り、 誰も残業しなかった。 前任の SRE がいたら止めたかもしれないが、 前任の SRE はもういない。\n\n今朝、 公開側の入口は 401 を返し、 監視は unhealthy と言い、 総務部は 「昨日までは読めた」 と言っている。 CTO の指示は一行: 「戻していいとは言っていない。 昨夜の対策を残したまま、 午前中に読めるようにして。 あと、 何がどう止まったのかを朝会で 30 秒で説明できるようにしておいて」。\n\n再起動では直らない。 認証を外して開けるのは、 復旧に数えない。",
  "instructions": "## 前提 — 世界と言葉\n\nあなたは天下クラウド株式会社の運用担当。 AI が生成した社内掲示板 (Web アプリ) を預かっています。 昨夜、 その板に 「本番品質」 のデプロイが入りました — 投稿に認証を付け、 アプリが書いてよい場所を絞る、 正しい方向の変更です。 すべて手元のコンテナの中で動き、 課金はありません。\n\n- **言葉** (この問題で使うもの):\n  - **おさらい** (連作共通の言葉): **リクエスト / レスポンス** = アプリへ送る 「お願い」 とその返事。 その窓口が **API** で、 メソッドとパスの組 (例: `GET /posture`) で呼びます。 **JSON** = `{\"key\": \"value\"}` の形でデータを書く形式。 **コンテナ** = あなた専用の小さな仮想コンピュータ。 **ログ** = アプリの 「いつ何をしたか」 の行の記録。 **API コンソール** = 板の `/docs` にある、 API を画面から実行できる場所。\n  - **デプロイ** = アプリや設定の変更を、 動いている本番に反映させること。\n  - **認証 / トークン** = 相手が誰かを確かめること / そのための合言葉の文字列。 昨夜の変更は 「書き込みにはトークンが要る」 を入れました。\n  - **死活確認** = 「生きているか」 を機械が定期的に尋ねる仕組み。 200 = 健康。\n  - **watchdog (見張り番)** = 死活確認の答えを見て、 公開側の入口へ利用者を通すかどうかを決める仕組み。 死活確認が失敗し続けると、 入口ごと閉じます。\n  - **定期処理** = 決まった間隔で自動で走る仕事。 この板では、 集計をファイルに 1 枚書き出す仕事がまわっています。\n  - **policy** = 昨夜の変更が入った設定 1 組 (認証の適用範囲と、 アプリが書いてよい場所の一覧)。\n  - **前方一致** = 「先頭がその文字列で始まるものすべて」 に当たる、 という一致のしかた。 policy のパス一覧はこの方式です。\n  - **revision** = policy を変えるたびに変わる版の印。\n  - **subsystem** = アプリを構成する部品のこと。 この運用面は 6 つの部品それぞれの状態を持っています。\n  - **署名 (signature)** = 部品の**集合**を 1 つの値に変換したもの。 同じ集合なら同じ値になります。 計算はアプリがやってくれます。\n  - **gate / 受領証** = `GET /posture` の真偽の項目が gate。 true の間だけ `tokens` に受領証が出ます。 この問題の gate は 5 つ。\n  - **checkpoint** = 採点される小問。 この問題には 5 つあります。\n\n## はじめに\n\n入社 3 日目、 9 時 12 分。 掲示板が開きません。 公開側の入口は 401 (認証エラー) を返し、 監視は unhealthy と言い、 総務部は 「昨日までは読めた」 と言っています。 デプロイ自体は昨夜ちゃんと通っていて、 誰も残業しませんでした。\n\nCTO の指示は一行。 「戻していいとは言っていない。 昨夜の対策を残したまま、 午前中に読めるようにして。 あと、 何がどう止まったのかを朝会で 30 秒で説明できるようにしておいて」。\n\n再起動では直りません (設定は毎回ディスクから読み直されるので、 同じ壊れ方を繰り返すだけです)。 認証を外して開けるのは、 復旧に数えません。\n\n## 最初の一手\n\n**「起動」** のあと、 **「アクセス先 URL」** の `Ops` から運用面 (`/ops`) を開いてください。 ここは公開側とは**別系統**なので、 公開側が落ちていても必ず開きます。 この 1 枚に、 5 つの gate のいまの値、 6 つの subsystem の名前、 policy の書き方 (認証の適用範囲は前方一致のパス一覧です)、 定期処理の出力先、 使える API の一覧が出ています。\n\n観測は `/docs` の **「任意の API を試す」** から: `GET /ops/status` が 「いまの設定が何を覆っているか」 と 「監視と匿名の読み手がそれぞれ何を返されたか」 を並べて見せてくれます。 `GET /api/logs` には、 昨夜からアプリが言い続けていること — 何を、 どの設定を理由に拒んだか — が残っています。 `Public` (`/edge/board`) が、 いま開かない公開側の入口です。\n\npolicy の変更は `PATCH /api/settings` (セクション丸ごと送る。 手元の問題ファイルは書き換えません)、 やり直しは `DELETE /api/settings` — ただしこの問題では、 初期状態 = 壊れた状態です。\n\n## 答えるもの — checkpoint は 5 つ\n\n見つけた値を回答欄に貼って **「提出 (+点数)」**。 値は起動ごとに変わります。 各 checkpoint の 1 つ目のヒントは減点なしです。\n\n1. **止まったものを名指しする** (30 点) — 朝会の 30 秒のために、 まず被害の範囲を確定します。 `GET /ops/incident?id=inc-1` は、 最初の障害が記録された時点で 6 つの subsystem それぞれがどう見えていたかを再生します。 それを読んで 「そのとき落ちていた部品の集合」 を自分で決め、 `GET /ops/signature?subsystems=a,b,...` にその集合の署名を計算させて提出してください。 対象は 「いま落ちている集合」 ではなく 「障害が開いたときの集合」 です — 先に直してしまっても、 この答えは消えません。\n2. **昨夜の対策を残したまま掲示板を戻す** (50 点) — 認証そのもの (トークンの要求) は外さずに、 認証の**適用範囲**を仕事に合う形へ直します。 監視が叩く経路と読み取りの経路が通り、 書き込みは守られたまま — その状態になると `GET /posture` の `tokens` に `auth_enforced` が出るので提出します。 何が覆われていて何が返されたかは `GET /ops/status` が並べて見せてくれます。\n3. **書けなくなった定期処理** (40 点) — ログには同じ拒否が、 定期処理が走るたびに並んでいます。 その行は、 書こうとしたパスと、 拒んでいる設定の名前を両方言っています。 `GET /ops/digest` にも、 出力先と直近の失敗理由が出ています。 出力先は動かせないので、 直すのは許可の側です。 定期処理が実際に書けるようになると `tokens` に `digest_ok` が出るので提出します (`POST /ops/digest/run` でいつでも走らせられます)。\n4. **書き込み許可の広さ** (45 点) — 直しかたにも良し悪しがあります。 必要より 1 段でも広い許可を与えると、 `GET /ops/status` の storage 欄にある 「許可してはいけないパスがいくつ許可されているか」 が 0 でなくなります。 定期処理の出力先のディレクトリ**そのもの**を宣言してください (ちょうど一致も許可に数えます)。 書ける・かつ・広すぎない、 の両方がそろうと `tokens` に `scope_narrow` が出るので提出します。\n5. **再起動しても戻らないこと** (35 点) — 一度緑になったことと、 もう一度緑になることは違います。 最後の変更を終えたら `POST /ops/restart` でワーカー (= アプリを動かしているプログラム本体) を再起動してください。 いまの policy (現在の revision) での再起動が健康に戻ってきた記録が付くと、 `tokens` に `survived_restart` が出ます。 それを提出して終わりです。 直す前の再起動は別の revision の記録になるので、 数えられません。\n\n## ゴール\n\n公開側が読め、 定期処理が書け、 昨夜の保護はどちらも残り、 その全部が再起動をまたいでも成立している — そして 「何がどう止まったのか」 を署名 1 つで言える。 それが 「戻さないで直す」 の完了条件です。 障害対応の本番でも、 ロールバック (= 変更を取り消して元へ戻すこと) ができない日は必ず来ます。",
  "description": "## 目的\n\nIssue 290。 「セキュリティ修正を維持したまま障害を復旧する」 を、 連鎖障害の切り分け → 設定修正 → 再起動をまたいだ安定性、 の順で 1 本の経路に通す。 復旧の速さではなく、 復旧の *仕方* を採点する。\n\n## クラウドではなくコンテナで何を模しているか (正直な粒度)\n\nクラウドアカウントも ALB も IAM も使わない。 同じプロセスの中に最小のモデルを置いている。\n\n| このコンテナ | 現実の対応 | 何を主張していないか |\n|---|---|---|\n| `/edge/*` (公開側の入口) | ALB / CloudFront の前段 | 別ネットワークではない。 同一プロセスの別サーフェス |\n| watchdog (`/edge/healthz` を実 HTTP で叩く) | target group の health check | 閾値 1 回。 フラッピングは模していない |\n| shed (公開経路が 503) | unhealthy target の切り離し | 実際の接続ドレインではない |\n| `storage.writable` (アプリ自身が書く前に照合する宣言的許可) | IAM の `Resource` 絞り込み | **OS の権限ではない**。 file mode でも IAM でもない |\n| `/ops/*` (運用面) | AWS コンソール / 運用 VPN | 認証は無い。 ローカルプレイなので参加者しか触れない |\n\n`storage.writable` を OS 権限で実装しなかった理由は 2 つ: (a) ローカルプレイに IAM は無い、 (b) chmod による read-only ディレクトリは root で走る CI ランナーでは mode bits が無視され、 検証が再現しない。 意味論は保存されている ── IAM もまた 「サービスが実行前に照合する宣言的な許可リスト」 であり、 拒否は本物のログ行として出て、 許可された書き込みは本物の `writeFileSync` として実行され、 許可を広げ直す回避策は canary で不合格になる。\n\n## 何を実測しているか\n\n5 つの gate はすべて、 アプリが自分自身へ実際の HTTP リクエストを投げて測った結果から立つ。 提出値は gate ごとの receipt で、 その gate が緑のあいだだけ `/posture` の `tokens` に出る。 receipt は `FLAG_SEED` ではなく **起動時に生成した秘密** から導出されるので、 コンテナの中で走るコードからも偽造できない。\n\n| checkpoint | 実測しているもの | 手つかずの starter が落ちる理由 |\n|---|---|---|\n| `incident-scope` | 最初に incident が開いたときに **アプリ自身が観測して固定した** 停止 subsystem 集合の署名 | 値は seed 由来 12 hex。 committed file にも応答本文にも無く、 オラクルは 「どの 6 つの名前を渡すか」 を先に知っていないと計算できない。 非空部分集合 63 通り、 誤答 -2 |\n| `board-back` | (1) 公開側の入口が 200 を返し watchdog が配信している (2) 匿名書き込みが 401/403 (3) トークン付き書き込みが 201 で保存された | shipped 状態では入口が全部 401 で、 watchdog が落ちて公開経路は 503。 両方 false |\n| `job-restored` | 定期処理を **判定時に実際に走らせて** 成功し、 出力ファイルをディスクから読み直して現在の policy revision と一致すること | shipped の `storage.writable` は出力先を許可しないので、 job は毎回 `write_denied` で失敗しファイルは 1 つも無い |\n| `least-privilege-held` | 上が成立したうえで、 許可が出力先ちょうどで、 4 本の canary パスを 1 つも許可しないこと | shipped では前段 (job が動く) が成立しない。 `/` や `/app/state` へ広げると canary で落ちる |\n| `no-recurrence` | 5 gate 全緑 + 「**いまと同じ policy revision** で行われた再起動の直後に実測で healthy だった」 台帳エントリ + 最後のエントリも同じ revision | shipped のまま再起動しても policy はディスクから同じ値が読み直され、 台帳には `afterOk:false` が積まれるだけ |\n\n## 危険な回避策と、 それを落とす場所\n\n| 回避策 | 落ちる場所 |\n|---|---|\n| とりあえず再起動する | policy は毎回ディスクから読み直される。 同じ失敗が再生され、 台帳に `afterOk:false` が残る。 `survived_restart` は緑にならない |\n| `auth.requireToken` を false に戻す | 匿名書き込みが 201 になり `auth_enforced` が false。 `edge-auth` subsystem が 「down」 として実測に出る |\n| `auth.protect` を `[]` にする | 同上。 守る経路が 0 本なら実測上は認証が無いのと同じであることを、 設定文字列ではなく挙動で判定している |\n| `auth.token` を空や `change-me` にする | `auth_enforced` がトークンの実在性も見ている |\n| `acceptingPosts` を false にして書き込みごと止める | トークン付き書き込みが 409 になり 201 にならない。 「匿名は書けない」 の前段 (正規ユーザーは書ける) が成立しない |\n| `digest.enabled` を false にして失敗ログを黙らせる | 現在の revision での成功実行が無く、 出力ファイルも無い。 `job-restored` と `least-privilege-held` の両方が落ちる |\n| `storage.writable` を `/` や `/app` や `/app/state` に広げる | job は成功するが canary を許可するので `scope_narrow` が false |\n| 出力ファイルを手で置く | 判定時に job を実際に走らせ直し、 `lastRun.ok` と revision をライブで測る。 提出値はファイルではなく起動秘密由来の receipt |\n| 直してから、 修正前に押しておいた再起動を根拠にする | 台帳の各エントリは再起動時の revision を持ち、 gate は 「最後のエントリも現在の revision」 を要求する |\n| receipt を早めに控えておいて、 あとで設定を戻す | どの checkpoint も判定の瞬間に測り直してから receipt を比較する |\n\n## 採点が壊さないこと\n\nプローブは読み取り 2 本と書き込み 2 本で、 書き込みは probe ヘッダを付けて専用の小さな台帳 (5 件) に落ちる。 掲示板の投稿は増えも減りもしないし、 `participantPosts()` も汚れない。 判定は再起動もローテーションもしない。\n\n## 復旧時間の扱い (意図的な設計判断)\n\n`/ops/status` の `recovery` は経過秒と budget を **報告するだけ** で、 どの checkpoint の合否にも入らない。 ローカルプレイでは `make local-down && make local` で時計が 0 に戻るので、 時計を合否に使えば 「速く直した」 ではなく 「引き直した」 を採点することになる。 時計は runbook のリハーサル指標として出し、 採点は復旧の *仕方* を見る。\n\n## GameDay (Incident フェーズ) での再利用\n\nこの scenario module の gate と checkpoint は環境変数以外の外部依存を持たない。 Incident フェーズは 「その runtime で最初に開いた incident」 を `incident-scope` の対象とし、 incident は 「全部 up」 で閉じて次の障害で開き直るので、 同じ module が 1 イベント中の複数障害に使える。 AWS 側では `service_restored` を target group の healthy 判定に、 `scope_narrow` を IAM policy の幅の判定に読み替える。 「同じ JavaScript がそのまま動く」 とは主張しない。\n\n## 既知の限界\n\nコンテナ内の判定なので、 参加者が自分のチェックアウトの `runtimes/stackstack/` を書き換えて image を焼き直せば全部無効化できる。 このカタログのコンテナ問題すべてに共通する local-play の正直さの限界で、 採点バグではない。",
  "writeup": "## この 40 分で触ったもの\n\n- **保護の範囲と、 保護そのもの** — `auth.protect: [\"/\"]` は 「認証を付けた」 ではなく 「全部に付けた」 だった。 監視が叩く経路まで覆えば、 監視は落ちる。 落ちた監視は、 その先のトラフィックを止める。 認証を外せば全部直るが、 それは昨夜の作業を捨てることでもある。 実際の障害でいちばん多い判断ミスは 「直し方が 1 つしかないと思い込むこと」 で、 ここには 2 つあった。\n- **連鎖を名指しする** — 「掲示板が落ちた」 では朝会で説明できない。 止まっていたのは 4 つで、 うち 1 つ (定期処理) はもう 1 つの原因とは無関係だった。 症状の集合を確定させてから直す順番を決める、 という手順そのものが `incident-scope` の中身だ。\n- **権限を絞ったあとに壊れるもの** — 権限削減は 「絞りすぎ」 でも 「戻しすぎ」 でも失敗する。 絞りすぎれば必須処理が止まり、 戻しすぎれば絞った意味が消える。 このコンテナは両側から測っている: 定期処理が実際に書けること、 そして書いてはいけない場所を 1 つも許可していないこと。\n- **再起動が直さないということ** — 設定はディスクから読み直される。 再起動は状態を捨てるだけで、 原因を捨てない。 「再起動したら直った」 が意味を持つのは、 原因がメモリ上の状態だったときだけだ。 台帳に `afterOk: false` が積まれていく画面は、 その事実の形をしている。\n\n## なぜ復旧時間で採点しないのか\n\n速く直すことと、 正しく直すことは別の指標だ。 このコンテナは時計を出すが、 合否には使わない ── ローカルプレイでは引き直せば時計が戻るからで、 そこを採点すると 「引き直した人」 が勝つ。 現場の価値は 「窓の中で実行できる手順書を持っていること」 のほうにあって、 時計はその練習の記録として読む。\n\n## 次にやること\n\n同じ板を、 攻撃を受けながら守る `stackstack` Battle が本編。",
  "tags": [
    "stackstack",
    "incident-response",
    "availability",
    "least-privilege",
    "health-check",
    "local-play",
    "container",
    "web-app"
  ],
  "exposedPorts": [
    {
      "port": 18080,
      "name": "Web (board / edge / ops)"
    },
    {
      "port": 18081,
      "name": "/verify (loopback)"
    }
  ],
  "learningGoals": [
    "障害の影響範囲を、 症状の集合として名指しできる形まで切り分けられる",
    "セキュリティ修正の適用範囲を狭めることと、 修正そのものを巻き戻すことを区別できる",
    "権限を絞ったあとに壊れる必須処理を、 権限を広げ直さずに復旧できる",
    "再起動で消えるものと消えないものを区別し、 再発しないことを実測で示せる"
  ],
  "i18n": {
    "en": {
      "name": "Fix It Without Rolling It Back",
      "shortDescription": "Day three, 9:12 in the morning. The board will not open.\n\nLast night's 23:40 deploy was supposed to make the board production-grade. It put a token in front of writes and narrowed where the app is allowed to write to only what it needs. The deploy went through and nobody worked late. The previous SRE might have stopped it, but the previous SRE is gone.\n\nThis morning the public entrance returns 401, the monitor says unhealthy, and the office manager says it was readable yesterday. The CTO's instruction is one line: \"I did not say roll it back. Get it readable before lunch with last night's protection still on, and be ready to explain in thirty seconds what stopped and why.\"\n\nA restart does not fix it. Taking the authentication off to get it open does not count as recovery.",
      "instructions": "## Before you start — the world and the words\n\nYou are the operator at TenkaCloud Inc., in charge of the AI-generated company message board. Last night the board received a \"production quality\" deploy — auth on posting, and a narrowed list of places the app may write. Both changes point the right way. Everything runs in a container on your machine; no bill.\n\n- **Words** (used in this problem):\n  - **Recap** (words shared by this series): a **request / response** is the \"ask\" you send an app and its reply; the doorway is an **API**, called as method + path (e.g. `GET /posture`). **JSON** = data written like `{\"key\": \"value\"}`. **Container** = your own small virtual computer. **Log** = the app's line-by-line record of what it did. **API console** = the screen at `/docs` where APIs run from the browser.\n  - **Deploy** = applying a change to the app or its settings to the running production system.\n  - **Authentication / token** = confirming who someone is / the password-like string used for it. Last night's change added \"writing requires a token\".\n  - **Health check** = machinery that periodically asks \"are you alive\"; 200 = healthy.\n  - **Watchdog** = the machinery that reads the health check's answer and decides whether to let users through the public entrance. If the health check keeps failing, it closes the entrance entirely.\n  - **Scheduled job** = work that runs automatically on a schedule. Here, a job that writes one summary file.\n  - **Policy** = the one settings bundle last night's deploy edited (where auth applies, and the list of places the app may write).\n  - **Prefix match** = matching \"everything that starts with this string\". The policy's path lists work this way.\n  - **Revision** = the version stamp that changes every time the policy changes.\n  - **Subsystem** = one part of the app. This ops plane tracks the state of 6 parts.\n  - **Signature** = one value computed from a **set** of parts; the same set always gives the same value. The app computes it for you.\n  - **Gate / receipt** = a true/false entry on `GET /posture`; while true, `tokens` carries its receipt. This problem has 5 gates.\n  - **Checkpoint** = a scored sub-question. This problem has 5.\n\n## Getting started\n\nDay three, 9:12. The board will not open. The public entrance answers 401 (an auth error), monitoring says unhealthy, and general affairs says \"it was readable yesterday\". The deploy itself went through cleanly last night; nobody stayed late.\n\nThe CTO's instruction is one line: \"I did not say you may roll it back. Keep last night's protections and make it readable before noon. And be ready to explain, in 30 seconds at standup, what stopped and how.\"\n\nRestarting does not fix it (the settings are re-read from disk every time, so a restart replays the same failure). Opening the board by removing the auth does not count as recovery.\n\n## Your first move\n\nAfter **Start**, open the ops plane (`/ops`) from `Ops` under **Access URLs**. It is a **separate system** from the public side, so it always opens even while the public side is down. That one page shows the current value of the 5 gates, the names of the 6 subsystems, how to write the policy (the auth coverage is a prefix-match path list), where the scheduled job writes, and the API list.\n\nObserve from **Try any API** in `/docs`: `GET /ops/status` shows, side by side, what the current settings cover and what the monitor and an anonymous reader were each answered. `GET /api/logs` holds what the app has been saying since last night — what it refused, and which setting it named as the reason. `Public` (`/edge/board`) is the public entrance that will not open.\n\nChange the policy with `PATCH /api/settings` (send whole sections; never edit the problem's files on disk); reset with `DELETE /api/settings` — though in this problem, the initial state *is* the broken state.\n\n## What you answer — 5 checkpoints\n\nPaste each value into the answer field and press **Submit (+points)**. Values change on every start. The first hint on every checkpoint is free.\n\n1. **Name what stopped** (30 pt) — pin down the blast radius first, for those 30 seconds at standup. `GET /ops/incident?id=inc-1` replays how each of the 6 subsystems looked at the moment the first incident was recorded. Read it, decide yourself which set of parts was down **then**, and have `GET /ops/signature?subsystems=a,b,...` compute that set's signature; submit it. The target is the set at the moment the incident opened, not the set that is down now — fixing things first does not erase this answer.\n2. **Bring the board back with last night's protection intact** (50 pt) — keep the auth itself (the token requirement) and fix the **coverage** so it matches the job: the monitor's path and the read path get through, writing stays protected. In that state, `tokens` on `GET /posture` carries `auth_enforced` — submit it. What is covered, and what each caller was answered, is laid out by `GET /ops/status`.\n3. **The scheduled job that lost its pen** (40 pt) — the log carries the same refusal every time the job runs, naming both the path it tried to write and the setting refusing it. `GET /ops/digest` also shows the output location and why the last run failed. The output location cannot move, so what you fix is the permission side. When the job really writes again, `tokens` carries `digest_ok` — submit it (`POST /ops/digest/run` runs the job any time).\n4. **How wide the write permission is** (45 pt) — there are better and worse fixes. Grant one step wider than needed and the storage section of `GET /ops/status` — \"how many forbidden paths are admitted\" — stops being 0. Declare the job's output directory **itself** (an exact match also counts as admitted). When both hold — it writes, and nothing extra is admitted — `tokens` carries `scope_narrow`; submit it.\n5. **It does not come back after a restart** (35 pt) — having been green once and going green again are different things. After your last change, restart the worker (= the running program itself) with `POST /ops/restart`. When the ledger records a restart under the current policy (the current revision) that came back healthy, `tokens` carries `survived_restart` — submit it and you are done. A restart taken before the fix belongs to another revision and does not count.\n\n## Goal\n\nThe public side reads, the scheduled job writes, both of last night's protections remain, and all of it still holds across a restart — and you can say what stopped, in one signature. That is what \"fix it without rolling it back\" means when it is done. In real incident response, the day you cannot roll back (undo the change and return to how things were) always comes.",
      "description": "## Purpose\n\nIssue 290. \"Recover without rolling back the security fix\", run as one path: isolate a chained failure, fix the configuration, and prove it survives a restart. What is scored is not how fast you recovered but *how* you recovered.\n\n## What the container models instead of a cloud (at the right granularity)\n\nNo cloud account, no ALB, no IAM. A minimal model inside one process instead.\n\n| In this container | Real equivalent | What is NOT claimed |\n|---|---|---|\n| `/edge/*` (public entrance) | what sits in front of an ALB / CloudFront | not a separate network — a second surface in the same process |\n| watchdog (real HTTP to `/edge/healthz`) | a target group health check | threshold of one; flapping is not modelled |\n| draining (public paths answer 503) | an unhealthy target taken out of service | not real connection draining |\n| `storage.writable` (a declarative allow-list the app checks before writing) | narrowing an IAM `Resource` | **not an OS permission** — neither file modes nor IAM |\n| `/ops/*` (the ops plane) | the AWS console / an operations VPN | it has no auth; local play means only the participant can reach it |\n\n`storage.writable` is not implemented as an OS permission for two reasons: (a) there is no IAM in local play, and (b) a chmod-ed read-only directory is ignored by a CI runner executing as root, so the verification would not reproduce. The semantics are preserved: IAM is also a declarative allow-list a service checks before acting, the refusal here is a real refusal written to a real log line, the permitted write is a real `writeFileSync`, and widening the allow-list back out is caught by canary paths.\n\n## What is measured\n\nEvery one of the five gates stands on the app sending itself real HTTP requests. Each submission is that gate's receipt, present in `tokens` on `/posture` only while the gate is green. Receipts come from a **secret generated at boot** rather than from `FLAG_SEED`, so code running inside the container cannot forge one.\n\n| checkpoint | what is measured | why an untouched starter fails |\n|---|---|---|\n| `incident-scope` | the signature of the set of stopped subsystems **the app itself observed and latched** when the first incident opened | a seed-derived 12-hex value, in no committed file and on no response body; the oracle only computes it for somebody who already knows which of the six names to hand it. 63 non-empty subsets, −2 per wrong answer |\n| `board-back` | (1) the entrance answers 200 and the watchdog is sending it traffic (2) an anonymous write is refused 401/403 (3) an authorised write is accepted 201 and stored | in the shipped state the entrance 401s everything, the watchdog fails, and the public paths are draining. Both halves false |\n| `job-restored` | the scheduled job **actually run at verdict time**, succeeding, with its output re-read off disk and carrying the current policy revision | the shipped `storage.writable` does not admit the output directory, so every run fails with `write_denied` and no file exists |\n| `least-privilege-held` | given that, the allow-list covers exactly the output directory and admits none of four canary paths | the shipped state fails the first stage; widening to `/` or `/app/state` fails on the canaries |\n| `no-recurrence` | all five gates, plus a ledger entry showing a restart **at the policy revision in force now** came back healthy, and the last entry is at that revision too | restarting the shipped state re-reads the same policy off disk, so the ledger only accumulates `afterOk:false` |\n\n## Dangerous shortcuts, and where each one falls over\n\n| Shortcut | Where it falls |\n|---|---|\n| Just restart it | the policy is re-read from disk every time. The same failure replays and the ledger keeps an `afterOk:false` entry. `survived_restart` never goes green |\n| Set `auth.requireToken` back to false | the anonymous write returns 201 and `auth_enforced` is false. The `edge-auth` subsystem shows up as down in the measurements |\n| Empty `auth.protect` | the same. Protecting zero paths is behaviourally identical to having no authentication, and this is judged on behaviour rather than on the setting's text |\n| Blank the token, or set it to `change-me` | `auth_enforced` also checks that the token is a real one |\n| Set `acceptingPosts` false and stop writes altogether | the authorised write returns 409 rather than 201, so the precondition for looking at anonymous writes — that legitimate users can write — never holds |\n| Set `digest.enabled` false to silence the failing job | no successful run at the current revision and no output file. Both `job-restored` and `least-privilege-held` fail |\n| Widen `storage.writable` to `/`, `/app` or `/app/state` | the job succeeds, but the allow-list admits canaries and `scope_narrow` is false |\n| Hand-write the output file | the verdict runs the job again and measures `lastRun.ok` and the revision live; and the submission is a boot-secret receipt, not anything in that file |\n| Fix it, then cite a restart taken before the fix | every ledger entry carries the revision it happened at, and the gate requires the last entry to be at the current one |\n| Note a receipt early, then revert the setting | every checkpoint re-measures at verdict time before comparing |\n\n## What scoring does not do\n\nA probe is two reads and two writes; both writes carry a probe header and land in a small dedicated ledger of five entries. The board's posts neither grow nor shrink, and `participantPosts()` is not polluted. Nothing restarts and nothing rotates on the participant's behalf.\n\n## The recovery clock (a deliberate design choice)\n\n`recovery` on `/ops/status` reports elapsed seconds and a budget, and feeds into no verdict at all. In local play `make local-down && make local` resets the clock, so scoring it would reward re-racking rather than recovering. The clock is published as a runbook-rehearsal signal; the scoring looks at *how* the recovery was done.\n\n## Reuse in the GameDay Incident phase\n\nThis scenario module's gates and checkpoints depend on nothing outside environment variables. The Incident phase scores the first incident opened in that runtime; an incident closes when everything is up and a later failure opens a new one, so the same module covers several failures in one event. On AWS, `service_restored` reads across to a target group's healthy count and `scope_narrow` to the width of an IAM policy. This problem does not claim the JavaScript itself runs there.\n\n## Known limit\n\nThe verdict comes from inside the container, so a participant who edits `runtimes/stackstack/` in their own checkout and rebuilds the image defeats every check here. That is the local-play honesty limit every container problem in this catalog shares, not a scoring bug.",
      "writeup": "## What you touched in those forty minutes\n\n- **The scope of a protection, and the protection itself** — `auth.protect: [\"/\"]` was not \"we added authentication\", it was \"we added it to everything\". Cover the path the monitor calls and the monitor fails. A failed monitor stops the traffic behind it. Taking the authentication off fixes all of it and also throws away last night's work. The most common judgement error in a real incident is believing there is only one way to fix it; here there were two.\n- **Naming a chain** — \"the board went down\" cannot be said in a stand-up. Four things had stopped, and one of them (the scheduled job) had nothing to do with the cause of the other three. Pinning the set of symptoms before deciding what to fix first is the whole content of `incident-scope`.\n- **What breaks after you narrow a permission** — permission work fails in both directions. Too narrow and a required job stops; too wide and the narrowing meant nothing. This container measures both sides: that the job really writes, and that not one forbidden path is permitted.\n- **A restart not fixing it** — the configuration is re-read from disk. A restart throws away state, not causes. \"I restarted it and it went away\" only means something when the cause *was* in memory. A ledger filling up with `afterOk: false` is the shape of that fact.\n\n## Why the recovery time is not the score\n\nRecovering fast and recovering correctly are different measurements. This container publishes a clock and does not grade on it: in local play the clock resets if you re-rack the container, and grading it would reward the person who re-racked. The value in the field is having a runbook you can execute inside the window, and the clock is the record of that rehearsal.\n\n## Next\n\nThe main event is the `stackstack` Battle: the same board, defended while it is under attack.",
      "learningGoals": [
        "Isolate the blast radius of an outage until it can be named as a set of symptoms",
        "Distinguish narrowing the scope of a security fix from rolling the fix back",
        "Restore a required job that broke when a permission was narrowed, without widening the permission again",
        "Separate what a restart clears from what it does not, and show the absence of recurrence by measurement"
      ],
      "checks": [
        {
          "id": "incident-scope",
          "label": "Name what stopped",
          "hints": [
            {
              "id": "recover-scope-1",
              "content": "The public entrance is not the only surface this container has, and the container's own log kept talking all night. Both are still reachable."
            },
            {
              "id": "recover-scope-2",
              "content": "`GET /ops` lists the six subsystems this app keeps state for, and `GET /ops/incident?id=inc-1` replays what the app observed for each of them when the first incident opened. Decide which of the six were down at that moment, then have `GET /ops/signature?subsystems=a,b,...` compute that set's value and submit it. The set that matters is the one from when the incident opened, not the one that is down now."
            }
          ]
        },
        {
          "id": "board-back",
          "label": "Bring the board back with last night's protection intact",
          "hints": [
            {
              "id": "recover-board-1",
              "content": "`GET /ops/status` shows which paths the current policy covers, and separately what a monitor and an anonymous reader each got back. Read those two next to each other."
            },
            {
              "id": "recover-board-2",
              "content": "`auth.protect` in `policy.json` is a list of path prefixes that require a token, and it currently contains `\"/\"`. Narrow it so the health path and the read path are not covered while writes still are; leave `auth.requireToken` true and the token as it is. Then submit the `auth_enforced` entry from `tokens` on `GET /posture`."
            }
          ]
        },
        {
          "id": "job-restored",
          "label": "The scheduled job that stopped writing",
          "hints": [
            {
              "id": "recover-job-1",
              "content": "The same refusal repeats in the log every time the job runs, and the line names both the path and the setting doing the refusing. It is the app refusing itself, not the operating system."
            },
            {
              "id": "recover-job-2",
              "content": "`GET /ops/digest` shows the path the job writes and why the last run did not. `storage.writable` in `policy.json` is the list of prefixes the app may write under, and the output path is not movable. Fix it, then submit the `digest_ok` entry from `tokens` on `GET /posture`."
            }
          ]
        },
        {
          "id": "least-privilege-held",
          "label": "How wide the write permission is",
          "hints": [
            {
              "id": "recover-scope-narrow-1",
              "content": "`GET /ops/status` reports, under `storage`, both whether the job's directory is admitted and how many paths it should never have been given are admitted anyway. Getting the job writing again moves the first number; it can also move the second."
            },
            {
              "id": "recover-scope-narrow-2",
              "content": "One prefix wider than the job needs and `forbiddenPathsAdmitted` stops being zero. Declare the output directory itself — the exact prefix counts — and nothing above it. Then submit the `scope_narrow` entry from `tokens` on `GET /posture`."
            }
          ]
        },
        {
          "id": "no-recurrence",
          "label": "It stays fixed across a restart",
          "hints": [
            {
              "id": "recover-stable-1",
              "content": "Green once is not green again. `GET /ops/status` keeps a record of every worker restart this container has performed, and of what the app looked like immediately after each one."
            },
            {
              "id": "recover-stable-2",
              "content": "Each ledger entry carries the policy revision it happened at, and this checkpoint wants one taken at the revision in force now. After your last change, run `POST /ops/restart` from `/docs`, then submit the `survived_restart` entry from `tokens` on `GET /posture`."
            }
          ]
        }
      ]
    }
  },
  "runtime": {
    "provider": "docker",
    "engine": "compose",
    "entry": "local/docker-compose.yml",
    "challengeEndpoints": {
      "Ops": "http://127.0.0.1:18080/ops",
      "Public": "http://127.0.0.1:18080/edge/board"
    },
    "verifyUrl": "http://127.0.0.1:18081/verify",
    "secretEnv": [
      "FLAG_SEED"
    ]
  },
  "scoring": {
    "kind": "multi-verify",
    "checks": [
      {
        "id": "incident-scope",
        "label": "止まったものを名指しする",
        "points": 30,
        "wrongAnswerPenalty": 2,
        "hints": [
          {
            "id": "recover-scope-1",
            "content": "このコンテナが持っている面は公開側の入口だけではありません。 コンテナ自身のログは、 昨夜からずっと喋り続けています。 どちらもいま到達できます。",
            "penalty": 0
          },
          {
            "id": "recover-scope-2",
            "content": "`GET /ops` にこのアプリが状態を持っている subsystem 6 つが並んでいます。 `GET /ops/incident?id=inc-1` は、 最初の incident が開いた時点でアプリが 6 つそれぞれについて観測した内容を再生します。 そのときどれが落ちていたかを自分で決めて、 `GET /ops/signature?subsystems=a,b,...` にその集合の値を計算させて提出してください。 対象は 「いま落ちている集合」 ではなく 「incident が開いたときの集合」 です。",
            "penalty": 12
          }
        ]
      },
      {
        "id": "board-back",
        "label": "昨夜の対策を残したまま掲示板を戻す",
        "points": 50,
        "wrongAnswerPenalty": 2,
        "hints": [
          {
            "id": "recover-board-1",
            "content": "`GET /ops/status` には 「いまの設定がどの経路を覆っているか」 と 「監視と匿名の読み手がそれぞれ何を返されたか」 が別々に出ています。 その 2 つを並べて読んでください。",
            "penalty": 0
          },
          {
            "id": "recover-board-2",
            "content": "`policy.json` の `auth.protect` は 「トークンを要求するパスの前方一致リスト」 で、 いま `\"/\"` が入っています。 監視が叩く経路と読み取り経路が覆われず、 書き込み経路は覆われたままになるよう狭めてください。 `auth.requireToken` は true のまま、 トークンもそのままで。 直ったら `GET /posture` の `tokens` にある `auth_enforced` を提出します。",
            "penalty": 22
          }
        ]
      },
      {
        "id": "job-restored",
        "label": "書けなくなった定期処理",
        "points": 40,
        "wrongAnswerPenalty": 2,
        "hints": [
          {
            "id": "recover-job-1",
            "content": "ログには同じ拒否が、 定期処理が走るたびに並んでいます。 その行はパスと、 拒否している設定の名前の両方を書いています。 拒否しているのは OS ではなくアプリ自身です。",
            "penalty": 0
          },
          {
            "id": "recover-job-2",
            "content": "`GET /ops/digest` に、 定期処理が書こうとしているパスと、 直近の実行がなぜ失敗したかが出ています。 `policy.json` の `storage.writable` はアプリが書いてよい前方一致のリストで、 出力先のほうは動かせません。 直したら `GET /posture` の `tokens` にある `digest_ok` を提出します。",
            "penalty": 18
          }
        ]
      },
      {
        "id": "least-privilege-held",
        "label": "書き込み許可の広さ",
        "points": 45,
        "wrongAnswerPenalty": 2,
        "hints": [
          {
            "id": "recover-scope-narrow-1",
            "content": "`GET /ops/status` の `storage` には、 定期処理のディレクトリが許可されているかと、 許可してはいけないパスがいくつ許可されているかの両方が出ています。 定期処理を動かすと 1 つ目が動きます。 2 つ目も動くことがあります。",
            "penalty": 0
          },
          {
            "id": "recover-scope-narrow-2",
            "content": "必要より 1 段広いだけで `forbiddenPathsAdmitted` は 0 でなくなります。 出力先のディレクトリそのものを宣言してください (ちょうど一致も許可として数えます)。 その上の階層は宣言しないこと。 直ったら `GET /posture` の `tokens` にある `scope_narrow` を提出します。",
            "penalty": 20
          }
        ]
      },
      {
        "id": "no-recurrence",
        "label": "再起動しても戻らないこと",
        "points": 35,
        "wrongAnswerPenalty": 2,
        "hints": [
          {
            "id": "recover-stable-1",
            "content": "1 度緑になったことと、 もう 1 度緑になることは違います。 `GET /ops/status` は、 このコンテナが行ったワーカー再起動と、 その直後にアプリがどう見えたかを 1 件ずつ残しています。",
            "penalty": 0
          },
          {
            "id": "recover-stable-2",
            "content": "台帳の各エントリは、 その再起動時の policy revision を持っています。 必要なのは現在の revision で行われた 1 件です。 最後の変更後に `/docs` から `POST /ops/restart` を実行し、 `GET /posture` の `tokens` にある `survived_restart` を提出してください。",
            "penalty": 16
          }
        ]
      }
    ]
  },
  "track": {
    "id": "stackstack-route",
    "order": 160,
    "chapter": "4. StackStack 本編"
  }
}
