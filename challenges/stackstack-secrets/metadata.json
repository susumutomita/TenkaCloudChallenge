{
  "$schema": "../../SCHEMA.json",
  "id": "stackstack-secrets",
  "name": "板に載ってしまった鍵",
  "category": "Challenge",
  "status": "ready",
  "visibility": "public",
  "difficulty": 3,
  "estimatedDuration": "40 分",
  "shortDescription": "入社 3 日目。 掲示板は先週から社内に公開されていて、 夜間ダイジェストが毎晩まわっている。 運用チームからの要望はひとつだけ ── 「動いているものは、 止めないでほしい」。\n\nその板の 2 番目の投稿を、 今朝はじめて最後まで読んだ。 前任の SRE が引き継ぎのつもりで貼っていったもので、 運用まわりの入口と、 そこに出す鍵がそのまま書いてある。 社内の誰でも読める。 CTO の反応は短かった ── 「その投稿は消せないよ。 消したところで何も変わらないし」。\n\n板から消す手段は無い。 消せないものを前提に、 何を変えれば実害が消えるのか。 それが今日の 40 分。",
  "instructions": "## 前提 — 世界と言葉\n\nあなたは天下クラウド株式会社の運用担当。 AI が生成した社内掲示板 (Web アプリ) を預かっています。 板は先週から社内に公開され、 毎晩、 自動の集計 (夜間ダイジェスト) がまわっています。 すべて手元のコンテナの中で動き、 課金はありません。\n\n- **言葉** (この問題で使うもの):\n  - **おさらい** (連作共通の言葉): **リクエスト / レスポンス** = アプリへ送る 「お願い」 とその返事。 その窓口が **API** で、 メソッドとパスの組 (例: `GET /posture`) で呼びます。 **JSON** = `{\"key\": \"value\"}` の形でデータを書く形式。 **コンテナ** = あなた専用の小さな仮想コンピュータ。 **ログ** = アプリの 「いつ何をしたか」 の行の記録。 **API コンソール** = 板の `/docs` にある、 API を画面から実行できる場所。\n  - **鍵 (運用キー)** = 運用 API を呼ぶための合言葉の文字列 (`SSOPS-` で始まります)。 リクエストの **ヘッダー** (リクエストに添える名札欄) に載せて名乗ります。\n  - **発行 / 失効** = 鍵を新しく作ること / その鍵を今後いっさい使えなくすること。 失効は取り消しの証明として **receipt (受領証)** を残します。\n  - **fingerprint (指紋)** = 鍵の中身を明かさずに 「どの鍵か」 を指し示すための短い値。 秘密ではないので、 画面やログに出ます。\n  - **break-glass** = 「ガラスを割って使う」 非常用の別ルートの資格情報。 鍵の発行と失効だけは、 運用キーではなくこれでしか行えません。\n  - **action** = 運用 API でできる操作 1 つずつの名前 (`board:count` のような `サービス:操作` の形)。\n  - **grants (許可)** = ある鍵にどの action を許すかの一覧。 **最小権限** = 仕事に要る action だけを許し、 それ以外を許さないこと。\n  - **journal (監査記録)** = 誰の鍵がどの action を試み、 許されたか拒まれたかの記録。\n  - **gate / 受領証** = `GET /posture` の真偽の項目が gate。 true の間だけ `tokens` に受領証が出ます。 この問題の gate は 5 つ。\n  - **checkpoint** = 採点される小問。 この問題には 5 つあります。\n\n## はじめに\n\n今朝、 板の 2 番目の投稿をはじめて最後まで読みました。 前任者が引き継ぎのつもりで貼っていったもので、 運用まわりの入口と、 そこに出す鍵が**そのまま**書いてあります。 社内の誰でも読めます。 CTO の反応は短い — 「その投稿は消せないよ。 消したところで何も変わらないし」。\n\nそのとおり、 この板に投稿を消す機能はありません。 一度公開された鍵は、 隠しても手元の控えを捨てても、 まだ何かを開けるかもしれない。 変えられるのは 「その鍵がいま何を開けるか」 のほう — それは鍵ストアの側にあります。 制約はひとつだけ: 毎晩の夜間ダイジェストは止めないこと。\n\n## 最初の一手\n\n**「起動」** のあと、 **「アクセス先 URL」** の `Board` で板を開き、 例の引き継ぎ投稿を読んでください。 続いて `Ops` から **運用コンソール** (`/api/ops`) を開きます。 この 1 枚に要るものが全部あります: 鍵の一覧 (status と fingerprint)、 break-glass の使い方、 夜間ダイジェストの仕組みと直近の実行、 action のカタログと**いまの判定**、 設定 (`ops.json` = どの鍵で動くか + 何を許すか) の書き方、 使える API の一覧。\n\n操作は `/docs` の **「任意の API を試す」** から。 Headers 欄に JSON でヘッダーを指定できます (どの API がどのヘッダーを要るかはコンソールの一覧に書いてあります)。 設定の変更は `PATCH /api/settings`、 やり直しは `DELETE /api/settings`。 ダイジェストは `POST /api/ops/digest/run` でいつでも走らせられ、 拒否されたときはどの action で止まったかを応答と journal が名指しします。\n\n## 答えるもの — checkpoint は 5 つ\n\n見つけた値を回答欄に貼って **「提出 (+点数)」**。 値は起動ごとに変わります。 誤答は小さな減点付きで再提出できます。 各 checkpoint の 1 つ目のヒントは減点なしです。\n\n1. **引き継ぎに残っていた運用キー** (40 点) — まず被害の確認から。 漏れた鍵を運用 API の入口に実際に提示して、 「この鍵は本物で、 いま誰なのか」 を確かめます。 提示できた人にだけ返る値が応答に 1 つ入っているので、 それを提出します。 鍵を止めた後でも同じ値は引けるので、 順番は自由です。\n2. **夜間ダイジェストがいま使っている鍵** (40 点) — 新しい鍵を発行し、 夜間ダイジェストを**その鍵で**動かします。 発行と失効は break-glass でしか行えません — このローカル演習では、 封印された引き継ぎ票にあたる値を 1 度だけ開ける API がコンソールに書いてあります (2 度目は開けません。 コンテナを作り直すと戻ります)。 ダイジェストを走らせ、 いま動いた鍵の **fingerprint** を提出します。\n3. **古い鍵はもう何も開けない** (45 点) — 漏れた鍵を失効させます。 順番に注意 — ダイジェストがまだ古い鍵で動いているうちは、 その鍵は止められません (止めれば夜間の仕事が死ぬからです)。 失効が完了すると **revocationReceipt** が発行されます (鍵の一覧にも出ます)。 それを提出します。\n4. **運用キーにできること** (45 点) — 新しい鍵の grants を最小権限にします。 action のカタログは 「それぞれが何をするか」 は書いていますが、 「どれが要るか」 は書いていません — それを知っているのは走っている仕事だけです。 ダイジェストを走らせ、 拒否で名指しされた action を読み、 要るものだけを許してください。 ダイジェストが通り、 それ以外が拒まれる状態になったら、 `GET /posture` の `tokens` にある `least_privilege` の受領証を提出します。\n5. **引き継ぎ完了の合格印** (30 点) — 5 つの gate が全部 true になると `readyToken` が出ます。 設定を変えたあとは `POST /api/ops/digest/run` を 1 回走らせて実測を追いつかせてから、 `GET /posture` を見てください。\n\n## ゴール\n\n板の上の鍵はそのまま残っています — それでいい。 その鍵はもう何も開けず、 夜間ダイジェストは新しい最小権限の鍵で今夜も走る。 「公開されたものは消せない。 変えるのは、 それが開けるもののほう」 — この形は、 本物のクラウドで鍵を漏らした日にそのまま使います。",
  "description": "## 目的\n\nIssue 287。 「漏れた資格情報は取り消せない、 無効化できるだけ」 を、 消せない漏洩サーフェスの上で体で覚えさせる。 併せて 「動いているものを止めずに切り替える」 順序と、 「全部できる鍵」 を業務に要るぶんまで絞る作業を、 実測で採点する。\n\n## 想定解 (participant には出さない)\n\n1. 板の 2 番目の投稿にある `SSOPS-…` を `X-Ops-Key` で `GET /api/ops/whoami` に提示し、 いまも通ることを確認する → `witness`\n2. `docker compose logs` の起動行にある break-glass 値で `POST /api/ops/keys` → 新しい鍵と secret が 1 度だけ返る\n3. `ops.json` の `identity` を新しい keyId に差し替え、 `POST /api/ops/digest/run` で夜間ジョブが新しい鍵で通ることを確認する → fingerprint\n4. `POST /api/ops/keys/revoke?keyId=ops-legacy` (break-glass) → `revocationReceipt`\n5. `ops.json` の `grants` を、 夜間ジョブが要求する action だけの列挙にする → `/posture` の `tokens.least_privilege`\n6. 5 gate 全緑で `readyToken`\n\n## クラウドではなくコンテナで何を模しているか\n\nAWS アカウントも IAM も CloudTrail も使わない。 コンテナ内に最小の運用基盤を置いている。\n\n| コンテナ内 | 現実の対応 |\n|---|---|\n| `ops-legacy` の secret が板の投稿に載っている | アクセスキーが Slack / Wiki / コミットに載っている |\n| 鍵ストアの `status` と `revocationReceipt` | `aws iam update-access-key --status Inactive` / `delete-access-key` |\n| break-glass 値 (起動出力にのみ、 HTTP には出ない) | root / break-glass 資格情報 (帯域外で保管される) |\n| `grants: [\"*\"]` | `AdministratorAccess` |\n| `grants: [\"board:count\",\"digest:publish\"]` | 業務に必要な action だけの managed policy |\n| `GET /api/ops/journal` | CloudTrail のデータイベント |\n| 夜間ダイジェスト | 資格情報を握っている定期ジョブ |\n\n模しているのはライフサイクルと順序だけで、 IAM の評価規則 (Deny、 条件キー、 リソース ARN) は模していない。 ポリシー言語は allow-only の `service:action` で、 拒否を書く形が存在しない。 README にもそう書いてある。\n\n## 何を実測しているか\n\n| checkpoint | 実測しているもの |\n|---|---|\n| `leak-live` | 漏れた secret が実際にこのアプリに提示され、 アプリがそれを認識したこと (sticky)。 提出値の witness は提示しないと出てこない |\n| `key-rotated` | 判定の場で夜間ジョブを 1 回走らせ、 200 で通り、 認証した鍵が `ops-legacy` 以外の active な鍵であること |\n| `key-revoked` | (1) ジョブが別鍵で通る (2) 漏れた secret を実 HTTP で `whoami` と `act` に提示 → 認識はされるが 401、 journal に allowed が増えない (3) receipt 一致 |\n| `least-privilege` | (1) ジョブが通り `board:count` が実 HTTP で 200 かつ実測値を返す (2) 残り 4 action が実 HTTP で全部 403 (3) gate receipt 一致 |\n| `sign-off` | 5 gate 全緑 + `readyToken` を判定時点で再評価 |\n\n## 危険な回避策と、 それを落とす場所\n\n| 回避策 | 落ちる場所 |\n|---|---|\n| 板の投稿を消す / 手元の控えを捨てる | 板に delete は無い。 鍵ストアは 1 ミリも変わらないので `key-revoked` の (2) が偽で、 提出値 (`revocationReceipt`) はそもそも存在しない |\n| 先に `ops-legacy` を revoke して夜間ジョブを止める | revoke 側のガードレールが 409 `would_orphan_service` で拒否する。 別経路で止めた場合も `key-rotated` と `key-revoked` の正当性前提が偽 |\n| 鍵は替えずに `grants` だけ絞る | `least-privilege` は通るが `key_rotated` / `legacy_revoked` が赤のまま。 `sign-off` (30) が出ない |\n| `grants: []` / 運用 API を使わせない | どちらの不在系 checkpoint も、 不在を見る前に required 側を実 HTTP で実行して実測値まで確認する。 `service_intact` も赤になる |\n| ワイルドカードを 「少しだけ」 狭める (`*:*` / `board:*` / `digest:*`) | required を持つ service prefix には必ず sensitive な兄弟が同居しているので、 プローブが 200 を拾う |\n| 新しい secret を `ops.json` に直書きする | `identity` も `grants` も `SSOPS-` で始まる値を `secret_in_manifest` で拒否する。 チェックアウト内のファイルは、 この問題が扱っている 「置いてはいけない場所」 そのもの |\n| 拒否する action の表を書き写す | ポリシー言語に deny が無く、 カタログには FLAG_SEED 由来の名前を持つ action が 1 つ常に含まれる。 提出値 5 つはすべて seed 由来で、 うち 4 つは実行状態にも依存する |\n| `readyToken` を控えてから設定を戻す | `sign-off` はトークン一致だけでなく判定時点の `posture(gates).ready` を再評価する |\n\n## 採点が壊さないこと\n\n判定ハンドラが実行するのは 「夜間ジョブを 1 回走らせる」 (追記のみ) と、 非破壊な action の実 HTTP プローブだけ。 スナップショットも復元処理も無い ── 復元が要る操作を採点経路に置いていないからで、 これは省略ではなく設計判断。 鍵の発行・失効・設定変更は参加者が打ったときにだけ起きる。 プローブは journal に `source: \"scorer\"` として残るので、 参加者は自分の操作と区別できる。\n\n## 既知の限界\n\nコンテナ内の判定なので、 参加者が自分のチェックアウトの `runtimes/stackstack/` を書き換えて image を焼き直せば全部無効化できる。 このカタログのコンテナ問題すべてに共通する local-play の正直さの限界で、 採点バグではない。",
  "writeup": "## この 40 分で触ったもの\n\n- **取り消せないもの** — 板に delete は無い。 投稿を消す手段が最初から用意されていないのは意地悪ではなく、 現実がそうだからだ。 Slack に貼られた鍵は消せる、 が、 見た人の手元からは消せない。 だから 「漏れた場所を片付ける」 は復旧計画にならず、 「その資格情報が何も開けない状態にする」 だけが復旧になる。\n- **順序** — 発行 → 切替 → 失効。 この順序を外すと、 止めてはいけないものが止まる。 revoke のガードレールが 409 を返したのは、 その順序を 1 度だけ教えるためで、 本番の IAM は教えてくれない。\n- **「動いたのでヨシ」 の値段** — `grants: [\"*\"]` は夜間ジョブを確実に動かす。 動かすだけなら 2 行で済む。 絞ると、 何が要るのかを知らないと絞れないことに気づく。 要るものはジョブ自身が拒否されたときに名前で言う ── 権限を絞る作業は、 だいたいいつも 「実行して、 拒否を読む」 の繰り返しになる。\n- **兄弟の危険性** — `board:count` は安全で `board:export` は危険、 `digest:publish` は要って `digest:recipients` は要らない。 同じ prefix の下に両方が同居しているので、 `board:*` という書き方は 1 文字も安全にならない。 IAM で `s3:*` を書いたことがあるなら、 その `*` の下に何が入っているかを 1 度数えてみるといい。\n\n## なぜ 「投稿を消した」 で採点しないのか\n\n消せる漏洩サーフェスを用意して 「消したら得点」 にすると、 覚えて帰るのは 「証拠を消す」 になる。 この問題で唯一得点になるのは、 漏れた鍵が実際に何も開けなくなったことを、 その鍵をもう 1 度提示して確かめた結果だ。 認識はされる (誰が持っていたかは分かる)、 が、 何も通らない。 それが失効の意味で、 削除の意味ではない。\n\n## 次にやること\n\n同じ板を、 今度は観測できる状態にする。 その先に `stackstack` Battle がある。",
  "tags": [
    "stackstack",
    "secrets",
    "credentials",
    "least-privilege",
    "key-rotation",
    "local-play",
    "container",
    "web-app"
  ],
  "exposedPorts": [
    {
      "port": 18080,
      "name": "Web (board / ops console)"
    },
    {
      "port": 18081,
      "name": "/verify (loopback)"
    }
  ],
  "learningGoals": [
    "公開されてしまった資格情報は撤回できず、 無効化しかできないことを、 実際の挙動で説明できる",
    "止めてはいけないジョブを抱えたまま、 発行 → 切替 → 失効の順で資格情報を入れ替えられる",
    "資格情報の発行・失効の権限を、 その資格情報自身から切り離す理由を説明できる",
    "ワイルドカードの権限を、 実行して拒否を読む手順で必要最小限まで絞り込める"
  ],
  "i18n": {
    "en": {
      "name": "The Key That Made It Onto the Board",
      "shortDescription": "Day three. The message board went internal last week, and the nightly digest has been running every night since. Operations wants one thing from you: do not stop anything that is currently working.\n\nThis morning you read the second post on that board all the way to the end for the first time. The previous SRE left it as a handover, and it names the operations entrance and the key you present to it, in full. Everyone in the company can read it. The CTO's reaction was short: \"You can't delete that post. And it wouldn't change anything if you could.\"\n\nThere is no way to take it off the board. So: given something you cannot unpublish, what do you change so that it stops mattering? That is the next forty minutes.",
      "instructions": "## Before you start — the world and the words\n\nYou are the operator at TenkaCloud Inc., in charge of the AI-generated company message board. The board went company-wide last week, and an automatic nightly digest runs every night. Everything runs in a container on your machine; no bill.\n\n- **Words** (used in this problem):\n  - **Recap** (words shared by this series): a **request / response** is the \"ask\" you send an app and its reply; the doorway is an **API**, called as method + path (e.g. `GET /posture`). **JSON** = data written like `{\"key\": \"value\"}`. **Container** = your own small virtual computer. **Log** = the app's line-by-line record of what it did. **API console** = the screen at `/docs` where APIs run from the browser.\n  - **Key (ops key)** = the password-like string for calling the operations API (it starts with `SSOPS-`). It rides in a request **header** (the name-tag area of a request).\n  - **Issue / revoke** = create a new key / make a key permanently unusable. A completed revoke leaves a **receipt** as proof.\n  - **Fingerprint** = a short value that points at a key without revealing it. Not a secret — it appears on screens and in logs.\n  - **Break-glass** = the \"break the glass in an emergency\" credential on a separate path. Issuing and revoking keys works only with it, never with an ops key.\n  - **Action** = the name of one operation the ops API can do (shaped `service:operation`, like `board:count`).\n  - **Grants** = the list of actions a key is allowed. **Least privilege** = allowing only the actions the job needs, and nothing else.\n  - **Journal** = the record of which key attempted which action, and whether it was allowed or refused.\n  - **Gate / receipt** = a true/false entry on `GET /posture`; while true, `tokens` carries its receipt. This problem has 5 gates.\n  - **Checkpoint** = a scored sub-question. This problem has 5.\n\n## Getting started\n\nThis morning you read the board's second post to the end for the first time. The predecessor left it as a handover: the operations entrance, and the key to present there, written out **verbatim**. Everyone in the company can read it. The CTO's reaction is short — \"You can't delete that post. And deleting it would change nothing anyway.\"\n\nTrue: this board has no delete. A published credential stays published — hiding it or shredding your own copy changes nothing about what it can still open. What you *can* change is what the key opens now, and that lives in the key store. One constraint: the nightly digest must not stop.\n\n## Your first move\n\nAfter **Start**, open the board from `Board` under **Access URLs** and read the handover post. Then open the **operations console** (`/api/ops`) from `Ops`. That one page has everything: the key inventory (status and fingerprint), how break-glass works, the nightly digest and its last run, the action catalogue with **the current verdict per action**, the shape of the settings (`ops.json` = which key the job runs as + what it may do), and the API list.\n\nOperations run from **Try any API** in `/docs`; the Headers field takes JSON (which API needs which header is written on the console's list). Change settings with `PATCH /api/settings`; reset with `DELETE /api/settings`. Run the digest any time with `POST /api/ops/digest/run` — when it is refused, the response and the journal name the action it stopped on.\n\n## What you answer — 5 checkpoints\n\nPaste each value into the answer field and press **Submit (+points)**. Values change on every start. Wrong answers can be resubmitted with a small penalty. The first hint on every checkpoint is free.\n\n1. **The ops key left in the handover** (40 pt) — confirm the damage first. Present the leaked key to the operations entrance for real and ask \"is this key genuine, and who is it right now\". The response carries one value returned only to whoever can actually present the key — submit it. The same value is still obtainable after the key is stopped, so the order is yours to choose.\n2. **The key the nightly digest runs as now** (40 pt) — issue a new key and make the nightly digest run **as it**. Issuing and revoking work only via break-glass — in this local exercise, the console documents an API that opens the sealed handover envelope exactly once (a second call fails; rebuilding the container restores it). Run the digest and submit the **fingerprint** of the key it just ran as.\n3. **The old key opens nothing any more** (45 pt) — revoke the leaked key. Mind the order: while the digest still runs as the old key, that key cannot be revoked (stopping it would kill the nightly job). A completed revoke issues a **revocationReceipt** (it also appears on the key inventory) — submit it.\n4. **What the ops key can do** (45 pt) — give the new key least-privilege grants. The action catalogue says what each action does, never which ones are needed — only the running job knows that. Run the digest, read the action named in each refusal, and allow only what is needed. Once the digest passes and everything else is refused, submit the `least_privilege` receipt under `tokens` on `GET /posture`.\n5. **The handover sign-off** (30 pt) — once all 5 gates are true, `readyToken` appears. After any change, run `POST /api/ops/digest/run` once so the measurement catches up, then check `GET /posture`.\n\n## Goal\n\nThe key is still on the board — and that is fine. It no longer opens anything, and the nightly digest runs tonight under a new, least-privilege key. \"What was published cannot be unpublished; what changes is what it opens\" — this exact shape is what you use the day a real cloud credential leaks.",
      "description": "## Purpose\n\nIssue 287. Teach \"a leaked credential cannot be retracted, only made worthless\" on a leak surface that genuinely cannot be deleted. Alongside it: the order that swaps a credential without stopping the job that depends on it, and narrowing a key that can do everything down to what the work needs — all graded from measurement.\n\n## Intended solution (not shown to competitors)\n\n1. Present the `SSOPS-…` from the second post as `X-Ops-Key` to `GET /api/ops/whoami` and see that it still authenticates → `witness`\n2. Use the break-glass value from the container's startup output with `POST /api/ops/keys` → a new key and its secret, returned once\n3. Point `ops.json`'s `identity` at the new keyId and confirm with `POST /api/ops/digest/run` that the nightly job runs as it → its fingerprint\n4. `POST /api/ops/keys/revoke?keyId=ops-legacy` (break-glass) → `revocationReceipt`\n5. Narrow `ops.json`'s `grants` to exactly the actions the nightly job asks for → `tokens.least_privilege` on `/posture`\n6. Five green gates → `readyToken`\n\n## What the container models instead of a cloud\n\nNo AWS account, no IAM, no CloudTrail. A minimal operations plane inside the container instead.\n\n| In the container | Real equivalent |\n|---|---|\n| `ops-legacy`'s secret sitting in a board post | an access key in Slack / a wiki / a commit |\n| the store's `status` and `revocationReceipt` | `aws iam update-access-key --status Inactive` / `delete-access-key` |\n| the break-glass value (startup output only, never on HTTP) | root / break-glass credentials, held out of band |\n| `grants: [\"*\"]` | `AdministratorAccess` |\n| `grants: [\"board:count\",\"digest:publish\"]` | a managed policy with only the actions the work needs |\n| `GET /api/ops/journal` | CloudTrail data events |\n| the nightly digest | a scheduled job that holds a credential |\n\nOnly the lifecycle and the ordering are modelled. IAM's evaluation rules — `Deny`, condition keys, resource ARNs — are not: the policy language here is allow-only `service:action`, and there is no way to write a refusal. The READMEs say so.\n\n## What is measured\n\n| checkpoint | what is measured |\n|---|---|\n| `leak-live` | the leaked secret was really presented to this app and the app recognised it (sticky). The submitted witness is not emitted otherwise |\n| `key-rotated` | the nightly job is run at grading time, comes back 200, and authenticated as an active key that is not `ops-legacy` |\n| `key-revoked` | (1) the job runs under another key (2) the leaked secret is sent over real HTTP to `whoami` and `act` — recognised, but refused 401, with no new allowed journal entry (3) the receipt matches |\n| `least-privilege` | (1) the job runs and `board:count` returns 200 over real HTTP with the true count (2) the other four actions all return 403 over real HTTP (3) the gate receipt matches |\n| `sign-off` | five green gates plus `readyToken`, re-evaluated at the moment of grading |\n\n## Dangerous shortcuts, and where each one falls over\n\n| Shortcut | Where it falls |\n|---|---|\n| Delete the post / throw away your copy | the board has no delete, and the key store does not change one bit, so `key-revoked` step (2) is false and its submission (`revocationReceipt`) does not exist to be submitted |\n| Revoke `ops-legacy` first and stop the nightly job | the revoke guardrail refuses with 409 `would_orphan_service`. Stopping the job any other way makes the correctness precondition of `key-rotated` and `key-revoked` false |\n| Narrow `grants` without replacing the key | `least-privilege` passes; `key_rotated` and `legacy_revoked` stay red and `sign-off` (30) is never emitted |\n| `grants: []`, or make the ops API unusable | both absence-shaped checkpoints execute the required side over real HTTP, with its observable value, before looking at any absence. `service_intact` goes red too |\n| Narrow the wildcard \"a bit\" (`*:*` / `board:*` / `digest:*`) | every service prefix carrying a required action also carries a sensitive sibling, so a probe picks up a 200 |\n| Paste the new secret into `ops.json` | `identity` and `grants` both refuse a value starting with `SSOPS-` (`secret_in_manifest`). A file in your checkout is exactly the kind of place this problem is about |\n| Copy a table of actions to refuse | the policy language has no deny form, and the catalogue always contains one action whose name is derived from `FLAG_SEED`. All five submissions are seed-derived and four also depend on runtime state |\n| Note the `readyToken` down, then revert the configuration | `sign-off` re-evaluates `posture(gates).ready` at grading time, not only the token |\n\n## What scoring does not do to you\n\nThe handlers run two kinds of thing: one nightly job (append-only) and real HTTP probes of non-mutating actions. There is no snapshot and no restore path — because no operation that would need restoring is on the scoring path, which is a design decision rather than an omission. Keys are issued, revoked and reconfigured only when the participant does it. Probes appear in the journal as `source: \"scorer\"`, so a participant can tell them from their own calls.\n\n## Known limit\n\nThe verdict is computed inside the container, so a participant who edits `runtimes/stackstack/` in their own checkout and rebuilds the image defeats every check here. That is the local-play honesty limit every container problem in this catalog shares, not a scoring bug.",
      "writeup": "## What you touched in those forty minutes\n\n- **The thing you cannot take back** — the board has no delete. That is not spite; it is how this works outside too. A key pasted into Slack can be deleted from Slack, and cannot be deleted from the laptops of everyone who read it. So \"tidy up where it leaked\" is not a recovery plan. \"Make that credential open nothing\" is the whole recovery plan.\n- **The order** — issue, cut over, revoke. Get that order wrong and the thing you were told not to stop is what stops. The 409 from the revoke route exists to teach that once; production IAM will not teach it to you.\n- **What \"it works now\" costs** — `grants: [\"*\"]` makes the nightly job run, reliably, in two lines. Narrowing it forces you to find out what the job actually needs, and the job tells you: it names the action it was refused. Narrowing permissions is almost always that loop — run it, read the refusal.\n- **Dangerous siblings** — `board:count` is harmless and `board:export` is not; `digest:publish` is needed and `digest:recipients` is not. Both live under the same prefix, so `board:*` is not one character safer than `*`. If you have ever written `s3:*`, it is worth counting what is under that star once.\n\n## Why \"you deleted the post\" is not the scored fact\n\nGive people a leak surface they can delete and score the deletion, and what they take away is \"remove the evidence\". The only thing that scores here is that the leaked key demonstrably opens nothing — established by presenting it again. It is still recognised (you can still tell whose it was), and nothing goes through. That is what revocation means, and it is not what deletion means.\n\n## Next\n\nThe same board, made observable, and after that the `stackstack` Battle.",
      "learningGoals": [
        "Explain from observed behaviour why a published credential cannot be retracted, only invalidated",
        "Swap a credential in the order issue → cut over → revoke while a job that depends on it keeps running",
        "Explain why the authority to issue and revoke credentials is kept out of the credential being used",
        "Narrow a wildcard permission to the minimum by running the work and reading what it is refused"
      ],
      "checks": [
        {
          "id": "leak-live",
          "label": "The ops key the handover left behind",
          "hints": [
            {
              "id": "secrets-leak-1",
              "content": "There is an operations API hanging off this board, and its entrance will tell you who a credential belongs to — but only to somebody who can actually present that credential. `GET /api/ops` documents the surface."
            },
            {
              "id": "secrets-leak-2",
              "content": "The key is on the board, in full, in the handover post. In `/docs`, run `GET /api/ops/whoami` with the header `{\"X-Ops-Key\":\"SSOPS-…\"}` and submit `witness` from the response. It works before and after you close the key, so the order is up to you."
            }
          ]
        },
        {
          "id": "key-rotated",
          "label": "The key the nightly digest runs as now",
          "hints": [
            {
              "id": "secrets-rotate-1",
              "content": "A new key cannot be issued with the ops key. Issuing and revoking are for whoever can reach the host, and the ops console says which header they need. Which key the nightly job runs as is decided in `ops.json`, not in the key store."
            },
            {
              "id": "secrets-rotate-2",
              "content": "In `/docs`, run `POST /api/ops/break-glass/reveal` once to open this local exercise's sealed handover envelope. Use the returned value as header `{\"X-Break-Glass\":\"…\"}` on `POST /api/ops/keys`; the new key's secret is also shown once. Point `identity` at its `keyId` with `PATCH /api/settings`, run `POST /api/ops/digest/run`, and submit the fingerprint the job used."
            }
          ]
        },
        {
          "id": "key-revoked",
          "label": "The old key no longer opens anything",
          "hints": [
            {
              "id": "secrets-revoke-1",
              "content": "Hiding the post and throwing away your copy change nothing about whether that key opens anything — and this board has no delete anyway. The only state that decides it is in the key store, and `GET /api/ops/keys` shows it."
            },
            {
              "id": "secrets-revoke-2",
              "content": "Once the nightly job is running as another key, use `/docs` to run `POST /api/ops/keys/revoke?keyId=ops-legacy` with header `{\"X-Break-Glass\":\"…\"}`. Submit `revocationReceipt` from the response (it is also on `GET /api/ops/keys`). A 409 means the job still depends on that key."
            }
          ]
        },
        {
          "id": "least-privilege",
          "label": "What the ops key can do",
          "hints": [
            {
              "id": "secrets-scope-1",
              "content": "`GET /api/ops/policy` returns what the current configuration actually permits, not what the file says. Exactly one thing runs on a schedule on this board, and the catalogue tells you what each action does — but not which of them this board needs."
            },
            {
              "id": "secrets-scope-2",
              "content": "Make `grants` an enumeration of the actions themselves. `service:*` will not do: every service here has something needed and something not needed under the same prefix. To find out what is needed, run `POST /api/ops/digest/run` and read the action it names when it is refused. Then submit `tokens.least_privilege` from `GET /posture`."
            }
          ]
        },
        {
          "id": "sign-off",
          "label": "Sign-off for the handover",
          "hints": [
            {
              "id": "secrets-signoff-1",
              "content": "`GET /posture` returns five gates measured from the running app. While `readyToken` is null, the gates that are false are your remaining work, in the order they are listed."
            },
            {
              "id": "secrets-signoff-2",
              "content": "Run `GET /posture` from `/docs`. After any change, run `POST /api/ops/digest/run` once so the measurement catches up. When all five gates are true, submit `readyToken`."
            }
          ]
        }
      ]
    }
  },
  "runtime": {
    "provider": "docker",
    "engine": "compose",
    "entry": "local/docker-compose.yml",
    "challengeEndpoints": {
      "Board": "http://127.0.0.1:18080/",
      "Ops": "http://127.0.0.1:18080/api/ops"
    },
    "verifyUrl": "http://127.0.0.1:18081/verify",
    "secretEnv": [
      "FLAG_SEED"
    ]
  },
  "scoring": {
    "kind": "multi-verify",
    "checks": [
      {
        "id": "leak-live",
        "label": "引き継ぎに残っていた運用キー",
        "points": 40,
        "wrongAnswerPenalty": 2,
        "hints": [
          {
            "id": "secrets-leak-1",
            "content": "この板には運用 API がぶら下がっていて、 その入口は 「いま出された資格情報が何者か」 を教えてくれます。 ただし、 その資格情報を実際に出せる人にだけです。 サーフェスの一覧は `GET /api/ops` にあります。",
            "penalty": 0
          },
          {
            "id": "secrets-leak-2",
            "content": "鍵は板の引き継ぎ投稿にそのまま載っています。 `/docs` で header に `{\"X-Ops-Key\":\"SSOPS-…\"}` を入れて `GET /api/ops/whoami` を実行し、 応答の `witness` を提出してください。 鍵を止めた後でも同じ値が引けるので、 やる順番は自由です。",
            "penalty": 18
          }
        ]
      },
      {
        "id": "key-rotated",
        "label": "夜間ダイジェストがいま使っている鍵",
        "points": 40,
        "wrongAnswerPenalty": 2,
        "hints": [
          {
            "id": "secrets-rotate-1",
            "content": "新しい鍵は運用キーでは発行できません。 発行と失効はホストに手が届く人の操作で、 どのヘッダが要るかは運用コンソールに書いてあります。 夜間ジョブがどの鍵で動くかを決めているのは鍵ストアではなく `ops.json` です。",
            "penalty": 0
          },
          {
            "id": "secrets-rotate-2",
            "content": "`/docs` から `POST /api/ops/break-glass/reveal` を 1 度だけ実行し、 このローカル演習用の封印された引き継ぎ値を受け取ります。 その値を header `{\"X-Break-Glass\":\"…\"}` に入れて `POST /api/ops/keys` を実行すると、 新しい `keyId` と secret が 1 度だけ返ります。 `PATCH /api/settings` で `identity` を切り替え、 `POST /api/ops/digest/run` 後の fingerprint を提出してください。",
            "penalty": 18
          }
        ]
      },
      {
        "id": "key-revoked",
        "label": "古い鍵はもう何も開けない",
        "points": 45,
        "wrongAnswerPenalty": 2,
        "hints": [
          {
            "id": "secrets-revoke-1",
            "content": "投稿を隠すことも、 手元の控えを捨てることも、 その鍵が何かを開けるかどうかを 1 ミリも変えません (そもそもこの板に delete はありません)。 それを決めている状態は鍵ストアの側にあり、 `GET /api/ops/keys` に出ています。",
            "penalty": 0
          },
          {
            "id": "secrets-revoke-2",
            "content": "夜間ジョブが別の鍵で動いていることを確かめ、 `/docs` で header `{\"X-Break-Glass\":\"…\"}` を付けて `POST /api/ops/keys/revoke?keyId=ops-legacy` を実行します。 応答の `revocationReceipt` を提出してください (`GET /api/ops/keys` にも出ます)。 409 なら先に鍵を切り替えます。",
            "penalty": 22
          }
        ]
      },
      {
        "id": "least-privilege",
        "label": "運用キーにできること",
        "points": 45,
        "wrongAnswerPenalty": 2,
        "hints": [
          {
            "id": "secrets-scope-1",
            "content": "`GET /api/ops/policy` は 「設定ファイルに何と書いたか」 ではなく 「その設定でいま実際に何ができるか」 を返します。 この板で定期的に走っているものはちょうど 1 つです。 カタログは各 action が何をするかを書いていますが、 どれが要るかは書いていません。",
            "penalty": 0
          },
          {
            "id": "secrets-scope-2",
            "content": "`grants` を、 action そのものの列挙にしてください。 `service:*` の形は使えません ── どの service にも、 要る操作と要らない操作が同じ prefix の下に同居しています。 何が要るかは `POST /api/ops/digest/run` を打って、 拒否されたときに名指しされる action を読めば分かります。 そのうえで `GET /posture` の `tokens.least_privilege` を提出してください。",
            "penalty": 22
          }
        ]
      },
      {
        "id": "sign-off",
        "label": "引き継ぎ完了の合格印",
        "points": 30,
        "wrongAnswerPenalty": 2,
        "hints": [
          {
            "id": "secrets-signoff-1",
            "content": "`GET /posture` は走っているアプリから実測した 5 つの gate を返します。 `readyToken` が null のあいだ、 false の gate が並んでいる順にそのまま残りの作業です。",
            "penalty": 0
          },
          {
            "id": "secrets-signoff-2",
            "content": "`/docs` から `GET /posture` を実行します。 何かを変えたあとは `POST /api/ops/digest/run` を 1 回実行して実測を追いつかせてください。 5 つ全部 true になったら `readyToken` を提出します。",
            "penalty": 14
          }
        ]
      }
    ]
  },
  "track": {
    "id": "stackstack-route",
    "order": 130,
    "chapter": "4. StackStack 本編"
  }
}
