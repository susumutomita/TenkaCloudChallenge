{
  "id": "sha256-schedule-logic",
  "name": "SHA-256 その 2: ビット演算とメッセージスケジュール",
  "category": "Challenge",
  "status": "ready",
  "visibility": "public",
  "difficulty": 3,
  "estimatedDuration": "60〜90 分",
  "shortDescription": "回転とシフトの違い、σ0 σ1 Σ0 Σ1 の 4 種類のずらし量、Ch が選択回路であること、Maj がパリティではないこと、そして 16 ワードを 64 ワードへ広げる漸化式。SHA-256 の配線を全部自分で書く。",
  "description": "SHA-256 を自分で追える手続きに変える 3 問シリーズの 2 問目。1 問目で作った 512 bit ブロックが入力になる。圧縮ループの手前、ビット演算のすべてをここで実装する。\n\nPortal editor のエディタ (`schedule.py`) に 8 つの関数があり、うち 5 つが意図的に壊れている。壊し方はどれも 1 行で、どれも実際によくある間違いにしてある。回転をマスクし忘れて 32 bit を超える、σ1 の第 3 項をシフトではなく回転にする、Σ0 に Σ1 のずらし量を書く、Ch の否定を落とす、Maj をパリティで書く、message schedule の 4 項を加算ではなく XOR で繋ぐ。\n\n採点は 6 checkpoint 独立。rotate (20) は回転とシフトを 1 語ずつ手で出す予測、mux (25) は Ch を選択回路として理解しているかの確認、dependency (30) は 1 bit 反転が 64 ワードのどこに最初に到達するかを漸化式から導く問題、sigma (45) は rotr と 4 つの σ、logic (30) は Ch と Maj、schedule (50) は expand_schedule を隠しテストで検証する。\n\n3 つのコード checkpoint は意図的に独立にしてある。schedule は「W[i] がどの 4 語を読み、それを加算するか」だけを見る。期待値は学習者自身の σ を使って計算するので、σ が間違っていても漸化式が正しければ schedule は通る。σ の正しさは sigma checkpoint の担当で、1 つの間違いを 2 回減点しない。\n\n公開テストは意図的に不十分にしてある。σ を仕様のずらし量と突き合わせず、Ch と Maj を混合セレクタで区別せず、展開するブロックは全ゼロだけ。全ゼロでは XOR と加算が一致するので、4 項を XOR する実装でも全部通る。隠しテストは σ の GF(2) 上の線形性、Maj の対称性、そして展開全体が XOR に対して線形でないこと (= 桁上がりが本当にあること) を検査する。\n\nmutation suite (local/mutation.py) が 33 変異を kill することを CI で担保する。加えて checkpoint の独立性そのものも検査する。σ を壊した変異が schedule suite に漏れていないことを assert してある。等価変異 5 種は意図的に除外し、理由を mutation.py の冒頭に書いた。うち 2 種は実際に survived してから外したものだ。",
  "instructions": "## 前提 — 8 個のスイッチをずらす\n\n- **その 1 の続き**: SHA-256 は材料を 64 バイトのかたまり(ブロック)に切りそろえ、それを 4 バイトずつ 16 組に分けます。 4 バイト = 32 ビットをひとつの数として見たものを **ワード** と呼びます(0 から 2^32 − 1 まで。 16 進なら 8 文字ちょうど)。 この問題は、その 16 個のワードを 64 個に増やし、ビットをかき混ぜる部分です。 その 1 を解いていなくても始められます。\n- **ずらす**: ビットを並べて書くと、ずらし方に 2 種類あることが見えます。 8 ビットの `1011 0010` を例にします。 **右シフト** を 2 回すると、右端の 2 ビットは捨てられ、左から 0 が 2 個入って `0010 1100`。 **右回転** を 2 回すると、右端から出た 2 ビットが左端に回り込んで `1010 1100`。 数えてみてください。 元は 1 が 4 個。 回転のあとも 4 個(何も捨てていない)。 シフトのあとは 3 個(1 個落ちた)。 この違いがこの問題の背骨です。\n- **混ぜる**: **XOR** は 2 つのビットが違えば 1、同じなら 0(`1101` と `1011` なら `0110`)。 **AND** は両方 1 のときだけ 1。 **NOT** は 0 と 1 を入れ替える。 どれも位置ごとに独立に計算します。\n- **32 ビットに収める**: Python の整数は桁が勝手に増えるので、左にずらすと 32 ビットを超えたところにビットが残ります。 32 ビットに戻すには 2^32 で割った余りを取ります(コードでは `& 0xFFFFFFFF`)。 超えても例外は出ません。 静かに間違うだけです。\n- **言葉**: σ(小さいシグマ)と Σ(大きいシグマ)= ギリシャ文字の名前で、それぞれ「回転とシフトを何ビットずつやって XOR で混ぜるか」を決めた関数です。 4 種類あり、違うのはずらす量だけ。 Ch(選ぶ) = 位置ごとに、e のビットが 1 なら f のビット、0 なら g のビットを取る関数。 Maj(多数決) = 位置ごとに、a・b・c の 3 つのうち多いほうの値を取る関数。 漸化式 = 前に作ったものから次を作る規則。 コンテナ = あなた専用に用意される、プログラムを動かす小さなコンピュータ。 問題エディタ = そのコンテナの中のファイルを画面で書き換えて、ボタンで動かせる画面。 checkpoint = 採点される小問。 この問題には 6 つあります。\n- **1 桁の例**(4 ビットで、上の定義どおりに手で): e = `1010`、f = `1100`、g = `0011` のとき Ch(e, f, g) は、左から順に「e が 1 → f の 1」「e が 0 → g の 0」「e が 1 → f の 0」「e が 0 → g の 1」で `1001`。 a = `1010`、b = `1100`、c = `0110` のとき Maj(a, b, c) は、各位置の 3 つを見て多いほうを取り `1110`。 ここで XOR だけで書いた `a ^ b ^ c` を計算すると `0000` になります。 見た目は近いのに、答えは別物です。\n\n## はじめに\n\n指紋が指紋として役に立つのは、元を 1 文字変えたら値が全然違うものになるからです。 そのために SHA-256 は、材料のビットを回し、混ぜ、足す作業を何度も繰り返します。 その配線一式が、この問題でエディタに出る `schedule.py` の 8 つの関数です。\n\nただし、そのうちいくつかは **わざと間違えて書いてあります**。 間違いはどれも 1 行で、どれも人が実際にやる種類のものです。 どの関数が何を満たすべきか、いまの中身がどこで違っているかは、それぞれの説明(docstring)に書いてあります。 あなたの仕事は、説明と中身を突き合わせて直すことです。\n\nやっかいなのは、この手の間違いが例外を出さないことです。 32 ビットを超えても、回転とシフトを取り違えても、プログラムは平然と動いて、違う数を返し続けます。\n\n## 最初の一手\n\nParticipant Portal(この問題文が出ているサイト)でこの問題を開き、ローカルランタイムの **「起動」** を押します。 あなた専用のコンテナが立ち上がり、問題文と同じ画面に **問題エディタ** が出ます。 エディタに出るファイルは `schedule.py` ひとつだけです。\n\n次に **「証拠を確認」** を押してください。 打ち込むものはありません。 出てくるのは次の 3 つで、どれも 16 進とビットの並びの両方で表示されます(ビット表示があるのは、位置を数えたいからです)。\n\n1. **ワード 1 個と、回転する回数・シフトする回数** — rotate で使います。\n2. **ワード 3 個(e, f, g)** — mux で使います。\n3. **ワード 16 個(W[0] から W[15])と、反転させる語の番号・ビットの位置** — dependency で使います。\n\nいちばん上に python のバージョンと health token も出ますが、この問題ではどの checkpoint にも使いません。 sigma・logic・schedule には読む材料がなく、代わりにどの関数がどの checkpoint に対応するかが出ます。 数値は起動するたびに変わるので、他の人の値を写しても合いません。\n\n## 答えるもの — checkpoint は 6 個\n\n6 つは別々に採点されるので、ひとつ詰まっても他は取れます。 直接回答は回答欄に値を入れてから、コードの checkpoint はエディタを書いてから、その checkpoint の **「提出 (+点数)」** を押します。 16 進は `\"1a2b3c4d\"` のように引用符で囲むと確実です(数字だけの並びでも 16 進として読ませるためです)。\n\n1. **rotate**(直接回答) — 表示されたワードを、指定された回数だけ右に **回転** させた結果と、指定された回数だけ右に **シフト** させた結果。 **回転が先** で、16 進 8 文字ずつ 2 個(`\"1a2b3c4d, 5f6e7d8c\"` の形)。 ビットの行を数えるのがいちばん速い方法です。\n2. **mux**(直接回答) — 表示された e・f・g に対する Ch(e, f, g) を、16 進 8 文字ひとつで。\n3. **dependency**(直接回答) — 表示された 16 個のワードのうち、指定された語の指定されたビットを 1 個だけ反転させます。 64 個に広げた並びの中で、**最初に値が変わる語の番号** を整数ひとつで。 64 個ぜんぶ計算しなくても、`expand_schedule` の説明に書かれた「W[i] が自分より前のどの 4 語を読むか」から追えます。\n4. **sigma**(コード) — `rotr` と 4 つの σ / Σ。 `rotr` は 4 つ全部の土台なので、まずここを疑います。\n5. **logic**(コード) — `choose`(Ch)と `majority`(Maj)。\n6. **schedule**(コード) — `expand_schedule`。 16 個のワードを 64 個に増やす関数で、最初の 16 個はそのまま残し、17 個目から先を、自分より前の 4 語から作ります。 その式と条件は docstring にあります。\n\nコードの checkpoint には回答欄がなく、押した時点のエディタの中身が送られます。 間違えても直して再提出できます(誤答 1 回につき 10 点。 合計が 0 点未満になることはありません)。 **「公開テストを実行」** はいまのエディタで公開テストを走らせ、**「初期状態に戻す」** はエディタを最初の状態に戻します。\n\n## ゴール\n\n6 つ全部が正解になれば終わりです。 ここでも先に言っておきます。 **「公開テストを実行」が全部緑でも、採点はそれより広い範囲で行われます。** 公開テストは限られた入力しか通しません。 ある入力の組み合わせでは、間違った書き方と正しい書き方がたまたま同じ答えを出すことがあります。 だから、緑を見て終わりにせず、docstring の条件を 1 行ずつ自分の実装と突き合わせてください。 ここで直した 8 つの部品は、その 3 でそのまま完成品として渡され、指紋を作る本体の計算に使われます。\n",
  "writeup": "## 何を確認したか\n\n### 回転とシフトは別物で、両方が必要\n\n回転は 32 ビットの置換です。ビットは 1 つも失われず、立っているビットの数は変わりません。シフトは失います。\n\nSHA-256 は両方を使い、どちらを使うかで意味が変わります。σ0 と σ1 は回転 2 つとシフト 1 つ、Σ0 と Σ1 は回転 3 つです。σ にシフトが入っているのは偶然ではありません。シフトがビットを落とすからこそ message schedule は単射でなくなり、64 ワードから 16 ワードを逆算できなくなります。Σ は圧縮関数の中で状態を撹拌するだけなので、情報を落とす必要がありません。\n\nずらし量は 4 種類あって覚え違いを誘います。σ0 は (7, 18, 3)、σ1 は (17, 19, 10)、Σ0 は (2, 13, 22)、Σ1 は (6, 11, 25) です。記憶ではなく仕様表を見るべき場所です。\n\nPython で書くときの罠がもう 1 つあります。`value << (32 - amount)` の結果は 32 bit を超えるので、`& 0xFFFFFFFF` でワードに戻す必要があります。忘れると値は静かに巨大化し、σ もスケジュールも全部おかしくなりますが、例外は出ません。\n\n### Ch は選択回路、Maj は多数決\n\nCh(e, f, g) = (e AND f) XOR (NOT e AND g) を式として覚えると何も分かりません。1 bit 分だけ書き出すと正体が見えます。e のビットが 1 なら f のビットが出て、0 なら g のビットが出る。つまり 32 個並んだマルチプレクサで、e がセレクタ、f と g が選択肢です。\n\nMaj(a, b, c) = (a AND b) XOR (a AND c) XOR (b AND c) はビット位置ごとの多数決です。入力が 3 つなので必ず 2 対 1 が付きます。XOR で書けるのは、3 つのペア項のうち浮いている 1 つが高々 1 つしかないからで、OR で書いても同じ値になります。\n\nよくある誤解は Maj をパリティ `a ^ b ^ c` と混同することです。1 bit で見ると、一致するのは全 0 と全 1 の 2 通りだけで、残る 6 通りで食い違います。それでも fixture が 1 つだと通ってしまう種類の間違いなので、隠しテストは 1 bit の真理値表 8 行を全部回します。\n\n### message schedule は加算で、XOR ではない\n\nW[i] = W[i-16] + σ0(W[i-15]) + W[i-7] + σ1(W[i-2]) を mod 2^32 で足します。XOR は安く見える代用品で、間違っている理由がはっきりしています。XOR に桁上がりが無いので、あるビット位置の情報がより上の位置へ伝わりません。message schedule が存在する理由そのものである拡散が起きなくなります。\n\nこれは検査もできます。回転・シフト・XOR はすべて GF(2) 上で線形なので、4 項を XOR する展開は全体として線形になり、`expand(a XOR b)` がちょうど `expand(a) XOR expand(b)` になります。加算には桁上がりがあるのでそうなりません。隠しテストはこの性質を直接見ています。固定の期待値ではなく関係なので、暗記では通りません。\n\n### 1 bit はどこまで伝わるか\n\ndependency checkpoint の答えは 1 つの式ではありません。W[i] が読むのは i-16、i-15、i-7、i-2 の 4 つですが、W[0] から W[15] は入力なので、計算されるワードは添字 16 以上だけです。したがって入力の添字 k を最初に読む計算済みワードは、k+16、k+15、k+7、k+2 のうち 16 以上になる最小のものです。k=0 なら 16、k=1..8 なら k+15、k=9..13 なら k+7、k=14,15 なら k+2 になります。\n\n途中で差分が打ち消えることはありません。σ は 1 bit の差分を 2 つか 3 つの別々の位置へ送り、その XOR がゼロになることはないからです。\n\n## 公開テストが通っても終わりではない\n\nこの問題の公開テストは、σ を仕様のずらし量と突き合わせず、Ch と Maj を混合セレクタで区別せず、展開するブロックは全ゼロだけです。全ゼロでは XOR と加算が一致するので、4 項を XOR する実装でも全部通ります。\n\n隠しテストは境界値 (0、0xffffffff、0x80000000、0x55555555) と seed 由来の語を回し、σ の線形性、Maj の対称性、Ch の「両選択肢が同じなら結果もそれ」という性質、そして展開が XOR に対して線形でないことを検査します。\n\n## 次に何を学ぶか\n\nここまでで圧縮関数の部品はすべて揃いました。次の問題では T1 と T2 を組み立て、a から h の 8 語を 64 round 回し、初期値に足し戻して digest を作ります。そのあとで 1 bit 変えた入力を通し、なぜ出力が半分変わるのかを測ります。",
  "tags": [
    "sha256",
    "hash-function",
    "bitwise",
    "message-schedule",
    "cryptography-basics",
    "local-play",
    "container"
  ],
  "exposedPorts": [
    {
      "port": 18091,
      "name": "Portal editor API + /verify (loopback only)"
    }
  ],
  "learningGoals": [
    "回転とシフトを区別し、どちらがビットを失うかを説明できる",
    "σ0 σ1 Σ0 Σ1 の 4 種類のずらし量を仕様どおりに実装できる",
    "σ にシフトが入っている理由を message schedule の非可逆性から説明できる",
    "Ch をビットごとの選択回路として説明し、実装できる",
    "Maj が多数決でありパリティではないことを真理値表で示せる",
    "message schedule の漸化式を加算で実装し、XOR では拡散が起きないことを説明できる",
    "入力 1 ワードの変更が 64 ワードのどこに最初に到達するかを漸化式から導ける"
  ],
  "runtime": {
    "provider": "docker",
    "engine": "compose",
    "entry": "local/docker-compose.yml",
    "verifyUrl": "http://127.0.0.1:18091/verify",
    "secretEnv": [
      "FLAG_SEED"
    ]
  },
  "scoring": {
    "kind": "multi-verify",
    "checks": [
      {
        "id": "rotate",
        "label": "回転とシフトを手で区別する",
        "points": 20,
        "wrongAnswerPenalty": 10,
        "hints": [
          {
            "id": "rotate-hint-1",
            "content": "`inspect` のビット行を数えるのが速い。回転は立っているビットの数を変えない。シフトは下から抜けた分だけ減り、上から 0 が入る。同じ語を 2 通りにずらして、結果を 2 つ出す。",
            "penalty": 8
          }
        ]
      },
      {
        "id": "mux",
        "label": "Ch が選択回路であることを使う",
        "points": 25,
        "wrongAnswerPenalty": 10,
        "hints": [
          {
            "id": "mux-hint-1",
            "content": "1 bit 分だけ考える。e が 1 のとき (e AND f) は f、(NOT e AND g) は 0。e が 0 なら逆になる。つまり各位置で f か g のどちらか一方が出る。この fixture では f と g が互いの補数なので、答えは e が 1 の位置は f、0 の位置は g をそのまま並べたものになる。",
            "penalty": 10
          }
        ]
      },
      {
        "id": "dependency",
        "label": "1 bit の変更がどこに最初に届くかを導く",
        "points": 30,
        "wrongAnswerPenalty": 10,
        "hints": [
          {
            "id": "dependency-hint-1",
            "content": "W[i] が読むのは W[i-16]、W[i-15]、W[i-7]、W[i-2] の 4 つ。反転させた添字 k を読む i は、この 4 つを逆に解いて k+16、k+15、k+7、k+2 になる。",
            "penalty": 8
          },
          {
            "id": "dependency-hint-2",
            "content": "ただし W[0] から W[15] は入力で、漸化式で計算されるのは添字 16 以上だけ。だから候補のうち 16 未満のものは答えにならない。16 以上の候補の最小値が答え。",
            "penalty": 7
          }
        ]
      },
      {
        "id": "sigma",
        "input": "multiline",
        "label": "rotr と 4 つの σ を実装する",
        "points": 45,
        "wrongAnswerPenalty": 10,
        "hints": [
          {
            "id": "sigma-hint-1",
            "content": "まず `rotr` を疑う。σ 4 つは全部これに乗っている。Python の左シフトは 32 bit で止まらないので、結果を `& 0xFFFFFFFF` でワードに戻す必要がある。戻さないと値は静かに 32 bit を超え、例外は出ない。",
            "penalty": 8
          },
          {
            "id": "sigma-hint-2",
            "content": "ずらし量は σ0 が (7, 18, 3)、σ1 が (17, 19, 10)、Σ0 が (2, 13, 22)、Σ1 が (6, 11, 25)。σ の第 3 項だけがシフトで、Σ は 3 つとも回転。記憶ではなく FIPS 180-4 §4.1.2 の表を見る場所。",
            "penalty": 14
          }
        ]
      },
      {
        "id": "logic",
        "input": "multiline",
        "label": "Ch と Maj を実装する",
        "points": 30,
        "wrongAnswerPenalty": 10,
        "hints": [
          {
            "id": "logic-hint-1",
            "content": "Maj をパリティ `a ^ b ^ c` で書いていないか確認する。1 bit の入力 8 通りの真理値表を書けば、一致するのは全 0 と全 1 の 2 通りだけだと分かる。Ch のほうは否定側のマスクを忘れていないか。",
            "penalty": 12
          }
        ]
      },
      {
        "id": "schedule",
        "input": "multiline",
        "label": "16 ワードを 64 ワードへ広げる",
        "points": 50,
        "wrongAnswerPenalty": 10,
        "hints": [
          {
            "id": "schedule-hint-1",
            "content": "4 項は XOR ではなく加算で、mod 2^32 に収める。この checkpoint は σ の正しさを見ていない。期待値はあなた自身の σ を使って計算されるので、σ が未完成でも漸化式が正しければ通る。",
            "penalty": 10
          },
          {
            "id": "schedule-hint-2",
            "content": "読む添字は i-16、i-15、i-7、i-2 の 4 つで、σ0 が i-15 側、σ1 が i-2 側。最初の 16 語はそのまま残し、16 から 63 まで追加して合計 64 語にする。",
            "penalty": 15
          }
        ]
      }
    ]
  },
  "i18n": {
    "en": {
      "name": "SHA-256 part 2: bit operations and the message schedule",
      "shortDescription": "Rotation versus shift, four different sigma amounts, Ch as a selector circuit, Maj as a majority rather than a parity, and the recurrence that grows sixteen words into sixty-four. All of SHA-256's wiring, written by hand.",
      "description": "Second of the series that turns SHA-256 into a procedure you can follow. The 512-bit block the first problem built is the input. Everything bitwise — all of it — is implemented here, just short of the compression loop.\n\nThe Portal editor file (`schedule.py`) has eight functions, five of them deliberately broken. Every break is one line, and every one is a mistake people actually make: forgetting to mask a rotation so it grows past 32 bits, writing σ1's third term as a rotation instead of a shift, giving Σ0 the shift amounts of Σ1, dropping the negation in Ch, writing Maj as parity, and chaining the schedule's four terms with XOR instead of addition.\n\nScoring is six independent checkpoints. rotate (20) is a word-by-hand prediction of rotation versus shift; mux (25) confirms Ch is understood as a selector circuit; dependency (30) derives from the recurrence where a single flipped bit first reaches among the 64 words; sigma (45) covers rotr and the four σ; logic (30) covers Ch and Maj; schedule (50) verifies expand_schedule against hidden tests.\n\nThe three code checkpoints are independent on purpose. schedule looks only at which four words W[i] reads and that they are added; the expected values are computed with the learner's own σ, so a wrong σ with a correct recurrence still passes schedule. σ's correctness belongs to the sigma checkpoint — one mistake is never charged twice.\n\nThe public tests are deliberately insufficient. They never compare σ against the specified shift amounts, never distinguish Ch and Maj with a mixed selector, and only ever expand an all-zero block — where XOR and addition agree, so an implementation that XORs the four terms passes everything. The hidden tests check σ's linearity over GF(2), Maj's symmetry, and that the expansion as a whole is not linear under XOR (that is, carries really happen).\n\nThe mutation suite (local/mutation.py) kills 33 mutants in CI, and additionally checks checkpoint independence itself — asserting that a broken-σ mutant never leaks into the schedule suite. Five equivalent mutants are excluded on purpose, with the reasons at the top of mutation.py; two of them were excluded only after actually surviving.",
      "instructions": "## Before you start — sliding eight switches\n\n- **Continuing from part 1**: SHA-256 cuts its input into 64-byte blocks and splits each block into sixteen four-byte groups. Four bytes — 32 bits — read as a single number is a **word** (0 up to 2^32 − 1; exactly eight hex characters). This problem grows those sixteen words into sixty-four and stirs the bits. You can start it without having solved part 1.\n- **Sliding**: written out as bits, two different kinds of sliding become visible. Take the eight bits `1011 0010`. A **right shift** by 2 drops the two rightmost bits and feeds two zeros in from the left: `0010 1100`. A **right rotation** by 2 wraps those two bits around to the left instead: `1010 1100`. Count the ones. The original has four. After the rotation, still four — nothing was thrown away. After the shift, three — one fell off. That difference is the spine of this problem.\n- **Mixing**: **XOR** gives 1 where two bits differ and 0 where they agree (`1101` with `1011` gives `0110`). **AND** gives 1 only where both are 1. **NOT** swaps 0 and 1. All three work position by position.\n- **Staying inside 32 bits**: Python integers grow on their own, so shifting left leaves bits sitting above bit 31. Getting back to a word means taking the remainder after dividing by 2^32 (in code, `& 0xFFFFFFFF`). Going over the edge raises nothing. It just quietly gives the wrong number.\n- **Words**: σ (small sigma) and Σ (big sigma) are Greek-letter names for functions that rotate and shift by fixed amounts and XOR the results together. There are four of them, differing only in the amounts. Ch (choose) takes, at each position, f's bit where e's bit is 1 and g's bit where e's bit is 0. Maj (majority) takes, at each position, whichever value at least two of a, b, c agree on. A recurrence is a rule that builds the next item out of ones already built. container = a small computer set up just for you. problem editor = the screen where you edit that container's files and run them with buttons. checkpoint = one small graded question; this problem has six.\n- **A worked example** (four bits, by hand, straight from the definitions above): with e = `1010`, f = `1100`, g = `0011`, Ch(e, f, g) reads left to right as \"e is 1, take f's 1\", \"e is 0, take g's 0\", \"e is 1, take f's 0\", \"e is 0, take g's 1\" — `1001`. With a = `1010`, b = `1100`, c = `0110`, Maj(a, b, c) takes the more common of the three at each position — `1110`. Now compute `a ^ b ^ c` with XOR alone: `0000`. The two look close and are not the same thing at all.\n\n## The situation\n\nA fingerprint is useful because changing one character of the original changes the value completely. To get there, SHA-256 rotates, mixes and adds the input's bits over and over. All of that wiring is the eight functions in `schedule.py`, the one file this problem opens in the editor.\n\nSeveral of them are **wrong on purpose**. Each defect is a single line, and each is a mistake people really make. What each function must satisfy, and where the current body departs from it, is written in that function's own docstring. Your job is to hold the two against each other and fix the code.\n\nThe awkward part is that mistakes of this kind raise nothing. Spill past 32 bits, or swap a rotation for a shift, and the program keeps running quite happily, returning the wrong numbers.\n\n## First move\n\nOpen this problem in Participant Portal (the site showing this statement) and select **Start**, the local runtime's start button. Your own container comes up and the **problem editor** appears on the same page as the statement. It shows one file: `schedule.py`.\n\nThen select **Inspect evidence**. There is nothing to type. It shows three things, each printed both in hex and as a row of bits — the bit rows are there because these questions are about bit positions.\n\n1. **One word, with a rotation amount and a shift amount** — used by rotate.\n2. **Three words (e, f, g)** — used by mux.\n3. **Sixteen words (W[0] through W[15]), with one word index and one bit position to flip** — used by dependency.\n\nA Python version and a health token are printed above all of this; no checkpoint in this problem uses them. There is no fixture to read for sigma, logic and schedule; instead the output names which functions belong to which checkpoint. The values change on every start, so copying somebody else's will not match.\n\n## What to answer — six checkpoints\n\nThe six are graded separately, so getting stuck on one does not cost you the others. For a direct answer, fill in the answer field; for a code checkpoint, edit the file. Then press that checkpoint's **Submit (+points)**. Put hex values inside quotes, like `\"1a2b3c4d\"`, so a run of digits is still read as hex.\n\n1. **rotate** (direct answer) — the displayed word **rotated** right by the given amount, and the same word **shifted** right by the given amount, **rotation first**, as two 8-character hex values (the shape is `\"1a2b3c4d, 5f6e7d8c\"`). Counting along the bit rows is the quickest way.\n2. **mux** (direct answer) — Ch(e, f, g) for the three displayed words, as one 8-character hex value.\n3. **dependency** (direct answer) — flip the single named bit of the named word among the sixteen displayed ones. Then give, as one integer, the **index of the first word that changes** in the sixty-four-word expansion. You do not have to expand all sixty-four: the docstring of `expand_schedule` says which four earlier words each W[i] reads, and that is enough to follow it.\n4. **sigma** (code) — `rotr` and the four σ / Σ functions. Everything else here stands on `rotr`, so suspect it first.\n5. **logic** (code) — `choose` (Ch) and `majority` (Maj).\n6. **schedule** (code) — `expand_schedule`, which grows sixteen words into sixty-four: the first sixteen stay as they are, and each later word is built from four earlier ones. The formula and the conditions are in its docstring.\n\nA code checkpoint has no answer field: whatever is in the editor when you press Submit is what gets sent. A wrong answer can be corrected and resubmitted (each wrong try costs 10 points, and the total never goes below zero). **Run public tests** runs the public suite against your current editor content, and **Restore starter** puts the editor back to its initial state.\n\n## Goal\n\nThe problem is done when all six are correct. The same warning as in part 1 applies: **all of Run public tests going green does not mean grading agrees — grading covers more than the public tests do.** The public suite feeds in only a few inputs, and on some inputs a wrong way of writing this and the right way happen to produce the same answer. So do not stop at green; hold the docstring's conditions against your implementation one line at a time. The eight pieces you fix here are handed to you finished in part 3, where they drive the calculation that actually produces the fingerprint.\n",
      "writeup": "## What this checked\n\n### Rotation and shift are different, and both are needed\n\nA rotation is a permutation of the 32 positions: no bit is lost and the population count is unchanged. A shift loses bits.\n\nSHA-256 uses both, and which one it uses changes the meaning. σ0 and σ1 are two rotations and one *shift*; Σ0 and Σ1 are three rotations. The shift in the small sigmas is not incidental — losing bits is what makes the message schedule non-injective, so the sixteen message words cannot be recovered from the sixty-four. The big sigmas only stir the state inside the compression function, so they have no reason to lose anything.\n\nFour sets of amounts invite a misremembering: σ0 is (7, 18, 3), σ1 is (17, 19, 10), Σ0 is (2, 13, 22), Σ1 is (6, 11, 25). This is a place to read the spec table rather than trust your memory.\n\nOne more Python trap: `value << (32 - amount)` overflows past 32 bits, so the result needs `& 0xFFFFFFFF` to come back to a word. Forget it and values grow silently — every sigma and the whole schedule go wrong, with no exception raised anywhere.\n\n### Ch is a selector, Maj is a majority\n\nCh(e, f, g) = (e AND f) XOR (NOT e AND g) teaches nothing as a formula. Write out a single bit and it appears: where e's bit is 1 you get f's bit, where it is 0 you get g's. It is thirty-two multiplexers side by side, with e as the selector and f and g as the choices.\n\nMaj(a, b, c) = (a AND b) XOR (a AND c) XOR (b AND c) is a per-position majority. With three inputs there is always a two-to-one, and the reason it can be written with XOR is the reason Maj works at all: at most one of the three pairwise terms is ever the odd one out, so nothing cancels. Writing it with OR gives the same value.\n\nThe usual mistake is confusing Maj with the parity `a ^ b ^ c`. On single bits they agree on exactly two of the eight inputs — all zeros and all ones — and disagree on the other six. Close enough to look right, wrong often enough to matter, and still the kind of thing one lucky fixture waves through, which is why the hidden tests run the whole eight-row truth table.\n\n### The message schedule adds; it does not xor\n\nW[i] = W[i-16] + σ0(W[i-15]) + W[i-7] + σ1(W[i-2]), modulo 2^32. Xor is the cheap-looking substitute and it is wrong for a nameable reason: xor has no carries, so information in one bit position can never reach a higher one, and the diffusion the schedule exists to provide never happens.\n\nThat is testable. Rotations, shifts and xor are all GF(2)-linear, so a schedule that xors its four terms is linear as a whole — expanding `a ^ b` gives exactly the xor of the two expansions. Addition has carries and breaks that. The hidden tests check the property directly. It is a relation, not a fixed expected value, so it cannot be memorized.\n\n### How far does one bit reach\n\nThe dependency checkpoint's answer is not one formula. W[i] reads i-16, i-15, i-7 and i-2, but W[0] through W[15] are inputs, so only indices 16 and above are computed. The first computed word to read input index k is therefore the smallest of k+16, k+15, k+7 and k+2 that reaches 16: 16 for k=0, k+15 for k in 1..8, k+7 for k in 9..13, and k+2 for k=14 and 15.\n\nThe difference cannot cancel on the way, because each sigma sends a one-bit difference to two or three distinct positions whose xor is never zero.\n\n## Passing the public tests is not the end\n\nThe public tests here never compare a sigma against its specified amounts, never distinguish Ch from Maj on a mixed selector, and only ever expand the all-zero block — where xor and addition give the same answer. An implementation that xors its four terms passes every one of them.\n\nThe hidden tests sweep boundary words (0, 0xffffffff, 0x80000000, 0x55555555) and seeded ones, and check the sigmas' linearity, Maj's symmetry, Ch's \"both choices equal means the selector is irrelevant\" property, and that the expansion is not linear over xor.\n\n## What to learn next\n\nEvery part of the compression function is now in place. The next problem assembles T1 and T2, runs the eight working words a through h for 64 rounds, and adds the result back into the incoming state to produce a digest. Then it changes one input bit and measures why half the output moves.",
      "learningGoals": [
        "Tell rotation from shift, and say which one loses bits",
        "Implement all four sigma functions with their specified amounts",
        "Explain why the small sigmas include a shift, from the schedule's non-invertibility",
        "Explain and implement Ch as a bitwise selector",
        "Show with a truth table that Maj is a majority and not a parity",
        "Implement the schedule recurrence with addition, and say why xor loses the diffusion",
        "Derive where a one-word change first reaches in the sixty-four-word schedule"
      ],
      "checks": [
        {
          "id": "rotate",
          "label": "Tell a rotation from a shift by hand",
          "hints": [
            {
              "id": "rotate-hint-1",
              "content": "Counting the bit rows from Portal editor `inspect` is quickest. A rotation never changes how many bits are set; a shift loses the ones that fall off the bottom and feeds zeros in at the top. Same word, two different displacements, two results."
            }
          ]
        },
        {
          "id": "mux",
          "label": "Use the fact that Ch is a selector",
          "hints": [
            {
              "id": "mux-hint-1",
              "content": "Think about one bit. Where e is 1, (e AND f) is f and (NOT e AND g) is 0; where e is 0 it is the other way round. So each position yields exactly one of f or g. In this fixture f and g are complements, so the answer is simply f's bits where e is 1 and g's where e is 0."
            }
          ]
        },
        {
          "id": "dependency",
          "label": "Derive where a single flipped bit first arrives",
          "hints": [
            {
              "id": "dependency-hint-1",
              "content": "W[i] reads W[i-16], W[i-15], W[i-7] and W[i-2]. Solve that the other way round: the words that read a flipped index k are k+16, k+15, k+7 and k+2."
            },
            {
              "id": "dependency-hint-2",
              "content": "But W[0] through W[15] are inputs — the recurrence only computes indices 16 and above. So candidates below 16 are not answers, and the smallest candidate that reaches 16 is."
            }
          ]
        },
        {
          "id": "sigma",
          "label": "Implement rotr and the four sigmas",
          "hints": [
            {
              "id": "sigma-hint-1",
              "content": "Suspect `rotr` first: all four sigmas are built on it. Python's left shift does not stop at 32 bits, so the result needs `& 0xFFFFFFFF` to come back to a word. Without it values grow silently and nothing raises."
            },
            {
              "id": "sigma-hint-2",
              "content": "The amounts are σ0 (7, 18, 3), σ1 (17, 19, 10), Σ0 (2, 13, 22), Σ1 (6, 11, 25). Only the small sigmas' third term is a shift; all three of a big sigma's terms are rotations. Read FIPS 180-4 §4.1.2's table rather than trusting memory."
            }
          ]
        },
        {
          "id": "logic",
          "label": "Implement Ch and Maj",
          "hints": [
            {
              "id": "logic-hint-1",
              "content": "Check whether you wrote Maj as the parity `a ^ b ^ c`. Write the eight-row single-bit truth table and you will see they agree on only two rows, all zeros and all ones. On the Ch side, check you did not drop the mask on the complement."
            }
          ]
        },
        {
          "id": "schedule",
          "label": "Grow sixteen words into sixty-four",
          "hints": [
            {
              "id": "schedule-hint-1",
              "content": "The four terms are added, not xored, and the sum is reduced modulo 2^32. This checkpoint does not score your sigmas: the expected value is computed with your own, so a correct recurrence passes even while the sigmas are unfinished."
            },
            {
              "id": "schedule-hint-2",
              "content": "The indices read are i-16, i-15, i-7 and i-2, with sigma0 on the i-15 side and sigma1 on the i-2 side. Keep the first sixteen words as they came in and append indices 16 through 63, for sixty-four in total."
            }
          ]
        }
      ]
    }
  }
}
