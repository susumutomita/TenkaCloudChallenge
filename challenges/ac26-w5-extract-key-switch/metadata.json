{
  "id": "ac26-w5-extract-key-switch",
  "name": "同じ数を、別の鍵の言葉で言う",
  "category": "Challenge",
  "status": "draft",
  "visibility": "public",
  "track": {
    "id": "advanced-cryptography-2026",
    "order": 550,
    "chapter": "Week 5 / Sample Extraction and Key Switching"
  },
  "courseAlignment": {
    "courseId": "advanced-cryptography-program",
    "edition": "2026",
    "week": 5,
    "role": "mechanism",
    "spoilerPolicy": "independent-reimplementation",
    "sources": [
      {
        "repository": "zk-tokyo/advanced-cryptography-2026",
        "ref": "c088f8e6f301dedcd80b6dd9c321a1cd83410637",
        "path": "week5/README.md",
        "kind": "lecture"
      },
      {
        "repository": "zk-tokyo/advanced-cryptography-2026",
        "ref": "c088f8e6f301dedcd80b6dd9c321a1cd83410637",
        "path": "week5/problems/tfhe-toy-python/README.md",
        "kind": "assignment"
      }
    ]
  },
  "difficulty": 4,
  "estimatedDuration": "75-105 分",
  "shortDescription": "回し終わった多項式に答えは載っている。でも欲しいのは係数 1 個だけで、多項式まるごとは大きすぎ、しかも鍵が環の鍵に化けている。定数項を LWE として抜き出し、復号せずに鍵だけ乗り換える。どちらの段にも、復号は 1 回も現れない。",
  "description": "Week 5 の 5 問目。 blind rotation が残した RLWE 暗号文から目的の係数を LWE sample として取り出し、 それを別の secret key・別の次元へ key switching で移す。\n\nblind rotation までは支給される。 環・RLWE・RGSW・external product・CMUX・回転ループはすべて前 3 問の成果物で、 この問題はその後の 2 段。\n\nsample extraction は phase 多項式 `b - a*s` の係数 k を、 環の secret の係数を並べた vector に対する LWE phase として書き直す。 `(a*s)_k` に集まる積のうち添字が degree を跨いだものは符号が反転しているので、 mask の該当スロットもその反転を持たなければならない。 これは回転問題の `X^N = -1` を反対側から見たもの。 復号は起きず noise も増えず、 取り出した phase は多項式の係数そのものになる。\n\nそして取り出した sample の secret は環の secret を vector として読んだもの。 これがシステムの他の部分が使う鍵ではないことが、 次の段が必要な理由。\n\nkey switching key は old index j と level l ごとに `LWE_new(B^(L-1-l) * s_old[j])` を持つ (gadget は降順、digit は大きい重みが先頭)。 mask を分解して対応する entry を引くと、 phase から `<mask, s_old>` がちょうど消える。 ここでも復号は起きない。 s_old は switching key の暗号文の中にしか現れず、 s_new はどこにも現れない。 decrypt して re-encrypt する操作ではない。\n\n採点の設計として、 mask のスロットが巻き戻るのはその添字が取り出す index より上のときだけなので、 最後の係数では巻き戻りが起きない。 そこだけ正しい実装は 1 つの index で止まるテストをすべて通る。 だから extraction は全 index で検査する。 また digit の順序と switching key の読み順を両方逆にした実装は自分自身と完全に整合するので、 検査は交差させる。\n\nこれは安全ではない。 パラメータは全列挙でき、 両方の secret は線形代数で復元できる。",
  "instructions": "## 回し終わった。でも、持ち帰れない\n\nblind rotation が終わると、欲しい答えは多項式の**係数 1 個**に載っています。残りの N − 1 個は用済みです。1 個のために多項式を丸ごと持ち回るのは大きすぎるので、その 1 個だけを LWE の暗号文に落としたい — これが取り出し（sample extraction）です。\n\nところが取り出した暗号文の鍵は、**環の秘密鍵を並びとして読んだもの**になっています。回転の舞台がそうだったからです。それはシステムの他の場所が使う鍵ではなく、このままでは次のゲートに入れられません。だから鍵を乗り換える — これが鍵の切り替え（key switching）です。TFHE の実装がゲートのたびに必ず通るこの 2 段を、この問題で書きます。\n\n## どちらの段でも、復号は起きない\n\n取り出しには鍵が一切渡されません。切り替えも、移し元の鍵を「切り替え鍵の暗号文の中」でしか見ず、移し先の鍵はどこにも現れません。それでも中の数は、ぴったり保たれます。\n\n**鍵が欲しくなったら、そこが設計の勘所です** — 切り替えは「復号してから暗号化し直す」ことではありません。復号できる場所を 1 つでも作れば、そこが秘密の漏れ口になります。\n\n## 前提（支給されるもの）\n\n- 環・RLWE・RGSW・external product・CMUX・回転ループは、前の 4 問の完成品が `participant.fhe` から渡されます。作り直しません\n- 内積 = 2 つの並びを順に掛けて全部足す。LWE の位相 = body − 内積（`ac26-w5-lwe-rlwe`）\n- 桁分け（base B、大きい重みが先頭、L 個、q = B^L）は `ac26-w5-rgsw-external` の約束のまま\n\n## 係数 1 個は、内積 1 本\n\n取り出しの芯は中 3 の展開です。(a₀ + a₁X)(s₀ + s₁X) を展開して X⁰ の係数だけ集めると、a₁s₁X² が X² = −1 で折り返ってくるので **a₀s₀ − a₁s₁**。これは並び **(a₀, −a₁)** と (s₀, s₁) の内積そのものです。\n\nつまり「多項式の積の係数 1 個を見る」ことは、「並びの内積 1 本を見る」ことと同じ。取り出すべき並びは (a₀, a₁) ではなく符号入りの側で、どの位置に符号が入るかは、取り出す係数の番号で決まります。この符号が、取り出しでいちばん間違えやすい場所です。\n\n## 切り替えは、引き算だけでできる\n\n切り替え鍵は「移し元の鍵の欠片 × gadget の重み」を、移し先の鍵で暗号化して並べた表です。取り出した sample のマスクを桁に割り、桁が指す欠片を順に引いていくと、位相から「マスクと移し元の鍵の内積」がちょうど消えます。残るのは同じメッセージと、少し増えたノイズ。変わったのは鍵と次元だけです。\n\n引き算しかしていないことに注意してください — どこにも復号は挟まっていません。\n\n## 最初の一手\n\n**「起動」** で専用環境が立ち上がると、エディタには `extract.py` が 1 つ。書きかけの関数を埋めるだけです。**「初期状態に戻す」** でいつでも戻せます。\n\n**「証拠を確認」** を押すと、この起動のためだけに作られた数値が並びます。\n\n- パラメータ: base B、桁数 L、法 q（= B^L）、環の次数 N（= 取り出した sample の次元）、移し先の次元、メッセージの通り数と持ち上げ幅、ノイズの上限と予算。\n- **本物の回転出力**の暗号文 `a` と `b`（ノイズ込み）。\n- **取り出しの対応表**: `target`（取り出したマスクの何番目か）/ `source`（それが `a` のどの係数から来たか）/ `sign`（+1 か −1）/ `wrapped`（次数をまたいだか）/ `value`（符号を適用したあとの値）。何番目の係数を取り出しているかも書いてあります。\n- 取り出した sample のマスクと body、その位相、そして**元の係数と完全に同じ数**であること。\n- マスクを桁に割った表（係数ごとに、大きい重みから並んだ桁）。\n- 切り替え鍵の情報（移し元と移し先の鍵の id、次元、base、桁数、法、適用できるかどうか、増えるノイズの上限）、切り替え後のマスクと body、そしてそれがどの鍵のものになったか。\n- **同じメッセージを 3 通りで読んだ行**: RLWE の係数として、取り出した sample として、切り替え後の sample として。\n\n**先に見るのはこの 2 か所です。** ひとつは対応表の `wrapped` の列 — 上から読むと `False` から `True` に変わる境目がちょうど 1 か所しかありません。その位置が何と一致しているかに気づくのが、取り出しの山です。もうひとつは「3 通りで同じメッセージ」の行 — 3 つとも同じ値で、しかも**どこにも復号が入っていない**。この 2 つが、8 つの checkpoint の骨格です。\n\n数値は起動のたびに変わり、採点は画面に出ていない別のパラメータでも同じコードを走らせます。**取り出す係数の番号も 0 とは限りません** — 0 番だけを特別扱いした実装はそこで落ちます。**「公開テストを実行」** で形の検査を手元で先に試せます。\n\n## 答えるもの — checkpoint は 8 つ\n\n8 つともコードで答えます（文字を打ち込む回答欄はありません）。`params` は `base`、`levels`、`degree`（N）、`dimension`、`target_dimension`、`modulus`（q）、`plaintext_modulus`、`delta` を持つ dict です。\n\n1. **phase** — `phase_coefficient(params, ring_key, ciphertext, index)`: `b − a·s` の `index` 番目の係数を整数で返します。**この問題で鍵が渡される唯一の関数**で、環の中で普通に計算してよい唯一の場所です。`rlwe_phase(params, secret, ciphertext)` が `participant.fhe` から渡されます。範囲外の `index` は断ります。\n2. **extract** — `extract_sample(params, ciphertext, index)`: `{\"mask\": …, \"body\": …}` を返します。マスクの長さは N（環の秘密鍵の係数 1 つにつき 1 枠）。この sample の位相が、1 で計算した係数と**ぴったり同じ数**にならなければいけません（丸めの範囲内、ではなく完全一致）。取り出しはノイズを足さず、何も復号しません。秘密鍵の各枠 j に対して、対になる `a` の係数はちょうど 1 つあります。それがどれか、そして**どこで次数をまたぐか**を見つけてください。範囲外の `index` は断り、0 番を特別扱いしないこと。\n3. **trace** — `extract_trace(params, ciphertext, index)`: **N 件**の記録を返します。各件は `target`（取り出したマスクの枠 0〜N−1）、`source`（元になった `a` の係数の番号）、`sign`（+1 か −1）、`wrapped`（その対応が次数をまたいだか）、`value`（符号を適用して 0 以上 q 未満に直した値）。\n4. **decompose** — `decompose_mask(params, mask)`: **マスクの係数 1 つにつき 1 組**の桁を返します（大きい重みが先頭、ちょうど L 個）。前々問と**束ね方が逆**です — あちらは「level ごとに 1 つの多項式」でしたが、ここでは各係数の桁が、その係数専用の切り替え鍵の項を指します。\n5. **switch** — `key_switch(params, switching_key, sample)`: `{\"mask\": …, \"body\": …, \"keyId\": …}` を移し先の鍵のもとで返します。`switching_key[\"entries\"][j][l]` が「移し先の鍵で暗号化された、移し元の鍵の j 番目 × 重み B^(L−1−l)」で、他に `sourceKeyId` / `targetKeyId` / `sourceDimension` / `targetDimension` / `base` / `levels` / `modulus` が入っています。**`(0, body)` から始めて引いていきます** — 1 つ引くたびに位相から何が消えるかを紙に書き下すと、答えの形が出ます。次元・パラメータ・sample の `keyId` のどれかが合わない鍵は**適用せずに断ります**（無理に適用すると、形は正しいのにどちらの鍵でもノイズにしか復号されない暗号文ができ、エラーより悪い結果になります）。返り値に秘密を載せないこと。\n6. **domains** — `domain_report(params, sample, switching_key)`: `sourceKeyId`、`targetKeyId`、`sourceDimension`、`targetDimension`、`modulus`、`base`、`levels`、`compatible`（この鍵でこの sample を移せるか）、`noiseAdded`（増えるノイズの**上限**を個数で）を返します。`compatible` は**宣言された情報から**決めます — ここには秘密が 1 つも無いので、「試して復号できるか見る」という決め方はできませんし、そうする仕組みは秘密を最も置いてはいけない場所に置くことになります。`noiseAdded` も同じ理由で実測ではなく上限です。\n7. **endtoend** — 2 と 5 をつないで、**RLWE の係数 / 取り出した sample / 切り替え後の sample の 3 つが同じメッセージを指す**ことを、すべての係数番号について確かめます。さらに、切り替え後の暗号文が入力とそのまま同じであってはいけません。\n8. **transfer** — 同じ `extract.py` が、**画面に出ていないパラメータ**で 1 から 7 まで全部走ります。\n\ncheckpoint ごとに提出ボタンがあります。押すと、そのときのエディタの中身がそのまま採点へ送られます。8 つは独立に採点されるので、1 つ詰まっても他は取れます。不正解でも出し直せます（1 回ごとに決まった減点があり、合計が 0 未満にはなりません）。正解した checkpoint は「解答済み」になり、それ以上は提出しません。\n\n## これで輪が閉じる\n\n8 つ通れば終わりです。入口が LWE、回転が環の上、出口がまた入口と同じ鍵・同じ次元の LWE — つまり出てきた暗号文は、**もう一度同じ装置に入れられます**。\n\nこの「輪が閉じる」が、次の問題 `ac26-w5-pbs-homnand` で 5 つの部品を 1 本の Programmable Bootstrapping につなぐときの、最後の条件です。これが無ければ、回路の 2 つ目のゲートは永遠に組めません。\n",
  "writeup": "## 最後の係数は例外的に易しい\n\nmask のスロットが巻き戻るのは、 その添字が取り出す index より上のとき。 `degree - 1` では上にスロットが無いので巻き戻りが起きず、 符号を完全に無視した実装でも phase が一致してしまう。 index 0 はその逆で、 1 つを除く全スロットが巻き戻る。 public test が 4 つとも最後の係数なのはそれを見せるためで、 hidden test は全 index を回す。\n\n## 何も復号していない\n\nextraction には鍵が渡らない。 key switching にも渡らない。 s_old は switching key の暗号文の中にしか現れず、 s_new はどこにも現れない。 それでも phase は保たれる。 key switching が decrypt して re-encrypt する操作だという理解は、 この導出のどこにも当てはまらない。\n\n## 取り出した鍵は使いたい鍵ではない\n\nextraction の結果の secret は環の secret を vector として読んだもの。 次元は degree で、 システムの他の部分が使う鍵ではない。 だから key switching が要る。 2 段が別々に存在する理由はここにある。\n\n## 交差させないと見えない\n\ndigit の順序と switching key の読み順を両方逆にすると、 自分の分解を自分の switch に通すテストは通る。 だから hidden test は fixture の sample を submission の switch へ、 submission の sample を fixture の switch へ通す。\n\n## compatible は metadata から決める\n\nどちらの secret も手元に無いので、 switch を試して復号できるかで判定することはできない。 できたとしても、 それは secret を最も置いてはならない場所に置くということ。 noise も同じ理由で bound を報告する。 測るには phase が要り、 phase には鍵が要る。\n\n## 対象外\n\nproduction の switching key 生成、 圧縮された switching key、 noise-security パラメータ解析、 multi-key や proxy re-encryption。\n\n## これは安全ではない\n\nパラメータは全列挙でき、 両方の secret は線形代数で復元できる。 機構の toy であって困難性の toy ではない。",
  "tags": [
    "advanced-cryptography-2026",
    "week5",
    "fhe",
    "tfhe",
    "sample-extraction",
    "key-switching",
    "local-play",
    "container"
  ],
  "exposedPorts": [
    {
      "port": 18111,
      "name": "Portal editor API + /verify (loopback only)"
    }
  ],
  "learningGoals": [
    "多項式の積の係数 1 個を、内積 1 本として書き下せる",
    "折り返しの符号まで含めて、取り出すべき並びを組める",
    "取り出しの前後でメッセージが同じ数のまま保たれることを確かめられる",
    "マスクを、切り替え鍵の並びに合う束ね方で桁に割れる",
    "切り替え鍵からの引き算だけで、暗号文を別の鍵へ移せる",
    "変わるのは鍵と次元だけで、メッセージが変わらない理由を言える",
    "移し元・移し先・環の 3 つの鍵を取り違えた欠陥を指摘できる",
    "鍵の切り替えが「復号して再暗号化」ではないことを説明できる"
  ],
  "runtime": {
    "provider": "docker",
    "engine": "compose",
    "entry": "local/docker-compose.yml",
    "verifyUrl": "http://127.0.0.1:18111/verify",
    "secretEnv": [
      "FLAG_SEED"
    ]
  },
  "scoring": {
    "kind": "multi-verify",
    "checks": [
      {
        "id": "phase",
        "input": "multiline",
        "label": "保つべき数を書き下す",
        "points": 30,
        "wrongAnswerPenalty": 15,
        "hints": [
          {
            "id": "phase-hint-1",
            "content": "`rlwe_phase(params, secret, ciphertext)` は支給されている。 ここは環の中で計算してよい唯一の場所で、 鍵が渡る唯一の関数でもある。 index の範囲は検査する。",
            "penalty": 15
          }
        ]
      },
      {
        "id": "extract",
        "input": "multiline",
        "label": "1 係数を取り出す",
        "points": 50,
        "wrongAnswerPenalty": 15,
        "hints": [
          {
            "id": "extract-hint-1",
            "content": "secret の添字 j ごとに、 対になる a の係数はちょうど 1 つ。 j <= index なら添字は index - j、 そうでなければ degree を跨ぐ。 跨いだ側に何が起きるかが要点。",
            "penalty": 25
          }
        ]
      },
      {
        "id": "trace",
        "input": "multiline",
        "label": "対応を見せる",
        "points": 35,
        "wrongAnswerPenalty": 15,
        "hints": [
          {
            "id": "trace-hint-1",
            "content": "記録は degree 個。 `wrapped` の列を上から読むと境界は 1 か所だけで、 その位置は偶然ではない。 `value` は符号を適用したあとの mask 係数。",
            "penalty": 17
          }
        ]
      },
      {
        "id": "decompose",
        "input": "multiline",
        "label": "mask を桁へ分解する",
        "points": 25,
        "wrongAnswerPenalty": 15,
        "hints": [
          {
            "id": "decompose-hint-1",
            "content": "level ごとではなく **係数ごと**に 1 タプル。 ac26-w5-rgsw-external とは束ね方が逆で、 そちらは環の元を掛けるためだった。",
            "penalty": 12
          }
        ]
      },
      {
        "id": "switch",
        "input": "multiline",
        "label": "別の鍵へ移す",
        "points": 55,
        "wrongAnswerPenalty": 15,
        "hints": [
          {
            "id": "switch-hint-1",
            "content": "`(0, body)` から始めて引く。 entry 1 つを digit 個だけ引くと phase から何が消えるかを書き下すと、 答えの形が出る。 合わない鍵は適用せず拒否する。",
            "penalty": 27
          }
        ]
      },
      {
        "id": "domains",
        "input": "multiline",
        "label": "どの鍵の話かを分類する",
        "points": 35,
        "wrongAnswerPenalty": 15,
        "hints": [
          {
            "id": "domains-hint-1",
            "content": "source は ring key、 target は別の鍵で、 次元も違う。 どちらかをもう一方から写すと 2 つの値が一致してしまう。 `compatible` は宣言された metadata から決める。",
            "penalty": 17
          }
        ]
      },
      {
        "id": "endtoend",
        "input": "multiline",
        "label": "3 通りが同じ答えになる",
        "points": 40,
        "wrongAnswerPenalty": 15,
        "hints": [
          {
            "id": "endtoend-hint-1",
            "content": "RLWE の係数、 取り出した sample、 移したあとの sample。 3 つとも同じ message で、 かつ最後のものは入力と同じ暗号文であってはならない。",
            "penalty": 20
          }
        ]
      },
      {
        "id": "transfer",
        "input": "multiline",
        "label": "見たことのない設定で成立させる",
        "points": 30,
        "wrongAnswerPenalty": 15
      }
    ]
  },
  "i18n": {
    "en": {
      "name": "Say the same number in another key's words",
      "shortDescription": "The rotated polynomial holds the answer — but you want one coefficient, the whole polynomial is too much to carry, and the key underneath has turned into the ring's. Extract the coefficient as an LWE sample, then change only the key. Nothing is decrypted at either step.",
      "description": "The fifth Week 5 problem. Take the wanted coefficient out of the RLWE ciphertext blind rotation left behind, as an LWE sample, then move that sample to a different secret key and dimension by key switching.\n\nEverything up to blind rotation is supplied -- the ring, RLWE, RGSW, the external product, CMUX and the rotation loop are the previous three problems' output. This problem is the two steps after.\n\nSample extraction rewrites coefficient k of the phase polynomial `b - a*s` as an LWE phase over the ring secret's own coefficients. Among the products collected into `(a*s)_k`, the ones whose indices crossed the degree arrive negated, so the matching mask slot has to carry that negation -- it is the rotation problem's `X^N = -1` seen from the other side. Nothing is decrypted, no noise is added, and the extracted phase is the polynomial's coefficient exactly.\n\nAnd the extracted sample's secret is the ring secret read as a vector. That being not the key the rest of the system uses is precisely why the second step exists.\n\nThe switching key holds `LWE_new(B^(L-1-l) * s_old[j])` for every old index and level (the gadget is descending; digits come most-significant weight first). Decompose the mask, subtract the matching entries, and `<mask, s_old>` cancels out of the phase. Nothing is decrypted here either: `s_old` appears only inside the switching key's ciphertexts and `s_new` appears nowhere. It is not a decrypt followed by a re-encrypt.\n\nOn scoring: a mask slot wraps only when its secret index is above the extracted one, so at the last coefficient nothing wraps and an implementation correct only there passes every test that checks a single index. Extraction is therefore checked at every index. And an implementation that reverses both the digit order and the key's read order agrees with itself completely, so the checks are crossed.\n\nNone of this is secure. The parameters are small enough to enumerate and both secrets fall to linear algebra.",
      "instructions": "## The turn is done — and you cannot carry it home\n\nWhen blind rotation finishes, the answer you want sits in **one coefficient** of a polynomial. The other N − 1 have served their purpose. Carrying a whole polynomial around for one number is too much, so that single coefficient gets dropped into an LWE ciphertext — that is sample extraction.\n\nBut the extracted ciphertext's key has become **the ring secret read as a row of numbers**, because that is the key the rotation ran under. It is not the key the rest of the system uses, and as it stands it cannot enter the next gate. So the key under it gets changed — that is key switching. These are the two steps every TFHE implementation walks after every gate, and they are what you write here.\n\n## Neither step decrypts anything\n\nExtraction is handed no key at all. The switch sees the source key only inside the switching key's ciphertexts, and the target key appears nowhere. The number inside is preserved exactly anyway.\n\n**If you catch yourself wanting a key, that is the design telling you something** — switching is not \"decrypt, then re-encrypt\". Build even one place that can decrypt, and that place is where the secret leaks.\n\n## Before you start (what is supplied)\n\n- The ring, RLWE, RGSW, the external product, CMUX and the rotation loop arrive finished from `participant.fhe` — the previous four problems' output, not rebuilt here\n- Inner product = multiply two rows pairwise and sum. An LWE phase = body − inner product (`ac26-w5-lwe-rlwe`)\n- The digit convention (base B, largest weight first, L digits, q = B^L) is unchanged from `ac26-w5-rgsw-external`\n\n## One coefficient is one inner product\n\nThe heart of extraction is school expansion. Expand (a₀ + a₁X)(s₀ + s₁X) and collect only the coefficient of X⁰: a₁s₁X² folds back through X² = −1, giving **a₀s₀ − a₁s₁**. That is exactly the inner product of **(a₀, −a₁)** with (s₀, s₁).\n\nSo reading one coefficient of a polynomial product is the same act as reading one inner product. The row to pull out is not (a₀, a₁) but the signed one — and where the signs go is decided by which coefficient is being extracted. That sign is the single easiest thing to get wrong here.\n\n## Switching is nothing but subtraction\n\nA switching key is a table of \"one piece of the source key × one gadget weight\", each encrypted under the target key. Split the extracted sample's mask into digits, subtract the entries the digits point at, and the inner product of mask and source key cancels out of the phase exactly. What remains is the same message and a little more noise. Only the key and the dimension changed.\n\nNotice that nothing but subtraction happened — no decryption is wedged in anywhere.\n\n## First move\n\n**Start** brings up your own environment; the editor holds one file, `extract.py`, already scaffolded — you fill in the existing functions. **Restore starter** puts it back at any time.\n\nPress **Inspect evidence** and this run's numbers appear.\n\n- The parameters: base B, the digit count L, the modulus q (= B^L), the ring degree N (which is the extracted sample's dimension), the target dimension, the number of message values and the lift, and the noise bound and budget.\n- A **real blind-rotation output** as a ciphertext `a` and `b`, noise and all.\n- **The extraction mapping table**: `target` (which slot of the extracted mask), `source` (which coefficient of `a` it came from), `sign` (+1 or −1), `wrapped` (whether that pairing crossed the degree), `value` (the coefficient with the sign applied). It also says which coefficient is being extracted.\n- The extracted sample's mask and body, its phase, and the fact that it is **exactly the same number** as the original coefficient.\n- The mask split into digits (per coefficient, largest weight first).\n- The switching key's details (source and target key ids, dimensions, base, levels, modulus, whether it applies, and the bound on the noise it adds), the switched mask and body, and which key it now belongs to.\n- **The same message read three ways**: as the RLWE coefficient, as the extracted sample, and as the switched sample.\n\n**Two places are worth looking at first.** One is the mapping table's `wrapped` column — read down it and there is exactly one place where `False` becomes `True`. Noticing what that position coincides with is the step that unlocks the extraction. The other is the \"same message three ways\" block: three identical values, and **no decryption anywhere in between**. Those two are the skeleton of all eight checkpoints.\n\nThe numbers change every run, and grading re-runs your code on parameters that were never shown. **The coefficient index is not always 0** either — an implementation that special-cases 0 fails there. **Run public tests** checks the shapes locally first.\n\n## What you answer — eight checkpoints\n\nAll eight are answered with code; there is no field to type a value into. `params` is a dict carrying `base`, `levels`, `degree` (N), `dimension`, `target_dimension`, `modulus` (q), `plaintext_modulus` and `delta`.\n\n1. **phase** — `phase_coefficient(params, ring_key, ciphertext, index)`: returns coefficient `index` of `b − a·s` as an integer. This is **the only function in this problem that is handed a secret**, and the only place you may just compute in the ring. `rlwe_phase(params, secret, ciphertext)` is supplied by `participant.fhe`. Reject an index outside the ring.\n2. **extract** — `extract_sample(params, ciphertext, index)`: returns `{\"mask\": …, \"body\": …}`. The mask has N entries, one per ring-secret coefficient. This sample's phase must come out as **exactly the same number** as checkpoint 1's coefficient — not to within a rounding step. Extraction adds no noise and decrypts nothing. For each secret slot j there is exactly one coefficient of `a` that pairs with it; find which, and find **where the pairing crosses the degree**. Reject an index outside the ring, and do not special-case index 0.\n3. **trace** — `extract_trace(params, ciphertext, index)`: returns **N records**, each carrying `target` (the extracted mask slot, 0 … N−1), `source` (which coefficient of `a` it came from), `sign` (+1 or −1), `wrapped` (whether that pairing crossed the degree) and `value` (the coefficient with the sign applied, reduced into 0 … q − 1).\n4. **decompose** — `decompose_mask(params, mask)`: returns **one digit tuple per mask coefficient**, most significant first, exactly L of them. The grouping is **the opposite way round** from the earlier problem: there it was one ring element per level, whereas here each coefficient's digits index into that coefficient's own switching-key entries.\n5. **switch** — `key_switch(params, switching_key, sample)`: returns `{\"mask\": …, \"body\": …, \"keyId\": …}` under the target key. `switching_key[\"entries\"][j][l]` is \"the source key's j-th entry times the weight B^(L−1−l), encrypted under the target key\", alongside `sourceKeyId` / `targetKeyId` / `sourceDimension` / `targetDimension` / `base` / `levels` / `modulus`. **Start from `(0, body)` and subtract** — write down what each subtraction removes from the phase and the shape of the answer follows. A key whose dimension, parameters, or `sourceKeyId` does not match the sample's `keyId` must be **refused rather than applied** (applying it anyway yields a well-formed ciphertext that decrypts to noise under both keys, which is worse than an error). The result must not carry a secret.\n6. **domains** — `domain_report(params, sample, switching_key)`: returns `sourceKeyId`, `targetKeyId`, `sourceDimension`, `targetDimension`, `modulus`, `base`, `levels`, `compatible` (whether this key can switch this sample) and `noiseAdded` (the **bound** on the noise the switch adds, as a count). Decide `compatible` **from the declared metadata** — there is no secret here at all, so it cannot be settled by trying the switch and seeing whether the result decrypts, and a system that settled it that way would need the secrets in the one place they must not be. `noiseAdded` is a bound for the same reason.\n7. **endtoend** — checkpoints 2 and 5 joined up: **the RLWE coefficient, the extracted sample and the switched sample must all name the same message**, for every coefficient index. And the switched ciphertext must not simply be its input passed through.\n8. **transfer** — the same `extract.py` runs checkpoints 1 to 7 again on **parameters that were never shown**.\n\nEach checkpoint has its own submit button; pressing it sends the editor's current contents straight to grading. The eight are graded independently, so one that will not budge does not block the rest. A wrong answer can be resubmitted (each wrong attempt carries a fixed penalty, and the total never drops below zero). A solved checkpoint is marked solved and is not submitted again.\n\n## The loop closes here\n\nPass all eight and you are done. LWE at the entrance, a ring in the middle, and LWE under the entrance's own key and dimension at the exit — meaning the ciphertext that comes out **can go back into the same machine**.\n\nThat closed loop is the last condition the next problem, `ac26-w5-pbs-homnand`, needs when it joins Week 5's five parts into one Programmable Bootstrapping. Without it, the second gate of any circuit could never be built.\n",
      "writeup": "## The last coefficient is unfairly easy\n\nA mask slot wraps when its secret index is above the extracted one, so at `degree - 1` nothing wraps at all -- an implementation that ignores the sign completely still preserves the phase there, and only there. Index 0 is the opposite: every slot but one wraps. All four public tests use the last coefficient to make that visible; the hidden tests run every index.\n\n## Nothing was decrypted\n\nExtraction is handed no key. Neither is key switching. `s_old` appears only inside the switching key's ciphertexts and `s_new` appears nowhere at all -- and the phase is still preserved. The picture of key switching as a decrypt followed by a re-encrypt fits no step of that derivation.\n\n## The key you extract is not the key you want\n\nThe extracted sample's secret is the ring secret read as a vector, at dimension `degree`. That is not the key the rest of the system uses, which is why key switching is needed. The two steps exist separately for that reason.\n\n## Crossed, or it proves nothing\n\nReverse the digit order and the key's read order together and a test that runs your decomposition through your switch still passes. So the hidden tests cross them: fixture-built samples through the submission's switch, and the submission's samples through the fixtures'.\n\n## `compatible` is decided from metadata\n\nNeither secret is on hand, so it cannot be settled by trying the switch and seeing whether the result decrypts -- and a system that settled it that way would need the secrets in the one place they must not be. The noise figure is a bound for the same reason: measuring it takes a phase, and a phase takes a key.\n\n## Not in scope\n\nProduction switching-key generation, compressed switching keys, noise-security parameter analysis, multi-key or proxy re-encryption.\n\n## This is not secure\n\nThe parameters are small enough to enumerate and both secrets fall to linear algebra. A toy of the mechanism, not of the hardness.",
      "learningGoals": [
        "Write one coefficient of a polynomial product down as one inner product",
        "Build the row to extract, folding signs included",
        "Confirm the message is the exact same number on both sides of extraction",
        "Split a mask into digits grouped to match the switching key",
        "Move a ciphertext to another key using nothing but subtractions of switching-key entries",
        "Explain why only the key and the dimension change, and the message does not",
        "Point out a defect that confuses the source, target and ring keys",
        "Explain why key switching is not a decrypt followed by a re-encrypt"
      ],
      "checks": [
        {
          "id": "phase",
          "label": "Write down the number to preserve",
          "hints": [
            {
              "id": "phase-hint-1",
              "content": "`rlwe_phase(params, secret, ciphertext)` is supplied. This is the only place the ring is computed in and the only function given a key. Check the index range."
            }
          ]
        },
        {
          "id": "extract",
          "label": "Take one coefficient out",
          "hints": [
            {
              "id": "extract-hint-1",
              "content": "For each secret index j there is exactly one coefficient of `a` that pairs with it: `index - j` when `j <= index`, and otherwise a pairing that crosses the degree. What happens on the far side is the point."
            }
          ]
        },
        {
          "id": "trace",
          "label": "Show the mapping",
          "hints": [
            {
              "id": "trace-hint-1",
              "content": "`degree` records. Reading down `wrapped` shows exactly one boundary, and where it sits is not a coincidence. `value` is the mask coefficient after the sign."
            }
          ]
        },
        {
          "id": "decompose",
          "label": "Break the mask into digits",
          "hints": [
            {
              "id": "decompose-hint-1",
              "content": "One tuple **per coefficient**, not per level. The grouping is the opposite of ac26-w5-rgsw-external's, where a level had to be a ring element."
            }
          ]
        },
        {
          "id": "switch",
          "label": "Move it to another key",
          "hints": [
            {
              "id": "switch-hint-1",
              "content": "Start from `(0, body)` and subtract. Write down what subtracting one entry `digit` times removes from the phase and the shape of the answer follows. A key that does not match is refused, not applied."
            }
          ]
        },
        {
          "id": "domains",
          "label": "Classify which key it is about",
          "hints": [
            {
              "id": "domains-hint-1",
              "content": "The source is the ring key, the target is a different key, and the dimensions differ too. Copying either from the other makes two values agree that should not. Decide `compatible` from the declared metadata."
            }
          ]
        },
        {
          "id": "endtoend",
          "label": "Three routes, one answer",
          "hints": [
            {
              "id": "endtoend-hint-1",
              "content": "The RLWE coefficient, the extracted sample, the switched sample -- the same message from all three, and the last one must not be the ciphertext it started as."
            }
          ]
        },
        {
          "id": "transfer",
          "label": "Hold up in a setting you have not seen"
        }
      ]
    }
  }
}
