{
  "id": "ac26-w3-field-inverse",
  "name": "曲線の前に、体を作る",
  "category": "Challenge",
  "status": "draft",
  "visibility": "public",
  "track": {
    "id": "advanced-cryptography-2026",
    "order": 310,
    "chapter": "Week 3 / Finite Fields"
  },
  "courseAlignment": {
    "courseId": "advanced-cryptography-program",
    "edition": "2026",
    "week": 3,
    "role": "mechanism",
    "spoilerPolicy": "independent-reimplementation",
    "sources": [
      {
        "repository": "zk-tokyo/advanced-cryptography-2026",
        "ref": "a3aa4b56fa88fbe803b57d320fbc87c1a203b480",
        "path": "week3/README.md",
        "kind": "lecture"
      },
      {
        "repository": "zk-tokyo/advanced-cryptography-2026",
        "ref": "a3aa4b56fa88fbe803b57d320fbc87c1a203b480",
        "path": "week3/problems/schnorr-from-scratch/README.md",
        "kind": "assignment"
      },
      {
        "repository": "zk-tokyo/advanced-cryptography-2026",
        "ref": "b1b46669949b5615be7941b2b62da4ea635e9c30",
        "path": "week3/week3_zksnark_slides.pdf",
        "kind": "slide"
      }
    ]
  },
  "difficulty": 3,
  "estimatedDuration": "45-60 分",
  "shortDescription": "余りの計算で「割る」を作る。掛けて余りが1になる相手を逆元と呼びます。まず −2 = (−1)×7 + 5 から、7で割った余り5を確認します。この割る数を法と呼びます。表を使い、逆元がある数とない数を見分けます。",
  "description": "余りの計算で「割る」を作る。掛けて余りが 1 になる相手を逆元と呼びます。まず −2 を法 7 の余り 5 に直し、表を使って逆元がある数とない数を見分けます。",
  "instructions": "## まず、−2 を「余り 5」に直す\n\nあなたは暗号の計算に使う「割り算」の部品を作ります。整数をそのまま割る代わりに、**掛けて余りが 1 になる相手（逆元）**を使います。\n\n`FieldElement` は法と余りをまとめる型、`__init__` は元を作るときに動くメソッド（型の中の関数）です。\n\n**起動 → 証拠を確認 → `field.py` の `FieldElement.__init__` を編集 → 公開テストを実行 →「余りを 0 以上にそろえる」の提出**。まず `self.value` に、`value` を `field.modulus` で割った余りを入れてみてください。負の数を含む公開テストで結果を確認できます。\n\n## 前提\n\n- 割る数 `m` を **法** と呼びます。この問題では整数 `m > 1` です。整数 `v` を `m` で割った余りは Python の `v % m`。`v = m × q + r` と書ける `0 ≤ r < m` を取ります。例：`−2 = 7 × (−1) + 5` なので `(-2) % 7 = 5`。\n- この範囲の余りにそろえる操作が **正規化**。法と余りの組が、このプログラムの **元（element）** です。`Field(m)` が法を保管し、`.element(v)` が元を作ります。`self` は「この元自身」、`self.value` は余り、`self.field.modulus` は法です。クラス内の関数を **メソッド** と呼びます。\n- `u ≡ v (mod m)` は「`u` と `v` を `m` で割った余りが同じ」。`≡` はこの意味の記号です。`gcd(a,m)` は `a` と `m` の最大公約数。\n- 法が素数 `p` なら、0 以外の全ての元に逆元があります。この余りの計算は **有限体**（有限個の元で四則ができ、0 以外で割れる仕組み）の一例です。合成数の法も同じコードで扱いますが、全ての非零の元では割れません。\n\n## 足す・引く・掛ける → 余りへ戻す\n\n| 操作 | 一般の式 | 法 7、a=5、b=4 |\n|---|---|---|\n| `a + b` | `(a+b) % m` | `9 % 7 = 2` |\n| `a - b` | `(a-b) % m` | `1 % 7 = 1` |\n| `a * b` | `(a*b) % m` | `20 % 7 = 6` |\n\n`__add__`・`__sub__`・`__mul__` に対応します。整数ではなく、同じ法の `FieldElement` を返します。元を作るたびに正規化すると、途中の答えも範囲に収まります。\n\n**等しいとは、法も余りも同じこと。** 法 7 の −2 と 5 は等しく、法 6 の 5 とは違います。`__eq__` がこの比較です。`__hash__` は辞書や集合が元を整理するための整数を返すメソッドで、等しい元なら同じ値が必要です。`hash((self.field.modulus, self.value))` が使えます。違う元のハッシュがたまたま同じでも構いません。\n\n足す順番や括弧で答えが変わらず、`x+0=x`、`x*1=x`、`x-x=0`、`x*(y+z)=x*y+x*z` も成り立ちます。たとえば法 7 で `2*(3+4)` と `2*3+2*4` は、どちらも余り 0 です。\n\n## 割る →「掛けて 1 になる相手」を掛ける\n\n`a` の逆元を `x` とすると **`a*x ≡ 1 (mod m)`**。法 7 では `3*5=15=7*2+1` なので 3 の逆元は 5。`4 / 3` は `4*5 % 7 = 6` です。\n\nどうやって相手を探すのでしょう。`a*s + m*t = g` の整数 `s,t` と最大公約数 `g` を同時に求める **拡張ユークリッドの互除法**を使います。`g=1` なら `m*t` は余り 0 なので **逆元は `s % m`**。`g>1` なら、`a` と `m` のどんな整数倍の和も `g` の倍数で、1 にはなりません。\n\n```text\n3 × (−2) + 7 × 1 = 1  →  3 の逆元は (−2) % 7 = 5\n2 × 1    + 6 × 0 = 2  →  2 と 6 の最大公約数は 2。逆元なし\n```\n\n### 表の作り方：同じ等式を壊さず引き算する\n\n各行を `(r,s,t)` とし、常に `a*s + m*t = r` を保ちます。\n\n1. 上の行を `(a,1,0)`、下の行を `(m,0,1)` にする。\n2. 下の `r` が 0 でなければ、商 `q = 上のr // 下のr` を求める。`//` は整数の商です。\n3. **いまの下の行**を `{\"q\":q,\"r\":r,\"s\":s,\"t\":t}` として記録。\n4. 新しい行の各数を `上 − q × 下` にする。上を古い下へ、下を新しい行へ移して繰り返す。\n5. 下の `r` が 0 なら終了。上の `(r,s,t)` が `egcd` の返す `(g,s,t)`。\n\nなぜ等式が保たれる？ 上下の等式を引くと `a*(上s−q*下s) + m*(上t−q*下t) = 上r−q*下r`。3 列とも同じ引き算だからです。`r` の列は割り算の余りを順に取り、最後の 0 でない余りが最大公約数になります。\n\n| a=3、m=7 | 上 (r,s,t) | 下 (r,s,t)：記録する行 | q | 次の下 |\n|---|---|---|---|---|\n| 1 | (3,1,0) | (7,0,1) | 0 | (3,1,0) |\n| 2 | (7,0,1) | (3,1,0) | 2 | (1,−2,1) |\n| 3 | (3,1,0) | (1,−2,1) | 3 | (0,7,−3) |\n\n`egcd(3,7)` は `(1,-2,1)`。`egcd_trace(3,7)` は順番通りの 3 行：\n`[{\"q\":0,\"r\":7,\"s\":0,\"t\":1}, {\"q\":2,\"r\":3,\"s\":1,\"t\":0}, {\"q\":3,\"r\":1,\"s\":-2,\"t\":1}]`。\n`egcd` の 3 数、trace の行の列は list / tuple のどちらでも構いません。各行は `q,r,s,t` を持つ dict、各数は整数（`True` や小数は不可）。`s,t` の負号はそのまま残し、逆元を元として返すときに正規化します。\n\n`egcd` と `egcd_trace` の提出テストの入力は `1 ≤ a < m`。`inverse()` 内からは正規化した `.value` を渡せます。0 の逆元は先に例外にして構いません。\n\n## 逆元がないときに、数を返さない\n\n0 は何を掛けても 0 なので逆元なし。合成数の法でも、最大公約数が 1 かで判断できます。\n\n| 法 m | 0 以外で逆元がある元 → 相手 | 0 以外で逆元なし | 最小の逆元なしを返す関数 |\n|---|---|---|---|\n| 6 | 1→1、5→5 | 2、3、4 | `non_invertible_element(6) = 2` |\n| 9 | 1→1、2→5、4→7、5→2、7→4、8→8 | 3、6 | `non_invertible_element(9) = 3` |\n| 7 | 1→1、2→4、3→5、4→2、5→3、6→6 | なし | `non_invertible_element(7) = 0` |\n\nこの関数の 0 は「該当する **0 以外の**元なし」の印です。2 から法未満を順に調べ、`gcd(a,m)>1` の最初の `a` を返し、なければ 0。1 は自分が逆元です。\n\n`inverse()` と `/` は、逆元がなければ `raise NotInvertible(\"no inverse\")`。異なる法の元を `+ - * /` で組み合わせるなら、計算前に `raise FieldMismatch(\"different moduli\")`。例外のクラスは用意済みで、説明文字列は自由です。\n\n素数 `p` で非零の `a` なら、`a` の `p−1` 乗の余りが 1 になる性質（Fermat の小定理）があり、`pow(a,p-2,p)` でも逆元が求まります。しかし一般の合成数での保証はありません。法 6 の 5 は逆元を持つのに `pow(5,4,6)=1`、`5*1 % 6=5` で失敗。互除法なら、素数か合成数かを先に決めずに判断できます。\n\n## 7 つの提出欄と、編集する場所\n\n| 欄 | 直す場所 | 確認したいこと |\n|---|---|---|\n| normalize | `FieldElement.__init__`, `__eq__`, `__hash__` | 負の数も余りへ。同じ法・余りなら等しく同じハッシュ |\n| arithmetic | `__add__`, `__sub__`, `__mul__` | 足し引き掛け算とその途中でも正しい余り |\n| egcd-trace | `egcd`, `egcd_trace` | 表の全行と最大公約数・係数が一致 |\n| inverse | `inverse`, `__truediv__` | 素数の法で 0 以外全てに逆元。割って掛けると元へ戻る |\n| errors | 逆元・4 演算の例外分岐 | 0 で割れず、異なる法を混ぜない |\n| composite | `non_invertible_element`, `inverse` | 最小の逆元なしを見つけ、それを逆元として返さない |\n| units | 同じ `inverse`, `__truediv__` | 未見の素数・合成数でも全ての非零の元を判断 |\n\n`field.py` のメソッド・関数の中は、条件分岐やループを追加して編集できます。枠の名前と引数は保ちます。**全欄コード提出で、数字や JSON を別入力しません。** 各欄の「提出 (+N pt)」が現在のファイルを送ります。7 欄は独立採点、誤答は各 −10 点です。\n\n「証拠を確認」は今回の法・表・逆元と検算を表示します。上の小例を計算してから、その表の `a` と `modulus` でも同じ手順を追ってください。「公開テストを実行」は小例と表示された素数を確認する下見で、全ての未見の法は検査しません。「初期状態に戻す」は編集を取り消します。\n\nこの部品は、次の楕円曲線（暗号で使う曲線上の点の計算）の傾きの式にも使われます。点の特殊な場合分けを含む、曲線演算や署名全体をここで実装するわけではありません。\n\n計算に使える標準ライブラリ（Python に付属する道具）は `collections`, `decimal`, `fractions`, `functools`, `hashlib`, `hmac`, `itertools`, `json`, `math`, `operator`, `random`, `statistics`, `time`, `typing`。自分のコード内で import できます。追加のパッケージ導入、ファイルの読み書き、ネットワーク通信はこの実行環境では使いません。必要な整数計算は組み込みの演算だけでも実装できます。\n",
  "writeup": "## 正規化から逆元へ\n\n法 m と余り v%m を組にして元を表す。同じ法と余りなら等しいので、hash も同じにする。足し引き掛け算は整数の演算後に同じ法で正規化する。\n\n拡張互除法の各行は a*s+m*t=r を満たす。全列に同じ「上−q×下」を施すのでこの等式が保たれ、最後の非零 r が最大公約数 g になる。g=1 なら s%m が逆元、g>1 なら1を作れず逆元はない。合成数でも同じ判定を使える。\n\nFermat の pow(a,m-2,m) は素数の法の非零元には保証があるが、一般の合成数にはない。法6の5は逆元5を持つが、近道の結果1は逆元ではない。特定の合成数・元で近道が偶然成功することと、全入力に対する保証は別である。\n\n## 検査と保証の範囲\n\n表は指定された整数商の手順と全行を比較する。逆元・除算は、選ばれた素数/合成数の非零元を全て確認する。これは有限の入力での検査であり、任意の実装を数学的に証明したことにはならない。\n\n採点器は値と法を親で検査し、誤った逆元と掛け算が打ち消し合うだけの成功や、失敗なしの印字を採点根拠にしない。新しい呼び出し識別子は事前印字を拒否するが、任意Pythonの関数内部のreturnや例外発生元を証明するものではない。同じ正しい数値を返す実装は同じ数学条件で評価される。\n\n表の分岐と行数は入力に依存するため、実際の秘密鍵を扱う定時間実装の見本ではない。次の曲線問題では一般の場合の傾きの割り算に逆元を用い、特別な点の組は別途場合分けする。\n",
  "tags": [
    "advanced-cryptography-2026",
    "week3",
    "finite-field",
    "modular-inverse",
    "local-play",
    "container"
  ],
  "exposedPorts": [
    {
      "port": 18100,
      "name": "Portal editor API + /verify (loopback only)"
    }
  ],
  "learningGoals": [
    "整数と体の元を区別できる",
    "マイナスや法以上の値を、0〜法未満の形に直せる",
    "加減乗算を法の下で実装できる",
    "拡張互除法の表から、逆元と gcd を同時に出せる",
    "逆元が無い元を「無い」とエラーで言える(0・合成数の非可逆元)",
    "逆元があるかないかの境目が gcd だと、素数の法と合成数の法の反例で示せる",
    "合成数の法で、逆元がある元と無い元を、同じ表で全元について言い分けられる"
  ],
  "runtime": {
    "provider": "docker",
    "engine": "compose",
    "entry": "local/docker-compose.yml",
    "verifyUrl": "http://127.0.0.1:18100/verify",
    "secretEnv": [
      "FLAG_SEED"
    ]
  },
  "scoring": {
    "kind": "multi-verify",
    "checks": [
      {
        "id": "normalize",
        "input": "multiline",
        "label": "余りを 0 以上にそろえる",
        "points": 25,
        "wrongAnswerPenalty": 10,
        "hints": [
          {
            "id": "normalize-hint-1",
            "content": "正規化は、同じ余りを持つ整数を一つの表し方にそろえます。元は余りだけでなく法も持つので、比較でも両方を見ます。",
            "penalty": 10
          },
          {
            "id": "normalize-hint-2",
            "content": "整数 v を m で割った余りは v % m。−2 = 7×(−1)+5 なので法 7 では 5。法 7 の −2 と 5 は等しく、法 6 の 5 は違います。等しい元の hash は同じ整数にします。",
            "penalty": 0
          },
          {
            "id": "normalize-hint-3",
            "content": "`field.py` の FieldElement.__init__ で value を field.modulus で割った余りにする → __eq__ で法と余りを比較 → __hash__ はその組を hash に渡す → 公開テストの elements-are-canonical を確認 → この欄で提出。",
            "penalty": 0
          }
        ]
      },
      {
        "id": "arithmetic",
        "input": "multiline",
        "label": "足し算・引き算・掛け算",
        "points": 30,
        "wrongAnswerPenalty": 10,
        "hints": [
          {
            "id": "arithmetic-hint-1",
            "content": "元の足し引き掛け算は、整数の計算をしたあと、同じ法の余りへ戻す操作です。途中で作った元も同じ規則を通します。",
            "penalty": 0
          },
          {
            "id": "arithmetic-hint-2",
            "content": "法 m なら (a+b)%m、(a-b)%m、(a*b)%m。法 7 で 5+4→2、5−4→1、5×4→6。2×(3+4) と 2×3+2×4 も同じ余り 0 です。",
            "penalty": 0
          },
          {
            "id": "arithmetic-hint-3",
            "content": "field.py の __add__・__sub__・__mul__ で self.value と other.value を計算し、同じ self.field の FieldElement を返す → 公開テストの arithmetic-on-small-values と small-seven-arithmetic を確認 → この欄で提出。",
            "penalty": 0
          }
        ]
      },
      {
        "id": "egcd-trace",
        "input": "multiline",
        "label": "最大公約数を求める表",
        "points": 35,
        "wrongAnswerPenalty": 10,
        "hints": [
          {
            "id": "egcd-hint-1",
            "content": "最大公約数を求める割り算の余りに、元の二つの数を何倍したかの列を添えます。表の各行が同じ関係を保つので、最後の行から逆元の有無と、ある場合の相手を取り出せます。",
            "penalty": 15
          },
          {
            "id": "egcd-hint-2",
            "content": "各行 (r,s,t) は a*s+m*t=r。上−q×下を全列にするとこの等式も保たれます。a=3,m=7 の記録 (q,r,s,t) は (0,7,0,1)→(2,3,1,0)→(3,1,−2,1)。最後は 3×(−2)+7×1=1。",
            "penalty": 0
          },
          {
            "id": "egcd-hint-3",
            "content": "egcd/egcd_trace で上=(a,1,0)、下=(b,0,1) → 下rが0になるまで q=上r//下r → いまの下とqをdictに記録 → 全列を上−q×下に更新。egcdは最後の上、traceは記録全行を返す。証拠を確認の表と比較して、この欄で提出。",
            "penalty": 0
          }
        ]
      },
      {
        "id": "inverse",
        "input": "multiline",
        "label": "掛けて 1 になる相手と割り算",
        "points": 35,
        "wrongAnswerPenalty": 10,
        "hints": [
          {
            "id": "inverse-hint-1",
            "content": "逆元は、掛けると余りが一になる相手です。割り算はその相手を掛けること。互除法の最大公約数が一なら相手を取り出せます。",
            "penalty": 15
          },
          {
            "id": "inverse-hint-2",
            "content": "gcdが1のとき a*s+m*t=1 から逆元=s%m。3×(−2)+7×1=1 なので3の逆元は5、4/3は4×5を7で割った余り6。",
            "penalty": 0
          },
          {
            "id": "inverse-hint-3",
            "content": "inverse() で正規化した self.value と self.field.modulus を egcd に渡す → gが1ならsからFieldElementを作る → __truediv__ は相手の inverse() を掛ける → 公開テスト small-seven-inverse を確認 → この欄で提出。",
            "penalty": 0
          }
        ]
      },
      {
        "id": "errors",
        "input": "multiline",
        "label": "割れない・法が違うときのエラー",
        "points": 25,
        "wrongAnswerPenalty": 10,
        "hints": [
          {
            "id": "errors-hint-1",
            "content": "逆元がないときの答えは数ではなく例外です。また、割る数が違う二つの計算を勝手に混ぜてはいけません。",
            "penalty": 0
          },
          {
            "id": "errors-hint-2",
            "content": "0×x はいつも0で余り1にはならないため NotInvertible。法7の3と法6の3を足す・引く・掛ける・割るときは FieldMismatch。法と値は別々に確認します。",
            "penalty": 0
          },
          {
            "id": "errors-hint-3",
            "content": "inverse と __truediv__ で逆元なしを raise NotInvertible にする。__add__・__sub__・__mul__・__truediv__ の最初で法を比べ、違えば raise FieldMismatch。例外クラス名は先頭に定義済み。公開テスト zero-and-mixed-moduli を確認して提出。",
            "penalty": 0
          }
        ]
      },
      {
        "id": "composite",
        "input": "multiline",
        "label": "逆元がない最小の数",
        "points": 25,
        "wrongAnswerPenalty": 10,
        "hints": [
          {
            "id": "composite-hint-1",
            "content": "合成数の法では、法と共通の約数を持つ数に逆元がありません。そのうち最小の、零ではない数を探します。素数なら該当する数はありません。",
            "penalty": 12
          },
          {
            "id": "composite-hint-2",
            "content": "判定は gcd(a,m)>1。法6では2が最小、法9では3、法7では該当なしの印0。合成数の場合だけ最小の素因数と一致し、素数自体を答えにはしません。",
            "penalty": 0
          },
          {
            "id": "composite-hint-3",
            "content": "non_invertible_element の modulus に対し2からmodulus未満まで順に egcd のgを調べる → 最初のg>1でその数を返す → 見つからなければ0。inverseもg>1なら例外にする。証拠を確認の compositeModulus と表の小例を比べ、この欄で提出。",
            "penalty": 0
          }
        ]
      },
      {
        "id": "units",
        "input": "multiline",
        "label": "別の法でも、逆元の有無を判断",
        "points": 25,
        "wrongAnswerPenalty": 10,
        "hints": [
          {
            "id": "units-hint-1",
            "content": "逆元がある元を unit と呼びます。最後の欄では新しいコードを増やすより、同じ仕組みが素数でも合成数でも、全ての元を正しく分けるか確かめます。",
            "penalty": 10
          },
          {
            "id": "units-hint-2",
            "content": "法9では2の逆元5（2×5を9で割った余り1）、3はgcd(3,9)=3でなし。法6の5は逆元5でも pow(5,4,6)=1 は誤り。逆元が存在する ⇔ gcd(a,m)=1（⇔ はどちら向きにも成り立つ意味）。見つけた x は a*x%m=1 で検算します。",
            "penalty": 0
          },
          {
            "id": "units-hint-3",
            "content": "inverse と __truediv__ に特定の法だけの分岐がないか確認 → egcdのgが1ならsから元、そうでなければNotInvertible → 本文の法6/9の全ての数で相手と例外を追う → 同じfield.pyをこの欄で提出。未見の法でも同じ規則が検査されます。",
            "penalty": 0
          }
        ]
      }
    ]
  },
  "i18n": {
    "en": {
      "name": "Build the field before the curve",
      "shortDescription": "Build division using remainders. An inverse makes a product leave remainder 1. Start with −2 = (−1)×7 + 5: the remainder is 5, and the divisor 7 is called the modulus. Use a table to find which numbers have inverses.",
      "description": "Build division using remainders. An inverse is a partner whose product leaves remainder1. Start by normalizing −2 to remainder5 modulo7, then use a table to decide which numbers have inverses.",
      "instructions": "## Start by turning −2 into remainder 5\n\nYou are building division for cryptographic arithmetic. Instead of ordinary fractions, use an **inverse: a partner whose product leaves remainder 1**.\n\n`FieldElement` is a type storing a modulus and a remainder. Its `__init__` method (a function inside a type) runs when an element is created.\n\n**Start → Inspect evidence → edit `FieldElement.__init__` in `field.py` → Run public tests → submit “Normalize the remainder”.** First make `self.value` hold the remainder of `value` divided by `field.modulus`. The public tests include a negative input.\n\n## Before you start\n\n- The divisor `m` is the **modulus**, an integer greater than 1 here. Python `v % m` gives the remainder `r` in `v = m*q+r`, with `0 ≤ r < m`. Example: `−2 = 7*(−1)+5`, so `(-2) % 7 = 5`.\n- Choosing that remainder is **normalization**. An **element** here stores a modulus and a remainder. `Field(m)` stores the modulus; `.element(v)` creates an element. `self` means this object, `self.value` its remainder, and `self.field.modulus` its modulus. A function inside a class is a **method**.\n- `u ≡ v (mod m)` means that `u` and `v` leave the same remainder when divided by `m`. The symbol `≡` means precisely that. `gcd(a,m)` is their greatest common divisor.\n- With a prime modulus `p`, every element except zero has an inverse. This arithmetic is an example of a **finite field**: finitely many elements with arithmetic including division by nonzero elements. The same code will handle composite moduli, where some nonzero elements cannot be divisors.\n\n## Add, subtract or multiply → take the remainder\n\n| Operation | General formula | Modulus 7, a=5, b=4 |\n|---|---|---|\n| `a + b` | `(a+b) % m` | `9 % 7 = 2` |\n| `a - b` | `(a-b) % m` | `1 % 7 = 1` |\n| `a * b` | `(a*b) % m` | `20 % 7 = 6` |\n\nThese are `__add__`, `__sub__`, `__mul__`. Return a `FieldElement` with the same modulus, not a plain integer. Normalizing on construction also normalizes intermediate results.\n\n**Equality requires both the same modulus and the same remainder.** Modulus 7 with −2 equals modulus 7 with 5, but differs from modulus 6 with 5. `__eq__` performs this comparison. `__hash__` returns an integer used by Python dictionaries and sets: equal elements must have equal hashes. You can use `hash((self.field.modulus, self.value))`. Different elements are allowed to share a hash.\n\nReordering additions or multiplications, or changing their grouping, preserves the answer. Also `x+0=x`, `x*1=x`, `x-x=0` and `x*(y+z)=x*y+x*z`. With modulus 7, `2*(3+4)` and `2*3+2*4` both leave remainder 0.\n\n## Divide → multiply by the partner that makes 1\n\nFor the inverse `x` of `a`, **`a*x ≡ 1 (mod m)`**. With modulus 7, `3*5=15=7*2+1`, so 3 has inverse 5. Thus `4 / 3` means `4*5 % 7 = 6`.\n\nTo find that partner, the **extended Euclidean algorithm** computes integers `s,t` and the greatest common divisor `g` such that `a*s + m*t = g`. If `g=1`, `m*t` leaves remainder zero, so **the inverse is `s % m`**. If `g>1`, every integer combination of `a` and `m` is divisible by `g` and cannot equal 1.\n\n```text\n3 * (−2) + 7 * 1 = 1  →  inverse of 3 is (−2) % 7 = 5\n2 * 1    + 6 * 0 = 2  →  greatest common divisor is 2: no inverse\n```\n\n### Build the table without breaking its equation\n\nEach row `(r,s,t)` maintains `a*s + m*t = r`.\n\n1. Start upper = `(a,1,0)`, lower = `(m,0,1)`.\n2. While lower `r` is not zero, compute quotient `q = upper_r // lower_r`. `//` is integer division.\n3. Record **the current lower row**, as `{\"q\":q,\"r\":r,\"s\":s,\"t\":t}`.\n4. For each column, new = `upper − q*lower`. Move upper to the old lower and lower to new. Repeat.\n5. When lower `r` is zero, return upper `(r,s,t)` as `egcd`'s `(g,s,t)`.\n\nWhy does the equation survive? Subtracting the two equations gives `a*(upper_s−q*lower_s) + m*(upper_t−q*lower_t) = upper_r−q*lower_r`. Each column undergoes the same subtraction. The `r` column repeatedly takes division remainders; its last nonzero remainder is the greatest common divisor.\n\n| a=3, m=7 | Upper (r,s,t) | Lower (r,s,t): the row to record | q | Next lower |\n|---|---|---|---|---|\n| 1 | (3,1,0) | (7,0,1) | 0 | (3,1,0) |\n| 2 | (7,0,1) | (3,1,0) | 2 | (1,−2,1) |\n| 3 | (3,1,0) | (1,−2,1) | 3 | (0,7,−3) |\n\n`egcd(3,7)` returns `(1,-2,1)`. `egcd_trace(3,7)` returns these three rows, in order:\n`[{\"q\":0,\"r\":7,\"s\":0,\"t\":1}, {\"q\":2,\"r\":3,\"s\":1,\"t\":0}, {\"q\":3,\"r\":1,\"s\":-2,\"t\":1}]`.\nThe three numbers of `egcd`, and the ordered rows of the trace, may use a list or tuple. Each row is a dict with `q,r,s,t`; every number is an integer, not a boolean or float. Keep negative `s,t` unchanged. Normalize when returning the inverse as an element.\n\nThe submitted tests call `egcd` and `egcd_trace` with `1 ≤ a < m`. Inside `inverse()`, use normalized `.value`. Zero can be rejected before calling the algorithm.\n\n## When there is no inverse, do not return a number\n\nZero times anything is zero, never one. Under a composite modulus, the greatest common divisor still decides whether an inverse exists.\n\n| Modulus | Nonzero elements with an inverse → partner | Nonzero elements without an inverse | Smallest nonzero element without an inverse |\n|---|---|---|---|\n| 6 | 1→1, 5→5 | 2, 3, 4 | `non_invertible_element(6) = 2` |\n| 9 | 1→1, 2→5, 4→7, 5→2, 7→4, 8→8 | 3, 6 | `non_invertible_element(9) = 3` |\n| 7 | 1→1, 2→4, 3→5, 4→2, 5→3, 6→6 | none | `non_invertible_element(7) = 0` |\n\nThe function's zero means “no such **nonzero** element”. Search from 2 up to modulus−1 and return the first `a` with `gcd(a,m)>1`; return zero if none exists. One is always its own inverse.\n\nIn `inverse()` and `/`, absence of an inverse requires `raise NotInvertible(\"no inverse\")`. Before combining different moduli with `+ - * /`, use `raise FieldMismatch(\"different moduli\")`. Both exception classes are provided; the explanation string is yours.\n\nFor prime `p` and nonzero `a`, `a` to the power `p−1` leaves remainder 1 (Fermat's little theorem), so `pow(a,p-2,p)` gives the inverse. There is no such guarantee for a general composite modulus. Modulus 6 with 5 has inverse 5, yet `pow(5,4,6)=1` and `5*1 % 6=5`, which fails. The Euclidean algorithm handles both without first deciding whether the modulus is prime.\n\n## Seven submission rows and where to edit\n\n| Row | Code to edit | What to establish |\n|---|---|---|\n| normalize | `FieldElement.__init__`, `__eq__`, `__hash__` | Negative inputs normalize too; same modulus/remainder means equality and equal hash |\n| arithmetic | `__add__`, `__sub__`, `__mul__` | Correct remainders, including intermediate results |\n| egcd-trace | `egcd`, `egcd_trace` | Every table row, greatest common divisor and coefficient |\n| inverse | `inverse`, `__truediv__` | Invert every nonzero prime-modulus element; division undoes multiplication |\n| errors | Inverse and four arithmetic methods | Reject zero divisors and mixed moduli |\n| composite | `non_invertible_element`, `inverse` | Find the smallest nonzero element without an inverse and refuse to invert it |\n| units | The same `inverse`, `__truediv__` | Handle every nonzero element under unseen prime and composite moduli |\n\nYou may add branches and loops inside `field.py`'s methods and functions. Keep the names and arguments. **All seven rows submit code: no separate number or JSON answer.** Each “Submit (+N pt)” sends the current file. The rows are independently graded, with a 10-point penalty per wrong answer.\n\n“Inspect evidence” shows this deployment's moduli, table, inverse and verification. After calculating a small example above, follow the same steps with the table's `a` and `modulus`. “Run public tests” checks small examples and the displayed prime; it does not exhaust unseen moduli. “Restore starter” discards your edits.\n\nThis component also supplies division in slope formulas for elliptic curves, the curves whose points are used in cryptography. You are not implementing the curve's special cases or a complete signature scheme here.\n\nAvailable computational standard-library modules (tools included with Python): `collections`, `decimal`, `fractions`, `functools`, `hashlib`, `hmac`, `itertools`, `json`, `math`, `operator`, `random`, `statistics`, `time`, `typing`. You may import them in your code. This environment does not support installing extra packages, file access or network communication. The required integer arithmetic can also use only built-in operations.\n",
      "writeup": "## From normalization to inversion\n\nAn element stores a modulus m and the remainder v%m. Equality compares both, and equal elements have equal hashes. Addition, subtraction and multiplication normalize integer results under the same modulus.\n\nEvery extended-Euclid row satisfies a*s+m*t=r. Applying upper−q*lower to all columns preserves the equation. The last nonzero r is the greatest common divisor g: g=1 gives inverse s%m, while g>1 makes an integer combination equal to1 impossible. The same rule covers composites.\n\nFermat's pow(a,m-2,m) has a guarantee for every nonzero element under prime m, not under a general composite. Modulus6 with5 has inverse5, but the shortcut returns1. Accidental success on individual composite inputs is different from a general guarantee.\n\n## Checks and limits\n\nThe trace is compared against the stated integer-quotient algorithm row by row. Inverse and division checks enumerate the nonzero elements of selected prime and composite moduli. These finite checks are not a mathematical proof about every possible implementation.\n\nThe parent validates the returned modulus and values mathematically. Wrong inverse and multiplication implementations cannot certify one another, nor can a printed empty failure list decide a grade. Fresh call identifiers reject preprinted replies; they do not attest native Python return statements or exception origins. A different implementation returning the same correct mathematical values is judged by the same rules.\n\nBranches and trace length depend on inputs, so this is not a constant-time secret-key implementation. The next curve exercise uses inverses in ordinary slope formulas and separately handles exceptional point pairs.\n",
      "learningGoals": [
        "Tell an integer apart from a field element",
        "Fold negatives and values past the modulus into 0 up to modulus-1",
        "Implement addition, subtraction and multiplication under the modulus",
        "Get the inverse and the gcd together from the extended-Euclid table",
        "Raise an error for an element with no inverse (zero, and a composite modulus's non-invertible element)",
        "Show that the gcd is the boundary between invertible and not, via a prime-vs-composite counterexample",
        "Over a composite modulus, sort every element into inverse-or-none with the same table"
      ],
      "checks": [
        {
          "id": "normalize",
          "label": "Normalize the remainder",
          "hints": [
            {
              "id": "normalize-hint-1",
              "content": "Normalization gives integers with the same remainder one representation. Elements store the modulus too, so equality must compare both parts."
            },
            {
              "id": "normalize-hint-2",
              "content": "For integer v and modulus m, use v % m. −2=7*(−1)+5 gives remainder 5. Modulus 7 with −2 equals modulus 7 with 5, but differs from modulus 6 with 5. Equal elements require equal integer hashes."
            },
            {
              "id": "normalize-hint-3",
              "content": "Edit FieldElement.__init__ in field.py to store value % field.modulus → compare modulus and value in __eq__ → hash that pair in __hash__ → check elements-are-canonical in Run public tests → submit this row."
            }
          ]
        },
        {
          "id": "arithmetic",
          "label": "Add, subtract and multiply",
          "hints": [
            {
              "id": "arithmetic-hint-1",
              "content": "Element addition, subtraction and multiplication perform integer arithmetic and return to the same modulus. Intermediate elements must follow this rule too."
            },
            {
              "id": "arithmetic-hint-2",
              "content": "Use (a+b)%m, (a-b)%m, (a*b)%m. Modulus 7 with 5 and4 gives 2,1,6. Both 2*(3+4) and 2*3+2*4 leave remainder 0."
            },
            {
              "id": "arithmetic-hint-3",
              "content": "In field.py, __add__, __sub__ and __mul__ calculate with self.value and other.value, then return a FieldElement with self.field → check arithmetic-on-small-values and small-seven-arithmetic in Run public tests → submit this row."
            }
          ]
        },
        {
          "id": "egcd-trace",
          "label": "Trace the greatest common divisor",
          "hints": [
            {
              "id": "egcd-hint-1",
              "content": "Attach columns tracking multiples of the two original numbers to the division remainders used to find their greatest common divisor. Every row preserves the relationship; the final row decides whether an inverse exists and supplies it when it does."
            },
            {
              "id": "egcd-hint-2",
              "content": "Each row (r,s,t) satisfies a*s+m*t=r. Subtracting q times the lower row in every column preserves it. For 3 and7 the recorded (q,r,s,t) rows are (0,7,0,1)→(2,3,1,0)→(3,1,−2,1); 3*(−2)+7*1=1."
            },
            {
              "id": "egcd-hint-3",
              "content": "In egcd/egcd_trace start upper=(a,1,0), lower=(b,0,1) → while lower_r is nonzero compute q=upper_r//lower_r → record q and the current lower as a dict → update every column by upper−q*lower. egcd returns final upper; trace returns all recorded rows. Compare Inspect evidence, then submit this row."
            }
          ]
        },
        {
          "id": "inverse",
          "label": "Inverse and division",
          "hints": [
            {
              "id": "inverse-hint-1",
              "content": "An inverse is a partner whose product leaves remainder one. Division multiplies by that partner. When the greatest common divisor is one, the extended algorithm supplies it."
            },
            {
              "id": "inverse-hint-2",
              "content": "If a*s+m*t=1, the inverse is s%m. Since3*(−2)+7*1=1, the inverse of 3 is 5 and4/3 leaves the same remainder as4*5:6."
            },
            {
              "id": "inverse-hint-3",
              "content": "In inverse(), pass normalized self.value and self.field.modulus to egcd → if g is 1 make a FieldElement from s → __truediv__ multiplies by the other element’s inverse() → check small-seven-inverse in Run public tests → submit this row."
            }
          ]
        },
        {
          "id": "errors",
          "label": "Reject missing inverses and mixed moduli",
          "hints": [
            {
              "id": "errors-hint-1",
              "content": "When no inverse exists, the result is an exception rather than a number. Arithmetic must also refuse to mix calculations using different moduli."
            },
            {
              "id": "errors-hint-2",
              "content": "Zero times x is always zero, never remainder 1: raise NotInvertible. Combining modulus 7 with 3 and modulus 6 with 3 through addition, subtraction, multiplication or division requires FieldMismatch."
            },
            {
              "id": "errors-hint-3",
              "content": "Make inverse and __truediv__ raise NotInvertible for missing inverses. Check moduli at the start of __add__, __sub__, __mul__, __truediv__; raise FieldMismatch if different. The exception classes are already defined. Check zero-and-mixed-moduli in Run public tests and submit."
            }
          ]
        },
        {
          "id": "composite",
          "label": "Find the smallest non-invertible value",
          "hints": [
            {
              "id": "composite-hint-1",
              "content": "Under a composite modulus, a number sharing a factor with that modulus has no inverse. Find the smallest nonzero such number. For a prime there is none."
            },
            {
              "id": "composite-hint-2",
              "content": "Test gcd(a,m)>1. Modulus 6 gives 2, modulus 9 gives 3, modulus 7 gives the no-such-number marker0. Only for a composite is the answer its smallest prime factor."
            },
            {
              "id": "composite-hint-3",
              "content": "In non_invertible_element, search from 2 to modulus−1 using egcd’s g → return the first number with g>1 → return0 if none is found. inverse must also raise when g>1. Compare Inspect evidence’s compositeModulus and the small examples; submit this row."
            }
          ]
        },
        {
          "id": "units",
          "label": "Decide invertibility under unseen moduli",
          "hints": [
            {
              "id": "units-hint-1",
              "content": "An element with an inverse is called a unit. The final row checks that the same mechanism classifies every element, under prime and composite moduli; it does not require a new function."
            },
            {
              "id": "units-hint-2",
              "content": "Modulus 9:2 has inverse 5 because2*5 leaves remainder 1, but3 has no inverse since gcd(3,9)=3. Modulus 6:5 has inverse 5 even though pow(5,4,6)=1 fails. An inverse exists if and only if gcd(a,m)=1. Check a candidate x using a*x%m=1."
            },
            {
              "id": "units-hint-3",
              "content": "Check inverse and __truediv__ for special cases tied to particular moduli → if egcd gives g=1 build the element from s, otherwise raise NotInvertible → follow every number in the modulus 6/9 tables → submit the same field.py here. The same rules are checked under unseen moduli."
            }
          ]
        }
      ]
    }
  }
}
