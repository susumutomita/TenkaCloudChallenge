{
  "$schema": "../../SCHEMA.json",
  "id": "stackstack-observability",
  "name": "黙っているアプリに口を割らせる",
  "category": "Challenge",
  "status": "ready",
  "visibility": "public",
  "difficulty": 3,
  "estimatedDuration": "45 分",
  "shortDescription": "「掲示板、 書いたのに後から探せないって声が来てる」。 月曜の朝、 CTO から飛んでくるのはその一行だけだ。\n\n板を開けば投稿はある。 ダッシュボードは緑。 死活確認は 200 を返し続けていて、 監視は一度も鳴っていない。 それでも社内チャットには 「書いたはずのものが出てこない」 が三件並んでいる。 前任の SRE は 「板から下流のアーカイブに流す relay を今朝入れました。 ログはうるさいので出さないようにしてあります」 と書き置きを残して、 もういない。\n\nアプリのログを開くと、 起動と投稿受付の行はある。 だが 「アーカイブに届かなかった投稿」 については 1 行も無い。 出ていないのは壊れていないからではない。 CTO 曰く: 「原因が分からないなら、 まず分かるようにして。 直す順番はそれから決める」。",
  "instructions": "## はじめに\n\n板そのものは動いていますが、 外部 relay の一部が落ち、 死活確認は 200 のままです。 メトリクス・ログ・health が同じ事実を語る状態へ直す問題です。\n\n## ブラウザだけで進める\n\n1. `/relay` を開く\n2. `/docs` の **任意の API を試す** で `GET /metrics` と `GET /relay/healthz` を実行し、 数字と health の食い違いを見る\n3. 同じ画面の `POST /api/posts` で投稿を何本か流し、 `GET /metrics` と `GET /relay/state` を見直す\n4. `PATCH /api/settings` で relay 設定を変える。 リポジトリのファイルは編集しない\n5. `GET /posture` で4つの gateを確認する。 やり直しは `DELETE /api/settings`\n\n## ゴール\n\n届かなかった書き込みを記録し、 落ちている slice を数字から特定し、 health が本当の状態を返し、 調査ログを引き渡せること。 再起動すると数字とログが消えるため、 調査の代わりにはなりません。",
  "description": "## 目的\n\nIssue 288。 アプリログを読む、 正常なリクエストと異常を数字で切り分ける、 死活確認の条件を理解する、 最低限の異常検知を自分で構成する、 AI や同僚に渡す証拠を集める、 そしてその証拠に秘密を混ぜない ── この 6 つを 1 本の障害調査の上で通す。\n\n## クラウドではなくコンテナで何を模しているか\n\nCloudWatch も ALB も使わない。 コンテナ内に最小の可観測性の面を置く。\n\n| このコンテナ | 現実の対応 |\n|---|---|\n| `GET /metrics` (Prometheus text format) | CloudWatch メトリクス / メトリクスフィルタ |\n| `relay.json` の `healthCheckProbes` | ALB ターゲットグループのヘルスチェック条件 |\n| `GET /relay/healthz` | ターゲットグループが叩く先 |\n| `relay.json` の `archiveLogging` | ログ出力そのものの有無 (アプリ側の設定) |\n| `relay.json` の `logDetail` | ログのマスキング / 構造化ログのフィールド選択 |\n| archive の 4 shard | 下流のパーティション / AZ / シャード |\n\n**アラームを作って発報させる工程は入れていない。** ローカルコンテナで自分宛にアラートを飛ばしても学習にならないので、 その要件は Battle 側 (実障害に対する検知) に送った。 ここで採点しているのは 「発報の前に、 発報できる状態になっているか」 までで、 それ以上は主張しない。\n\n## 何を実測しているか\n\n| checkpoint | 実測しているもの |\n|---|---|\n| `silent-failures` | (1) いまの設定で relay ログが on (2) いまの epoch に `relay drop` 行が実在 (3) 成功と失敗が両方現に起きている ── その上で incident id を完全一致 |\n| `honest-health` | 死活条件を 3 つの世界 `(config,archive)` = (t,f)/(f,t)/(t,t) で評価して (not ok, not ok, ok) になること + その設定になってから実際に `/relay/healthz` が叩かれたこと。 receipt は boot 秘密由来で導出不能 |\n| `leak-shut` | 先に A (いまの epoch に drop 行と archived 行が両方 「実在する」 ── archived 行があること自体が 「この設定で archive に届いた書き込みがある」 証跡) / B (shard・code・target・epoch が残っている) を実測し、 両方通ってから初めて不在を見る |\n| `root-cause` | 落ちている shard に自分のトラフィックで失敗が記録されていること + 他の shard に成功が記録されていること + その shard の code を完全一致 |\n\n## 危険な回避策と、 それを落とす場所\n\n| 回避策 | 落ちる場所 |\n|---|---|\n| 再起動する | shard の写像は seed 由来で状態を持たないので直らない。 それどころかカウンタが 0 に戻り log ring も消えるので、 通っていた 3 つが落ちる |\n| 投稿を止める / `acceptingPosts: false` | 成功も失敗も 0 になり `silent-failures` / `root-cause` / gate `traffic_seen` が全部 false。 「エラーが 1 件も出ていない」 は満点ではなくゼロ点 |\n| ログを止めて漏洩を 「直す」 | `leak-shut` は不在を見る **前に** A と B を実測する。 ログが空だと A で落ちるので不在チェックに到達しない。 gate `credential_out_of_logs` にも `failures_logged` の連言が埋め込んである |\n| `logDetail: \"masked\"` で末尾だけ伏せる | 不在チェックは完全一致ではなく先頭 12 文字の前方一致なので、 残った前半で落ちる。 safe-log の値もこの設定では出ない |\n| `logDetail: \"minimal\"` で行を削る | 秘密は本当に消えるので不在チェックは通るが、 その前の B (shard・code・target・epoch) で落ちる。 `silent-failures` も incident が消えるので落ちる |\n| `/relay/healthz` を鳴らないように固定する | `healthCheckProbes` を空にすると常に 503 になるが、 3 世界評価は (t,t) で ok を要求するので落ちる |\n| `healthCheckProbes: [\"archive\"]` だけにする | (f,t) で not ok を要求するので落ちる ── 設定ファイルが壊れても緑のまま |\n| 答えを表にする / 固定値ソルバ | incident id・safe token・4 つの shard 名・4 つの code・title→shard の写像がすべて `FLAG_SEED` 由来。 receipt は boot 秘密由来でさらに再起動でも変わる |\n| メトリクスを開いて code をコピーするだけ | shard の系列は 「その shard に 1 度でも書き込みがあった後」 にしか出ない。 4 つの code は全部出るので、 落ちている 1 枚との対応付けが要る |\n\n## ログに秘密を書くことについて (レビュア向け)\n\n出荷状態の `logDetail: \"full\"` は、 relay が archive に提示する資格情報をログ行に書く。 これは実在の資格情報ではなく `FLAG_SEED` 由来のダミーで、 loopback 束縛のコンテナの外には出ず、 **それをログから外すこと自体がこの問題の合格条件** である。 カタログが漏洩を出荷しているのではない。 参加者向けの文言 (shortDescription / instructions / label / hint) では 「調査ログをそのまま渡せる状態にする」 と資産と症状の言い方に留め、 脆弱性クラス名は書かない (AGENTS.md §10 / §13)。\n\n## 履歴のログは消えない\n\n`leak-shut` の不在チェックは **いまの設定世代 (epoch)** の行だけを見る。 `logDetail` を `\"safe\"` にしても、 すでに書かれてしまった行は書かれたままで、 それが見えたままなのは意図的である ── 設定変更は漏洩をなかったことにしない。 実際の対処が 「ログを grep する」 ではなく 「資格情報をローテートする」 である理由をそのまま残してある。 README の 「漏れたものは漏れたまま」 節に明記した。\n\n## 採点が壊さないこと\n\n4 つの checkpoint はどれも読み取りだけで、 投稿も設定も shard も動かさない。 誤答でも再送でも参加者の環境は変わらない。 `/posture` の `readyToken` はこの問題の答えには使っていない (家族内で scenario をまたいで同じ値になるため)。\n\n## 既知の限界\n\nコンテナ内の判定なので、 参加者が自分のチェックアウトの `runtimes/stackstack/` を書き換えて image を焼き直せば全部無効化できる。 このカタログのコンテナ問題すべてに共通する local-play の正直さの限界で、 採点バグではない。 また `FLAG_SEED` が注入されない起動 (`docker compose up` 直打ち) では compose の既定値が使われるため、 推測不能性はプラットフォームによる注入が前提である。",
  "writeup": "## この 45 分で触ったもの\n\n- **黙っているアプリと壊れているアプリは違う** — 最初のログには 「投稿を受け付けた」 行はあった。 無かったのは 「アーカイブに届かなかった」 行のほうで、 それは壊れていないからではなく、 設定でログを出さないようにしてあったからだった。 「ログに何も出ていません」 は障害報告としては情報がゼロで、 実際には 「まだ調べていません」 と同じ意味になることがある。\n- **数字とログの分担** — 「何が落ちているか」 は `/metrics` の数、 「なぜ落ちているか」 と 「どの 1 件か」 はログの行だった。 片方だけでは足りない。 数字は 4 枚のうち 1 枚だけが 0 を返していることを見せ、 ログは incident id と失敗理由を見せた。\n- **死活確認は 「見ている先」 がすべて** — 出荷時の条件は設定ファイルが読めるかどうかだけを見ていた。 だから設定ファイルが壊れない限り永遠に緑で、 その裏で書き込みは落ち続けていた。 正しく直した瞬間に `/relay/healthz` は 503 になる。 **赤くなったのが成果** で、 一度も赤くなったことのない死活確認は、 正しいことが確かめられていないだけだ。\n- **調査ログをそのまま貼らない** — シグナルを出した瞬間、 relay が下流に提示している値がログ行に乗った。 チケットにも AI にも、 貼る前に一度自分で読む。 そして `logDetail` を落としても、 すでに書かれた行は書かれたままだった ── だから現実の対処はログの編集ではなく、 値のローテーションになる。\n\n## なぜ 「エラーが出なくなった」 で採点しないのか\n\nこの問題でいちばん簡単に 「直せた気」 になれるのは、 投稿を止めることと relay のログを止めることだ。 どちらもエラーの表示は消える。 だから 4 つの checkpoint はどれも 「失敗が現に起きていて、 それが記録されている」 ことを合格の前提に置いてある。 沈黙は健全さではない。\n\n## 次にやること\n\n同じ板を守る側に回る `stackstack-defend`、 落ちたところから戻す `stackstack-recover`、 そして本編の `stackstack` Battle。",
  "tags": [
    "stackstack",
    "observability",
    "logging",
    "metrics",
    "health-check",
    "incident-response",
    "local-play",
    "container",
    "web-app"
  ],
  "exposedPorts": [
    {
      "port": 18080,
      "name": "Web (board / relay / metrics)"
    },
    {
      "port": 18081,
      "name": "/verify (loopback)"
    }
  ],
  "learningGoals": [
    "アプリのログに 「出ていない」 ことと 「起きていない」 ことを区別できる",
    "メトリクスの内訳から、 落ちている依存先を 1 つに絞り込める",
    "死活確認の条件を、 正常・異常・見落としの 3 通りで評価して直せる",
    "調査に使ったログを、 秘密を落として調査項目を残したまま人に渡せる",
    "沈黙 (エラーが出ない状態) を復旧と取り違えない"
  ],
  "i18n": {
    "en": {
      "name": "Making a Silent App Talk",
      "shortDescription": "\"People are saying they write on the board and then can't find it later.\" That is the whole message from the CTO on Monday morning.\n\nOpen the board and the posts are there. The dashboard is green. The health check keeps answering 200 and monitoring has never once fired. And there are still three messages in the company chat saying things that were written have gone missing. The previous SRE left a note — \"put a relay in this morning that ships posts from the board to the downstream archive; its logging is noisy so I turned it off\" — and then left.\n\nOpen the app log and the boot lines are there, and the post-accepted lines are there. What is not there is a single line about a post that never reached the archive. Nothing being printed is not the same as nothing being wrong. The CTO: \"If you don't know the cause, make it knowable first. We'll decide the fix order after that.\"",
      "instructions": "## Getting started\n\nThe board runs, but one external relay slice is down while health still says 200. Make metrics, logs, and health tell the same truth.\n\n## Work entirely in the browser\n\n1. Open `/relay`\n2. In `/docs` under **Try any API**, run `GET /metrics` and `GET /relay/healthz` and observe the disagreement\n3. Send a few posts with `POST /api/posts`, then inspect `GET /metrics` and `GET /relay/state` again\n4. Change relay settings with `PATCH /api/settings`. Do not edit repository files\n5. Check the four gates with `GET /posture`; retry with `DELETE /api/settings`\n\n## Goal\n\nRecord failed writes, identify the down slice from numbers, make health report reality, and leave an investigation log ready to hand over. Restarting erases the evidence and is not a repair.",
      "description": "## Purpose\n\nIssue 288. Read the app log, separate healthy traffic from failures using numbers, understand what a health check's condition actually covers, configure a minimal detection yourself, collect the evidence you would hand to a colleague or an AI, and keep secrets out of that evidence — all six on one investigation.\n\n## What the container models instead of a cloud\n\nNo CloudWatch and no load balancer. A minimal observability surface inside the container instead.\n\n| In this container | Real equivalent |\n|---|---|\n| `GET /metrics` (Prometheus text format) | CloudWatch metrics / a metric filter |\n| `healthCheckProbes` in `relay.json` | an ALB target group's health check condition |\n| `GET /relay/healthz` | what the target group polls |\n| `archiveLogging` in `relay.json` | whether the application logs at all |\n| `logDetail` in `relay.json` | log masking / structured-log field selection |\n| the archive's four shards | a downstream partition / AZ / shard |\n\n**There is no \"create an alarm and make it fire\" step.** Paging yourself from a container on your own laptop teaches nothing, so that requirement went to the Battle side, against a real injected fault. What is scored here stops at \"is this system in a state where something could fire\", and nothing beyond that is claimed.\n\n## What is measured\n\n| checkpoint | what is measured |\n|---|---|\n| `silent-failures` | (1) relay logging is on in the live settings (2) a `relay drop` line exists at the current epoch (3) both successes and failures are really happening — then the incident id, exactly |\n| `honest-health` | the health condition evaluated in three worlds `(config,archive)` = (t,f)/(f,t)/(t,t), which must give (not ok, not ok, ok), plus `/relay/healthz` having actually been served since that condition was written. The receipt comes from the boot secret and cannot be derived |\n| `leak-shut` | preconditions first — A (a drop line and an archived line are both *present* at the current epoch; an archived line is itself the evidence that writes are reaching the archive under these settings) / B (the shard, its code, the target and the epoch all survived) — and only then the absence |\n| `root-cause` | the down shard has recorded a failure from the participant's own traffic, another shard has recorded a success, and the submitted code matches that shard exactly |\n\n## Dangerous shortcuts, and where each one falls over\n\n| Shortcut | Where it falls |\n|---|---|\n| Restart the container | the title-to-shard map is seed-derived and stateless, so nothing is fixed. Worse, the counters reset and the log ring clears, so three checkpoints that were passing stop |\n| Stop posting / `acceptingPosts: false` | successes and failures both go to zero and `silent-failures`, `root-cause` and the `traffic_seen` gate all go false. \"No errors\" is zero marks, not full marks |\n| Turn the log off to \"fix\" the leak | `leak-shut` measures A and B **before** it looks at the absence, so an empty log fails at A and never reaches the absence check. The `credential_out_of_logs` gate embeds the `failures_logged` conjunction for the same reason |\n| `logDetail: \"masked\"`, which blanks only the tail | the absence check matches the first 12 characters, so what is left still fails — and no safe-log value is issued on those lines |\n| `logDetail: \"minimal\"`, which cuts the line down | the secret really is gone and the absence check would pass, but precondition B (shard, code, target, epoch) is measured first. `silent-failures` fails too, since the incident reference goes with it |\n| Nail `/relay/healthz` shut so it never goes green | an empty probe list is always 503, and the three-world evaluation requires ok in (t,t) |\n| Point the health check only at `archive` | (f,t) must be not-ok, so it fails — it would stay green through a broken settings file |\n| Write an answer table / a fixed-value solver | the incident id, the safe token, the four shard names, the four codes and the title-to-shard map are all derived from `FLAG_SEED`; the receipt comes from a boot secret and changes even across a restart of the same container |\n| Just open `/metrics` and copy a code | a shard's series appears only after that shard has been written to, and all four codes are printed — so the answer is a correlation, not the only value on the page |\n\n## About writing a secret to the log (reviewer-facing)\n\nThe shipped `logDetail: \"full\"` writes the credential the relay presents to the archive into the log line. It is not a real credential: it is derived from `FLAG_SEED`, it never leaves a loopback-bound container, and **removing it from the log is one of this problem's pass conditions**. The catalog is not shipping a leak. Participant-visible text (shortDescription / instructions / label / hint) stays on the asset and the symptom — \"make the investigation log safe to hand over\" — and never names a vulnerability class (AGENTS.md §10 / §13).\n\n## The history of the log is not erased\n\n`leak-shut`'s absence check looks only at lines written under the **settings generation (epoch) in force now**. Switching `logDetail` to `\"safe\"` does not un-write the lines that already carry the credential, and leaving them visible is deliberate: a config change does not undo a leak. That is exactly why the real remediation is rotating the credential rather than grepping the log, and the READMEs say so under \"what leaked stays leaked\".\n\n## What scoring does not do\n\nAll four checkpoints are read-only. They do not post, do not edit settings, and do not move a shard. A wrong answer and a retried request both change nothing. `/posture`'s `readyToken` is not used as an answer here — the same value appears for every scenario on one seed.\n\n## Known limits\n\nThe verdict is computed inside the container, so a participant who edits `runtimes/stackstack/` in their own checkout and rebuilds the image defeats every check. That is the local-play honesty limit every container problem in this catalog shares, not a scoring bug. Unguessability likewise assumes the platform injects `FLAG_SEED`; a bare `docker compose up` uses the compose default.",
      "writeup": "## What you touched in those forty-five minutes\n\n- **A silent app is not a healthy app** — the first log had boot lines in it, and post-accepted lines in it. What it did not have was a line about a post that never reached the archive, and that was not because nothing was failing. \"There's nothing in the logs\" carries zero information as an incident report; quite often it means \"I have not looked yet\".\n- **The split between numbers and lines** — *what* is failing was a count on `/metrics`; *why*, and *which one*, was a line in the log. Neither is enough alone. The numbers showed that one of four slices was returning nothing; the line carried the incident id and the reason.\n- **A health check is only its condition** — the shipped condition looked at whether the settings file could be read, so it stayed green forever while writes kept being dropped behind it. The moment you fix it, `/relay/healthz` returns 503. **Going red is the result.** A health check that has never once gone red has not been shown to work; it has only been shown to be pointed at something that never fails.\n- **Don't paste the investigation log** — the moment you turned the signal on, the value the relay presents downstream landed in the lines. Read it yourself before it goes into a ticket or into an AI prompt. And note that turning the detail down did not clean up the lines already written — which is why the real remediation is rotating the value, not editing the past.\n\n## Why \"the errors stopped\" is not the scored fact\n\nThe two easiest ways to feel fixed here are to stop accepting posts and to turn the relay's log back off. Both make the errors disappear from view. So all four checkpoints require that failures are really happening *and* being recorded before anything else is graded. Silence is not health.\n\n## Next\n\n`stackstack-defend` puts you on the defending side of the same board, `stackstack-recover` brings it back from down, and the main event is the `stackstack` Battle.",
      "learningGoals": [
        "Tell \"nothing is being logged\" apart from \"nothing is going wrong\"",
        "Narrow a failing dependency down to one slice from a metrics breakdown",
        "Evaluate and fix a health check's condition against the healthy, failing and blind-spot cases",
        "Hand over an investigation log with the secret removed and the diagnostic fields intact",
        "Avoid mistaking silence — no errors on screen — for recovery"
      ],
      "checks": [
        {
          "id": "silent-failures",
          "label": "Get the writes that never landed into the record",
          "hints": [
            {
              "id": "obs-signal-1",
              "content": "The numbers already count failures, but the log says nothing about them. That gap is controlled by the relay's runtime settings. Read and change them through `GET` and `PATCH /api/settings` in `/docs`; there is nothing to restart."
            },
            {
              "id": "obs-signal-2",
              "content": "In `/docs`, turn relay logging on with `PATCH /api/settings`, then post about eight times with different titles (some will not reach the archive — that is the point). Run `GET /api/logs?limit=200` and submit only the value after `incident=` on a dropped line. Turning the setting back off takes the score back with it."
            }
          ]
        },
        {
          "id": "honest-health",
          "label": "Make the health check tell the truth",
          "hints": [
            {
              "id": "obs-health-1",
              "content": "What is `/relay/healthz` actually looking at right now, and what does this app say its own dependencies are? One of those two lists is shorter than the other, and `GET /metrics` publishes the long one. `GET /relay/state` shows you whether the condition you wrote passes the three cases the gate checks."
            },
            {
              "id": "obs-health-2",
              "content": "`healthCheckProbes` is an array of dependency names, and the names are exactly what `relay_dependency_up{name=\"...\"}` prints on `/metrics`. List every dependency the app has — narrowing it to one means lying whenever the other one breaks. Save, then actually call `/relay/healthz` once (a corrected condition that nobody has run is not yet a check), and submit the `health_honest` entry from `tokens` on `GET /posture`."
            }
          ]
        },
        {
          "id": "leak-shut",
          "label": "Make the investigation log safe to hand over",
          "hints": [
            {
              "id": "obs-handover-1",
              "content": "Read the log you just turned on once more, yourself, before it goes into a ticket. One of the values the relay presents downstream is sitting there in the line. Note that turning the log back off is not the answer here — it takes the score back to the previous step."
            },
            {
              "id": "obs-handover-2",
              "content": "`logDetail` in `relay.json` decides how much each line carries, and it has four settings. Exactly one of them passes: the one that replaces the downstream value outright and leaves shard, code, target, reason and incident in place. The setting that blanks only the tail leaves the front behind; the setting that cuts the line down loses the investigation along with what you were trying to remove. Save, post a few more times so fresh lines exist under the new setting, and submit the `safe-token=` value from any of them."
            }
          ]
        },
        {
          "id": "root-cause",
          "label": "Name the slice that is actually down",
          "hints": [
            {
              "id": "obs-slice-1",
              "content": "There is one surface you can read without changing any setting, and it has the archive's breakdown on it. It starts almost empty: a slice appears there only after something has been written to it. Post several times with different titles first, then look for where the numbers stop moving."
            },
            {
              "id": "obs-slice-2",
              "content": "Run `GET /metrics` from `/docs`. Find the shard whose `relay_posts_total{result=\"dropped\",shard=\"...\"}` is non-zero, then submit the `code` label from `relay_shard_up` for that same shard. Every shard has a code, so the wrong shard's code is a wrong answer. Different titles reach different slices."
            }
          ]
        }
      ]
    }
  },
  "runtime": {
    "provider": "docker",
    "engine": "compose",
    "entry": "local/docker-compose.yml",
    "challengeEndpoints": {
      "Board": "http://127.0.0.1:18080/",
      "Relay": "http://127.0.0.1:18080/relay"
    },
    "verifyUrl": "http://127.0.0.1:18081/verify",
    "secretEnv": [
      "FLAG_SEED"
    ]
  },
  "scoring": {
    "kind": "multi-verify",
    "checks": [
      {
        "id": "silent-failures",
        "label": "届かなかった書き込みを記録に残す",
        "points": 50,
        "wrongAnswerPenalty": 3,
        "hints": [
          {
            "id": "obs-signal-1",
            "content": "数字のほうはすでに失敗を数えていますが、 ログは何も言っていません。 その差は relay の実行時設定で制御されています。 `/docs` の `GET /api/settings` と `PATCH /api/settings` で確認・変更でき、 再起動は不要です。",
            "penalty": 0
          },
          {
            "id": "obs-signal-2",
            "content": "`/docs` から `PATCH /api/settings` で relay のログを on にし、 違うタイトルで 8 回ほど投稿してください (いくつかは archive に届きません)。 続けて `GET /api/logs?limit=200` を実行し、 届かなかった行の `incident=` の後ろの値だけを提出します。 設定を戻すと採点も戻ります。",
            "penalty": 20
          }
        ]
      },
      {
        "id": "honest-health",
        "label": "死活確認に本当のことを言わせる",
        "points": 45,
        "wrongAnswerPenalty": 2,
        "hints": [
          {
            "id": "obs-health-1",
            "content": "`/relay/healthz` はいま何を見て 200 と言っているのか。 そして、 このアプリが 「自分の依存先」 として名乗っているものは何か。 2 つのリストのうち片方が短く、 長いほうは `GET /metrics` に出ています。 自分が書いた条件が gate の見る 3 つの場合を通っているかは `GET /relay/state` が教えてくれます。",
            "penalty": 0
          },
          {
            "id": "obs-health-2",
            "content": "`healthCheckProbes` は依存先の名前の配列で、 名前は `/metrics` の `relay_dependency_up{name=\"...\"}` がそのまま正解です。 このアプリが持っている依存先を全部並べてください ── どれか 1 つに絞ると、 絞らなかったほうが落ちたときに嘘をつきます。 保存したうえで実際に `/relay/healthz` を 1 回叩き (直しただけで誰も走らせていない条件は、 まだ死活確認ではありません)、 `GET /posture` の `tokens` にある `health_honest` を提出してください。",
            "penalty": 18
          }
        ]
      },
      {
        "id": "leak-shut",
        "label": "調査ログをそのまま渡せる状態にする",
        "points": 60,
        "wrongAnswerPenalty": 3,
        "hints": [
          {
            "id": "obs-handover-1",
            "content": "いま出るようにしたログを、 チケットに貼る前にもう一度自分で読み直してください。 relay が下流に渡している値が 1 つ、 行にそのまま載っています。 なお 「ログを止める」 はこの checkpoint の答えではありません ── 止めると採点は 1 つ前の段階に戻ります。",
            "penalty": 0
          },
          {
            "id": "obs-handover-2",
            "content": "`relay.json` の `logDetail` が 1 行に載せる情報量を決めています。 設定は 4 つあり、 合格になるのは 「下流に渡している値を差し替えたうえで、 shard・code・target・reason・incident をそのまま残す」 1 つだけです ── 後半だけ伏せる設定は前半が残るので落ち、 行ごと削る設定は調査に要る項目まで一緒に失うので落ちます。 保存してから何回か投稿を流し、 新しい設定で書かれた行の `safe-token=` の値を提出してください。",
            "penalty": 24
          }
        ]
      },
      {
        "id": "root-cause",
        "label": "落ちている一枚を名指しする",
        "points": 45,
        "wrongAnswerPenalty": 2,
        "hints": [
          {
            "id": "obs-slice-1",
            "content": "設定を何も変えなくても読める面がひとつあり、 そこに archive の内訳が出ています。 最初はほとんど空です ── 内訳はそこに 1 度でも書き込みがあってから初めて出ます。 まず違うタイトルで何回か投稿を流して、 数字が動かない場所を探してください。",
            "penalty": 0
          },
          {
            "id": "obs-slice-2",
            "content": "`/docs` から `GET /metrics` を実行します。 `relay_posts_total{result=\"dropped\",shard=\"...\"}` が非ゼロの shard を見つけ、 その同じ shard の `relay_shard_up` にある `code` を提出してください。 別の shard の code は誤答です。 違うタイトルで投げると別の一枚に当たります。",
            "penalty": 18
          }
        ]
      }
    ]
  },
  "track": {
    "id": "stackstack-route",
    "order": 150,
    "chapter": "4. StackStack 本編"
  }
}
