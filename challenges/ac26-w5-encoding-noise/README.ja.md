# どこまで押せるか

> このトラックは Advanced Cryptography Program 2026 の非公式・独立コンパニオンです。
> コース運営とは無関係で、承認も受けていません。問題文・コード・fixture・図はすべて独立に書いています。
> このトラックへの質問はコース運営ではなく TenkaCloud リポジトリへお願いします。

**Track:** `advanced-cryptography-2026` · **Order:** 510 · **Chapter:** Week 5 / Encoding
and Noise · **Role:** `mechanism` · **想定時間:** 45〜60 分 · **配点:** 200 ·
**Status:** draft

## この問題を始める前に — FHE という名前はいったん忘れる

まず、秘密の数字 `5` が入った**鍵付きの箱**を想像してください。

普通の暗号が得意なのは「箱に鍵をかけて、中身を見えなくすること」です。

```text
5
↓ 暗号化
[ 5 が入った鍵付きの箱 ]
```

これならクラウドに箱を預けても、クラウドは中身の `5` を読めません。

ところが、クラウドに「その秘密の数字に `3` を足して」と頼みたいとします。
普通の暗号だけなら、計算するには箱を開けて `5` を見る必要があります。
それでは秘密を守ったまま計算を任せられません。

Week 5 が解こうとしているのは、この困りごとです。

```text
[ 5 ] 🔒
   │
   │ 箱を開けずに +3
   ▼
[ 8 ] 🔒
```

計算したクラウドは、途中で `5` も `8` も見ません。
最後に秘密鍵を持つ人だけが箱を開け、`8` を受け取ります。

この「**暗号文を復号せず、暗号文のまま計算する**」考え方を
**準同型暗号（Homomorphic Encryption）** と呼びます。
そして、足し算や掛け算などを十分に組み合わせて一般的な計算を作れるものが
**完全準同型暗号（Fully Homomorphic Encryption / FHE）** です。

ここで重要なのは「FHE は単にもっと強く暗号化する方式」ではないことです。
目的は、**秘密を隠したまま第三者の計算能力を使うこと**です。

### では、なぜ noise を勉強するのか

暗号文のまま計算できれば話は終わりそうですが、実際には計算のたびに暗号文の中の
**noise（小さな誤差）** が増えていきます。

```text
暗号化した直後      noise 小
      ↓ 計算
                    noise 中
      ↓ さらに計算
                    noise 大
      ↓
大きくなりすぎると、元のメッセージを正しく復号できない
```

したがって、FHE を理解する最初の問いは

> **「暗号文は、noise にどこまで押されても元の答えへ戻れるのか？」**

です。

この問題では、まだ本物の LWE や TFHE を実装しません。
まず小さな整数だけを使い、メッセージを離れた点に置いて、そこへ noise を足し、
**どこまでなら正しく復号でき、どこで初めて壊れるのか**を目で見える形にします。

この境界が分かると、このあと Week 5 で出てくる流れが一本につながります。

```text
暗号文のまま計算したい
        ↓
計算すると noise が増える
        ↓
noise が大きすぎると復号が壊れる
        ↓
noise を小さく戻したい
        ↓
Bootstrapping
        ↓
TFHE / Programmable Bootstrapping
```

この問題を解く段階では `Bootstrapping` の仕組みを理解している必要はありません。
まず「**noise には正しく復号できる限界がある**」ことを自分の手で確認できれば十分です。

### 30 秒チェック

先へ進む前に、次の 3 問に答えられるか確認してください。

1. クラウドは計算中に秘密の `5` を見る必要がある？ → **ない**
2. FHE の目的は「暗号を強くすること」だけ？ → **違う。暗号文のまま計算できるようにすること**
3. なぜこの問題で noise の限界を測る？ → **計算で noise が増え、限界を超えると復号が壊れるから**

ここが分かってから、下の toy model に進みます。

## 物語

この公開の符号化模型では、メッセージを離れた位置に置き、どこまでずれても同じ値へ戻るかを調べます。秘密鍵はなく、誰でも復号できます。正しく戻せる範囲を扱い、実用暗号の秘匿性やノイズ増加は評価しません。

```text
message      m   は [0, p) の範囲
scaling      D   が p 個のメッセージを環全体に広げる
ring         q = p * D
encode       encode(m) = (m * D) mod q
decode       c が一番近い encoding point のメッセージ。同点は切り上げ
```

`p`・`D`・`q` は `params` から来て、checkpoint ごとに変わります。決め打ちしたものはどこかで外れます。

## 本当に難しいのは 3 つ

| | なぜ効くか |
|---|---|
| **同点の扱い** | 2 点のちょうど中間の値は**切り上げ**る。deltaが偶数なら整数の中間点があり区間は非対称、奇数なら中間点がなく対称です。 |
| **負の noise** | `e` は負になりうる。 Python の `%` は正の法に対して非負を返すので特別扱いは要らない。 `abs(e)` は別の関数。 |
| **巻き戻り** | 最後のメッセージの次の点はメッセージ p ではなく 0。 p 個のうち 2 個だけがこれに気づく。 |

## 予測してから測る

`success_interval` はパラメータに対して採点され、あなたの decoder に対しては採点されません。
これは意図的です。全 noise 値を試して**測った**区間は decoder が何をしていようとそれに一致するので、
間違った decoder と間違った区間が一緒に通ってしまいます。
hidden test は両方を fixture から計算し、decoder を区間に対して、区間を decoder に対して、別々に検査します。

## Participant Portal での進め方

1. Participant Portal で問題を起動する。同じ画面に問題エディタが表示される。
2. **証拠を調べる**で、この deploy 固有の fixture と公開された証拠を読む。
3. Portal のエディタで starter のソースを編集する。
4. **公開テストを実行**を押し、直接回答欄があれば証拠から埋める。
5. 各 checkpoint をそのまま提出する。Portal が現在のファイルと回答を準備して送る。

checkout、ターミナル、ローカルエディタ、別画面、コピペは不要です。code checkpoint は現在の
エディタ内容を使います。直接回答は現在の deploy seed へ結び付くため、別 deploy からコピーした
値は拒否されます。

## 採点

7 つの checkpoint を独立に採点します。誤答は 1 回 10 点。

| Checkpoint | 配点 | 検査内容 |
|---|---:|---|
| `encode` | 25 | 点が相異なり `[0, q)` に入ること、範囲外メッセージの正規化 |
| `noise` | 30 | centered 表現とその往復、負の noise と q を超える noise |
| `decode` | 30 | 環の全値、`decode(encode(m)) = m`、`+q` 不変性、同点の扱い |
| `interval` | 30 | パラメータから予測した区間、`delta` の偶奇の両方 |
| `first-failure` | 30 | 各方向で最初に失敗する noise と、その復号結果 |
| `transfer` | 25 | 見たことのない seed 由来のパラメータで上記すべて |
| `validate` | 30 | 使えない 5 組を reject し、`delta = 1` を含む使える 3 組を通す |

7項目すべてに各2点の3段ヒントがあり、それぞれの採点項目の50%上限以内です。

## equivalent mutant について

mutation の候補のうち 2 つは、生き残らせるのではなく**外しました**。どちらも議論ではなく全数検査で確認しています。

- `validate_params` の `delta >= 1` は検出可能な形で壊せません。 `q = p * delta` かつ `q >= 1` かつ
  `p >= 2` の時点で `delta >= 1` は導かれるので、この境界を緩めてもどの入力でも判定が変わりません。
  reference には残していますが、それはエラーメッセージのためであって、判定を担っているからではありません。
- `encode` の `m % p` も検出可能な形では壊せません。 `(m % p) * D` と `m * D` は
  任意の整数 m について `p * D` を法として合同です。この剰余は意図を書いているだけで、
  仕事をしているのは外側の `% q` の方であり、mutation はそちらを壊します。

殺せない mutation を一覧に残すと `SURVIVED` の行を無視してよいと教えることになるので、どちらも残していません。

## これは安全ではないし、安全なふりもしない

`p` と `q` は手で全列挙できる大きさで、境界が見えるのはそのおかげだけです。
実運用のパラメータは格子問題の裏にメッセージを隠しますが、これは何の裏にも隠していません。
toy の正しさと production の安全性は別の主張で、この問題は前者しか主張しません。

同様に、noise は padding ではなく、無料でもありません。安全性が依拠する対象であり、
正しさが消費する対象です。 1 単位足すごとに困難性を買い、余裕を払います。
ここで計算する区間が、 Week 5 の残りが使っていく予算そのものです。

## 出典との対応

Week 5 の教材は公開済みなので、 `courseAlignment` は `week5/README.md` を `lecture`、
`week5/problems/tfhe-toy-python/README.md` を `assignment` として、
トラックが記録している commit で pin しています。 `spoilerPolicy` は
`independent-reimplementation` です。ここでのパラメータは seed から生成し、
encoding rule は上に全部書いてあるので、公式課題の fixture や `solution.py` からは何も写しておらず、
これを読んでも公式課題の近道にはなりません。

## 保証範囲

ローカル実行は**自習用の honor-system 検証**です。compose stack のすべてのコンテナと
Docker デーモンを管理する人を、中身の閲覧から止める手立てはありません。ここにある境界は
秘匿ではなく誤配送の防止です。build して動かす Workbench コンテナには starter と公開テスト
しか入っておらず、fixture も hidden test も参照解答も verifier 本体も入っていません。
それらは Workbench がネットワーク越しに話す、公開されていない second container と、
`make reference-test` が build する author 専用 image にだけあります。

verifier は提出コードの実行時間と出力を制限し、親プロセスで結果を検査します。
これは一般的なサービス拒否耐性や Docker 管理者への機密性を保証しません。 checkpoint は echo した id しか加点できません。
結果は期待値を漏らしません。 fixture はこのデプロイの seed 由来なので、暗記した答えは持ち越せません。

これは自習と誠実な練習を支えます。競技順位・試験・修了判定は**支えません**。
それらには participant が管理しない verifier が必要で、
[#271](https://github.com/susumutomita/TenkaCloudChallenge/issues/271) で追跡しています。

## コスト

ゼロ。クラウドアカウントも AWS リソースも不要です。

## 作問者向け

`make reference-test` が mutation suite を走らせます。 17 個の壊れた実装のほとんどは、
exact な encoding point をすべて正しく復号します。学習者が最初に確かめる性質がそれであり、
それでは足りない理由もそれです。


7項目すべてに各2点の3段ヒントを用意します（合計42点）。整数の丸め式、符号付き位置、0を含む安全区間、最初の失敗の求め方、パラメータの受理条件は無料の問題文に記載します。公開の符号化と暗号を区別し、実用暗号のノイズ増加や安全性の模型とは主張しません。参加者向け資料だけを読む独立読解で、入力範囲の不足・折り返しの曖昧さ・秘密性と区間の誤説明を発見し、修正しました。ヒントの提供範囲と減点を変更します（最大合計53点から42点）。採点器にも真偽値・小数・キー欠落の拒否検査を加え、無料の入力条件と一致させました。カタログ検証と文章確認は作者確認であり、ブラウザでのプレー実績ではありません。作者の誤実装テストは模範実装が通り、型やキー欠落を見逃すものを含む20件を拒否しました。


最後のtransferにはcounterexample(params, bug)で反例を構成する課題を追加しました。切り捨て・折り返し忘れ・負のずれの符号除去と正しい式を比較します。式と入力範囲は無料、差を起こす入力は参加者が構成します。採点器は双方を独立計算し、公開テストでは現在のparamsで確認できます。作者テストは差のない反例やbug無視を含む23件の誤実装を拒否しました。


独立読解で2メッセージ・奇数間隔の反例なしの場合を発見しました。Noneの契約を追加し、採点では反例がある場合とない場合の両方を確認します。常にNoneを返す実装は拒否します。

## 親プロセスによる採点（Issue #837、2026-09-08）

判定条件・入力生成・最終得点判定を信頼する verifier 側に置き、提出関数だけを別の Linux worker で実行します。親へ渡すのは型を保ったデータ値だけです。tuple/list、辞書の整数キー、bool/int を区別し、契約上の入力拒否は派生型を含む `ValueError` として扱います。他の AC26 教材で使う有界 worker と seccomp による分離を再利用しています。

採点全体の期限は12秒で、既存の転送側15秒以内です。両サービスは UID 10001 と Compose の `init: true` を使います。worker のファイル読み取り・ネットワーク・別プロセスへのアクセス・親へのシグナルを制限し、Linux の分離機能が利用できなければ不合格にします。Docker を管理する人への機密性や本番セキュリティを保証するものではありません。公開 API、得点、入力データ、正しい計算上の別解は維持しています。

作者検証：`make reference-test` で参考解が通り、既存の計算誤り 23 件をすべて拒否しました。`make boundary-test` は 7 個の Linux テストで、全7項目の参考解、未実装・途中終了、型の保持、期限、ファイル/プロセス分離、非 root 実行を検査します。実 Workbench の HTTP 経路（config → inspect → starter → test → prepare → verify）で参考解の公開例 4 件と全7項目の提出が通り、途中終了は全項目で拒否されました。作者による回帰検証であり、独立した参加者プレーや実 Portal の得点履歴の検証ではありません。

このホストでは `make test` を実行しましたが、Docker の既定アドレスプール枯渇でネットワークを作成できませんでした。Workbench 確認には重複しない専用サブネットの一時 Compose override を使い、リポジトリのネットワーク設定や他作業のネットワークは変更していません。実 AWS、実 Portal の得点履歴、チーム間の動作は未確認です。デプロイは行っていません。

無料本文の式から `encode` だけを実装し、後の関数は未実装のまま最初の1項目が得点できることも別に確認しました。

### Supported computation imports / 計算用の標準ライブラリ

Supported computation imports / 計算用に使える標準ライブラリ:
array, base64, binascii, bisect, collections, contextlib, copy, dataclasses, decimal, enum, fractions, functools, hashlib, heapq, hmac, itertools, json, math, operator, random, re, statistics, string, struct, time, typing.
This list covers optional standard-library helpers. Imports already supplied by the starter (including __future__ and problem APIs) are also supported. Other optional imports and file/network access are not supported in grading.
この一覧は追加できる標準ライブラリです。スターターに最初からあるimport（__future__や教材のAPIなど）も、そのまま使えます。それ以外の追加importとファイル・通信操作には採点時は対応しません。
