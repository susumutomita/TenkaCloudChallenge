# Issue #716 — co-SNARK privacy acceptance

2026-09-08. Scope: this problem directory only. No platform changes or deployment.

## Participant-role first read

The initial public-only read used the statement, starter, Inspect contract and public tests. It found:

- The first screen used witness/relation/party/open/mask before defining them.
- `classify` required a precedence rule the statement withheld as the task.
- The second probe did not explain obtaining the first row or `malformed_row`.
- The four exit shapes and the three recovery formulas were incomplete.
- The repair's supplied imports and publication action were unclear.
- Transfer repeated previous checks with changed parameters; seven costly hints were not eight three-step staircases.

The rewrite provides the prerequisite bridge, first edit, glossary, complete free API, ordered classification rules, one-digit recovery examples, publication contract, eight bilingual hint staircases (24 × 2 = 48 points), and updated Workbench labels. README and post-completion writeup now state the instrumented-model boundary, without claiming this is an actual SNARK proof or a general privacy proof.

An independent subagent reread only the public guides, metadata, starter and public tests. It found five remaining leaps: serialized object shape, integer/mask precedence, the location of `d` within `values`, recovery's first-match exception to full reporting, and mask ancestry confused with a security guarantee. All five were addressed. `serialized` preserves the four-attribute Disclosure object; only Share contents become opaque identifiers. The guide shows the actual attribute access, same-record rule and complete p=7 example.

## Grading consistency

The original repair check could accept a correct returned C while the artifact represented a different calculation. It also accepted a swallowed spent-triple refusal. It now verifies the current returned A/B/C, d/e and identifiers; artifact agreement; exactly the authorized openings; and propagation of TripleMisuse with all four exits empty.

Transfer now combines defects in unseen normal and failure executions. Two mutations that return only the first leakage/opening finding still pass all seven earlier checkpoints, but fail transfer. This demonstrates an additional composition obligation, not a repeat of earlier testing. The public rules introduce no secret condition at the final checkpoint.

## Executed evidence

- `make install` and `make agent-gate`: all 116 catalog entries valid.
- `cd local && python3 mutation.py` and Docker `make reference-test`: reference passes; 51 broken implementations plus forged-verdict output are rejected (52 mutations).
- Reference accepted under three additional settings; no hidden values are included in this record.
- Transfer-only mutations: truncated leakage results fail transfer in three cases; truncated opening results fail it in two. Both pass earlier seven checkpoints.
- Python compilation and `git diff --check` pass.
- Bilingual check IDs, labels, 8 × 3 hint structure, 2-point penalties, 48-point total and guide/metadata parity checked.

A local HTTP smoke used the real participant server, verifier and public API, with listeners bound to loopback only:

1. `/api/config` listed eight code checkpoints and only prover.py; `/api/inspect` returned the actual row, records and first-edit instruction.
2. `/api/starter` → `/api/test` imported successfully and exposed unfinished function failures.
3. A first `classify` edit based on the free rule table passed its public shape test and earned `classify=true` through `/api/prepare` → `/verify`, while later functions remained unfinished.
4. Separately, as author regression, the reference passed all seven public tests and all eight matching checkpoint verdicts through the same prepare/proxy route.
5. A mismatched artifact was rejected with a public property-level explanation, capped at 1900 characters.

The first-edit smoke supports the documented entry route; author-reference acceptance does not prove an independent beginner can complete all eight. This run exercised real Workbench HTTP handlers, not browser clicks. Browser rendering, AWS deployment, event persistence and score aggregation were not exercised in this change. The Dockerfile and participant/verifier separation are unchanged. Existing published make commands remain available for Docker rehearsal.
