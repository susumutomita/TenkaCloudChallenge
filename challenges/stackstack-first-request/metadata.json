{
  "$schema": "../../SCHEMA.json",
  "id": "stackstack-first-request",
  "name": "はじめてのリクエスト",
  "category": "Challenge",
  "status": "draft",
  "visibility": "public",
  "difficulty": 1,
  "estimatedDuration": "10 分",
  "shortDescription": "明日が天下クラウド株式会社の入社初日。 夜、 CTO からメッセージが届く。 「明日から社内掲示板をまかせます。 練習用のデモ板を置いておいたので、 今夜はブラウザから触ってみてください。 壊しても大丈夫。 作り直せば戻ります」\n\nWeb アプリと話す方法は、 リクエストを送って、 レスポンスを読む。 それだけだ。 この 10 分で、 その一往復を 3 回体験する ── 読む、 断られて直す、 書く。\n\nすべて手元のコンテナで動く。 クラウドのアカウントも、 クレジットカードも、 課金も要らない。",
  "instructions": "## 前提 — この連作の世界\n\nあなたは明日から 「天下クラウド株式会社」 で働きます。 仕事は、 AI が自動生成した社内掲示板 (Web アプリ) を預かって動かし続けること。 前任の担当者はもう会社におらず、 この掲示板はこれからしばらく、 あなたの相棒です。 この問題はその第 1 話です。\n\n道具はすべて手元のコンテナの中だけで動きます。 クラウドのアカウントも、 クレジットカードも、 課金も要りません。 壊しても、 コンテナを作り直せば元に戻ります。 罠もありません。\n\n- **言葉** (この問題で使うもの):\n  - **Web アプリ** = ブラウザから使うプログラム。 ここでは小さな掲示板。\n  - **リクエスト** = ブラウザなどがアプリへ送る 「お願い」。 **レスポンス** = それへの返事。 Web との会話はこの一往復の繰り返しです。\n  - **メソッド** = お願いの種類。 **GET** は 「見せて」、 **POST** は 「これを受け取って」。\n  - **パス** = アプリの中の場所。 `/api/postcard` のように書きます。\n  - **ステータスコード** = 返事に付く 3 桁の数字。 200 番台 = できた (200 = OK、 201 = 作った)。 400 番台 = 頼み方を直せば通る。 500 番台 = アプリ側の故障。\n  - **JSON** = `{\"name\": \"you\"}` のようにデータを書く形式。 Python の辞書とそっくりの見た目です。\n  - **API** = プログラム向けの窓口。 メソッドとパスの組 (例: `GET /api/postcard`) で呼びます。\n  - **コンテナ** = あなた専用に立ち上がる小さな仮想コンピュータ。 この問題のアプリは全部この中で動きます。\n  - **token (トークン)** = 合言葉として使う、 推測できない文字列。\n  - **checkpoint** = 採点される小問。 この問題には 4 つあります。\n\n## はじめに\n\n明日が入社初日。 夜、 CTO (技術の責任者) からメッセージが届きます。 「明日から社内掲示板をまかせます。 練習用のデモ板を置いておいたので、 今夜はブラウザから触ってみてください。 壊しても大丈夫。 作り直せば戻ります」。\n\n今夜やることは、 この板との 3 回の会話です — 読む、 断られて直す、 書く。 全部ブラウザだけでできます。\n\n## 最初の一手\n\nこの問題文が出ている画面 (Participant Portal) で **「起動」** を押してください。 コンテナが立ち上がると、 同じ画面に **「アクセス先 URL」** の一覧が出ます。 `Web` を開くと掲示板が表示されます。 ページが開けたなら、 実はそれがあなたの最初の GET リクエストです。\n\n板には CTO の投稿が 2 つあり、 今夜の宿題 (3 つの API) がそのまま書いてあります。 板の上のほうにある **「API コンソールを開く」** から `/docs` を開いてください。 ここが今夜の作業台です。\n\n- 一覧に並んでいる API は、 各項目の **Try it out** から実行できます。\n- 一覧に無いこの問題専用の API は、 上の **「任意の API を試す」** の欄にメソッドとパスを入れ、 **「実行」** を押します。 ターミナルは要りません。\n\n## 答えるもの — checkpoint は 4 つ\n\n答えはすべて、 自分が実行したリクエストのレスポンスの中に表示されます。 見つけた値をその checkpoint の回答欄に貼り付けて **「提出 (+点数)」** を押してください。 値は起動のたびに変わるので、 他の人の答えは使えません。\n\n1. **葉書を読む** (25 点) — 「任意の API を試す」 で `GET /api/postcard` を実行します。 返ってきた JSON の `token` (`postcard-` で始まる文字列) が答えです。 レスポンスの `message` も読んでください — この token の使いみちが書いてあります。\n2. **断られて、 直す** (25 点) — `GET /api/door` を実行します。 最初は 400 で断られますが、 400 は行き止まりではなく、 直し方の書いてある返事です。 レスポンスの `detail` の言うとおりにリクエストを直してもう一度実行すると、 200 の返事に `token` (`TC{door_...}`) が出ます。 それが答えです。\n3. **芳名帳に書く** (25 点) — メソッドを POST、 パスを `/api/guestbook` にして、 body の欄に `{\"name\":\"あなたの名前\",\"message\":\"ひとこと\"}` (JSON) を入れて実行します。 うまくいくと 201 で `receipt` (`TC{guestbook_...}`) が返ります。 それが答えです。 形が違うと 400 と直すところが返るので、 読んで直してください。\n4. **一周の証明** (25 点) — `GET /posture` を実行します。 これはアプリが自分自身を実測した結果で、 上の 3 つの会話が本当に起きたかどうかが 3 つの **gate** (真偽の項目) として出ます。 3 つとも true になると `readyToken` (`TC{ready_...}`) が現れます。 それが答えです。 まだ null なら、 false の gate がそのまま残りの宿題です。\n\n間違えても直して再提出できます (誤答には checkpoint ごとに決まった小さな減点があり、 合計が 0 点未満になることはありません)。 詰まったら **「ヒントを公開する」** を押してください — 各 checkpoint の 1 つ目のヒントは減点なしです。\n\n## ゴール\n\n4 つの checkpoint がすべて正解になったら、 今夜の宿題は終わりです。 ここでやった 「リクエストを送る → ステータスコードと本文を読む → 直してもう一度」 は、 この先の全部の問題で使う基本の動きです。 次の 「初日の 15 分」 では、 同じ板を、 今度はログと設定まで含めて一周します。",
  "description": "## 目的\n\nstackstack-route の章 0。 Web アプリと HTTP で会話した経験がゼロの人 (バイブコーダーを含む) に、 リクエスト → レスポンスの一往復を 3 回、 ブラウザの API コンソールだけで完了させる。 恐怖の除去も目的に含む: 板の投稿と問題文の両方で「コンテナの中だけ・課金なし・壊してよい」を明示する。\n\n## 何を確かめるか\n\n4 つのチェックポイントは、 参加者の申告ではなく **走っているアプリの実測とレスポンス由来の値** で判定する。\n\n| checkpoint | 実測しているもの |\n|---|---|\n| `postcard` | 最初の往復が完了したか (token は `GET /api/postcard` のレスポンスにしか出ない) |\n| `locked-door` | 400 を読んで直せたか (door token は正しい `?key=` を運んだリクエストにしか返らない) |\n| `guestbook` | body 付き POST が通ったか (receipt は 201 のレスポンスにしか出ない) |\n| `round-trip` | 上 3 つが全部緑か (`/posture` が緑になるまで token を出さない) |\n\n## 意図的にやらないこと\n\n- 脆弱性も障害も置かない (それは 2 章以降の仕事)。\n- 設定編集を要求しない (それは `stackstack-onboarding` の仕事)。 出荷する `app.json` は `acceptingPosts: true` で、 好奇心で板に投稿しても 409 に当たらない。\n- クラウドアカウントを要求しない。 ローカル (`make local`) と Codespaces のどちらでも同じ手順で動く。\n\n## 共有ベースアプリ\n\nコンテナ image は StackStack 系問題が共有する `runtimes/stackstack/` を `SCENARIO=first-request` で起動したもの。 postcard / door / guestbook は scenario 固有 route で、 答えはすべて deploy ごとの `FLAG_SEED` からコンテナ内で導出する。",
  "writeup": "## この 10 分でやったこと\n\n3 回とも、 やったことは同じ形をしている。 **リクエスト (メソッド + パス + 必要なら query や body) を送り、 レスポンス (ステータスコード + body) を読む。** ブラウザのアドレスバーにアドレスを打つのも、 API コンソールの実行ボタンも、 中身はこの同じ一往復だ。\n\n- **`GET /api/postcard`** — 読むだけの往復。 欲しい値はレスポンスの中にある。\n- **`GET /api/door`** — 最初の応答は 400 だった。 4xx は「あなたの頼み方が違う」という種類の返事で、 故障 (5xx) とは違う。 まともな API は断るときに直し方 (`detail`) を添える。 `?key=...` という query parameter で情報を運んで、 200 になった。\n- **`POST /api/guestbook`** — 送る側が body で情報を運ぶ往復。 通れば 201 Created、 形が違えば 400 と理由。\n- **`GET /posture`** — アプリ自身による実測。 申告ではなく、 起きたことだけが緑になる。\n\n## ステータスコードの読み方 (最小セット)\n\n| 範囲 | 意味 | この問題での例 |\n|---|---|---|\n| 2xx | できた | 200 (postcard / door)、 201 (guestbook) |\n| 4xx | 頼み方を直せば通る | 400 (key が無い・body の形が違う)、 404 (そのパスは無い) |\n| 5xx | アプリ側の問題 | この問題では起きない |\n\n## 自分のアプリでも同じ\n\nAI に作らせた API が本当は何を返すのか、 公開する前にこうやって 1 リクエストずつ確かめられる。 エラーレスポンスを読んで直す今日の動きは、 そのまま自分のプロダクトのデバッグの最小形だ。\n\n## 次にやること\n\n`stackstack-onboarding` (初日の 15 分)。 同じ板を、 今度はログと設定まで含めて一周し、 「設定を変えただけでは終わりではなく、 挙動で確かめるまでが 1 セット」を身につける。",
  "tags": [
    "stackstack",
    "getting-started",
    "local-play",
    "container",
    "web-app",
    "http",
    "beginner"
  ],
  "exposedPorts": [
    {
      "port": 18080,
      "name": "Web (board)"
    },
    {
      "port": 18081,
      "name": "/verify (loopback)"
    }
  ],
  "learningGoals": [
    "リクエストを送ってレスポンスを読む一往復を、 メソッドとパスを自分で選んで完了できる",
    "エラーレスポンス (400) の本文を読み、 自分のリクエストを直して再送できる",
    "ステータスコード (200 / 201 / 400) を「アプリからの返事の種類」として読み分けられる"
  ],
  "i18n": {
    "en": {
      "name": "Your First Request",
      "shortDescription": "Tomorrow is day one at TenkaCloud. In the evening, a message arrives from the CTO: \"The company message board is yours starting tomorrow. I set up a practice board — poke at it from your browser tonight. Don't worry about breaking it. A rebuild puts it back.\"\n\nTalking to a web app means sending a request and reading the response. That is the whole trick. In the next ten minutes you will make that round trip three times — read, get refused and fix it, write.\n\nEverything runs in containers on your own machine. No cloud account, no credit card, no bill.",
      "instructions": "## Before you start — the world of this series\n\nStarting tomorrow you work at TenkaCloud Inc. Your job: take over the company message board — a web app an AI generated — and keep it running. The previous operator is already gone, and this board will be your companion for a while. This problem is episode one.\n\nEverything runs inside a container on your own machine. No cloud account, no credit card, no bill. Break something and rebuilding the container puts it back. There are no traps.\n\n- **Words** (used in this problem):\n  - **Web app** = a program you use from a browser. Here, a small message board.\n  - **Request** = the \"ask\" a browser sends to an app. **Response** = the reply. Talking to the web is this round trip, over and over.\n  - **Method** = the kind of ask. **GET** means \"show me\", **POST** means \"take this\".\n  - **Path** = a place inside the app, written like `/api/postcard`.\n  - **Status code** = the 3-digit number on a reply. 200s = done (200 = OK, 201 = created). 400s = fix your ask and it will pass. 500s = the app's own failure.\n  - **JSON** = a way of writing data like `{\"name\": \"you\"}` — it looks almost exactly like a Python dict.\n  - **API** = a doorway for programs, called as method + path (e.g. `GET /api/postcard`).\n  - **Container** = a small virtual computer started just for you. Everything in this problem runs inside it.\n  - **Token** = an unguessable string used as a password-like value.\n  - **Checkpoint** = a scored sub-question. This problem has 4.\n\n## Getting started\n\nTomorrow is day one. In the evening, a message arrives from the CTO (the head of engineering): \"The company message board is yours starting tomorrow. I set up a practice board — poke at it from your browser tonight. Don't worry about breaking it. A rebuild puts it back.\"\n\nTonight's homework is three conversations with the board — read, get refused and fix it, write. All of it happens in the browser.\n\n## Your first move\n\nOn this screen (the Participant Portal), press **Start**. Once the container is up, an **Access URLs** list appears on the same screen. Open `Web` and the board shows up. If the page opened, that was in fact your first GET request.\n\nThe board carries two posts from the CTO, spelling out tonight's homework (three APIs). Near the top of the board, follow **Open the API console** to `/docs`. That is where tonight's work happens:\n\n- APIs in the list run from **Try it out** on each entry.\n- This problem's own APIs are not in the list — enter the method and path under **Try any API** and press **Send**. No terminal needed.\n\n## What you answer — 4 checkpoints\n\nEvery answer appears in the response to a request you made yourself. Paste the value into that checkpoint's answer field and press **Submit (+points)**. Values change on every start, so nobody else's answers work.\n\n1. **Read the postcard** (25 pt) — under **Try any API**, run `GET /api/postcard`. The answer is `token` in the JSON that comes back (a string starting with `postcard-`). Read the response's `message` too — it tells you what this token is for.\n2. **Get refused, then fix it** (25 pt) — run `GET /api/door`. It refuses you with a 400 first, but a 400 is not a dead end: it is a reply that carries the fix. Do what its `detail` says and run it again; the 200 reply carries a `token` (`TC{door_...}`). That is the answer.\n3. **Sign the guestbook** (25 pt) — method POST, path `/api/guestbook`, body `{\"name\":\"you\",\"message\":\"hello\"}` (JSON), then send. Success is a 201 carrying `receipt` (`TC{guestbook_...}`) — the answer. A malformed body earns a 400 that tells you what to fix.\n4. **Prove the full lap** (25 pt) — run `GET /posture`. This is the app measuring itself: whether the three conversations above really happened shows up as three **gates** (true/false entries). Once all three are true, `readyToken` (`TC{ready_...}`) appears. That is the answer. While it is still null, the false gates are your remaining homework.\n\nWrong answers can be corrected and resubmitted (each wrong attempt costs that checkpoint's small penalty; the total never drops below 0). If you get stuck, press **Reveal hint** — the first hint on every checkpoint costs nothing.\n\n## Goal\n\nWhen all four checkpoints are correct, tonight's homework is done. The move you practised — send a request, read the status code and body, fix it, ask again — is the basic move of every problem ahead. Next comes \"The First Fifteen Minutes\": the same board, this time including its log and its config.",
      "description": "## Purpose\n\nChapter 0 of the stackstack-route. For someone who has never deliberately spoken HTTP (vibe coders included), complete the request-response round trip three times using only the in-browser API console. Fear removal is an explicit goal: the board's posts and the problem statement both state \"containers only, no bill, breaking things is fine\".\n\n## What it measures\n\nEach of the four checkpoints is judged from the **running app and values that exist only in responses**, never from a claim.\n\n| checkpoint | what is measured |\n|---|---|\n| `postcard` | the first round trip completed (the token appears only in the `GET /api/postcard` response) |\n| `locked-door` | a 400 was read and repaired (the door token is only returned to a request carrying the right `?key=`) |\n| `guestbook` | a POST with a body went through (the receipt appears only in the 201 response) |\n| `round-trip` | all three gates are green (`/posture` withholds the token until then) |\n\n## Deliberate non-goals\n\n- No vulnerability, no injected failure — that starts in chapter 2.\n- No config editing — that is `stackstack-onboarding`'s job. The shipped `app.json` has `acceptingPosts: true`, so a curious post to the board never hits a 409.\n- No cloud account. The same steps work under `make local` and under Codespaces.\n\n## Shared base app\n\nThe image is the StackStack family's shared board app (`runtimes/stackstack/`) started with `SCENARIO=first-request`. postcard / door / guestbook are scenario routes, and every submittable value is derived inside the container from the per-deploy `FLAG_SEED`.",
      "writeup": "## What you did, three times over\n\nAll three conversations had the same shape: **send a request (method + path + query or body when needed), read the response (status code + body).** Typing an address into the browser bar and pressing the console's Send button are the same round trip underneath.\n\n- **`GET /api/postcard`** — a read-only trip. The value you needed was in the response.\n- **`GET /api/door`** — the first answer was a 400. A 4xx means \"your request needs fixing\", which is different from a breakdown (5xx). A decent API attaches the fix (`detail`) when it refuses. You carried the key in a query parameter and got a 200.\n- **`POST /api/guestbook`** — a trip where the sender carries information in the body. Well-formed earns a 201 Created; malformed earns a 400 with the reason.\n- **`GET /posture`** — the app measuring itself. Only what actually happened turns green.\n\n## Status codes, minimum viable set\n\n| range | meaning | here |\n|---|---|---|\n| 2xx | done | 200 (postcard / door), 201 (guestbook) |\n| 4xx | fix the request and it will pass | 400 (missing key, malformed body), 404 (no such path) |\n| 5xx | the app's problem | never in this problem |\n\n## The same trick works on your own app\n\nWhat an AI-built API really returns is something you can check one request at a time, exactly like tonight — before you ship it. Reading the error response and fixing your request is the smallest form of debugging your own product.\n\n## Next\n\n`stackstack-onboarding` (the first fifteen minutes): the same board, now including its log and its config, and the habit of confirming a change by behaviour rather than intent.",
      "learningGoals": [
        "Complete the request-response round trip choosing the method and path yourself, and read the value you need out of the response",
        "Read a 400's body and repair your own request until it succeeds",
        "Tell status codes apart (200 / 201 / 400) as kinds of reply, not as noise"
      ],
      "checks": [
        {
          "id": "postcard",
          "label": "Read the postcard",
          "hints": [
            {
              "id": "postcard-1",
              "content": "In `/docs`, use **Try any API** with method GET and path `/api/postcard`. The value this checkpoint wants is in that response."
            },
            {
              "id": "postcard-2",
              "content": "Submit the response's `token` value — the string starting with `postcard-` — exactly as printed."
            }
          ]
        },
        {
          "id": "locked-door",
          "label": "Get refused, then fix it",
          "hints": [
            {
              "id": "locked-door-1",
              "content": "Run `GET /api/door` as-is first. The 400 that comes back tells you in `detail` exactly what to do next."
            },
            {
              "id": "locked-door-2",
              "content": "Change the path to `/api/door?key=<the postcard token>` and send again. Submit the `token` from the 200 response (`TC{door_...}`)."
            }
          ]
        },
        {
          "id": "guestbook",
          "label": "Sign the guestbook",
          "hints": [
            {
              "id": "guestbook-1",
              "content": "Method POST, path `/api/guestbook`, body `{\"name\":\"you\",\"message\":\"hello\"}` — then Send."
            },
            {
              "id": "guestbook-2",
              "content": "Submit the `receipt` from the 201 response (`TC{guestbook_...}`). If you get a 400, its `detail` explains what to fix."
            }
          ]
        },
        {
          "id": "round-trip",
          "label": "Prove the full lap",
          "hints": [
            {
              "id": "round-trip-1",
              "content": "`GET /posture` reports three gates measured from the running app. Any gate still false is your remaining homework."
            },
            {
              "id": "round-trip-2",
              "content": "Once all three gates are true, `readyToken` (`TC{ready_...}`) appears. Submit it."
            }
          ]
        }
      ]
    }
  },
  "runtime": {
    "provider": "docker",
    "engine": "compose",
    "entry": "local/docker-compose.yml",
    "challengeEndpoints": {
      "Web": "http://127.0.0.1:18080"
    },
    "verifyUrl": "http://127.0.0.1:18081/verify",
    "secretEnv": [
      "FLAG_SEED"
    ]
  },
  "scoring": {
    "kind": "multi-verify",
    "checks": [
      {
        "id": "postcard",
        "label": "葉書を読む",
        "points": 25,
        "wrongAnswerPenalty": 2,
        "hints": [
          {
            "id": "postcard-1",
            "content": "`/docs` の「任意の API を試す」で、 メソッド GET・パス `/api/postcard` を実行してください。 このチェックポイントが求めている値は、 そのレスポンスに入っています。",
            "penalty": 0
          },
          {
            "id": "postcard-2",
            "content": "レスポンス JSON の `token` の値 (`postcard-` で始まる文字列) を、 そのまま提出してください。",
            "penalty": 8
          }
        ]
      },
      {
        "id": "locked-door",
        "label": "断られて、 直す",
        "points": 25,
        "wrongAnswerPenalty": 2,
        "hints": [
          {
            "id": "locked-door-1",
            "content": "まず `GET /api/door` をそのまま実行してください。 400 が返りますが、 その `detail` に次にやることがそのまま書いてあります。",
            "penalty": 0
          },
          {
            "id": "locked-door-2",
            "content": "パスを `/api/door?key=<葉書の token>` に変えてもう一度実行します。 200 のレスポンスの `token` (`TC{door_...}`) を提出してください。",
            "penalty": 8
          }
        ]
      },
      {
        "id": "guestbook",
        "label": "芳名帳に書く",
        "points": 25,
        "wrongAnswerPenalty": 1,
        "hints": [
          {
            "id": "guestbook-1",
            "content": "メソッド POST・パス `/api/guestbook`、 body に `{\"name\":\"あなたの名前\",\"message\":\"ひとこと\"}` を入れて実行してください。",
            "penalty": 0
          },
          {
            "id": "guestbook-2",
            "content": "201 のレスポンスの `receipt` (`TC{guestbook_...}`) を提出してください。 400 が返るときは、 `detail` が直すところを教えてくれます。",
            "penalty": 8
          }
        ]
      },
      {
        "id": "round-trip",
        "label": "一周の証明",
        "points": 25,
        "wrongAnswerPenalty": 0,
        "hints": [
          {
            "id": "round-trip-1",
            "content": "`GET /posture` は走っているアプリから実測した 3 つの gate を返します。 まだ false の gate が、 そのまま残りの宿題です。",
            "penalty": 0
          },
          {
            "id": "round-trip-2",
            "content": "3 つの gate が全部 true になると `readyToken` (`TC{ready_...}`) が現れます。 それを提出してください。",
            "penalty": 8
          }
        ]
      }
    ]
  },
  "track": {
    "id": "stackstack-route",
    "order": 2,
    "chapter": "0. 安全に始める"
  }
}
