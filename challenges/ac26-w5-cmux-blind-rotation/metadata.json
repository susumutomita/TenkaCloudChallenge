{
  "id": "ac26-w5-cmux-blind-rotation",
  "name": "誰も知らない角度で回す",
  "category": "Challenge",
  "status": "draft",
  "visibility": "public",
  "track": {
    "id": "advanced-cryptography-2026",
    "order": 540,
    "chapter": "Week 5 / CMUX and Blind Rotation"
  },
  "courseAlignment": {
    "courseId": "advanced-cryptography-program",
    "edition": "2026",
    "week": 5,
    "role": "mechanism",
    "spoilerPolicy": "independent-reimplementation",
    "sources": [
      {
        "repository": "zk-tokyo/advanced-cryptography-2026",
        "ref": "c088f8e6f301dedcd80b6dd9c321a1cd83410637",
        "path": "week5/README.md",
        "kind": "lecture"
      },
      {
        "repository": "zk-tokyo/advanced-cryptography-2026",
        "ref": "c088f8e6f301dedcd80b6dd9c321a1cd83410637",
        "path": "week5/problems/tfhe-toy-python/README.md",
        "kind": "assignment"
      }
    ]
  },
  "difficulty": 4,
  "estimatedDuration": "75-105 分",
  "shortDescription": "暗号化されたビットで 2 つの暗号文から一方を選び、 それを繰り返して暗号化された量だけ多項式を回す。 回転量はループの中の誰も知らない。",
  "description": "Week 5 の 4 問目。 CMUX と単項式回転を実装し、 その 2 つを鎖にして blind rotation を完成させる。\n\n環・RLWE・RGSW・external product は支給される。 それらは ac26-w5-lwe-rlwe と ac26-w5-rgsw-external の成果物で、 この問題は選択そのものと、 その選択が積み上がる先。\n\nCMUX は `ct0 + ExternalProduct(c, ct1 - ct0)` の 1 行。 selector 0 なら ct0、 1 なら ct1 の平文になり、 分岐は存在しない。 両方の候補を毎回計算するのは無駄ではなく機構そのもので、 片方だけ計算するにはどちらが要るかを知る必要がある。\n\n平文 modulus は 4 にしてある。 これは判定を担う。 negacyclic な回転は degree N を跨ぐたびに係数を反転させるが、 平文 modulus が 2 だと `-delta == delta (mod q)` で符号反転が**見えない**。 `X^N = -1` を完全に無視した実装が満点を取れてしまう。 4 なら反転は `m -> (-m) mod 4` として平文に出る。\n\n回転の指数は 2N を法として正規化する。 `X^(2N) = 1` だから N ではなく 2N であり、 N で割った余りを取ると失われるのはちょうど符号。 それが circular shift との差。\n\nblind rotation は LWE sample `(mask, body)` を Z_(2N) の上で受け取り、 `phase = body - <mask, secret>` の指数へ辿り着く。 secret は渡らない。 body は公開値なので最初の offset 回転に CMUX は要らず、 残りは bootstrapping key の各行が担う。\n\n採点の設計として、 回転方向をすべての場所で一貫して逆にした実装は自分自身とは完全に整合し、 ループを自分の部品と突き合わせても通る。 だから blind rotation は phase から平文で計算した別モデルと比較する。 そのモデルは submission の関数を 1 つも呼ばない。\n\nこれは安全ではない。 パラメータは全列挙でき、 secret は線形代数で復元できる。",
  "instructions": "## 前提 — 中学の数学から\n\n- **学校で習ったこと**: 場合分けを式 1 本で書く方法。b が 0 か 1 のとき、**c₀ + b·(c₁ − c₀)** は b = 0 なら c₀、b = 1 なら c₁ になります — if 文なしで選択ができる。もう 1 つは、多項式に X を掛けると係数が 1 つずつ上の次数へずれること。X⁴ のようにはみ出した係数は、X^N = −1 の約束で**符号を変えて先頭に戻ってきます**。これが「回転」です。\n- **数の世界**: 係数はすべて「q で割ったあまり」で考えます。0 から q − 1 までの q 個しかなく、q まで行くと 0 に戻ります（「q を法とする」「mod q」。法 = 割る数）。マイナスになったら 0 以上になるまで q を足します。Python の `%` は割る数が正なら必ず 0 以上を返すので、`(-1) % 8` はそのまま 7 です。**回す量（指数）だけは、q ではなく 2N で割ったあまりで数えます** — 1 周 N で符号が裏返り、2 周で元に戻るからです（X^N = −1、X^(2N) = +1）。だから `X^-1` は `X^(2N-1)` と同じで、マイナスの指数も断らずに直します。\n- **1 桁の例**: 選択のほうは c₀ = 3、c₁ = 5 で、b = 0 → 3 + 0·(5 − 3) = 3、b = 1 → 3 + 1·(5 − 3) = 5。回転のほうは N = 4 で、X × (2 + 3X + 5X² + 7X³) = 2X + 3X² + 5X³ + 7X⁴ となり、7X⁴ が X⁴ = −1 で **−7** になって先頭へ戻るので、答えは **(−7, 2, 3, 5)** = −7 + 2X + 3X² + 5X³ です。\n- **言葉**: 多項式 = 係数を次数の低い順に並べたもの。コードでは長さ N の整数の並びです。環 = 足し算・引き算・掛け算がその中で完結する数の世界（ここでは上の折り返し規則を持つ多項式の世界）。RLWE 暗号文 = マスク `a` と本体 `b` の 2 つの多項式の組で、`{\"a\": …, \"b\": …}` の dict で表します。位相 = b − a·s で、中身は「メッセージの持ち上げ + ノイズ」だけ。RGSW = 暗号化された 1 ビットを、重みのぶんだけ並べた暗号文の束（前の問題で作ったもの）。external product = その RGSW と暗号文を掛ける操作で、`RLWE(0) + ビット × 暗号文` を返します。CMUX = 暗号化されたビットで 2 つの暗号文から一方を選ぶ仕掛け。単項式 X^k = 「k 個ずらす」という掛け算。blind rotation = 回す量そのものが暗号化されていて、**回している側も何回転させたのか知らない**まま多項式を回すこと。bootstrapping key = 秘密鍵の 1 ビットずつを RGSW で暗号化して並べたもの。digest = 暗号文を短い文字列にまとめた指紋で、`participant.ring` が用意してくれます。checkpoint = 採点される小問で、この問題には 8 つあります。\n- **この問題の位置づけ**: Week 5 の 4 問目。環・RLWE・RGSW・external product は前の 3 問で作ったものが `participant.ring` から**完成した形で渡されます** — 作り直しません。ここで書くのは CMUX と回転とループです。\n\n## はじめに\n\nここまでで、暗号文のままの「もし〜なら」（external product）が手に入りました。この問題では、それを 1 本の式にまとめて**選択の部品**にし、その部品を秘密鍵のビットの数だけ並べて、**誰も知らない量だけ多項式を回す**装置を組み上げます。\n\n選択の部品はこれだけです — `CMUX(暗号化ビット, ct₀, ct₁) = ct₀ + 暗号化ビット ⊠ (ct₁ − ct₀)`。前提の c₀ + b·(c₁ − c₀) と同じ式を、暗号文の上でやっているだけです。\n\n回す量は、ある LWE の位相 `body − マスクと秘密鍵の内積` です。**`body` は公開されていますが、秘密鍵は暗号文の中にしかありません。** だからループは位相を計算できませんし、してはいけません。それでも結果は `X^(−位相) × もとの多項式` にならなければならない — 「知らないまま正しく回す」をどう組むかが、この問題です。\n\nメッセージの取りうる値は 4 通り（0, 1, 2, 3）に固定してあります。2 通りだと、折り返しで符号が裏返っても同じ値に見えてしまい、符号を無視した実装が正しく見えてしまうからです。\n\n## 最初の一手\n\nParticipant Portal（この問題文が表示されているサイト）で **「起動」** を押すと、問題文と同じ画面に **問題エディタ** が出ます。ファイルは `cmux.py` ひとつだけで、書きかけの関数を埋めるだけです。**「初期状態に戻す」** でいつでも元に戻せます。\n\n**「証拠を確認」** を押すと、この起動のためだけに作られた数値が並びます。\n\n- パラメータ: base B、桁数 L、法 q（= B^L）、多項式の長さ N、LWE の長さ、メッセージの通り数と持ち上げ幅、ノイズの上限と予算。\n- **1 周まわす表**: `(1, 0, …)` に X⁰, X¹, X², … を掛けた結果を並べたもの。どこで符号が裏返り、どこで元に戻るかが目で見えます。`X^-1` がどの指数と同じになるかも出ます。\n- **CMUX の実演**: 分岐 0 が運ぶ値、分岐 1 が運ぶ値、selector 0 と 1 それぞれの出力の digest、その復号結果、そして**出力が ct₀ とも ct₁ とも一致しない**という行。\n- **blind rotation を 1 段ずつ**: 段 / 使った係数 / 指数 / どのビットで選んだか / 候補 0 の digest / 候補 1 の digest / 出力の digest。段 0 は公開の `body` によるずらしなので、そこだけ候補が 2 つとも同じ暗号文になります。\n- 最後に出た暗号文の復号結果、平文だけで計算した答え、そして作問者だけが計算できる位相の値。\n\n**先に見るのはこの 2 か所です。** ひとつは 1 周まわす表 — X を何回掛けたら符号が裏返り、何回で戻るのか。ここで「指数を割る数は N ではない」が腑に落ちます。もうひとつは CMUX の実演の最後の行 — **出力がどちらの入力とも一致しない**のはなぜか。ここが「平文で分岐していない」ことの外から見える証拠です。\n\n数値は起動のたびに変わり、採点は画面に出ていない別のパラメータでも同じコードを走らせます。**「公開テストを実行」** で形の検査を手元で先に試せます。\n\n## 答えるもの — checkpoint は 8 つ\n\n8 つともコードで答えます（文字を打ち込む回答欄はありません）。`params` は `base`、`levels`、`degree`（N）、`dimension`、`modulus`（q）、`plaintext_modulus`、`delta` を持つ dict です。\n\n1. **combine** — `rlwe_add(params, left, right)` と `rlwe_sub(params, left, right)`: 暗号文 2 つを受け取り、`{\"a\": …, \"b\": …}` を返します。**触るのは片側だけではありません** — 引き算は順番が入れ替わらないので、どちらから引くかも見られます。\n2. **cmux** — `cmux(params, rgsw, ct0, ct1)`: 上の 1 本の式そのままです。`external_product(params, rgsw, ciphertext)` は `participant.ring` から渡され、`RLWE(0) + ビット × 暗号文` を返します。これを式に代入して、ビットが 0 のときと 1 のときに何が出るかを紙で書き下してから実装してください。**秘密鍵は渡されませんし、要りません。**\n3. **constant** — 2 と同じ `cmux` を、別の角度から見ます。選択が計算で行われた（`if` で選ばれたのではない）ことの検査で、**両方の候補が計算されていること、鍵の全部の行が読まれていること、出力が ct₀ とも ct₁ とも一致しないこと**を見ます。入力のどちらかをそのまま返す実装は、外から見ると平文の分岐に見えます。\n4. **rotate** — `monomial_rotate(params, poly, exponent)`: 長さ N の多項式と整数の指数を受け取り、`X^exponent` を掛けた長さ N の並びを返します。ただの巡回シフトではありません（はみ出しは符号が裏返る）。指数は 2N で割ったあまりへ直し、マイナスも受け付けます。`normalize(params, coefficients)` が `participant.ring` から渡され、どんな長さの係数列にも折り返し規則を適用してくれます。`rotate_ciphertext(params, ciphertext, exponent)`: 暗号文を回して、**回った後のメッセージの暗号文**を返します。位相が一緒に回るには両側に何が要るかを考えてください。\n5. **conditional** — `conditional_rotate(params, rgsw, ciphertext, exponent)`: 暗号化されたビットが 1 なら回し、0 ならそのまま保ちます。**候補は必ず両方計算します** — 片方だけ計算するには、どちらが要るかを知っている必要があるからです。\n6. **blind** — `blind_rotate(params, key, sample, accumulator)`: `sample[\"mask\"]` は 2N の世界の係数の並び、`sample[\"body\"]` は**公開**、`key[i]` は秘密鍵の i ビット目の RGSW です。結果は `X^(−位相) × accumulator` でなければならず、位相はここでは計算できません。**公開されているものから始めてください。**\n7. **trace** — `blind_rotate_trace(params, key, sample, accumulator)`: **`dimension` + 1 件**の記録を返します。各件は `step`（0 が公開のずらし、以降 1 … dimension）、`mask`（その段が消費した係数。段 0 は `body`、以降は `mask[i-1]`）、`exponent`（それを 2N の範囲へ直した値。段 0 では符号を反転）、`selector`（段 0 は `\"phase-offset\"`、以降は `\"bk[0]\"`, `\"bk[1]\"`, …）、`candidate0` / `candidate1` / `output`（それぞれ digest）。digest は `participant.ring` の `digest(params, ciphertext)` を使うので、形式を推測する必要はありません。**最後の記録の `output` が、6 の結果の digest と一致すること。**\n8. **transfer** — 同じ `cmux.py` が、**画面に出ていないパラメータ**で 1 から 7 まで全部走ります。\n\n## 提出のしかた\n\ncheckpoint ごとに **「提出 (+点数)」** ボタンがあります。押すと、そのときのエディタの中身がそのまま採点へ送られます。8 つは独立に採点されるので、1 つ詰まっても他は取れます。不正解でも出し直せます（1 回ごとに決まった減点があり、合計スコアが 0 未満になることはありません）。正解した checkpoint は「解答済み」になり、それ以上は提出しません。\n\n## ゴール\n\n暗号文の足し引きが両側そろい、1 本の式で選択ができ、その選択が外から見ても平文の分岐でないと分かり、回転が符号の裏返りごと正しく、条件つきの回転が両候補を計算し、そして誰も位相を計算しないまま `X^(−位相) × accumulator` に着地する — 8 つの checkpoint がすべて通れば終わりです。ループの中に、秘密鍵の平文は一度も現れません。\n",
  "writeup": "## 両方計算することが機構\n\nCMUX は `ct0 + ExternalProduct(c, ct1 - ct0)`。 external product が `RLWE(0) + mu * (ct1 - ct0)` を返すので、 和は mu が 0 なら ct0、 1 なら ct1 になる。 どちらの場合も fresh noise が乗るため出力は新しい暗号文であり、 入力のどちらでもない。\n\n必要な候補だけ計算する実装は、 どちらが必要かを知っていなければ書けない。 両方計算するのは無駄ではなく、 暗号化されたまま選べる理由そのもの。\n\n## 平文 modulus 4 は判定を担う\n\nnegacyclic な回転は degree を跨ぐたびに係数を反転する。 平文 modulus が 2 だと `-delta` と `delta` が mod q で同じ値なので、 この反転は平文に出ない。 `X^N = -1` を無視して circular shift を書いた実装が全部通ってしまう。 4 にすると反転は `m -> (-m) mod 4` として現れ、 1 と 3 が動く。\n\n## 2N であって N ではない\n\n`X^(2N) = 1` なので指数の法は 2N。 N で割った余りを取ると、 落ちるのはちょうど「何回巻いたか」の偶奇 — つまり符号。 circular shift との差は全部そこにある。\n\n## 一貫して逆な実装は自分では捕まらない\n\n`monomial_rotate` の方向とループの方向を両方逆にすると、 ループを自分の `conditional_rotate` と突き合わせるテストは通る。 public test の最後の 1 つがまさにそれで、 何も証明していない。\n\nだから hidden test は phase から平文で回転を計算した別モデルと比較する。 そのモデルは submission の関数を 1 つも呼ばない。\n\n## 候補が一致する退化ケース\n\nmask の係数が 0 のとき 2 つの候補は同じ暗号文になり、 差はゼロ、 その桁もゼロ、 external product はゼロ暗号文そのものになる。 出力は候補と bit 単位で一致する。 これは平文分岐ではなく、 分岐する対象が最初から無かったということ。 mask の係数は Z_(2N) から引くので、 これは 2N 回に 1 回起きる。\n\n## body は公開値\n\nsecret は LWE の秘密ビットだけで、 body は違う。 だから最初の offset 回転に CMUX は要らない。 trace の step 0 が `phase-offset` なのはそれを示すためで、 公開部分と暗号化された部分の境目がそこにある。\n\n## 対象外\n\nsample extraction と key switching (次の問題)、 programmable bootstrapping と HomNAND (その次)、 modulus switching、 constant-time 保証、 最適化された blind rotation。\n\n## これは安全ではない\n\nパラメータは全列挙でき、 secret は線形代数で復元できる。 機構の toy であって困難性の toy ではない。",
  "tags": [
    "advanced-cryptography-2026",
    "week5",
    "fhe",
    "tfhe",
    "cmux",
    "blind-rotation",
    "local-play",
    "container"
  ],
  "exposedPorts": [
    {
      "port": 18110,
      "name": "Portal editor API + /verify (loopback only)"
    }
  ],
  "learningGoals": [
    "CMUX を external product の 1 行として実装できる",
    "selector 0 / 1 で選ばれる分岐が切り替わることを確認できる",
    "単項式の積が negacyclic な回転になることを説明できる",
    "回転の指数を 2N を法として正規化できる",
    "暗号化されたビットで回転するか保持するかを選べる",
    "LWE mask を使った conditional rotation を積み重ねられる",
    "blind rotation の結果を平文の参照モデルと突き合わせられる",
    "toy 実装と production の blind rotation の差を明記できる"
  ],
  "runtime": {
    "provider": "docker",
    "engine": "compose",
    "entry": "local/docker-compose.yml",
    "verifyUrl": "http://127.0.0.1:18110/verify",
    "secretEnv": [
      "FLAG_SEED"
    ]
  },
  "scoring": {
    "kind": "multi-verify",
    "checks": [
      {
        "id": "combine",
        "input": "multiline",
        "label": "暗号文を足し引きする",
        "points": 25,
        "wrongAnswerPenalty": 15,
        "hints": [
          {
            "id": "combine-hint-1",
            "content": "a と b の両方。 RLWE が加法準同型なのは mask も一緒に足されるからで、 b だけ足した暗号文は noise に復号される。",
            "penalty": 12
          }
        ]
      },
      {
        "id": "cmux",
        "input": "multiline",
        "label": "暗号化されたビットで選ぶ",
        "points": 45,
        "wrongAnswerPenalty": 15,
        "hints": [
          {
            "id": "cmux-hint-1",
            "content": "external_product は `RLWE(0) + mu * ciphertext` を返す。 `ct0 + ExternalProduct(c, ct1 - ct0)` に代入して、 mu = 0 と mu = 1 で何が出るかを書き下す。",
            "penalty": 22
          }
        ]
      },
      {
        "id": "constant",
        "input": "multiline",
        "label": "平文で分岐していないことを示す",
        "points": 35,
        "wrongAnswerPenalty": 15,
        "hints": [
          {
            "id": "constant-hint-1",
            "content": "出力が ct0 でも ct1 でもないことが条件。 external product は両方の経路で fresh noise を足すので、 正しい CMUX は必ず新しい暗号文を返す。",
            "penalty": 17
          }
        ]
      },
      {
        "id": "rotate",
        "input": "multiline",
        "label": "単項式で回す",
        "points": 40,
        "wrongAnswerPenalty": 15,
        "hints": [
          {
            "id": "rotate-hint-1",
            "content": "指数の法は 2N。 N で割ると落ちるのはちょうど符号。 `normalize` は任意長の係数列に negacyclic 則を適用するので、 右に shift して渡せば済む。",
            "penalty": 20
          }
        ]
      },
      {
        "id": "conditional",
        "input": "multiline",
        "label": "回すか保つかを選ぶ",
        "points": 35,
        "wrongAnswerPenalty": 15,
        "hints": [
          {
            "id": "conditional-hint-1",
            "content": "候補 0 が「保つ」、 候補 1 が「回す」。 順序を取り違えると 0 と 1 の意味が入れ替わり、 両方通らない。",
            "penalty": 17
          }
        ]
      },
      {
        "id": "blind",
        "input": "multiline",
        "label": "誰も知らない量だけ回す",
        "points": 55,
        "wrongAnswerPenalty": 15,
        "hints": [
          {
            "id": "blind-hint-1",
            "content": "公開されているものから始める。 body は秘密ではないので offset 回転に CMUX は要らない。 残りは各 mask 係数を鍵の対応する行で条件付きに掛けるだけ。",
            "penalty": 27
          }
        ]
      },
      {
        "id": "trace",
        "input": "multiline",
        "label": "積み上がりを見せる",
        "points": 35,
        "wrongAnswerPenalty": 15,
        "hints": [
          {
            "id": "trace-hint-1",
            "content": "記録は dimension + 1 個。 step 0 は公開 offset で、 候補が 2 つとも同じ暗号文になる唯一の step。 `mask` は渡された値、 `exponent` はそれを正規化した値。",
            "penalty": 17
          }
        ]
      },
      {
        "id": "transfer",
        "input": "multiline",
        "label": "見たことのない設定で成立させる",
        "points": 30,
        "wrongAnswerPenalty": 15
      }
    ]
  },
  "i18n": {
    "en": {
      "name": "Turn it by an angle nobody knows",
      "shortDescription": "Pick one of two ciphertexts with an encrypted bit, then chain that into rotating a polynomial by an encrypted amount. Nothing inside the loop knows the angle.",
      "description": "The fourth Week 5 problem. Build CMUX and monomial rotation, then chain the two into blind rotation.\n\nThe ring, RLWE, RGSW and the external product are supplied -- they are ac26-w5-lwe-rlwe's and ac26-w5-rgsw-external's output, and this problem is the selection itself and what it accumulates into.\n\nCMUX is one line: `ct0 + ExternalProduct(c, ct1 - ct0)`. Selector 0 gives ct0's plaintext, selector 1 gives ct1's, and there is no branch. Computing both candidates every time is not waste; it is the mechanism, because computing only the one you need requires knowing which one that is.\n\nThe plaintext modulus is 4, and that is load-bearing. Negacyclic rotation negates a coefficient every time it wraps past degree N, and modulo 2 that flip is **invisible** -- `-delta == delta (mod q)` -- so an implementation that ignored `X^N = -1` entirely would score full marks. Modulo 4 the flip shows up in the plaintext as `m -> (-m) mod 4`.\n\nRotation exponents are normalized modulo 2N. `X^(2N) = 1` is why it is 2N rather than N, and reducing modulo N instead loses exactly the sign -- which is the whole difference from a circular shift.\n\nBlind rotation takes an LWE sample `(mask, body)` over `Z_(2N)` and lands on the exponent `phase = body - <mask, secret>`. No secret is passed. `body` is public, so the opening offset rotation needs no CMUX; the rest is carried by the rows of the bootstrapping key.\n\nOn scoring: an implementation whose rotation direction is reversed everywhere is perfectly consistent with itself and passes any test that compares the loop against its own parts. So the blind rotation is compared against a separate model computed in the clear from the phase, which calls none of the submission's functions.\n\nNone of this is secure. The parameters are small enough to enumerate and the secret falls to linear algebra.",
      "instructions": "## Before you start — from school math\n\n- **What you already know**: writing a case split as a single expression. When b is 0 or 1, **c₀ + b·(c₁ − c₀)** is c₀ for b = 0 and c₁ for b = 1 — a selection with no `if`. The other piece: multiplying a polynomial by X shifts every coefficient one degree up, and a coefficient pushed out past the top comes back **with its sign flipped**, because the rule here is X^N = −1. That is what \"rotation\" means below.\n- **The number world**: every coefficient lives in \"remainders after dividing by q\". There are only the q values 0 … q − 1, and reaching q brings you back to 0 (read \"modulo q\"; the modulus is the number you divide by). If a result is negative, add q until it is 0 or more; Python's `%` always returns a non-negative result for a positive modulus, so `(-1) % 8` is already 7. **The rotation amount (the exponent) is counted modulo 2N, not q** — one turn of N flips the sign and two turns restore it (X^N = −1, X^(2N) = +1). So `X^-1` is the same as `X^(2N-1)`, and a negative exponent is normalized rather than rejected.\n- **A one-digit example**: for the selection, c₀ = 3 and c₁ = 5 give b = 0 → 3 + 0·(5 − 3) = 3 and b = 1 → 3 + 1·(5 − 3) = 5. For the rotation, with N = 4, X × (2 + 3X + 5X² + 7X³) = 2X + 3X² + 5X³ + 7X⁴, and 7X⁴ becomes **−7** by X⁴ = −1 and lands at the front: **(−7, 2, 3, 5)** = −7 + 2X + 3X² + 5X³.\n- **Words**: polynomial = its coefficients written lowest degree first; in code, a row of N integers. ring = a number world closed under addition, subtraction and multiplication (here, the polynomial world with the folding rule above). RLWE ciphertext = a pair of polynomials, a mask `a` and a body `b`, carried as the dict `{\"a\": …, \"b\": …}`. phase = b − a·s, holding only \"the lifted message plus noise\". RGSW = an encrypted single bit spread across a bundle of ciphertexts, one per weight (built in the previous problem). external product = multiplying that RGSW into a ciphertext; it returns `RLWE(0) + bit × ciphertext`. CMUX = picking one of two ciphertexts using an encrypted bit. monomial X^k = the multiplication that \"shifts by k\". blind rotation = rotating a polynomial by an amount that is itself encrypted, so that **the code doing the rotating does not know how far it turned**. bootstrapping key = the secret key's bits, each encrypted as an RGSW. digest = a short fingerprint of a ciphertext, supplied by `participant.ring`. checkpoint = one small graded question; this problem has eight.\n- **Where this problem sits**: the fourth problem of Week 5. The ring, RLWE, RGSW and the external product **arrive finished and correct** from `participant.ring` — you do not rebuild them. What you write here is CMUX, rotation, and the loop.\n\n## The idea\n\nYou now have an \"if\" that runs on ciphertexts (the external product). This problem folds it into a single expression to make a **selection component**, then stacks that component once per secret-key bit to build a machine that **rotates a polynomial by an amount nobody knows**.\n\nThe component is just this: `CMUX(encrypted bit, ct₀, ct₁) = ct₀ + encrypted bit ⊠ (ct₁ − ct₀)`. It is the premise's c₀ + b·(c₁ − c₀), performed on ciphertexts.\n\nThe rotation amount is an LWE phase, `body − (inner product of mask and secret)`. **`body` is public, but the secret exists here only inside ciphertexts.** So the loop cannot compute the phase, and must not. The result still has to be `X^(−phase) × the original polynomial` — building \"turn it correctly without knowing how far\" is this problem.\n\nMessages are fixed to four values (0, 1, 2, 3). With two, a sign flip from the fold would land on the same value, and an implementation that ignored the sign entirely would look correct.\n\n## First move\n\nIn Participant Portal (the site showing this statement), press **起動 (Start)**; the **problem editor** appears on the same page. It holds one file, `cmux.py`, already scaffolded — you fill in the existing functions. **Restore starter** puts it back at any time.\n\nPress **Inspect evidence** and this run's numbers appear.\n\n- The parameters: base B, the digit count L, the modulus q (= B^L), the polynomial length N, the LWE length, the number of message values and the lift, and the noise bound and budget.\n- **A full turn, tabulated**: `(1, 0, …)` multiplied by X⁰, X¹, X², … in a row, so you can see where the sign flips and where it comes back. It also shows which exponent `X^-1` equals.\n- **A CMUX worked out**: what branch 0 carries, what branch 1 carries, the digest of the output for selectors 0 and 1, what each decrypts to, and the line saying **the output equals neither ct₀ nor ct₁**.\n- **The blind rotation step by step**: step / the coefficient consumed / the exponent / which bit chose / candidate 0's digest / candidate 1's digest / the output's digest. Step 0 is the shift by the public `body`, and it is the one step where both candidates are the same ciphertext.\n- What the final ciphertext decrypts to, the answer computed in the clear, and the phase value only the author can compute.\n\n**Two places are worth looking at first.** One is the full-turn table — how many multiplications by X flip the sign, and how many bring it back. That is where \"the exponent's modulus is not N\" lands. The other is the last line of the CMUX demonstration: **why does the output match neither input?** That is the externally visible evidence that nothing branched in the clear.\n\nThe numbers change every run, and grading re-runs your code on parameters that were never shown. **Run public tests** checks the shapes locally first.\n\n## What you answer — eight checkpoints\n\nAll eight are answered with code; there is no field to type a value into. `params` is a dict carrying `base`, `levels`, `degree` (N), `dimension`, `modulus` (q), `plaintext_modulus` and `delta`.\n\n1. **combine** — `rlwe_add(params, left, right)` and `rlwe_sub(params, left, right)`: take two ciphertexts, return `{\"a\": …, \"b\": …}`. **Only touching one half is not enough**, and subtraction does not commute, so which way round it goes is graded too.\n2. **cmux** — `cmux(params, rgsw, ct0, ct1)`: the single expression above, exactly. `external_product(params, rgsw, ciphertext)` is supplied by `participant.ring` and returns `RLWE(0) + bit × ciphertext`. Substitute it into the expression and write out on paper what comes out for bit 0 and for bit 1 before implementing. **No secret is passed in, and none is needed.**\n3. **constant** — the same `cmux` seen from another angle. This audits that the selection was performed arithmetically rather than chosen with an `if`: it checks that **both candidates were computed, that every row of the key was read, and that the output equals neither ct₀ nor ct₁**. An implementation that returns one of its inputs unchanged is what a plaintext branch looks like from outside.\n4. **rotate** — `monomial_rotate(params, poly, exponent)`: takes a polynomial of length N and an integer exponent, returns `X^exponent` times it as a row of length N. Not a circular shift (what wraps comes back negated). The exponent is normalized modulo 2N and negatives are accepted. `normalize(params, coefficients)` is supplied by `participant.ring` and already applies the folding rule to a coefficient row of any length. `rotate_ciphertext(params, ciphertext, exponent)`: rotate a ciphertext so it becomes **an encryption of the rotated message**. Think about what both halves need for the phase to rotate with them.\n5. **conditional** — `conditional_rotate(params, rgsw, ciphertext, exponent)`: rotate when the encrypted bit is 1, hold when it is 0. **Both candidates are always computed** — computing only the one you need would require knowing which one that is.\n6. **blind** — `blind_rotate(params, key, sample, accumulator)`: `sample[\"mask\"]` is a row of coefficients over the 2N world, `sample[\"body\"]` is **public**, and `key[i]` is the RGSW of the secret's i-th bit. The result must be `X^(−phase) × accumulator`, and the phase is not computable here. **Start from what is public.**\n7. **trace** — `blind_rotate_trace(params, key, sample, accumulator)`: returns **`dimension` + 1 records**. Each carries `step` (0 for the public shift, then 1 … dimension), `mask` (the coefficient that step consumed: `body` at step 0, then `mask[i-1]`), `exponent` (that coefficient normalized into the 2N range, negated at step 0), `selector` (`\"phase-offset\"` at step 0, then `\"bk[0]\"`, `\"bk[1]\"`, …), and `candidate0` / `candidate1` / `output` as digests. Use `participant.ring`'s `digest(params, ciphertext)` so the format is not yours to guess. **The last record's `output` must equal the digest of checkpoint 6's result.**\n8. **transfer** — the same `cmux.py` runs checkpoints 1 to 7 again on **parameters that were never shown**.\n\n## How submitting works\n\nEach checkpoint has its own **Submit (+points)** button; pressing it sends the editor's current contents straight to grading. The eight are graded independently, so one that will not budge does not block the rest. A wrong answer can be resubmitted (each wrong attempt carries a fixed penalty, and the total never drops below 0 pt). A solved checkpoint is marked solved and is not submitted again.\n\n## Goal\n\nCiphertext addition and subtraction touch both halves; a single expression performs the selection; that selection is visibly not a plaintext branch from outside; rotation carries the sign flip correctly; the conditional rotation computes both candidates; and the loop lands on `X^(−phase) × accumulator` without anyone computing the phase. All eight checkpoints green means done — and the secret key never appears in the clear anywhere inside the loop.\n",
      "writeup": "## Computing both is the mechanism\n\nCMUX is `ct0 + ExternalProduct(c, ct1 - ct0)`. The external product returns `RLWE(0) + mu * (ct1 - ct0)`, so the sum is ct0 when mu is 0 and ct1 when mu is 1. Fresh noise lands on both paths, so the output is a new ciphertext either way and never one of the inputs.\n\nAn implementation that computed only the candidate it needs cannot be written without knowing which one that is. Computing both is not waste -- it is the reason an encrypted bit can steer anything.\n\n## A plaintext modulus of 4 is what makes the sign visible\n\nNegacyclic rotation negates a coefficient every time it wraps past the degree. Modulo 2, `-delta` and `delta` are the same value, so that flip never reaches the plaintext and an implementation that wrote an ordinary circular shift passes everything. Modulo 4 it appears as `m -> (-m) mod 4`, which moves 1 and 3.\n\n## 2N, not N\n\n`X^(2N) = 1`, so exponents reduce modulo 2N. Reducing modulo N instead drops the parity of how many times the coefficient wrapped -- that is, the sign. The entire difference between this and a circular shift lives there.\n\n## A consistently reversed implementation cannot catch itself\n\nReverse the direction in `monomial_rotate` and in the loop together, and a test that compares the loop against your own `conditional_rotate` still passes. The last public test is exactly that test, and it proves nothing.\n\nSo the hidden tests compare against a model that computes the rotation in the clear from the phase and calls none of your functions.\n\n## The degenerate case where the candidates coincide\n\nWhen a mask coefficient is zero the two candidates are the same ciphertext: the difference is zero, its digits are zero, and the external product is exactly the zero ciphertext. The output matches the candidate bit for bit. That is not a plaintext branch -- there was nothing to branch on. Mask coefficients are drawn from `Z_(2N)`, so this happens one time in 2N, not rarely.\n\n## body is public\n\nThe secret is the LWE bits; the body is not. That is why the opening offset rotation needs no CMUX, and why step 0 of the trace is labelled `phase-offset` -- it marks where the public part of the phase ends and the encrypted part begins.\n\n## Not in scope\n\nSample extraction and key switching (the next problem), programmable bootstrapping and HomNAND (the one after), modulus switching, constant-time guarantees, optimized blind rotation.\n\n## This is not secure\n\nThe parameters are small enough to enumerate and the secret falls to linear algebra. A toy of the mechanism, not of the hardness.",
      "learningGoals": [
        "Implement CMUX as one line on top of the external product",
        "Confirm that selector 0 and 1 switch which branch is selected",
        "Explain why multiplying by a monomial is a negacyclic rotation",
        "Normalize a rotation exponent modulo 2N",
        "Rotate or hold a ciphertext according to an encrypted bit",
        "Chain conditional rotations driven by an LWE mask",
        "Check a blind rotation against a plaintext reference model",
        "State plainly how the toy differs from a production blind rotation"
      ],
      "checks": [
        {
          "id": "combine",
          "label": "Add and subtract ciphertexts",
          "hints": [
            {
              "id": "combine-hint-1",
              "content": "Both halves. RLWE is additively homomorphic because the masks are added too, and a ciphertext with only `b` summed decrypts to noise."
            }
          ]
        },
        {
          "id": "cmux",
          "label": "Select with an encrypted bit",
          "hints": [
            {
              "id": "cmux-hint-1",
              "content": "`external_product` returns `RLWE(0) + mu * ciphertext`. Substitute that into `ct0 + ExternalProduct(c, ct1 - ct0)` and write out what comes back for mu = 0 and mu = 1."
            }
          ]
        },
        {
          "id": "constant",
          "label": "Show you did not branch in the clear",
          "hints": [
            {
              "id": "constant-hint-1",
              "content": "The output must be neither ct0 nor ct1. The external product adds fresh noise on both paths, so a correct CMUX always returns a new ciphertext."
            }
          ]
        },
        {
          "id": "rotate",
          "label": "Turn by a monomial",
          "hints": [
            {
              "id": "rotate-hint-1",
              "content": "The exponent's modulus is 2N. Reducing modulo N loses exactly the sign. `normalize` applies the negacyclic rule to a coefficient list of any length, so shifting right and handing it over is enough."
            }
          ]
        },
        {
          "id": "conditional",
          "label": "Choose between turning and holding",
          "hints": [
            {
              "id": "conditional-hint-1",
              "content": "Candidate 0 holds, candidate 1 turns. Swap them and the meanings of 0 and 1 swap with them, so neither passes."
            }
          ]
        },
        {
          "id": "blind",
          "label": "Turn by an amount nobody knows",
          "hints": [
            {
              "id": "blind-hint-1",
              "content": "Start from what is public. `body` is not a secret, so the offset rotation needs no CMUX. The rest is each mask coefficient applied conditionally through the matching key row."
            }
          ]
        },
        {
          "id": "trace",
          "label": "Show the accumulation",
          "hints": [
            {
              "id": "trace-hint-1",
              "content": "`dimension + 1` records. Step 0 is the public offset and the only step whose two candidates are the same ciphertext. `mask` is what you were handed; `exponent` is that normalized."
            }
          ]
        },
        {
          "id": "transfer",
          "label": "Hold up in a setting you have not seen"
        }
      ]
    }
  }
}
