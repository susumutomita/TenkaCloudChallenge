{
  "id": "sha256-compress-digest",
  "name": "SHA-256 その 3: 圧縮関数と digest、そしてパスワード保存",
  "category": "Challenge",
  "status": "ready",
  "visibility": "public",
  "difficulty": 4,
  "estimatedDuration": "90〜120 分",
  "shortDescription": "1 round の T1 / T2、64 round、digest、そして「64 round は可逆で、最後の足し戻しだけが一方向性を作っている」ことを反例で確かめる。最後にハッシュ関数の性質とパスワード保存を問う。",
  "description": "SHA-256 を自分で追える手続きに変える 3 問シリーズの 3 問目、そして最後。1 問目のパディングと 2 問目のビット演算は `local/given/primitives.py` として与えられているので、この問題は単独で解ける。\n\nPortal editor のエディタ (`compress.py`) に 5 つの関数があり、全部が意図的に壊れている。T1 から K[i] を落とす、64 round のかわりに 63 round 回す、最後の足し戻しを省く、逆変換で h を復元せずに 0 で埋める、複数ブロックのうち最初の 1 つだけを圧縮する。最後のものは `abc` の公開テストベクタを通してしまうので、いちばん厄介な種類のバグだ。\n\n採点は 7 checkpoint 独立。round (45) は 1 round、compress (45) は 64 round と足し戻し、feedforward (45) は逆変換、digest (55) は完成した SHA-256、avalanche (40) は 1 bit 違いの 2 つのメッセージの digest が何 bit 違うかの実測、properties (35) はハッシュ関数の性質の真偽判定、storage (35) はパスワード保存の真偽判定。\n\nfeedforward がこの問題の核心。64 round は何も捨てていないので状態の置換であり、逆算できる。逆算できるものがハッシュになるのは、最後に入力状態を足し戻す Davies-Meyer の feed-forward があるからだ。checkpoint は逆変換を書かせたうえで、足し戻した後の状態には同じ逆変換が効かないことを確かめる。隠しテストは検査側が計算した順方向を逆算させるので、学習者の順方向と辻褄が合っているだけの逆変換では通らない。\n\nproperties と storage は真偽判定だが、暗記できない。文は固定 (seed からまともな主張は生成できない) で、seed が変えるのは順序だ。提出は T と F の並びなので、別のデプロイの答えは役に立たない。\n\n公開テストは意図的に不十分にしてある。使うメッセージが全部 1 ブロックに収まるので、最初のブロックだけを圧縮する実装が FIPS 公開ベクタごと全部通る。mutation suite (local/mutation.py) が 46 変異を kill することを CI で担保する。等価変異 5 種は意図的に除外し、うち 1 種は実際に survived してから外した — round 定数と schedule 語は T1 の中で足されるだけなので、引数を入れ替えても出力は変わらない。",
  "instructions": "## 前提 — 4 つの数で予行演習\n\n- **ここまでの続き**: その 1 で材料を 64 バイトのブロックに切りそろえ、その 2 でビットをずらして混ぜる部品を作りました。 その 2 つの成果物は、この問題では **完成品として用意されています**(パディング、ワード分け、σ / Σ、Ch、Maj、16 個のワードを 64 個に広げる計算)。 名前は `compress.py` の先頭の import 行に並んでいます。 だから、その 1・その 2 を解いていなくてもこの問題は始められます。\n- **状態(state)**: SHA-256 は 8 個のワード(4 バイト = 32 ビットの数)を持ち歩き、それをかき混ぜ続けます。 この 8 個をまとめて **状態** と呼び、順に a, b, c, d, e, f, g, h という名前が付いています。\n- **round(ラウンド)**: 状態を 1 回だけかき混ぜる手順です。 SHA-256 はこれを **64 回** 繰り返します。 1 回の中で作る 2 つの中間の数に T1・T2 という名前が付いています。\n- **足し戻し(feed-forward)**: 64 回が終わったあと、**入り口にあった状態を、出てきた状態に語ごとに足す** 操作です。 この問題の山場はここです。\n- **digest(ダイジェスト)**: 最後に残った 8 個のワードを、16 進 8 文字ずつ順に並べた 64 文字。 あの「指紋」の正体です。 64 文字 = 256 ビットなので SHA-**256** といいます。\n- **逆変換**: 手順を逆にたどって、元の状態に戻すこと。\n- **言葉**: 雪崩(avalanche) = 入力をほんの少し変えると出力が大きく変わること。 コンテナ = あなた専用に用意される、プログラムを動かす小さなコンピュータ。 問題エディタ = そのコンテナの中のファイルを画面で書き換えて、ボタンで動かせる画面。 checkpoint = 採点される小問。 この問題には 7 つあります。\n- **1 桁の例**(数 4 個で、この問題の 3 つの考えを全部やってみます)。 状態を (A, B, C, D) の 4 個とし、round の規則を「新しい状態 = (A + D, A, B, C)」と決めます。 (2, 3, 4, 5) を 1 回動かすと、新しい A は 2 + 5 = 7 で、残りは 1 個ずつずれるので **(7, 2, 3, 4)**。 これを **戻して** みます。 古い A・B・C は、ずれを逆にたどるだけで 2・3・4 と読めます。 残る古い D は、新しい A から古い A を引いて 7 − 2 = 5。 元の (2, 3, 4, 5) に戻りました。 **捨てていないものは戻せます。** では **足し戻し** を入れます。 出口 (7, 2, 3, 4) に入り口 (2, 3, 4, 5) を足すと (9, 5, 7, 9)。 これに同じ戻し方を当てると、古い A = 5、古い D = 9 − 5 = 4 で (5, 7, 9, 4) — 入り口には戻りません。 足し戻しが 1 回入っただけで、戻る道が消えました。 SHA-256 が一方通行なのは、この形のおかげです。\n\n## はじめに\n\nここで指紋が完成します。 エディタに出る `compress.py` に並んでいる関数は、**どれもわざと間違えて書いてあります**。 間違いはどれも 1 行で、どれも人が実際にやる種類のものです。 何を満たすべきか、いまの中身がどこで違うかは、それぞれの説明(docstring)に書いてあります。\n\nいちばん厄介な 1 つを先に言っておきます。 いまの `sha256_hex` は最初のブロックしか処理しません。 だから短いメッセージでは **正しい答えを返します**。 公開されている有名なテスト値とも一致します。 長いメッセージにした瞬間だけ壊れる。 例外は出ません。\n\n最後の 2 つの checkpoint はコードではありません。 完成した道具について、**何が言えて、何が言えないか** を答えます。 パスワードの保存に SHA-256 を使ってよいか、という現場の話も含みます。\n\n## 最初の一手\n\nParticipant Portal(この問題文が出ているサイト)でこの問題を開き、ローカルランタイムの **「起動」** を押します。 あなた専用のコンテナが立ち上がり、問題文と同じ画面に **問題エディタ** が出ます。 エディタに出るファイルは `compress.py` ひとつだけです。\n\n次に **「証拠を確認」** を押してください。 打ち込むものはありません。 出てくるのは次のものです。\n\n1. **用意されている部品の一覧** と、定数 3 個(K[0]、K[63]、最初の状態の 1 語目)。 これらが素数の平方根・3 乗根の小数部分から作られていることも表示されます — 数が「どこかから拾ってきた謎の値」ではないことを見せるためです。\n2. **round 用の状態 8 語** と、何番目の round か、その round で使う定数、その round で使うワード。\n3. **feedforward 用の状態 8 語** と、使うワードの並びの先頭と末尾。\n4. **avalanche 用のメッセージ 2 つ**(16 進) — 元のものと、1 ビットだけ反転させたもの。\n5. **properties の 10 文**(英語と日本語)。\n6. **storage の 10 文**(英語と日本語)。\n\ncompress と digest には読む材料がなく、そう書いてあります。 いちばん上に python のバージョンと health token も出ますが、この問題ではどの checkpoint にも使いません。 数値も文の並び順も起動するたびに変わるので、他の人の答えを写しても合いません。\n\n## 答えるもの — checkpoint は 7 個\n\n7 つは別々に採点されるので、ひとつ詰まっても他は取れます。 直接回答は回答欄に値を入れてから、コードの checkpoint はエディタを書いてから、その checkpoint の **「提出 (+点数)」** を押します。\n\n1. **round**(コード) — `round_step`。 1 回の round で **新しく計算されるのは 8 語のうち 2 語だけ** で、残りは 1 個ずつずれます。 T1 と T2 の式は docstring にそのまま書いてあります。\n2. **compress**(コード) — `compress_rounds`(64 回まわす)と `compress_block`(そのあと入り口の状態を足し戻す)。 2 つの仕事をはっきり分けてあります。\n3. **feedforward**(コード) — `invert_round` と `invert_rounds`。 64 回の round は何も捨てていないので、逆にたどれます。 採点は **検査側が回した順方向** を戻せるかで見るので、自分の順方向と辻褄が合っているだけでは通りません。\n4. **digest**(コード) — `sha256_hex`。 パディングしたメッセージの **すべての** ブロックを処理して、最後の 8 語を 16 進 8 文字ずつ並べた 64 文字を返します。 ブロックが 2 個以上あるときの繋ぎ方も docstring にあります。\n5. **avalanche**(直接回答) — 表示された 2 つのメッセージ(元と、1 ビット反転したもの)の digest を作り、**その 2 つで値が違っているビットが 256 個のうち何個か** を整数ひとつで。\n6. **properties**(直接回答) — ハッシュ関数について 10 個の文が出ます。 正しいものを T、正しくないものを F として、**表示された順に 10 文字** 並べます(`TFTFTFTFTF` のような形)。 並び順は起動ごとに変わります。\n7. **storage**(直接回答) — パスワードを保存する立場で、同じ形式の 10 文に答えます。\n\nコードの checkpoint には回答欄がなく、押した時点のエディタの中身が送られます。 間違えても直して再提出できます(誤答 1 回につき 15 点。 合計が 0 点未満になることはありません)。 **「公開テストを実行」** はいまのエディタで公開テストを走らせ、**「初期状態に戻す」** はエディタを最初の状態に戻します。\n\n## ゴール\n\n7 つ全部が正解になれば、シリーズは終わりです。 ここでも **「公開テストを実行」が全部緑でも、採点はそれより広い範囲で行われます** — 上に書いた「最初のブロックしか処理しない」実装が、公開テストを全部通ってしまうことを思い出してください。\n\nこの問題で本当に持ち帰ってほしいのは feedforward です。 64 回のかき混ぜは、それ単体では何も捨てていません。 だから戻せます。 戻せるものが一方通行の指紋になるのは、最後に入り口を足し戻しているからです。 予行演習の 4 個の数でやったことを、8 個のワードと 64 回の round で、自分の手で確かめてください。\n",
  "writeup": "## 何を確認したか\n\n### 1 round は思ったより小さい\n\n1 round で計算されるのは 8 語のうち 2 語だけです。新しい `a` は T1 + T2、新しい `e` は古い `d` + T1。残る 6 語は古い a, b, c と e, f, g がひとつずつずれたもので、古い `d` と `h` は消えます。\n\nT1 = h + Σ1(e) + Ch(e, f, g) + K[i] + W[i]、T2 = Σ0(a) + Maj(a, b, c) です。項が 5 つと 2 つ。1 つ落としても例外は出ず、全入力に対して違う digest が出ます。starter が K[i] を落としているのはそれを一度体験するためです。\n\n### 64 round は可逆で、それがまさに問題\n\n情報を捨てていないので、64 round は状態の置換です。固定された schedule に対して全単射で、逆写像を書けます。書けるということは、digest から中間状態へ逆算できるということでもあります。\n\n逆算を止めているのは最後の 1 行、入力状態の足し戻しです。Davies-Meyer 構成の feed-forward と呼ばれます。足し戻しがあると、出力から入力状態を求めるには「その状態を入れたときの round 出力」を知る必要があり、それには入力状態が要る、という循環になります。\n\nこの checkpoint で確かめたのは、一方向性が「64 回混ぜたから」ではなく「混ぜた結果に元を足したから」だという点です。round をいくら増やしても、足し戻しが無ければ可逆なままです。\n\n### digest は鎖\n\n各ブロックの圧縮出力が次のブロックの入力状態になります。最初のブロックだけを圧縮する実装は、55 バイト以下のメッセージでは正解を出し、`abc` の FIPS 公開ベクタも通ります。56 バイトから壊れます。\n\n公開テストベクタで検証するときは、必ず複数ブロックのケースを含めてください。これは SHA-256 に限らず、境界を跨がないテストで安心してしまう一般的な失敗です。\n\n### avalanche\n\n入力を 1 bit 変えると、出力 256 bit のおよそ半分が変わります。「およそ」であって「ちょうど」ではありません。理想的なランダム関数なら期待値が 128 bit で、実測は 100 から 160 くらいの範囲に散ります。あなたの計測値もその範囲に入ったはずです。\n\n半分変わるのが望ましいのは、出力の差分から入力の差分を推測できなくするためです。1 bit 入力を変えて出力が 1 bit しか変わらないハッシュは、差分をたどって入力を復元できてしまいます。\n\n### パスワード保存に SHA-256 を使ってはいけない\n\nここまで実装してきて分かるとおり、SHA-256 は速いです。それは設計目標であり、パスワード保存では欠点です。汎用 GPU で毎秒数十億回試せる関数の出力を保存すると、漏洩時に総当たりが現実的になります。\n\nソルトは必要ですが十分ではありません。ソルトが無効化するのは事前計算 (レインボーテーブル) であって、その場での総当たりは止めません。パスワードごとに別のソルトを使うのも必要です。テーブル全体で 1 つを共有すると、1 回の総当たりが全ユーザーに効いてしまいます。\n\n使うべきは意図的に遅く、コストを設定で上げられる関数です。Argon2 (できれば Argon2id)、bcrypt、scrypt、PBKDF2。Argon2id と scrypt は時間だけでなくメモリも要求するので、GPU や ASIC の並列性の優位を削ります。\n\n最後にひとつ。「SHA-256 を数千回繰り返せば PBKDF2 と同じ」ではありません。PBKDF2 は HMAC を鍵付きで反復する定義された構成で、ソルトと反復回数の扱いも規定されています。自分で書いた loop は、その規定のどこかを外していると考えるのが妥当です。暗号の実装で自作の loop に頼らないのは、ここまで 3 問かけて手で書いてきたあとでも (むしろそのあとだからこそ) 正しい判断です。\n\n## 公開テストが通っても終わりではない\n\nこの問題の公開テストが使うメッセージは全部 1 ブロックに収まります。最初のブロックだけを圧縮する実装が、FIPS の `abc` ベクタごと全部通ります。\n\n隠しテストは長さ 0・1・55・56・63・64・65・119・120・191・192 と seed 由来の長さ、UTF-8 混在を回し、`digest` だけは既知の答えと突き合わせます。`feedforward` は検査側が計算した順方向を逆算させるので、学習者の順方向と辻褄が合っているだけの逆変換では通りません。\n\n## ここまでで得たもの\n\nSHA-256 の全工程を自分で書きました。バイト列への変換、パディング、ワード分割、message schedule、64 round の圧縮、足し戻し、digest。ライブラリの `sha256()` が何をしているかは、もうブラックボックスではありません。\n\n同じ道具立ては HMAC、PBKDF2、Merkle tree、Bitcoin の proof-of-work にそのまま繋がります。どれも「圧縮関数を何にどう当てるか」の違いでしかありません。",
  "tags": [
    "sha256",
    "hash-function",
    "compression-function",
    "avalanche",
    "password-storage",
    "cryptography-basics",
    "local-play",
    "container"
  ],
  "exposedPorts": [
    {
      "port": 18091,
      "name": "Portal editor API + /verify (loopback only)"
    }
  ],
  "learningGoals": [
    "1 round で計算されるのが 8 語のうち 2 語だけであることを説明し、実装できる",
    "64 round と足し戻しを分けて実装し、それぞれの役割を述べられる",
    "64 round の逆変換を書き、round が情報を捨てていないことを示せる",
    "一方向性が feed-forward の足し戻しから来ることを反例で示せる",
    "複数ブロックを鎖のように繋いだ SHA-256 を完成させられる",
    "1 bit の入力変化が出力の約半分を変えることを実測し、なぜ望ましいかを説明できる",
    "ハッシュ関数について何が言えて何が言えないかを区別できる",
    "パスワード保存に汎用ハッシュを使ってはいけない理由と、代わりに何を使うかを説明できる"
  ],
  "runtime": {
    "provider": "docker",
    "engine": "compose",
    "entry": "local/docker-compose.yml",
    "verifyUrl": "http://127.0.0.1:18091/verify",
    "secretEnv": [
      "FLAG_SEED"
    ]
  },
  "scoring": {
    "kind": "multi-verify",
    "checks": [
      {
        "id": "round",
        "input": "multiline",
        "label": "1 round を実装する",
        "points": 45,
        "wrongAnswerPenalty": 15,
        "hints": [
          {
            "id": "round-hint-1",
            "content": "計算されるのは 8 語のうち 2 語だけ。新しい a は T1 + T2、新しい e は古い d + T1。残る 6 語は古い a, b, c と e, f, g がひとつずつずれたものになる。",
            "penalty": 8
          },
          {
            "id": "round-hint-2",
            "content": "T1 = h + Σ1(e) + Ch(e, f, g) + K[i] + W[i] で 5 項、T2 = Σ0(a) + Maj(a, b, c) で 2 項。どちらも mod 2^32。項を 1 つ落としても例外は出ないので、5 項と 2 項を数えて確認する。",
            "penalty": 14
          }
        ]
      },
      {
        "id": "compress",
        "input": "multiline",
        "label": "64 round と足し戻しを実装する",
        "points": 45,
        "wrongAnswerPenalty": 15,
        "hints": [
          {
            "id": "compress-hint-1",
            "content": "`compress_rounds` は 64 回で、i 番目の round は K[i] と schedule[i] を使う。`compress_block` はその出力に入力状態を語ごとに mod 2^32 で足す。この足し算が feed-forward で、省くと後段の checkpoint も落ちる。",
            "penalty": 22
          }
        ]
      },
      {
        "id": "feedforward",
        "input": "multiline",
        "label": "64 round を逆にたどる",
        "points": 45,
        "wrongAnswerPenalty": 15,
        "hints": [
          {
            "id": "feedforward-hint-1",
            "content": "6 語は逆向きにずらすだけで戻る。新しい (b, c, d) が古い (a, b, c)、新しい (f, g, h) が古い (e, f, g) だったのだから、逆はその読み替えになる。",
            "penalty": 8
          },
          {
            "id": "feedforward-hint-2",
            "content": "残る d と h は T1 の中にしか現れない。T2 は復元済みの a, b, c だけで決まるので、新しい a から T1 = a_new - T2 が求まる。そこから d = e_new - T1 と h = T1 - Σ1(e) - Ch(e, f, g) - K[i] - W[i] が出る。",
            "penalty": 14
          }
        ]
      },
      {
        "id": "digest",
        "input": "multiline",
        "label": "SHA-256 を完成させる",
        "points": 55,
        "wrongAnswerPenalty": 15,
        "hints": [
          {
            "id": "digest-hint-1",
            "content": "公開テストのメッセージは全部 1 ブロックに収まる。56 バイト以上のメッセージで自分の実装を試してほしい。各ブロックの出力状態が次のブロックの入力状態になる。",
            "penalty": 12
          },
          {
            "id": "digest-hint-2",
            "content": "出力は 8 語をそれぞれ 8 桁の小文字 hex にして順に連結した 64 文字。先頭の 0 を落とさないよう、`f'{word:08x}'` のように桁数を指定する。",
            "penalty": 15
          }
        ]
      },
      {
        "id": "avalanche",
        "label": "1 bit の変化がどこまで広がるかを測る",
        "points": 40,
        "wrongAnswerPenalty": 15,
        "hints": [
          {
            "id": "avalanche-hint-1",
            "content": "`inspect` が元のメッセージと 1 bit 反転した後のメッセージを両方 hex で出している。両方を hash して、2 つの digest を XOR し、立っているビットを数える。答えは 256 のうちのおよそ半分になるはずだが、ちょうど半分ではない。",
            "penalty": 20
          }
        ]
      },
      {
        "id": "properties",
        "label": "ハッシュ関数について何が言えるか",
        "points": 35,
        "wrongAnswerPenalty": 15,
        "hints": [
          {
            "id": "properties-hint-1",
            "content": "迷う行があるなら、出力が 256 bit で固定なのに入力の長さに上限が無い、という一点から考える。入力の個数が出力の個数より多いなら何が言えるか。「まだ見つかっていない」と「存在しない」の違いも同じ話。",
            "penalty": 17
          }
        ]
      },
      {
        "id": "storage",
        "label": "パスワードをどう保存するか",
        "points": 35,
        "wrongAnswerPenalty": 15,
        "hints": [
          {
            "id": "storage-hint-1",
            "content": "軸は 3 つ。(1) SHA-256 が速いことはここでは欠点。(2) ソルトが無効化するのは事前計算だけで、その場の総当たりは止まらない。(3) 必要なのはコストを上げられる関数で、メモリまで要求できるとさらに良い。",
            "penalty": 17
          }
        ]
      }
    ]
  },
  "i18n": {
    "en": {
      "name": "SHA-256 part 3: the compression function, the digest, and password storage",
      "shortDescription": "T1 and T2 for one round, then 64 of them, then the digest — and a counterexample showing that the 64 rounds are invertible and only the final addition makes SHA-256 one-way. Ends with what you can and cannot claim about a hash, and how to store a password.",
      "description": "Third and last of the series that turns SHA-256 into a procedure you can follow. The padding from problem one and the bit operations from problem two are supplied as `local/given/primitives.py`, so this problem stands alone.\n\nThe Portal editor file (`compress.py`) has five functions, all deliberately broken: K[i] dropped from T1, 63 rounds run instead of 64, the final add-back omitted, the inverse transform filling h with zeros instead of recovering it, and only the first of multiple blocks compressed. That last one passes the public `abc` test vector — the nastiest kind of bug.\n\nScoring is seven independent checkpoints. round (45) is one round; compress (45) is 64 rounds plus the add-back; feedforward (45) is the inverse transform; digest (55) is the finished SHA-256; avalanche (40) measures how many bits differ between the digests of two messages one bit apart; properties (35) is true/false judgement about hash-function properties; storage (35) is true/false judgement about password storage.\n\nfeedforward is the heart of the problem. The 64 rounds discard nothing, so they are a permutation of the state — invertible. What turns an invertible thing into a hash is the Davies-Meyer feed-forward that adds the input state back at the end. The checkpoint has the learner write the inverse, then confirms the same inverse no longer works on the state after the add-back. The hidden tests have the learner invert a forward computation done by the checker, so an inverse that merely agrees with the learner's own forward pass does not pass.\n\nproperties and storage are true/false but cannot be memorised: the statements are fixed (a seed cannot generate sensible claims), and what the seed changes is their order. The submission is a sequence of T and F, so another deployment's answer is useless.\n\nThe public tests are deliberately insufficient: every message they use fits in one block, so a first-block-only implementation passes all of them, FIPS public vectors included. The mutation suite (local/mutation.py) kills 46 mutants in CI. Five equivalent mutants are excluded on purpose — one after actually surviving: the round constant and the schedule word are only ever added inside T1, so swapping the arguments cannot change the output.",
      "instructions": "## Before you start — a rehearsal with four numbers\n\n- **Continuing from the earlier parts**: part 1 cut the input into 64-byte blocks and part 2 built the pieces that slide and mix bits. Both of those arrive here **already finished** — padding, splitting into words, σ / Σ, Ch, Maj, and growing sixteen words into sixty-four. Their names are listed in the import at the top of `compress.py`. So you can start this problem without having solved parts 1 and 2.\n- **The state**: SHA-256 carries eight words (four bytes, 32 bits, each) around and keeps stirring them. Those eight together are the **state**, conventionally named a, b, c, d, e, f, g, h.\n- **A round**: one single stirring of the state. SHA-256 does **sixty-four** of them. The two intermediate values built inside one round are called T1 and T2.\n- **The feed-forward**: after the sixty-four rounds, **the state that went in is added, word by word, to the state that came out**. This is where the problem peaks.\n- **The digest**: the eight words left at the end, written as eight hex characters each, in order — 64 characters. That is what the \"fingerprint\" actually is. 64 characters is 256 bits, which is the 256 in SHA-**256**.\n- **Inverting**: walking the steps backwards to recover the state you started from.\n- **Words**: avalanche = a tiny change in the input producing a large change in the output. container = a small computer set up just for you. problem editor = the screen where you edit that container's files and run them with buttons. checkpoint = one small graded question; this problem has seven.\n- **A worked example** (four numbers, covering all three ideas of this problem). Let the state be four values (A, B, C, D) and let one round be \"new state = (A + D, A, B, C)\". Stirring (2, 3, 4, 5) once gives a new A of 2 + 5 = 7 with the rest sliding along one place: **(7, 2, 3, 4)**. Now **undo it**. Old A, B and C come straight back by sliding the other way: 2, 3, 4. The remaining old D is the new A minus the old A: 7 − 2 = 5. Back to (2, 3, 4, 5). **Nothing was thrown away, so it comes back.** Now add the **feed-forward**: add the incoming state (2, 3, 4, 5) to the output (7, 2, 3, 4) and you get (9, 5, 7, 9). Apply the same undo to that: old A = 5, old D = 9 − 5 = 4, giving (5, 7, 9, 4) — not the state you started from. One addition closed the way back. That shape is why SHA-256 only goes one way.\n\n## The situation\n\nThe fingerprint is finished here. Every function in `compress.py`, the one file this problem opens in the editor, is **wrong on purpose**. Each defect is a single line, and each is a mistake people really make. What each function must satisfy, and where the current body departs from it, is in that function's docstring.\n\nOne of them deserves a warning up front. The current `sha256_hex` compresses only the first block, so for short messages it **returns the right answer**. It even agrees with the famous published test vector. It breaks only once the message gets long. Nothing raises.\n\nThe last two checkpoints are not code. They ask what can and cannot be claimed about the tool you just finished — including the practical question of whether SHA-256 belongs anywhere near stored passwords.\n\n## First move\n\nOpen this problem in Participant Portal (the site showing this statement) and select **Start**, the local runtime's start button. Your own container comes up and the **problem editor** appears on the same page as the statement. It shows one file: `compress.py`.\n\nThen select **Inspect evidence**. There is nothing to type. It shows:\n\n1. **The list of pieces already provided**, plus three constants (K[0], K[63], and the first word of the initial state), together with the fact that they come from the fractional parts of square roots and cube roots of primes — so the numbers are visibly derived rather than conjured.\n2. **Eight state words for round**, with which round it is, that round's constant, and that round's schedule word.\n3. **Eight state words for feedforward**, with the first and last entries of the schedule to use.\n4. **Two messages for avalanche**, in hex — the original, and the same message with a single bit flipped.\n5. **The ten properties statements**, in English and Japanese.\n6. **The ten storage statements**, in English and Japanese.\n\nThere is no fixture to read for compress and digest, and the output says so. A Python version and a health token are printed above all of this; no checkpoint in this problem uses them. Both the numbers and the order of the statements change on every start, so copying somebody else's answers will not match.\n\n## What to answer — seven checkpoints\n\nThe seven are graded separately, so getting stuck on one does not cost you the others. For a direct answer, fill in the answer field; for a code checkpoint, edit the file. Then press that checkpoint's **Submit (+points)**.\n\n1. **round** (code) — `round_step`. One round **computes only two of the eight words**; the other six slide along one place. The formulas for T1 and T2 are written out in the docstring.\n2. **compress** (code) — `compress_rounds` (run all sixty-four) and `compress_block` (then add the incoming state back). The two jobs are deliberately separate functions.\n3. **feedforward** (code) — `invert_round` and `invert_rounds`. The sixty-four rounds throw nothing away, so they can be walked backwards. Grading runs **the checker's own forward pass**, so an inverse that merely agrees with your own forward pass does not pass.\n4. **digest** (code) — `sha256_hex`. Compress **every** block of the padded message and return the final eight words as eight hex characters each, 64 characters in all. How the blocks chain together when there is more than one is also in the docstring.\n5. **avalanche** (direct answer) — hash both displayed messages (the original and the one-bit-flipped one) and give, as one integer, **how many of the 256 digest bits differ** between them.\n6. **properties** (direct answer) — ten statements about hash functions appear. Answer T for the true ones and F for the false ones, **ten characters in the order shown** (the shape is `TFTFTFTFTF`). The order changes on every start.\n7. **storage** (direct answer) — the same format, ten statements, answered as somebody responsible for storing passwords.\n\nA code checkpoint has no answer field: whatever is in the editor when you press Submit is what gets sent. A wrong answer can be corrected and resubmitted (each wrong try costs 15 points, and the total never goes below zero). **Run public tests** runs the public suite against your current editor content, and **Restore starter** puts the editor back to its initial state.\n\n## Goal\n\nThe series is done when all seven are correct. Once more: **all of Run public tests going green does not mean grading agrees — grading covers more than the public tests do.** Remember the implementation described above, the one that compresses only the first block: it passes every public test there is.\n\nThe part worth carrying away is feedforward. The sixty-four rounds of stirring, on their own, throw nothing away, which is exactly why they can be undone. What turns something undoable into a one-way fingerprint is that final addition of the state that went in. Do with eight words and sixty-four rounds what the rehearsal did with four numbers, and see it for yourself.\n",
      "writeup": "## What this checked\n\n### A round is smaller than it looks\n\nOne round computes two of the eight words. The new `a` is T1 + T2 and the new `e` is the old `d` + T1. The other six are the old a, b, c and e, f, g shifted one position along, and the old `d` and `h` are gone.\n\nT1 = h + Σ1(e) + Ch(e, f, g) + K[i] + W[i]; T2 = Σ0(a) + Maj(a, b, c). Five terms and two. Drop one and nothing raises — you simply get a different digest for every input. The starter drops K[i] so that happens to you once, cheaply.\n\n### The 64 rounds are invertible, and that is the point\n\nNothing is discarded, so the 64 rounds are a permutation of the state: for a fixed schedule, a bijection with a writable inverse. Writable means somebody could walk a digest backwards into an intermediate state.\n\nWhat stops them is the last line: adding the incoming state back in. That is the Davies-Meyer feed-forward. With it, recovering the input state from the output would require knowing the round output for that state, which requires the input state — the circularity is the construction.\n\nSo the thing this checkpoint establishes is that one-wayness does not come from mixing 64 times. It comes from adding the original back to the mixture. Without the feed-forward, more rounds would still be invertible.\n\n### The digest is a chain\n\nEach block's compressed output is the next block's input state. An implementation that compresses only the first block is right for messages up to 55 bytes and passes the published `abc` vector. It breaks at 56.\n\nWhen you check an implementation against published test vectors, include a multi-block case. This generalizes well past SHA-256: a test suite that never crosses the boundary is the most reassuring kind of insufficient.\n\n### Avalanche\n\nFlip one input bit and roughly half the 256 output bits change. Roughly, not exactly. For an ideal random function the expected count is 128 and measurements scatter, typically somewhere between 100 and 160. Yours will have landed in that band.\n\nHalf is the target because it stops an attacker reading the input difference off the output difference. A hash where one input bit moved one output bit would let you walk differences back to the input.\n\n### Do not store passwords with SHA-256\n\nHaving implemented it, you know SHA-256 is fast. That is a design goal, and for password storage it is a defect: storing the output of a function a commodity GPU evaluates billions of times per second makes brute force practical the moment the database leaks.\n\nA salt is necessary and not sufficient. What a salt defeats is precomputation — rainbow tables — not brute force at the time of the attack. Per-password salts are also necessary: one salt shared across the table lets a single brute-force run pay off for every user at once.\n\nWhat you want is a deliberately slow function whose cost you can raise: Argon2 (ideally Argon2id), bcrypt, scrypt, PBKDF2. Argon2id and scrypt demand memory as well as time, which cuts into the parallelism advantage GPUs and ASICs have.\n\nOne last thing. \"Iterating SHA-256 a few thousand times is what PBKDF2 does\" is not right. PBKDF2 is a specified construction over HMAC with defined handling of the salt and the iteration count, and a loop you wrote yourself is most likely missing part of that specification. Not hand-rolling cryptographic constructions is the correct call even after three problems of writing SHA-256 by hand — arguably especially then.\n\n## Passing the public tests is not the end\n\nEvery message the public tests use fits in one block, so an implementation that compresses only the first block passes all of them, published `abc` vector included.\n\nThe hidden tests sweep lengths 0, 1, 55, 56, 63, 64, 65, 119, 120, 191, 192, a seeded length and a mixed UTF-8 message, and `digest` is the one checkpoint compared against known answers. `feedforward` inverts a forward pass the checker computed itself, so an inverse that only agrees with your own broken forward pass does not pass.\n\n## What you have now\n\nYou have written every stage of SHA-256: bytes, padding, word splitting, the message schedule, 64 rounds of compression, the feed-forward, the digest. Whatever your library's `sha256()` is doing, it is no longer a black box.\n\nThe same pieces carry directly into HMAC, PBKDF2, Merkle trees and Bitcoin's proof of work. Each of those is a different answer to \"what do we apply the compression function to, and how often\".",
      "learningGoals": [
        "Explain and implement a round in which only two of the eight words are computed",
        "Implement the 64 rounds and the feed-forward separately, and say what each does",
        "Write the inverse of the 64 rounds, showing they discard nothing",
        "Show by counterexample that one-wayness comes from the feed-forward addition",
        "Finish a SHA-256 that chains every block of a message",
        "Measure that one input bit moves about half the output bits, and say why that is wanted",
        "Tell apart what can and cannot be claimed about a hash function",
        "Say why a general-purpose hash is wrong for passwords, and what to use instead"
      ],
      "checks": [
        {
          "id": "round",
          "label": "Implement one round",
          "hints": [
            {
              "id": "round-hint-1",
              "content": "Only two of the eight words are computed. The new a is T1 + T2 and the new e is the old d + T1; the other six are the old a, b, c and e, f, g shifted one along."
            },
            {
              "id": "round-hint-2",
              "content": "T1 = h + Sigma1(e) + Ch(e, f, g) + K[i] + W[i] is five terms; T2 = Sigma0(a) + Maj(a, b, c) is two. Both modulo 2^32. Dropping a term raises nothing, so count them: five and two."
            }
          ]
        },
        {
          "id": "compress",
          "label": "Implement the 64 rounds and the feed-forward",
          "hints": [
            {
              "id": "compress-hint-1",
              "content": "`compress_rounds` runs 64 times and round i uses K[i] and schedule[i]. `compress_block` adds the incoming state to that output word by word, modulo 2^32. That addition is the feed-forward, and skipping it fails later checkpoints too."
            }
          ]
        },
        {
          "id": "feedforward",
          "label": "Walk the 64 rounds backwards",
          "hints": [
            {
              "id": "feedforward-hint-1",
              "content": "Six words come back by shifting the other way: the new (b, c, d) were the old (a, b, c) and the new (f, g, h) were the old (e, f, g), so the inverse is that reading, reversed."
            },
            {
              "id": "feedforward-hint-2",
              "content": "The remaining d and h only appear inside T1. T2 depends only on the a, b, c you have already recovered, so the new a gives you T1 = a_new - T2 — and then d = e_new - T1 and h = T1 - Sigma1(e) - Ch(e, f, g) - K[i] - W[i]."
            }
          ]
        },
        {
          "id": "digest",
          "label": "Finish SHA-256",
          "hints": [
            {
              "id": "digest-hint-1",
              "content": "Every public-test message fits in one block. Try your implementation on something 56 bytes or longer: each block's output state is the next block's input state."
            },
            {
              "id": "digest-hint-2",
              "content": "The output is 64 characters: eight words, each as 8 lower-case hex digits, in order. Specify the width — `f'{word:08x}'` — so leading zeros are not dropped."
            }
          ]
        },
        {
          "id": "avalanche",
          "label": "Measure how far one bit reaches",
          "hints": [
            {
              "id": "avalanche-hint-1",
              "content": "Portal editor `inspect` prints both the original message and the one-bit-flipped one in hex. Hash both, xor the two digests, and count the set bits. Expect roughly half of 256 — not exactly half."
            }
          ]
        },
        {
          "id": "properties",
          "label": "What can be claimed about a hash function",
          "hints": [
            {
              "id": "properties-hint-1",
              "content": "For any row you are unsure of, start from one fact: the output is fixed at 256 bits while the input length has no bound. If there are more inputs than outputs, what follows? \"Not found yet\" versus \"does not exist\" is the same question."
            }
          ]
        },
        {
          "id": "storage",
          "label": "How to store a password",
          "hints": [
            {
              "id": "storage-hint-1",
              "content": "Three axes. SHA-256 being fast is a defect here. A salt defeats precomputation only, not brute force at attack time. What you need is a function whose cost you can raise — better still, one that demands memory."
            }
          ]
        }
      ]
    }
  }
}
