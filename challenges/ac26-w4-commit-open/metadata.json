{
  "id": "ac26-w4-commit-open",
  "name": "先に聞かれたら、何でも通せる",
  "category": "Challenge",
  "status": "draft",
  "visibility": "public",
  "track": {
    "id": "advanced-cryptography-2026",
    "order": 420,
    "chapter": "Week 4 / Commit-Challenge-Open"
  },
  "courseAlignment": {
    "courseId": "advanced-cryptography-program",
    "edition": "2026",
    "week": 4,
    "role": "transfer",
    "spoilerPolicy": "independent-reimplementation",
    "sources": [
      {
        "repository": "zk-tokyo/advanced-cryptography-2026",
        "ref": "c088f8e6f301dedcd80b6dd9c321a1cd83410637",
        "path": "week4/README.md",
        "kind": "lecture"
      },
      {
        "repository": "zk-tokyo/advanced-cryptography-2026",
        "ref": "c088f8e6f301dedcd80b6dd9c321a1cd83410637",
        "path": "week4/acp-2026-week4-redacted.pdf",
        "kind": "slide"
      }
    ]
  },
  "difficulty": 4,
  "estimatedDuration": "60-90 分",
  "shortDescription": "聞かれてから答えを決められるなら、どんな嘘でも通ります。だから証明の 3 手は順序が全部 — 先に封じ、質問を受け、聞かれた所だけ開ける。その封筒を Merkle 木で自分で作り、順序と縛りが欠けると何ができてしまうかを、自分の手で構成します。",
  "description": "Week 4 の 2 問目。 Week 4 の講義のうち、 コミットメントスキーム（スライド 4: setup / commit / open / verify と binding / hiding）、 IOP の 「オラクル ≈ コミットメント」（スライド 10・12）、 Merkle 木による多項式コミットメント（スライド 22）に対応する。 講義が 1 枚で通り過ぎる 「先に封をしてから質問を受ける」 という順序を、 手を動かして壊す位置にある。 role は transfer（多項式ではなく行の表に対する commitment へ転用）。\n\n主題は 3 手の順序 (commit → challenge → open) と、 その順序が意味を持つために commitment へ何が binding されていなければならないか。 **polynomial commitment ではない**し、 1 箇所の開示は聞かれなかった行について何も言わない。 writeup で明記している。\n\nMerkle 木を使うが、 木そのものは道具であって主題ではない。 主題は 3 つ。\n\n1. **順序**。 challenge を先に知った prover は、 聞かれる場所以外に何を入れてもよい。 adaptive checkpoint でこの vector を構成させる。\n2. **葉の binding**。 index が入っていなければ、 葉はどこから来たことにもできる。 区切りが無ければ (1, 23) と (12, 3) が同じ葉になる。 弱い符号化は fixtures 側に固定してあり、 participant 自身の弱いコードを攻める答えは成立しない。\n3. **path の方向**。 各段で兄弟が左か右かが決まっていなければ、 verifier は 2 通りに hash でき、 prover は都合のよい方を選べる。\n\n**等価変異について**: verify_opening の index 範囲検査と path 長検査は、 domain tag (LEAF_TAG / NODE_TAG) が既に葉と内部節点の取り違えを防いでいるため、 外しても検出できない。 mutation suite には入れていない。 代わりに Session.receive_challenge の範囲検査を変異させている — こちらは負の index が黙って巻き戻り、 聞かれていない行が開示されるので実際に検出できる。 検出できない変異を一覧に残すと、 suite 全体が無視されるようになる。\n\nquery は 1 回なので、 当てずっぽうの prover は 1/length で勝つ。 これは健全性の増幅を扱っていないためで、 writeup に書いてある。",
  "instructions": "## 質問を先に知っていたら\n\nじゃんけんは、後から手を出せば必ず勝てます。テストは、問題を見てから答案を書き換えられたら採点になりません。証明も同じです。「この 1000 行の表を規則どおりに埋めました」と主張する側（prover）に、確かめる側（verifier）が「じゃあ、ここを見せて」と数箇所だけ聞く — このとき **質問を先に知っていた prover は、聞かれる所だけ本物にして、残りを全部嘘にできます**。\n\nこれは教科書の中だけの話ではありません。2022 年には、質問の作り方（transcript への縛り）を誤った実装が複数の本物の証明システムからまとめて見つかり、\"Frozen Heart\" と名付けられました。順序と縛りは実装の細部ではなく、健全性そのものです。\n\nだから 3 手の順序が全部です: **先に封をする（commit）→ 質問を受ける（challenge）→ 聞かれた所だけ開ける（open）**。この問題で作るのは、その封筒です。\n\n## なぜ数箇所で足りるのか — この問題が引き受ける範囲\n\n規則を 1 箇所でも破ると、破れが表の広い範囲に痕跡として散るように作ってあるからです（散らす仕掛けは SumCheck・FRI・PLONK のドリルの主題で、ここでは扱いません）。だから抜き打ち数発でどれかに当たる。**この問題が引き受けるのは「当たった 1 発が嘘をつけないこと」だけ**です。開けた 1 箇所は、聞かれなかった行について何も語りません — 語らせるのが散らす側、当たった所で誤魔化させないのが封筒の側です。\n\n## 前提はハッシュだけ\n\n- ハッシュは「書類の指紋」。1 文字変えると指紋が変わり、指紋から書類は戻せず、**同じ指紋になる別の書類も見つけられない**。最後の性質が「入れた物しか出せない」を支えます。\n- この問題に mod も体も出てきません。Python の `hashlib.sha256` だけです。\n\n## 封筒の作り — なぜ木なのか\n\n4 マスの数 [3, 1, 4, 1] を封じ、「3 番目を見せて」と聞かれ、4 を開ける — これが 1 往復です。表全体を 1 個の指紋にまとめると、1 箇所開けるのに表を全部見せる羽目になります。1 マスずつ指紋を配ると、渡す物がマスの数だけ増えます。**Merkle 木**は 4 枚の指紋を 2 段で 1 個の「根」にまとめ、根だけ渡します。開示は「その場所の値 + 根まで登るのに要る兄弟の指紋 + 各段でその兄弟が左か右か」で、verifier は同じ作り方で根を計算し直して照合します。1000 行でも、開示に要る兄弟は 10 個ほどです。\n\n## 封の強さは、3 つの細部で決まる\n\n木は道具で、主題はこの 3 つです。\n\n1. **順序**。質問を先に知った prover に何ができるか — その「後出し一式」（開示 1 箇所と、それを通してしまう根・path の組）を、adaptive checkpoint であなたが実際に構成します。\n2. **葉の縛り**。葉に index が入っていなければ、その葉はどこから来たことにもできます。さらに、番地と値を区切り無しでつなぐと、別の (index, 値) の組が同じ葉になり得ます。この弱い符号化は出題側の材料（fixtures）に固定してあり、攻める相手は常に出題側の実装です — あなた自身が書いた弱いコードを攻める答えは成立しません。\n3. **path の方向**。各段で兄弟が左か右か決まっていなければ、verifier は 2 通りに hash でき、prover は都合のよい方を選べます。\n\n言葉を 2 つだけ: **binding** = 開けられるのは最初に入れた値だけ。**hiding** = 開けるまで見えない。**この封筒が保証するのは binding だけ**です — 葉は index と値だけから作るので、値の候補が少なければ総当たりで当てられます。hiding が要るときは乱数を混ぜますが、この問題の範囲外です。\n\n## 最初の一手\n\n1. Participant Portal（この問題文が出ているサイト）で **「起動」** を押します。問題文と同じ画面に **問題エディタ**が出て、`commit.py` が入っています。編集するのはこの 1 ファイルだけです。\n2. エディタの **「証拠を確認」** を押します。この起動だけの証拠が出ます。\n   - コンテナが動いた証拠の文字列（health token。この問題では提出に使いません）\n   - **マスの数**（2 の冪なので、木のどの段も半端が出ません）と **domain**（transcript に使う短い文字列）\n   - **verifier が聞いてくる index**\n   - **表** — 各マスの i、値、その葉の指紋の先頭（聞かれる index には目印が付きます）\n   - **根**、**木の段数**、**path の長さ**\n   - **その index の path** — 各段について「兄弟が左か右か」と、その兄弟の指紋の先頭\n3. まず紙で、表の 2 つの葉の指紋から 1 つ上の段がどう作られるかをたどってください。段数と path の長さの関係（path は段数 − 1）が腑に落ちれば、`merkle_root` と `open_at` の形は見えています。\n\n**この根と path はあなたの答え合わせに使えます**。あなたの `commit.py` が同じマスの並びから同じ根と同じ path を出せば、そこまでは合っています。ただし採点は見ていない設定でも走ります。\n\n## 答えるもの — checkpoint は 8 個\n\n8 個とも **`commit.py` を走らせて採点します**。この問題に「回答欄に値を書く」種類の checkpoint はありません。関数とクラスの枠は最初から `commit.py` に入っています。\n\n**葉と節の作り方は決まっています。** あなたの根が「証拠を確認」に出ている根と一致する必要があるので、この通りに作ってください。\n\n- **葉**: `sha256( b\"leaf/v1\" + index を 4 バイト big-endian + 値を 8 バイト big-endian )`\n- **節**: `sha256( b\"node/v1\" + 左の子の 32 バイト + 右の子の 32 バイト )`\n\nPython では `index.to_bytes(4, \"big\")`、`value.to_bytes(8, \"big\")`、`hashlib.sha256(...).digest()` です。この形が上の 3 つの主題をどう満たしているかは見てのとおりで、`b\"leaf/v1\"` と `b\"node/v1\"` が違うので、葉の指紋と節の指紋が偶然一致することもありません。\n\n1. **encoding** — `encode_leaf(index, value)` と `node_hash(left, right)`。前者は葉が commit する **bytes** を返します（**別々の (index, 値) の組が同じ bytes になってはいけません**）。後者は子 2 つの bytes をまとめて 1 つの bytes にします（**左右の順が結果を変えること**）。`leaf_hash(index, value)` はその葉の指紋です。\n2. **root** — `merkle_root(values)`。マスの並び（長さは 2 の冪）から根を **bytes** で返します。\n3. **opening** — `open_at(values, index)` と `verify_opening(root, index, value, path, length)`。`open_at` は `[{\"hash\": bytes, \"sibling_is_left\": bool}, ...]` を葉に近い段から順に返します。`verify_opening` は受け取った側として同じ作り方で根を計算し直し、一致したかを `True` / `False` で返します。ここで作る開示は 1 回につき 1 箇所です — 前に出てきた「数箇所」は、この 1 箇所の手続きを独立に何回か繰り返すことに当たります。\n4. **order** — `Session` クラス。`commit()` → `receive_challenge(index)` → `open()` の順でしか使えないようにします（`open()` は `{\"index\": ..., \"value\": ..., \"path\": [...]}` を返す）。**この順序以外で呼ばれたら `ProtocolError` を投げること**（この例外の枠も `commit.py` にあります）。範囲外の index（負の数や、マスの数以上）を受け取ったときも同じく `ProtocolError` です — 負の index は黙って別の行を指してしまうので、ここで止めます。\n5. **adaptive** — `adaptive_witness(setting)`。引数 `setting` は、その回の出題条件（**どこを聞かれるかを含む**）が入った辞書です。質問を先に知ってしまった prover が作れる一式、つまり `{\"values\": [...], \"query\": ...}` を返します — **聞かれる場所以外はでたらめなのに、その 1 箇所ではあなたの `verify_opening` が通ってしまう**並びです。長さは条件と同じ、`query` は渡された質問と同じにします。\n6. **ambiguity** — `ambiguity_witness()`。引数はありません。出題側が固定した弱い符号化（`fixtures.generate.weak_leaf`。index と値を区切り無しでつなぐ）では **区別できない 2 組**の (index, 値) を `{\"first\": (index, 値), \"second\": (index, 値)}` で返します。同時に、**あなたの `encode_leaf` ではその 2 組が区別できること**も要求されます。\n7. **transcript** — `transcript(domain, root, statement)` と `challenge(domain, root, statement, length)`。verifier にその場にいてもらう代わりに、質問そのものを封筒から作ります。`transcript` は 3 つを決まった順で 1 本につないだ **bytes**、`challenge` はそこから決めた **0 以上 length 未満の整数**（何度呼んでも同じ値で、3 つの入力のどれが変わっても transcript が変わること）。ここでも葉と同じ注意が要ります — 3 つを区切り無しでつなぐと、境目の違う別の 3 つ組が同じ bytes になり得ます。封をした後でなければ質問を知れず、質問を変えたければ封も変わる — だから「先に封をした」ことが、後から記録だけを見ても確かめられます。ここで質問を封筒から導いたのに、なぜ 4 の `Session` でも順序を禁じるのか — **導出そのものが、封が先にあることを前提にしている**からです。根が決まる前に `challenge` を呼べる実装は、その前提が崩れた状態で答えを返してしまいます。\n8. **transfer** — 同じ `commit.py` を、**一度も見ていない設定**で 1 から 7 まで全部もう一度走らせます。見えている値に合わせて書いた実装はここで落ちます。\n\n**提出のしかた**: `commit.py` を書いて、その checkpoint の **「提出 (+点数)」** を押すだけです。いまのエディタの中身がそのまま採点へ送られます。8 個は別々に採点されるので、1 つ詰まっても他は取れます。**不正解のときは、どの決まりが破れたかの短い説明が返ります**（期待される値そのものは返りません）— それを読んで直し、何度でも出し直せます。1 回外すごとに 15 点引かれます（8 個で合計 300 点。合計が 0 点未満になることはありません）。エディタを壊してしまったら **「初期状態に戻す」** で最初の `commit.py` に戻せます。**「公開テストを実行」** で手元でも先に試せます。\n\n## この封筒の上に、週の残りが載る\n\n8 つ通れば終わりです。封筒（葉・根・開示・検証）を自分で作り、3 手の順序をコードで強制し、質問を封筒から導き、順序と縛りが欠けると何ができてしまうかを、自分で構成した 2 つの反例で示しました。\n\ncheckpoint 7 で作った「verifier にその場にいてもらう代わりに、質問を封筒から導く」手は **Fiat–Shamir** と呼ばれ、やりとりまるごとを 1 通の証明に畳みます — zkRollup がチェーンに置くのは、この 1 通です。そして SumCheck・FRI・PLONK の 3 本のドリルは、どれも「先に封じて、数点だけ開ける」の上で動きます。封筒は、この週の床です。",
  "writeup": "## 順序が protocol である\n\ncommitment は 「あとから変えられない」 と言っているだけで、 それ自体は何も証明しない。 意味を与えるのは、 challenge が commitment の**あと**に来ることだ。 逆なら prover は聞かれる場所だけ正しくして、 残りを好きにできる。 adaptive checkpoint で作る vector は 16 要素中 15 要素が嘘で、 それでも開示は検証を通る。\n\n## 葉に何が入っていなければならないか\n\n**index**。 入っていなければ、 葉は 「どこから来たか」 を主張していない。 prover は同じ葉を都合のよい位置から来たことにできる。\n\n**区切り**。 index と value を区切り無しで並べると、 (1, 23) と (12, 3) がどちらも \"123\" になる。 異なる 2 つの主張に 1 つの commitment が対応する。 弱い符号化は fixtures 側に固定してある。 自分で書いた弱いコードを自分で破るのは反例ではない。\n\n**方向**。 path の各段で兄弟が左か右かが決まっていなければ、 verifier は (sibling, node) と (node, sibling) のどちらでも hash でき、 prover は root に合う方を選べる。\n\n## 等価変異の話\n\nverify_opening の index 範囲検査と path 長検査は、 外しても検出できない。 LEAF_TAG と NODE_TAG があるので葉のハッシュと節点のハッシュが一致することはなく、 長さの違う path は root と異なる値に再計算されて比較で落ちるからだ。 したがって mutation suite に入れていない。\n\n代わりに Session.receive_challenge の範囲検査を変異させている。 こちらは負の index が黙って巻き戻り、 聞かれていない行が開示されるので、 実際に検出できる。\n\n検出できない変異を一覧に残すと、 「survived は無視してよい」 を教えることになる。 だから残さない。\n\n## これは polynomial commitment ではない\n\nMerkle root は列に commit する。 多項式の評価を証明するものではない。 また 1 箇所の開示は、 聞かれなかった行について何も言わない。 query が 1 回だけなので、 当てずっぽうの prover は 1/length で勝つ。 健全性の増幅 (複数 query など) はここでは扱っていない。\n\n## binding と hiding は別\n\nMerkle root は binding を与えるが、 hiding は自動では付かない。 値の空間が小さければ root から中身を総当たりできる。 隠したければ、 各葉に別途 randomness を入れる必要がある。",
  "tags": [
    "advanced-cryptography-2026",
    "week4",
    "zkp",
    "commitment",
    "local-play",
    "container"
  ],
  "exposedPorts": [
    {
      "port": 18105,
      "name": "Portal editor API + /verify (loopback only)"
    }
  ],
  "learningGoals": [
    "封じる・質問する・開ける の 3 手を、順序ごと区別できる",
    "質問を先に知った証明者に何ができるかを、実物の後出し一式で示せる",
    "根と path から、開示 1 箇所を自分で検証できる",
    "番地・値・左右の向きを封に縛る理由を、それぞれ破れた場合で言える",
    "binding と hiding が別の性質であることを説明できる",
    "質問を封筒から導くとき、transcript に何を入れるべきかを言える",
    "開けた 1 箇所が、聞かれなかった行について何も言わないことを説明できる"
  ],
  "runtime": {
    "provider": "docker",
    "engine": "compose",
    "entry": "local/docker-compose.yml",
    "verifyUrl": "http://127.0.0.1:18105/verify",
    "secretEnv": [
      "FLAG_SEED"
    ]
  },
  "scoring": {
    "kind": "multi-verify",
    "checks": [
      {
        "id": "encoding",
        "input": "multiline",
        "label": "葉の符号化を一意にする",
        "points": 35,
        "wrongAnswerPenalty": 15,
        "hints": [
          {
            "id": "encoding-hint-1",
            "content": "同じ値が違う位置にあったら、符号化は変わるべきか。 そして 2 つの可変長でない値をどう並べれば取り違えられないか。",
            "penalty": 15
          }
        ]
      },
      {
        "id": "root",
        "input": "multiline",
        "label": "commitment を作る",
        "points": 30,
        "wrongAnswerPenalty": 15
      },
      {
        "id": "opening",
        "input": "multiline",
        "label": "一箇所だけ開示して検証する",
        "points": 45,
        "wrongAnswerPenalty": 15,
        "hints": [
          {
            "id": "opening-hint-1",
            "content": "path の各段で、 兄弟は左か右か。 それが分からない verifier は 2 通りに hash できてしまう。",
            "penalty": 20
          }
        ]
      },
      {
        "id": "order",
        "input": "multiline",
        "label": "順序を強制する",
        "points": 40,
        "wrongAnswerPenalty": 15
      },
      {
        "id": "adaptive",
        "input": "multiline",
        "label": "challenge が先に来たら何ができるか示す",
        "points": 45,
        "wrongAnswerPenalty": 15,
        "hints": [
          {
            "id": "adaptive-hint-1",
            "content": "聞かれる場所を先に知っていたら、 他の場所に何を入れておくか。",
            "penalty": 20
          }
        ]
      },
      {
        "id": "ambiguity",
        "input": "multiline",
        "label": "符号化の曖昧さを反例で示す",
        "points": 40,
        "wrongAnswerPenalty": 15,
        "hints": [
          {
            "id": "ambiguity-hint-1",
            "content": "`weak_leaf` は index と value を区切り無しで並べる。 同じ文字列になる 2 組を探す。",
            "penalty": 18
          }
        ]
      },
      {
        "id": "transcript",
        "input": "multiline",
        "label": "challenge を transcript から導く",
        "points": 35,
        "wrongAnswerPenalty": 15,
        "hints": [
          {
            "id": "transcript-hint-1",
            "content": "commitment が transcript に入っていなければ、 その challenge は commit する前に計算できる。",
            "penalty": 15
          }
        ]
      },
      {
        "id": "transfer",
        "input": "multiline",
        "label": "見たことのない設定でも成立させる",
        "points": 30,
        "wrongAnswerPenalty": 15
      }
    ]
  },
  "i18n": {
    "en": {
      "name": "Ask me first and I can pass anything",
      "shortDescription": "If you can decide your answer after hearing the question, any lie goes through. So the three moves of a proof are all about order: seal first, take the question, open only what was asked. You build that envelope as a Merkle tree — then build, with your own hands, what becomes possible when the order or the binding is missing.",
      "description": "Second problem of Week 4. It corresponds to the lecture's commitment scheme (slide 4: setup / commit / open / verify, binding / hiding), the IOP idea \"oracle ≈ commitment\" (slides 10 and 12), and Merkle-tree polynomial commitment (slide 22). The lecture passes in one slide over \"seal first, then take the question\"; this problem is where you break that order by hand. Its role is transfer (commitment to a table of rows rather than to a polynomial).\n\nThe subject is the three-move order (commit → challenge → open) and what must be bound into the commitment for that order to mean anything. It is **not a polynomial commitment**, and one opening says nothing about the rows that were not asked. The writeup says so explicitly.\n\nA Merkle tree is used, but the tree is a tool, not the subject. The subject is three things.\n\n1. **Order.** A prover who learns the challenge first may put anything anywhere except the queried position. The adaptive checkpoint has the participant construct that vector.\n2. **Leaf binding.** Without the index inside, a leaf can be claimed to come from anywhere. Without a separator, (1, 23) and (12, 3) are the same leaf. The weak encoding is fixed on the fixtures side, so an answer that attacks the participant's own weak code cannot count.\n3. **Path direction.** If which side the sibling sits on is not fixed at each level, the verifier can hash two ways and the prover picks the convenient one.\n\n**On equivalent mutants**: verify_opening's index-range and path-length checks cannot be detected if removed, because the domain tags (LEAF_TAG / NODE_TAG) already prevent leaf/inner-node confusion; they are not in the mutation suite. What is mutated instead is Session.receive_challenge's range check — there a negative index silently wraps and discloses an unasked row, so it is actually detectable. Keeping undetectable mutants on the list is how a suite gets ignored wholesale.\n\nThere is one query, so a guessing prover wins with probability 1/length. Soundness amplification is not covered here, and the writeup says so.",
      "instructions": "## If you knew the question in advance\n\nRock-paper-scissors is unlosable if you throw second. An exam stops being an exam if you can rewrite answers after seeing the questions. Proofs are the same. The claiming side (the prover) says \"I filled in this 1000-row table by the rules\"; the checking side (the verifier) asks about just a few places. And **a prover who knew the question in advance can make exactly the asked-about places genuine and everything else a lie**.\n\nNor is this a textbook-only worry. In 2022, implementations that got the question's derivation wrong — what the transcript binds — were found across several real proof systems at once, and the bug family was named \"Frozen Heart\". Order and binding are not implementation details; they are soundness itself.\n\nSo the order of the three moves is everything: **seal first (commit) → take the question (challenge) → open only what was asked (open)**. What you build in this problem is that envelope.\n\n## Why a few places are enough — and what this problem does and does not cover\n\nBecause these systems are built so that breaking a rule anywhere scatters traces across a wide stretch of the table (the scattering machinery is the subject of the SumCheck, FRI and PLONK drills — not of this one). A few spot-checks land on one. **This problem covers exactly one thing: the place that was hit cannot lie.** One opened place says nothing about the rows never asked about — making them speak is the scatterer's job; keeping the hit place honest is the envelope's.\n\n## Before you start — the only prerequisite is a hash\n\n- A hash is a document's fingerprint: change one character and the fingerprint changes, the document cannot be recovered from it, and **no second document with the same fingerprint can be found**. That last property is what supports \"nothing comes out except what went in\".\n- No mod, no field in this problem. Python's `hashlib.sha256` is all.\n\n## How the envelope is built — and why a tree\n\nSeal four cells [3, 1, 4, 1], get asked \"show me the third\", open a 4 — that is one round trip. Fingerprint the whole table as one and a single opening drags the whole table out with it. Hand out one fingerprint per cell and you hand over as many things as there are cells. A **Merkle tree** folds the 4 fingerprints through 2 levels into a single \"root\", and only the root is handed over. An opening is \"the value at that place + the sibling fingerprints needed to climb to the root + which side each sibling is on\", and the verifier recomputes the root the same way and compares. Even at 1000 rows, an opening needs about 10 siblings.\n\n## The strength of the seal lives in three details\n\nThe tree is a tool. The subject is these three:\n\n1. **Order.** What can a prover who learned the question in advance do? In the adaptive checkpoint you personally construct that after-the-fact package — one opening, plus the root and path that let it pass.\n2. **What the leaf binds.** If the index is not inside the leaf, the leaf can claim to be from anywhere. Worse, join the address and the value with no separator and a different (index, value) pair can produce the same leaf. That weak encoding is fixed in the problem setter's materials (fixtures) — what you attack is always the setter's implementation, never a weakness you wrote yourself.\n3. **The path's directions.** If a level does not fix whether the sibling is left or right, the verifier can hash two ways, and the prover picks the convenient one.\n\nTwo words only: **binding** = only the value that first went in can come out. **hiding** = nothing shows until opened. **This envelope guarantees binding only** — the leaf is built from just the index and the value, so if few values are plausible, brute force finds them. When hiding is needed, randomness gets mixed in; that is outside this problem.\n\n## First move\n\n1. In Participant Portal (the site showing this statement) press **起動 (Start)**. The **problem editor** appears on the same page with `commit.py` in it. That one file is the only thing you edit.\n2. Press **Inspect evidence** in the editor. This deployment's evidence appears:\n   - a string proving the container ran (the health token; this problem never asks you to submit it)\n   - **the number of cells** (a power of two, so no level of the tree is ragged) and the **domain** (a short string used in the transcript)\n   - **the index the verifier will ask about**\n   - **the table** — each cell's i, its value, and the start of its leaf fingerprint (the queried index is marked)\n   - the **root**, the **number of tree levels**, and the **path length**\n   - **the path for that index** — for each level, which side the sibling is on and the start of its fingerprint\n3. On paper, trace how two leaf fingerprints in the table make the level above them. Once the relation between the level count and the path length (path = levels − 1) lands, the shape of `merkle_root` and `open_at` is visible.\n\n**That root and path are your own answer key**: if your `commit.py` produces the same root and the same path from the same cells, you are right so far. Grading still runs on settings you have not seen.\n\n## What you answer — eight checkpoints\n\nAll eight are graded **by running your `commit.py`**. This problem has no \"type a value into an answer field\" checkpoint. The function and class stubs are already in `commit.py`.\n\n**How leaves and nodes are built is fixed.** Your root has to match the one shown by Inspect evidence, so build them exactly like this:\n\n- **leaf**: `sha256( b\"leaf/v1\" + index as 4 bytes big-endian + value as 8 bytes big-endian )`\n- **node**: `sha256( b\"node/v1\" + the left child's 32 bytes + the right child's 32 bytes )`\n\nIn Python that is `index.to_bytes(4, \"big\")`, `value.to_bytes(8, \"big\")` and `hashlib.sha256(...).digest()`. You can see for yourself how this shape satisfies the three subjects above, and since `b\"leaf/v1\"` differs from `b\"node/v1\"`, a leaf fingerprint can never coincide with a node fingerprint.\n\n1. **encoding** — `encode_leaf(index, value)` and `node_hash(left, right)`. The first returns the **bytes** a leaf commits to (**two different (index, value) pairs must never give the same bytes**). The second combines two children's bytes into one (**the left/right order must change the result**). `leaf_hash(index, value)` is that leaf's fingerprint.\n2. **root** — `merkle_root(values)`. From the list of cells (its length is a power of two), return the root as **bytes**.\n3. **opening** — `open_at(values, index)` and `verify_opening(root, index, value, path, length)`. `open_at` returns `[{\"hash\": bytes, \"sibling_is_left\": bool}, ...]`, nearest the leaf first. `verify_opening` plays the receiving side: recompute the root the same way and return `True` / `False` for whether it matched. An opening here covers one place at a time — the \"few places\" mentioned earlier means repeating this one-place procedure independently a few times.\n4. **order** — the `Session` class. Make it usable only in the order `commit()` → `receive_challenge(index)` → `open()` (`open()` returns `{\"index\": ..., \"value\": ..., \"path\": [...]}`). **Called out of that order, it must raise `ProtocolError`** (that exception's stub is in `commit.py` too). An index outside the range (negative, or at least the number of cells) must raise `ProtocolError` as well — a negative index would silently point at a different row, so it is stopped here.\n5. **adaptive** — `adaptive_witness(setting)`. The argument `setting` is a dictionary of that round's conditions, **including which place will be asked about**. Return the package a prover who learned the question in advance could build — `{\"values\": [...], \"query\": ...}` — a list that is **rubbish everywhere except the queried place, and yet passes your own `verify_opening` there**. Its length matches the conditions, and `query` matches the question you were handed.\n6. **ambiguity** — `ambiguity_witness()`. No arguments. Return, as `{\"first\": (index, value), \"second\": (index, value)}`, **two pairs the setter's fixed weak encoding cannot tell apart** (`fixtures.generate.weak_leaf`, which joins index and value with no separator). At the same time, **your own `encode_leaf` must be able to tell those two pairs apart**.\n7. **transcript** — `transcript(domain, root, statement)` and `challenge(domain, root, statement, length)`. Instead of needing the verifier present, the question is derived from the envelope itself. `transcript` returns the three joined in a fixed order as **bytes**; `challenge` returns the index derived from it, an **integer in 0 .. length − 1** (the same value however often it is called, and any of the three inputs changing must change the transcript). The same caution as the leaf applies here — join the three with no separator and a differently split triple can give the same bytes. The prover cannot learn the question before sealing, and changing the question changes the seal — which is why \"it was sealed first\" is checkable afterwards from the record alone. If the question is derived from the envelope here, why also forbid the order inside the `Session` of checkpoint 4? Because **the derivation itself assumes the seal came first.** An implementation where `challenge` can be called before the root exists answers with that assumption broken.\n8. **transfer** — the same `commit.py` runs through checkpoints 1–7 all over again on **a setting you have never seen**. An implementation fitted to the values on screen fails here.\n\n**How submitting works**: write `commit.py` and press that checkpoint's **Submit (+points)**; the editor's current contents are sent as they are. The eight are graded separately, so being stuck on one does not block the rest. **A wrong answer comes back with a short note saying which rule broke** (never the expected value) — read it, fix it, and resubmit as often as you like. Each miss costs 15 points (the eight total 300; the total never goes below 0). **Restore starter** puts `commit.py` back to its original state, and **Run public tests** lets you try part of it locally first.\n\n## The rest of the week sits on this envelope\n\nYou are done when all eight pass. You built the envelope yourself (leaf, root, opening, verification), enforced the three-move order in code, derived the question from the envelope, and showed — with two counterexamples you constructed — what becomes possible when the order or the binding is missing.\n\nThe move from checkpoint 7 — deriving the question from the envelope instead of keeping a verifier in the room — is called **Fiat–Shamir**, and it folds the whole dialogue into a single message. What a zkRollup posts on-chain is that one message. And the three drills — SumCheck, FRI, PLONK — all run on \"seal first, open a few points\". The envelope is this week's floor.",
      "writeup": "## The order is the protocol\n\nA commitment only says \"this cannot be changed later\". What gives it meaning is that the challenge arrives **after** it. Reverse them and the prover only has to be right where they already know they will be checked. The vector built in the `adaptive` checkpoint is wrong in fifteen of sixteen positions and its opening verifies.\n\n## What has to be in a leaf\n\n**The index.** Without it a leaf makes no claim about where it came from, and the prover can present it as having come from wherever suits.\n\n**Field boundaries.** Run the index and value together and `(1, 23)` and `(12, 3)` both render as \"123\": two different claims, one commitment. The weak encoding lives in the fixtures — breaking code you deliberately weakened yourself is not a counterexample.\n\n**Direction.** Without knowing which side each sibling is on, a verifier can hash `(sibling, node)` or `(node, sibling)`, and the prover picks whichever reaches the root.\n\n## A note on equivalent mutants\n\nThe index-range and path-length checks in `verify_opening` cannot be caught if removed. `LEAF_TAG` and `NODE_TAG` already mean a leaf hash never equals a node hash, so a path of the wrong length recomputes to something that is not the root and the comparison rejects it anyway. They are not in the mutation suite.\n\nThe range check in `Session.receive_challenge` is mutated instead — there a negative index silently wraps and the prover opens a row nobody asked about, which is detectable.\n\nLeaving an unkillable mutant in the list teaches that a \"SURVIVED\" line can be ignored. So it is not left in.\n\n## This is not a polynomial commitment\n\nA Merkle root commits to a vector. It does not prove anything about a polynomial's evaluation, and one opening says nothing about the rows nobody asked about. There is a single query here, so a guessing prover wins with probability 1/length — soundness amplification is not covered.\n\n## Binding and hiding are different\n\nA Merkle root gives binding. It does not give hiding: with a small value space, the contents can be recovered from the root by brute force. Hiding needs separate randomness in each leaf.",
      "learningGoals": [
        "Tell the three moves — commit, challenge, open — apart, order included",
        "Show what a prover who knew the question in advance can do, with a working after-the-fact package",
        "Verify one opening yourself from a root and a path",
        "Say why index, value and direction must each be bound to the seal, via what breaks without them",
        "Explain why binding and hiding are different properties",
        "Say what must go into the transcript when the question is derived from the envelope",
        "Explain why one opened place says nothing about the rows never asked about"
      ],
      "checks": [
        {
          "id": "encoding",
          "label": "Make one leaf mean one thing",
          "hints": [
            {
              "id": "encoding-hint-1",
              "content": "If the same value sits at a different position, should the encoding change? And how do you lay two fields out so they cannot be misread?"
            }
          ]
        },
        {
          "id": "root",
          "label": "Build the commitment"
        },
        {
          "id": "opening",
          "label": "Open one position and verify it",
          "hints": [
            {
              "id": "opening-hint-1",
              "content": "At each step, is the sibling on the left or the right? A verifier that does not know can hash it two ways."
            }
          ]
        },
        {
          "id": "order",
          "label": "Enforce the order"
        },
        {
          "id": "adaptive",
          "label": "Show what a challenge-first protocol allows",
          "hints": [
            {
              "id": "adaptive-hint-1",
              "content": "If you already know which position will be checked, what do you put in the others?"
            }
          ]
        },
        {
          "id": "ambiguity",
          "label": "Show the encoding ambiguity with a counterexample",
          "hints": [
            {
              "id": "ambiguity-hint-1",
              "content": "`weak_leaf` runs the index and value together with no separator. Find two pairs that render the same."
            }
          ]
        },
        {
          "id": "transcript",
          "label": "Derive the challenge from the transcript",
          "hints": [
            {
              "id": "transcript-hint-1",
              "content": "If the commitment is not in the transcript, the challenge could have been computed before committing."
            }
          ]
        },
        {
          "id": "transfer",
          "label": "Hold up in a setting you have not seen"
        }
      ]
    }
  }
}
