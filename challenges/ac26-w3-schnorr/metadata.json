{
  "id": "ac26-w3-schnorr",
  "name": "何をハッシュに入れ忘れたか",
  "category": "Challenge",
  "status": "draft",
  "visibility": "public",
  "track": {
    "id": "advanced-cryptography-2026",
    "order": 330,
    "chapter": "Week 3 / Sigma Protocol and Fiat-Shamir"
  },
  "courseAlignment": {
    "courseId": "advanced-cryptography-program",
    "edition": "2026",
    "week": 3,
    "role": "assignment-companion",
    "spoilerPolicy": "independent-reimplementation",
    "sources": [
      {
        "repository": "zk-tokyo/advanced-cryptography-2026",
        "ref": "a3aa4b56fa88fbe803b57d320fbc87c1a203b480",
        "path": "week3/README.md",
        "kind": "lecture"
      },
      {
        "repository": "zk-tokyo/advanced-cryptography-2026",
        "ref": "a3aa4b56fa88fbe803b57d320fbc87c1a203b480",
        "path": "week3/problems/schnorr-from-scratch/README.md",
        "kind": "assignment"
      },
      {
        "repository": "zk-tokyo/advanced-cryptography-2026",
        "ref": "b1b46669949b5615be7941b2b62da4ea635e9c30",
        "path": "week3/week3_zksnark_slides.pdf",
        "kind": "slide"
      }
    ]
  },
  "difficulty": 5,
  "estimatedDuration": "75-105 分",
  "shortDescription": "「秘密を知っている」を、その秘密を 1 度も送らずに相手に納得させたい。使い捨ての乱数で秘密を覆う 3 手を、ハッシュ 1 本で 1 通の署名に畳む。ハッシュに入れ忘れたものは何一つ守られない——domain separator を落とすと、1 つの署名が 2 つのプロトコルで通る。",
  "description": "Week 3 の 3 問目、 role は assignment-companion。 群は前問で作ったものを与え、 participant が書くのは protocol と serialization と Fiat-Shamir 変換に絞ってある。\n\nこの問題の主題は 「何をハッシュに入れるか」 と 「どう並べるか」 の 2 点で、 どちらも正常系では一切現れない。 domain・message・commitment・public key のどれを落としても sign と verify は完璧に動く。 だから checkpoint はそこを直接攻める。\n\ncross-protocol checkpoint は ASSESSMENT.md が assignment-companion に要求する counterexample 型で、 participant は 「1 つの署名が 2 つの domain で通る」 witness を構成する。 弱い challenge (domain separator を落としたもの) は fixtures 側に固定してあり、 participant 自身の弱いコードを攻める答えは成立しない。\n\nserialization は length prefix の有無を検査する。 参照実装は可変長 2 フィールド (domain と message) を**隣接**させてある。 固定長の点表現を間に挟むと、 length prefix を落としても衝突には点の並びの偶然が要るようになり、 「prefix は飾り」 と自分を納得させられてしまうため。 隣接させてあることで ('ab','cd') と ('a','bcd') が決定的に衝突する。\n\n**群位数と確率の扱い**: toy 群は n が 29〜43 しかない。 Schnorr の偽造成功確率は 1/n なので、 「message を 1 byte 変えて verify」 は正しい実装でも 40 回に 1 回通る。 したがって拒否側の assertion は secp256k1 で回し、 toy 群では受理側と、 ハッシュ入力 (preimage) が変わることだけを検査する。 preimage の比較は決定的で、 challenge 値の比較は 1/n で衝突するため。 この設計自体を writeup で説明している。\n\nsecret key と nonce は show.py が出力しない。 protocol が守る対象そのものであり、 それを表示する lab は逆のことを教えるため。",
  "instructions": "## あなたの仕事\n\n公開情報だけでメッセージを確かめられるよう、配布された署名関数を直します。Participant Portalで「起動」し、問題エディタのschnorr.pyを開きます。まずpublic_keyを書き、「公開テストを実行」で未完成の関数を確認。「keygenを提出」で最初の項目が採点されます。\n\n## パスワードを送らずに「知っている」と示す\n\n「私はこの秘密を知っている」を、**秘密を 1 度も送らずに** 相手に納得させたい。ハッシュにかけて送るのでもない——見せずに、それでも相手は確信する。これがパスキーがログインでやっていることであり、電子署名の中身です。\n\n背骨でいえば、x が **自分だけが知る答え**。P = x·G は公開しますが、x は出しません。仕掛けは 3 手です。使い捨ての乱数 k を引いて **R = k·G** を先に渡す(コミット)→ 相手が数 e を出す(チャレンジ)→ **z = k + e·x** を返す(レスポンス)。相手は **z·G = R + e·P** を確かめるだけ。成り立つ理由は中 1 の分配法則 1 行——z·G = (k + e·x)·G = k·G + e·(x·G) = R + e·P。x も k も知らないまま、等式だけで納得できます。\n\n## 会話を、ハッシュ 1 本で 1 通に畳む\n\n3 手は会話です。相手がいなければ e が出てこない。そこで **e を相手からもらうかわりに、ハッシュで作る**:`e = H(domain, R, P, message)`。往復が消え、渡すものは (R, z) だけ。message がハッシュに入っているので、これは **その message への署名** になります(Fiat–Shamir 変換)。\n\n## 前提(点の足し算が不安なら、先に ec-group へ)\n\n- **割る数が 2 つ**: 点の座標は **p**(曲線の乗る数)、z や e などスカラーは **n**(G の位数 = 何回足すと O に戻るか)。**別の数** で、取りちがえると「たまに通る」実装になります(`(mod n)` は Python の `% n`)。\n- **群は用意済み**: 点の足し算と k 倍は `ac26-w3-ec-group` で作ったものを `group` として使います(`group.generator`・`group.n`・`group.contains`・`group.point`・`group.infinity`・`点.scalar_mul(k)`)。\n- **言葉**: 秘密鍵 x、公開鍵 P = x·G、nonce k(署名ごとの使い捨て)、ハッシュ(同じ入力なら必ず同じ数。ただし違う入力でも同じ出力になる衝突があり、1バイト変わっても出力が必ず違う保証ではありません。ここでは SHA-256)、domain separator(どのプロトコルの署名かを表す札)。checkpoint = 8 つ。\n\n## 紙で 1 回(p = 23, a = 1, b = 4, G = (0, 2), n = 29)\n\n秘密 x = 7 なら P = 7G = (15, 6)。k = 3 なら R = 3G = (11, 9)。e = 5 なら z = 3 + 5×7 = 38 を **29** で割って z = 9。検証: 9G = (4, 7)、R + 5P = (11, 9) + 5(15, 6) = (4, 7) で一致。**座標は 23 の世界、z は 29 の世界** です——ここを取りちがえた実装は公開テストを通り、採点で落ちます。\n\n## 山は「何をハッシュに入れ忘れたか」\n\n**ハッシュに入れなかったものは、その署名が何も約束していないもの** です。domain・message・R・P のどれを落としても、正しい人の署名は正しく通り続ける——正常系だけ見ても気づけません。この問題の最後では、**domain separator だけを落としたハッシュ** を相手取り、「1 つの署名が 2 つの別プロトコルで通ってしまう」ことを、自分で作った具体例で示します。\n\n## 最初の一手\n\nParticipant Portal で **「起動」** を押すと、同じ画面に **問題エディタ**(`schnorr.py`)が出ます。\n\n**「証拠を確認」** を押すと 6 つ: ①health token(提出しない)②練習用の群(式、G、位数 n。「スカラーはここに住む、mod p ではない」の注意つき)③あなたの主張(P = x·G の座標)④秘密 x と nonce k は表示されない(守る対象だから)⑤2 つの domain 文字列(最後の checkpoint 用)⑥protocol の 4 行(R・e・z・検証式)と secp256k1 の n(練習用の群は n が小さく、でたらめでも 1/n で通るので、「まぐれで通ってはいけない」検査は secp256k1 側、という注意も)。\n\n**紙で**(提出欄なし): 上の例を、その例の n=29 のままなぞり、**z を割る数はどちらか** を指で確かめてから書き始めてください。\n\n## 答えるもの — checkpoint は 8 つ\n\n書き換えるのは `schnorr.py` だけ。エラー名 `InvalidKey`・`InvalidEncoding` と 12 個の関数枠は入っています。`group.as_public()` が `p`・`a`・`b`・`n`・`gx`・`gy` と、符号化幅 `coordinate_bytes`・`scalar_bytes` を返します。\n\n1. **keygen**(30) — `public_key(secret, group)` は P = x·G を返し、x が 1〜n−1 でなければ `InvalidKey`。`validate_public_key(point, group)` は公開鍵として使えるかを True/False で返す。**曲線の上にあるだけでは足りません**——O も曲線上ですが公開鍵には使えず、別の曲線の点も断ります。\n2. **sigma**(40) — `commit(nonce, group)` は R = k·G。`respond(nonce, challenge, secret, group)` は z = k + e·x を **0 以上 n 未満**(p ではない)の整数で返す。\n3. **transcript**(35) — `verify_transcript(public, commitment, challenge, response, group)` が z·G = R + e·P を確認。正直な組は通し、z・e・R のどれかを変えた組と、公開鍵が O の組は落とす。\n4. **serialization**(40) — `encode_point(point, group)` は **固定長**(x・y を `coordinate_bytes` バイトずつ、計 2 倍)。`decode_point(raw, group)` は逆で、**形が違うもの**(座標が p 以上、長さ不足)は `InvalidEncoding`。さらに `challenge_preimage(domain, commitment, public, message, group)` は、**`(\"ab\", …, b\"cd\")` と `(\"a\", …, b\"bcd\")` が同じバイト列にならないこと**(隣接する2項の一般例は b\"ab\"+b\"cd\" == b\"a\"+b\"bcd\"。このテストで途中に点の符号化を挟むと、長さなしでも必ず衝突するとは限りません)。\n5. **fiat-shamir**(45) — `challenge(...)` は 0 以上 n 未満の整数で、同じ入力から必ず同じ値。そして **domain・R・P・message のどれを 1 つ変えても、ハッシュに入るバイト列が変わること**(練習用の群は「入力が変わること」で、値そのものは secp256k1 で確かめられます)。\n6. **sign-verify**(40) — `sign(secret, nonce, message, domain, group)` は `(R, z)`、`verify(public, message, signature, domain, group)` は True/False。正直な署名は通り、secp256k1 で message・domain・公開鍵・R・z のどれを変えても落ちること。\n7. **cross-protocol**(40) — `cross_protocol_witness(group)` が domain_a・domain_b・message・secret・nonce をキーとし、それぞれの値を持つ辞書を返す(domain は別々の文字列、message は `bytes`、secret と nonce は 1〜n−1)。採点側は、**domain をハッシュに入れない弱いチャレンジ**(`fixtures` 側に固定、あなたのコードではない)でその鍵と nonce から署名を 1 つ作り、それが **両方の domain で通ってしまう** ことを確かめます。同時に、あなたの `challenge_preimage` が 2 つの domain で違うバイト列を出すことも見ます。弱さは相手側にあるので、自分のコードを弱くして攻める答えは不可。\n8. **transfer**(30) — 同じコードが secp256k1 でそのまま動くこと。パラメータは採点側が渡します。\n\n各 checkpoint の **「提出 (+N pt)」** を押すと、そのときの `schnorr.py` が送られて採点(打ち込む欄なし)。8 つは独立採点、まちがえるごとに −15 点。**「公開テストを実行」** は下見、**「初期状態に戻す」** で元に戻ります。\n\n## この署名が、次の 2 つの落とし穴の土台\n\n3 手の会話がハッシュ 1 本で 1 通の署名になり、変更を検証で検出し（小さい群では偶然同じ質問値になることもあります）、、**ハッシュから 1 つ落とすと何が壊れるか** をコードと具体例で示せたら終わりです。ここで組んだものは実際に使われている署名とほぼ同じ形。次の 2 問は、この署名の落とし穴を見ます——`ac26-w3-nonce-reuse` は乱数 k の使い回し、`ac26-w3-passkey-assertion` は「署名が正しいだけでは足りない」です。\n\n## 式をPythonへ移す道具\n\n点の座標は `point.x` と `point.y` です。Oは足しても点が変わらない点で、`group.infinity()` で作れます。公開鍵からOを除外し、`group.contains(point)` で点を検査します。secp256k1は移植の確認に使う大きな曲線の名前です。数を固定せずgroupから読みます。\n\n幅wの座標は `x.to_bytes(w,\"big\")`、逆は `int.from_bytes(raw,\"big\")`。bigは大きい桁のバイトが先という意味です。domain文字列は `domain.encode(\"utf-8\")` でバイト列にします。各バイト列bの前へ `len(b).to_bytes(4,\"big\")` を付けてからbを並べれば、境界を区別できます。隣接する `b\"ab\"+b\"cd\"` と `b\"a\"+b\"bcd\"` が同じになる混同を防げます。座標の範囲はgroup.pointへ渡す前に検査します。\n\n質問は `int.from_bytes(hashlib.sha256(preimage).digest(),\"big\") % group.n`。preimageはdomain、R、P、messageを含むchallenge_preimageの出力です。小さい群では別入力が同じ余りになることもあります。\n\n辞書には値も書きます。例えば `{\"domain_a\":\"login\", \"domain_b\":\"payment\", \"message\":b\"ok\", \"secret\":1, \"nonce\":2}`。secretとnonceは渡された群の範囲内で選びます。自分のchallenge_preimageは用途を区別し続けます。challengeの返り値は質問の数であり、合否ではありません。\n\n\n入力条件：public_keyとcommitは、整数でない値（True/Falseも含む）と1〜n−1の範囲外をInvalidKeyで拒否します。respondへ渡すnonce・challenge・secretは整数で、結果をnで割った余りにします。verify_transcriptは使えない点と0〜n−1の範囲外のresponseを拒否し、整数のchallengeはnで割った余りにして点を掛けます。これが本問の計算用インターフェースです。\n",
  "writeup": "## 入れ忘れたものは守られない\n\nchallenge に含めなかったものは、 署名が何も主張していないものになる。 message を入れなければ同じ署名が任意の message に付く。 public key を入れなければ鍵の付け替えができる。 commitment を入れなければ Fiat-Shamir が Fiat-Shamir でなくなる。 domain を入れなければ、 別のプロトコル用の署名がこちらでも通る。\n\nどれも正常系では起きない。 sign して verify すれば全部通る。 mutation suite の 10 個中 5 個がこの型で、 だから checkpoint は正常系を確認したあと直接そこを攻める。\n\n## cross-protocol counterexample\n\ndomain separator を落とした challenge を fixtures 側に固定してある (`weak_challenge`)。 participant はそれに対して 1 つの署名が 2 つの domain で通る witness を構成する。 弱い challenge を participant 自身に書かせないのは、 「自分の弱いコードを自分で破る」 が答えになってしまわないようにするため。\n\nそのうえで、 participant 自身の challenge が 2 つの domain で**異なるバイト列をハッシュする**ことを検査する。 攻撃を作れることと、 自分の実装がそれを防いでいることは別の主張であり、 両方が要る。\n\n## length prefix と隣接\n\n可変長フィールドを長さ無しで連結すると、 ('ab', 'cd') と ('a', 'bcd') が同じバイト列になる。 異なる 2 つの主張に 1 つの証明が付く。\n\n参照実装は domain と message を**隣接**させてある。 間に固定長の点表現を挟む並びでも原理的には不健全だが、 衝突には点の並びの偶然が要るようになり、 レビューで 「実際には起きないから prefix は飾り」 と結論できてしまう。 隣接させれば衝突は決定的で、 その議論の余地が無い。\n\n## 群位数と確率\n\ntoy 群は位数が 29〜43 しかない。 Schnorr の偽造成功確率は 1/n なので、 「message を 1 byte 変えて verify」 は**正しい実装でも** 40 回に 1 回通る。 これはコードの欠陥ではなくパラメータの性質であり、 実際に作問中に踏んだ。\n\nしたがって:\n\n- 受理側 (正直な署名が通る) は toy 群で検査する。 決定的だから。\n- 拒否側 (改変した署名が落ちる) は secp256k1 で検査する。 1/n が無視できるから。\n- 「binding input を変えると challenge が変わる」 は toy 群では **preimage** を比較する。 challenge 値は 1/n で衝突するが、 ハッシュ入力が変わることは決定的だから。\n\nテストが確率で落ちる設計は、 学習者に 「たまに落ちるので再実行する」 を教えてしまう。 それは採点の失敗である。\n\n## 署名は暗号化ではない\n\n検証式が成立しても message は隠れていない。 message は verifier が持っている前提であり、 署名はそれに対する主張である。 「valid な式 = 機密性」 は成り立たない。\n\n## 次につながるところ\n\n次の問題は nonce 再利用。 ここで z = k + e*x を書いたので、 同じ k で 2 つの署名を作ると連立方程式になり x が解けることが直接見える。",
  "tags": [
    "advanced-cryptography-2026",
    "week3",
    "schnorr",
    "fiat-shamir",
    "sigma-protocol",
    "local-play",
    "container"
  ],
  "exposedPorts": [
    {
      "port": 18102,
      "name": "Portal editor API + /verify (loopback only)"
    }
  ],
  "learningGoals": [
    "公開鍵 P = x·G と秘密鍵 x の関係を、一方通行として説明できる",
    "コミット・チャレンジ・レスポンスの 3 手を実装できる",
    "検証式 z·G = R + e·P の左辺と右辺を点として突き合わせられる",
    "Fiat–Shamir で、往復の会話をハッシュ 1 本の署名に畳める",
    "domain・message・R・P をハッシュに縛る必要を、反例で示せる",
    "可変長の項をそのまま連結すると符号化が曖昧になる条件を説明できる"
  ],
  "runtime": {
    "provider": "docker",
    "engine": "compose",
    "entry": "local/docker-compose.yml",
    "verifyUrl": "http://127.0.0.1:18102/verify",
    "secretEnv": [
      "FLAG_SEED"
    ]
  },
  "scoring": {
    "kind": "multi-verify",
    "checks": [
      {
        "id": "keygen",
        "input": "multiline",
        "label": "鍵を作り、使えない鍵を断る",
        "points": 30,
        "wrongAnswerPenalty": 15,
        "hints": [
          {
            "id": "keygen-hint-1",
            "content": "公開鍵は、秘密の回数だけ基準の点Gを足した点です。足しても何も変わらない点Oは、秘密の所有を示す公開鍵には使いません。",
            "penalty": 2
          },
          {
            "id": "keygen-hint-2",
            "content": "n=7の計算模型なら秘密は1〜6です。x=3の公開鍵はG+G+G。x=0はOになるので拒否します。実際の点は配布されたgroupで計算します。",
            "penalty": 2
          },
          {
            "id": "keygen-hint-3",
            "content": "schnorr.pyのpublic_keyでsecretが整数で1以上group.n未満か検査し、group.generator.scalar_mul(secret)を返します。validate_public_keyではgroup.contains(point)とpoint.is_infinityを確認し、不適切な点はFalse。keygenを提出します。",
            "penalty": 2
          }
        ]
      },
      {
        "id": "sigma",
        "input": "multiline",
        "label": "3 手を実装する",
        "points": 40,
        "wrongAnswerPenalty": 15,
        "hints": [
          {
            "id": "sigma-hint-1",
            "content": "先に乱数から点を送り、質問を受けてから秘密と乱数を混ぜた数を返します。毎回の乱数が、返す数から秘密を直接読むことを防ぐ役を持ちます。",
            "penalty": 2
          },
          {
            "id": "sigma-hint-2",
            "content": "整数部分だけn=7、k=2、e=3、x=2で試すと、z=(2+3×2)を7で割った余り=1です。Pythonでは(nonce+challenge*secret)%group.n。座標を割るpとは別です。",
            "penalty": 2
          },
          {
            "id": "sigma-hint-3",
            "content": "commitは1〜n−1のnonceからgroup.generator.scalar_mul(nonce)を返します。respondはnonce、challenge、secretで足し算と掛け算をし、group.nで余りを取ります。sigmaを提出します。",
            "penalty": 2
          }
        ]
      },
      {
        "id": "transcript",
        "input": "multiline",
        "label": "検証式の両辺を突き合わせる",
        "points": 35,
        "wrongAnswerPenalty": 15,
        "hints": [
          {
            "id": "transcript-hint-1",
            "content": "検証者は秘密や乱数を知りません。公開された2点と質問・返答から、等しいはずの2点を別々に計算します。",
            "penalty": 2
          },
          {
            "id": "transcript-hint-2",
            "content": "zG=(k+ex)G=kG+e(xG)=R+ePです。n=7の整数模型でk=2,e=3,x=2なら、左は1G、右は8G=1G。最後の等号は7G=Oによります。",
            "penalty": 2
          },
          {
            "id": "transcript-hint-3",
            "content": "publicとcommitmentを検査し、responseは0〜n−1の整数に限ります。整数のchallengeはgroup.nで余りを取ります。group.generator.scalar_mul(response)とcommitment+public.scalar_mul(challenge%group.n)を比較し、True/Falseを返してtranscriptを提出します。",
            "penalty": 2
          }
        ]
      },
      {
        "id": "serialization",
        "input": "multiline",
        "label": "一意な符号化を書く",
        "points": 40,
        "wrongAnswerPenalty": 15,
        "hints": [
          {
            "id": "serialization-hint-1",
            "content": "ハッシュへ渡す前に、点や文字をバイト列へ直します。境界を決めないと違う入力が同じ列になり、何に署名したかを区別できません。",
            "penalty": 2
          },
          {
            "id": "serialization-hint-2",
            "content": "文字列abとcdを並べたabcdは、aとbcdでも作れます。各文字列の前に長さを付けると2:ab,2:cdと1:a,3:bcdで別になります。点はxとyを同じ決まった幅で並べます。",
            "penalty": 2
          },
          {
            "id": "serialization-hint-3",
            "content": "group.as_public()[\"coordinate_bytes\"]を幅wとし、座標をto_bytes(w,\"big\")で並べます。decode_pointは長さ2*wと座標範囲を検査してgroup.pointへ戻します。challenge_preimageはdomain.encode()、R、P、messageを境界付きで連結します。serializationを提出します。 group.pointで作った点もgroup.contains(point)で曲線上か確認し、偽ならInvalidEncodingにします。",
            "penalty": 2
          }
        ]
      },
      {
        "id": "fiat-shamir",
        "input": "multiline",
        "label": "challenge を transcript から作る",
        "points": 45,
        "wrongAnswerPenalty": 15,
        "hints": [
          {
            "id": "fiat-shamir-hint-1",
            "content": "相手が質問を返す代わりに、署名の対象と先に出した点から質問を計算します。ハッシュに含めた情報が変わると入力列も変わります。",
            "penalty": 2
          },
          {
            "id": "fiat-shamir-hint-2",
            "content": "質問を作る計算はe=int.from_bytes(hashlib.sha256(preimage).digest(),\"big\")%nです。整数化した結果を仮に9、n=7とすればe=2。同じ余りになる別入力もあり、毎回違う数になる保証ではありません。",
            "penalty": 2
          },
          {
            "id": "fiat-shamir-hint-3",
            "content": "challengeでchallenge_preimage(domain,commitment,public,message,group)を呼び、SHA-256の結果を整数にしてgroup.nで余りを取ります。4種類の入力を順に変え、ハッシュ前の列が変わるか確かめてfiat-shamirを提出します。",
            "penalty": 2
          }
        ]
      },
      {
        "id": "sign-verify",
        "input": "multiline",
        "label": "署名して検証する",
        "points": 40,
        "wrongAnswerPenalty": 15,
        "hints": [
          {
            "id": "sign-verify-hint-1",
            "content": "署名側は秘密と乱数を使い、検証側は公開鍵だけを使います。両者が同じ対象から同じ質問を作ることで、メッセージに結び付きます。",
            "penalty": 2
          },
          {
            "id": "sign-verify-hint-2",
            "content": "署名はR=kG、e=H(domain,R,P,message)、z=(k+ex)の余りから(R,z)。検証は受け取ったRと公開鍵Pでeを作り直しzG=R+ePを確かめます。秘密xを検証側へ渡しません。",
            "penalty": 2
          },
          {
            "id": "sign-verify-hint-3",
            "content": "signでpublic_key→commit→challenge→respondの順に呼びます。verifyはsignatureをR,zへ分け、受け取ったpublicからchallengeを作り、verify_transcriptへ渡します。不正な形はFalse。sign-verifyを提出します。",
            "penalty": 2
          }
        ]
      },
      {
        "id": "cross-protocol",
        "input": "multiline",
        "label": "domain を落とすと何が起きるか示す",
        "points": 40,
        "wrongAnswerPenalty": 15,
        "hints": [
          {
            "id": "cross-protocol-hint-1",
            "content": "用途の名前domainを署名へ含めないと、別用途へ署名を持ち込めます。自分の正常な実装を弱くせず、domainを省く相手の方式の反例を作ります。",
            "penalty": 2
          },
          {
            "id": "cross-protocol-hint-2",
            "content": "domain_a=\"login\"とdomain_b=\"payment\"は別用途です。同じR,P,messageからdomainなしで作った質問は両方同じです。domainを含む自分のchallenge_preimageはこの2つで別の列になります。",
            "penalty": 2
          },
          {
            "id": "cross-protocol-hint-3",
            "content": "cross_protocol_witnessで別々のdomain_a/domain_b、bytesのmessage、1〜group.n−1のsecret/nonceを辞書にします。採点側は弱い質問で署名を作ります。自分のpreimageが用途を区別するままcross-protocolを提出します。",
            "penalty": 2
          }
        ]
      },
      {
        "id": "transfer",
        "input": "multiline",
        "label": "実運用パラメータでも動かす",
        "points": 30,
        "wrongAnswerPenalty": 15,
        "hints": [
          {
            "id": "transfer-hint-1",
            "content": "小さい練習用の数でできた理由を、そのまま大きい群でも保ちます。特定の点や割る数を覚えて返す方法は移植できません。",
            "penalty": 2
          },
          {
            "id": "transfer-hint-2",
            "content": "z=k+exを割る数は常にgroup.n、座標の範囲はgroup.pです。整数の幅が変わってもto_bytesへ渡す幅はgroup.as_public()から取得します。",
            "penalty": 2
          },
          {
            "id": "transfer-hint-3",
            "content": "schnorr.pyから練習用p,nとバイト幅の固定値を除き、渡されたgroupの値を使います。署名→検証、符号化→復号の往復を公開テストで確認しtransferを提出します。",
            "penalty": 2
          }
        ]
      }
    ]
  },
  "i18n": {
    "en": {
      "name": "What did you leave out of the hash",
      "shortDescription": "You want to convince someone you know a secret without ever sending it. Cover the secret with a throwaway random number across three moves, then fold them into one signature with a single hash. Whatever you leave out of the hash is not protected — drop the domain separator and one signature is valid under two protocols.",
      "description": "Third problem of Week 3; its role is assignment-companion. The group is supplied from the previous problem; what the participant writes is narrowed to the protocol, the serialization and the Fiat-Shamir transform.\n\nThe problem's two subjects — what goes into the hash, and how it is laid out — never show up on the happy path. Drop the domain, the message, the commitment or the public key and sign/verify still work perfectly. So the checkpoints attack exactly there.\n\nThe cross-protocol checkpoint is the counterexample form ASSESSMENT.md requires of an assignment-companion: the participant constructs a witness that one signature verifies under two domains. The weak challenge (with the domain separator dropped) is fixed on the fixtures side, so an answer that attacks the participant's own weak code cannot count.\n\nSerialization checks for the length prefix. The reference layout puts the two variable-length fields (domain and message) **adjacent** — with a fixed-length point encoding between them, dropping the prefix would need a coincidence in the point bytes to collide, letting you convince yourself the prefix is decoration. Adjacent, ('ab','cd') and ('a','bcd') collide deterministically.\n\n**Group order and probability**: the toy groups have n of only 29–43. Schnorr's forgery probability is 1/n, so \"flip one byte of the message and verify\" passes one time in 40 even for a correct implementation. Rejection-side assertions therefore run on secp256k1; on the toy groups only acceptance and the change of hash input (preimage) are checked — preimage comparison is deterministic while challenge comparison collides at 1/n. The writeup explains this design choice itself.\n\nshow.py never prints the secret key or the nonce. They are what the protocol protects, and a lab that displayed them would teach the opposite.",
      "instructions": "## Your task\n\nRepair the supplied signature functions so a verifier can check a message using only public information. Start the problem in Participant Portal, then open schnorr.py in the problem editor. Implement public_key first and run the public tests; they show which function still fails. Submit keygen to have the first checkpoint checked.\n\n## Prove you know a password without sending it\n\nYou want to convince someone \"I know this secret\" **without ever sending it** — not hashed, not once — and still have them believe you. That is what a passkey does at login, and what a signature does inside. In the backbone's terms, x is the answer **only you know**: P = x·G is public, x is not. The trick is three moves. Draw a throwaway nonce k and hand over **R = k·G** first (commit); the other side produces a number e (challenge); you return **z = k + e·x** (response). They check just **z·G = R + e·P**. It holds by one line of the distributive law — z·G = (k + e·x)·G = k·G + e·(x·G) = R + e·P — so they are convinced knowing neither x nor k.\n\n## Fold the conversation into one message with a hash\n\nThe three moves are a conversation: no other party, no e. So instead of receiving e you **compute it with a hash**: `e = H(domain, R, P, message)`. The round trip disappears, you hand over just (R, z), and because the message is inside the hash, this *is* a signature on that message (the Fiat–Shamir transform).\n\n## Before you start (shaky on point addition? do ec-group first)\n\n- **Two divisors**: point coordinates live mod **p** (the curve's number); scalars like z and e live mod **n** (the order of G — how many additions return to O). They are **different numbers**, and swapping them gives an implementation that passes \"sometimes\" (`(mod n)` is Python's `% n`).\n- **The group is ready-made**: the point addition and \"k times\" you built in `ac26-w3-ec-group` arrive as `group` (`group.generator`, `group.n`, `group.contains`, `group.point`, `group.infinity`, `point.scalar_mul(k)`).\n- **Words**: secret key x, public key P = x·G, nonce k (throwaway per signature), hash (same input → same number; different inputs can share an output, called a collision; here SHA-256), domain separator (a label naming which protocol a signature belongs to). checkpoint = eight.\n\n## Once on paper (p = 23, a = 1, b = 4, G = (0, 2), n = 29)\n\nSecret x = 7 gives P = 7G = (15, 6). k = 3 gives R = 3G = (11, 9). e = 5 gives z = 3 + 5×7 = 38 reduced by **29** to z = 9. Check: 9G = (4, 7) and R + 5P = (11, 9) + 5(15, 6) = (4, 7) — a match. **Coordinates live mod 23, z lives mod 29** — swap those and your implementation passes the public tests and fails the grading.\n\n## The crux: what did you forget to put in the hash?\n\n**Whatever you left out of the hash is whatever the signature promises nothing about.** Drop the domain, the message, R or P, and honest signatures still verify perfectly — the working path never shows it. At the end you take on a hash with **only the domain separator removed** and demonstrate, with a case of your own construction, that one signature then passes under two different protocols.\n\n## First move\n\nIn Participant Portal, select **起動 (Start)**; the problem editor (`schnorr.py`) appears on the same page.\n\nPress **Inspect evidence**. Six things: ① a health token (not submitted), ② the practice group (equation, G, order n, with \"scalars live here, not mod p\"), ③ your statement (P = x·G coordinates), ④ your secret x and nonce k are not printed (they are what this protects), ⑤ two domain strings (for the last checkpoint), ⑥ the protocol's four lines (R, e, z, the verification equation) and secp256k1's n, with a note that a practice group is small enough that a nonsense signature passes by luck with probability 1/n — so the \"must not pass by luck\" checks run over secp256k1.\n\n**On paper** (nothing to submit): walk the example above with its own n=29, and put your finger on **which divisor reduces z** before writing any code.\n\n## What you answer — eight checkpoints\n\n`schnorr.py` is the only file you edit. The error names `InvalidKey` and `InvalidEncoding` and the twelve function skeletons are there. `group.as_public()` returns `p`, `a`, `b`, `n`, `gx`, `gy` and the encoding widths `coordinate_bytes`, `scalar_bytes`.\n\n1. **keygen** (30) — `public_key(secret, group)` returns P = x·G and raises `InvalidKey` unless x is in 1..n−1. `validate_public_key(point, group)` returns True/False for \"usable as a public key\". **Being on the curve is not enough** — O is on the curve but unusable, and a point from another curve is refused.\n2. **sigma** (40) — `commit(nonce, group)` is R = k·G. `respond(nonce, challenge, secret, group)` returns z = k + e·x as an integer in **0..n−1** (not p).\n3. **transcript** (35) — `verify_transcript(public, commitment, challenge, response, group)` checks z·G = R + e·P: honest triples pass; a triple with z, e or R altered fails; a public key of O fails.\n4. **serialization** (40) — `encode_point(point, group)` is **fixed-width** (x and y as `coordinate_bytes` bytes each, twice that total). `decode_point(raw, group)` is the inverse and raises `InvalidEncoding` for anything off-form (a coordinate at or above p, a truncated string). And `challenge_preimage(domain, commitment, public, message, group)` must make **`(\"ab\", …, b\"cd\")` and `(\"a\", …, b\"bcd\")` produce different bytes** (a general ambiguity example for adjacent fields is b\"ab\"+b\"cd\" == b\"a\"+b\"bcd\"; the point encodings between fields in this test do not necessarily collide).\n5. **fiat-shamir** (45) — `challenge(...)` returns an integer in 0..n−1 and is deterministic, and **changing any one of domain, R, P or the message must change the bytes going into the hash** (asserted on the input for the small practice groups; the values themselves are compared over secp256k1).\n6. **sign-verify** (40) — `sign(secret, nonce, message, domain, group)` returns `(R, z)` and `verify(public, message, signature, domain, group)` returns True/False. Honest signatures pass, and over secp256k1 changing the message, domain, public key, R or z must make it fail.\n7. **cross-protocol** (40) — `cross_protocol_witness(group)` returns a dictionary with keys `domain_a`, `domain_b`, `message`, `secret`, `nonce` and their values (two different domain strings, a `bytes` message, secret and nonce in 1..n−1). The grader takes **a weak challenge that leaves the domain out of the hash** (fixed on the fixtures side, not your code), builds one signature from your key and nonce, and confirms it **verifies under both domains** — while confirming your own `challenge_preimage` gives different bytes for the two. The weakness is pinned on the other side, so weakening your own code is not an answer.\n8. **transfer** (30) — the same code runs on secp256k1. The grader supplies the parameters.\n\nEach checkpoint's **提出 (Submit, +N pt)** sends the current `schnorr.py` and grades it (nothing to type). The eight are graded independently; each wrong try costs 15 points. **Run public tests** is a preview; **Restore starter** brings back the original.\n\n## This signature is the ground under the next two traps\n\nYou are done when the three-move conversation becomes one signature through one hash, detects the tested alterations of message, domain, public key, R and z (small groups can admit chance collisions), and you have shown **what breaks when one input is dropped from the hash** in both code and a concrete case. What you built has nearly the shape of a signature scheme in real use. The next two problems look at its traps: `ac26-w3-nonce-reuse` reuses the random k, and `ac26-w3-passkey-assertion` shows that a valid signature alone is not enough.\n\n## Python tools supplied with the formula\n\nPoint coordinates are `point.x` and `point.y`. O is `group.infinity()` (the point that changes nothing when added); exclude it as a public key. Check points with `group.contains(point)`. `secp256k1` names the larger curve used to check that the same functions transfer; read its numbers from group rather than inserting the toy values.\n\nFor a coordinate width w, encode with `x.to_bytes(w,\"big\")` and decode with `int.from_bytes(raw,\"big\")`; big means the most significant byte comes first. Convert domain text with `domain.encode(\"utf-8\")`. One unambiguous way to join fields is to prefix EACH byte string b with `len(b).to_bytes(4,\"big\")`, then append b. Read point coordinates and validate their range BEFORE calling group.point.\n\nThe challenge is `int.from_bytes(hashlib.sha256(preimage).digest(),\"big\") % group.n`. Here preimage is the output of challenge_preimage and includes domain, R, P and message. The remainder can coincide for different inputs, especially in a small group.\n\nA return dictionary has values, for example `{\"domain_a\":\"login\", \"domain_b\":\"payment\", \"message\":b\"ok\", \"secret\":1, \"nonce\":2}`; choose secrets/nonces in the supplied group's range. The counterexample demonstrates the weak verifier's missing domain. Your own preimage must distinguish the domains; challenge returns a number, not an acceptance decision.\n\n\nInput rules: public_key and commit reject non-integer secrets/nonces (including True/False), and values outside1..n−1, with InvalidKey. respond is called with integer nonce, challenge and secret; its result is reduced by n. verify_transcript rejects unusable points and responses outside0..n−1; its integer challenge is reduced by n for point multiplication. These are the arithmetic interfaces of this exercise.\n",
      "writeup": "## What you leave out is not protected\n\nAnything absent from the challenge is something the signature does not claim. Leave out the message and one signature fits every message. Leave out the public key and it can be re-attributed. Leave out the commitment and Fiat-Shamir stops being Fiat-Shamir. Leave out the domain and a signature made for another protocol verifies here.\n\nNone of that shows on the happy path — sign, verify, green. Five of the ten mutations in this problem's suite are exactly that shape, which is why the checkpoints go after it directly instead of only round-tripping.\n\n## The cross-protocol counterexample\n\nThe weakened challenge — the one that omits the domain separator — is fixed in the fixtures, not written by the submission. That matters: \"my attack works against my own weakened code\" would not be an answer.\n\nOn top of building the attack, the learner's own challenge must hash *different bytes* for the two domains. Being able to construct the attack and being immune to it are two separate claims, and the checkpoint wants both.\n\n## Length prefixes, and why the fields are adjacent\n\nConcatenating variable-length fields without their lengths makes `('ab', 'cd')` and `('a', 'bcd')` produce the same bytes: two different statements sharing one proof.\n\nThe reference puts the domain and the message **next to each other**. A layout that separates them with fixed-width point encodings is still unsound in principle, but a collision then needs the point bytes to line up by luck — and a reviewer can talk themselves into believing the length prefixes are decoration. Adjacent, the collision is deterministic and there is nothing to argue about.\n\n## Group order and probability\n\nThe toy groups have order between 29 and 43. A Schnorr forgery succeeds with probability 1/n, so \"change one byte of the message and verify\" really does pass about one time in forty **for a correct implementation**. That is a property of the parameters, not a defect, and it was hit while writing this problem.\n\nSo the checks are split:\n\n- acceptance (an honest signature verifies) runs on the toy groups, where it is deterministic;\n- rejection (a modified signature fails) runs on secp256k1, where 1/n is unreachable;\n- \"changing a binding input changes the challenge\" is asserted on the **preimage** over toy groups, because two preimages collide mod n with probability 1/n while the hash input differing is deterministic.\n\nA test that fails at random teaches learners to re-run it. That is a grading failure, not a flake.\n\n## A signature is not encryption\n\nThe verification equation holding says nothing about confidentiality. The message is not hidden; the verifier is assumed to have it already, and the signature is a claim about it.\n\n## Where this leads\n\nThe next problem is nonce reuse. Having written `z = k + e*x` yourself, two signatures under the same `k` are visibly two equations in two unknowns, and `x` falls out.",
      "learningGoals": [
        "Explain the public key P = x·G and secret x as a one-way street",
        "Implement the commitment, challenge and response",
        "Check the verifier's equation as points on both sides",
        "Fold the round-trip conversation into a one-hash signature with Fiat-Shamir",
        "Show by counterexample why domain, message, R and P must all be bound into the hash",
        "Explain when concatenating variable-length fields makes an encoding ambiguous"
      ],
      "checks": [
        {
          "id": "keygen",
          "label": "Make a key, and refuse an unusable one",
          "hints": [
            {
              "id": "keygen-hint-1",
              "content": "A public key adds the base point G as many times as the secret specifies. The identity O changes nothing when added and is not a usable public key."
            },
            {
              "id": "keygen-hint-2",
              "content": "In a model with n=7, secrets range from1 to6. Secret3 gives G+G+G; secret0 gives O and must be rejected. Use the supplied group to compute actual points."
            },
            {
              "id": "keygen-hint-3",
              "content": "In schnorr.py, public_key checks that secret is an integer from1 through group.n−1 and returns group.generator.scalar_mul(secret). In validate_public_key check group.contains(point) and point.is_infinity, returning False for unusable points. Submit keygen."
            }
          ]
        },
        {
          "id": "sigma",
          "label": "Implement the three moves",
          "hints": [
            {
              "id": "sigma-hint-1",
              "content": "Send a point made from a fresh random number before receiving the challenge. Then combine randomness with the secret in a response; the randomness prevents reading the secret directly from that response."
            },
            {
              "id": "sigma-hint-2",
              "content": "For just the integer part, n=7,k=2,e=3,x=2 gives z=(2+3×2) remainder7 =1. Python: (nonce+challenge*secret)%group.n, not the coordinate modulus p."
            },
            {
              "id": "sigma-hint-3",
              "content": "commit returns group.generator.scalar_mul(nonce) for a nonce in1..n−1. respond multiplies challenge by secret, adds nonce, and takes the remainder modulo group.n. Submit sigma."
            }
          ]
        },
        {
          "id": "transcript",
          "label": "Check both sides of the equation",
          "hints": [
            {
              "id": "transcript-hint-1",
              "content": "The verifier knows neither the secret nor the randomness. It computes two points independently using the public points, challenge and response."
            },
            {
              "id": "transcript-hint-2",
              "content": "zG=(k+ex)G=kG+e(xG)=R+eP. In an integer model with n=7,k=2,e=3,x=2, the left side is1G and the right is8G=1G because7G=O."
            },
            {
              "id": "transcript-hint-3",
              "content": "Validate public and commitment, and require an integer response in0..n−1. Reduce the integer challenge by group.n. Compare group.generator.scalar_mul(response) with commitment+public.scalar_mul(challenge%group.n), return True/False, and submit transcript."
            }
          ]
        },
        {
          "id": "serialization",
          "label": "Write an encoding with one reading",
          "hints": [
            {
              "id": "serialization-hint-1",
              "content": "Convert points and text into bytes before hashing. Without boundaries, different inputs can become the same byte sequence and the signature cannot distinguish them."
            },
            {
              "id": "serialization-hint-2",
              "content": "Joining ab with cd gives abcd, as does a with bcd. Prefix lengths to distinguish2:ab,2:cd from1:a,3:bcd. Encode point coordinates x and y with the same fixed width."
            },
            {
              "id": "serialization-hint-3",
              "content": "Use w=group.as_public()[\"coordinate_bytes\"] and coordinate.to_bytes(w,\"big\"). decode_point checks length2*w and coordinate ranges before group.point. challenge_preimage joins domain.encode(), R, P and message with boundaries. Submit serialization. After group.point constructs the point, check group.contains(point); raise InvalidEncoding if false."
            }
          ]
        },
        {
          "id": "fiat-shamir",
          "label": "Derive the challenge from the transcript",
          "hints": [
            {
              "id": "fiat-shamir-hint-1",
              "content": "Instead of waiting for a verifier to send a challenge, derive it from the signed material and the previously sent point. Changing included information changes the input bytes."
            },
            {
              "id": "fiat-shamir-hint-2",
              "content": "Compute e=int.from_bytes(hashlib.sha256(preimage).digest(),\"big\")%n. If the digest integer were9 and n=7, e would be2. Different inputs can still give equal remainders; distinct challenges are not guaranteed every time."
            },
            {
              "id": "fiat-shamir-hint-3",
              "content": "Call challenge_preimage(domain,commitment,public,message,group), hash with SHA-256, convert to an integer, and reduce by group.n. Change each of the four inputs and check the preimage changes. Submit fiat-shamir."
            }
          ]
        },
        {
          "id": "sign-verify",
          "label": "Sign and verify",
          "hints": [
            {
              "id": "sign-verify-hint-1",
              "content": "The signer uses the secret and randomness; the verifier uses only the public key. Both derive the challenge from the same signed material, binding the signature to its message."
            },
            {
              "id": "sign-verify-hint-2",
              "content": "Signing computes R=kG, e=H(domain,R,P,message), and z=(k+ex) reduced by n, returning(R,z). Verification recomputes e with received R and public P, then checks zG=R+eP without receiving x."
            },
            {
              "id": "sign-verify-hint-3",
              "content": "In sign call public_key, commit, challenge and respond. In verify unpack R,z, compute challenge with the received public key, and call verify_transcript. Malformed shapes return False. Submit sign-verify."
            }
          ]
        },
        {
          "id": "cross-protocol",
          "label": "Show what dropping the domain costs",
          "hints": [
            {
              "id": "cross-protocol-hint-1",
              "content": "If a signature omits the purpose label domain, it can be carried into a different use. Construct a counterexample against that weak scheme without weakening your own implementation."
            },
            {
              "id": "cross-protocol-hint-2",
              "content": "domain_a=\"login\" and domain_b=\"payment\" name different uses. A challenge using only R,P,message is identical for both. Your domain-bound challenge_preimage must produce different bytes."
            },
            {
              "id": "cross-protocol-hint-3",
              "content": "cross_protocol_witness returns distinct domain_a/domain_b strings, a bytes message, and secret/nonce in1..group.n−1. The checker constructs the weak signature. Keep your own preimage domain-bound and submit cross-protocol."
            }
          ]
        },
        {
          "id": "transfer",
          "label": "Run it on real parameters",
          "hints": [
            {
              "id": "transfer-hint-1",
              "content": "Preserve the reasons the small example works when the supplied group becomes large. Returning memorized points or moduli cannot transfer."
            },
            {
              "id": "transfer-hint-2",
              "content": "Always reduce z=k+ex by group.n, while coordinate bounds use group.p. Obtain byte widths from group.as_public() even when the integers become larger."
            },
            {
              "id": "transfer-hint-3",
              "content": "Replace fixed toy moduli and byte widths in schnorr.py with the supplied group properties. Run public tests for sign/verify and encode/decode round trips, then submit transfer."
            }
          ]
        }
      ]
    }
  }
}
