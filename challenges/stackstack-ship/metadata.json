{
  "$schema": "../../SCHEMA.json",
  "id": "stackstack-ship",
  "name": "外から見えるところまで",
  "category": "Challenge",
  "status": "ready",
  "visibility": "public",
  "difficulty": 3,
  "estimatedDuration": "45 分",
  "shortDescription": "板は動いた。 まだ、 運用コンソールの内側だけで。\n\n朝いちばんの Slack に CTO の一行が落ちてくる。 「例の掲示板、 今日じゅうに公開側の入口から見えるようにして」。 前任の SRE が残していったデプロイ用のファイルは 1 枚だけ ── `release.json`。 開いてみると、 配る成果物の ID は前任者のノート PC でビルドしたときのものがそのまま書いてあり、 アプリが起動時に要求する設定はひとつ足りない。 リリース基盤 (Shipyard) に投げても、 パイプラインは途中の stage で止まる。 止まった理由は、 デプロイログに stage 名と一緒に 1 行ずつ書いてある。\n\nビルドはもう終わっている。 デプロイはまだ 1 度も始まっていない。 この 2 つが別物だと体で分かるところが、 今日の最初の 45 分だ。",
  "instructions": "## 前提 — 世界と言葉\n\nあなたは天下クラウド株式会社の運用担当。 AI が生成した社内掲示板 (Web アプリ) を、 前任者のいない中で預かっています。 板は動いています — ただし、 まだ運用コンソールの内側だけで。 すべて手元のコンテナの中で動き、 課金はありません。 壊しても、 やり直せます。\n\n- **言葉** (この問題で使うもの):\n  - **おさらい** (連作共通の言葉): **リクエスト / レスポンス** = アプリへ送る 「お願い」 とその返事。 その窓口が **API** で、 メソッドとパスの組 (例: `GET /posture`) で呼びます。 **JSON** = `{\"key\": \"value\"}` の形でデータを書く形式。 **コンテナ** = あなた専用の小さな仮想コンピュータ。 **ログ** = アプリの 「いつ何をしたか」 の行の記録。 **API コンソール** = 板の `/docs` にある、 API を画面から実行できる場所。\n  - **ビルド** = プログラムを配れる形にまとめること。 その結果できた 1 個のかたまりが **成果物 (artifact)** で、 id が付きます。\n  - **デプロイ** = 成果物を実際に起動して、 利用者に見える場所へ配ること。 「ビルドできた」 と 「配られている」 は別の事実です。\n  - **リリース** = デプロイ 1 回ぶんの記録。 いま配られているものは **live** と呼びます。\n  - **manifest** = 「何をどう配るか」 を書いた指示書 (この問題では `release.json` という JSON 1 枚)。\n  - **環境変数 (env)** = 起動するアプリに渡す設定の入れ物。 名前と値の組です。\n  - **鍵 (signing key)** = アプリが返事に署名するのに使う秘密の文字列。 プラットフォームの **secret store** (鍵の保管庫) に入っていて、 定期的に新しいものへ**入れ替え (ローテーション)** されます。\n  - **パイプライン / stage** = デプロイを工程 (stage) に分けて順に進める仕組み。 進めない工程で止まり、 工程名と理由を言います。\n  - **gate / 受領証** = `GET /posture` が返す真偽の項目が gate。 gate が true の間だけ、 `tokens` にその gate の受領証 (文字列) が出ます。\n  - **checkpoint** = 採点される小問。 この問題には 5 つあります。\n\n## はじめに\n\n朝いちばんの連絡は CTO の一行。 「例の掲示板、 今日じゅうに公開側の入口から見えるようにして」。\n\n前任者が残したデプロイ関連のファイルは manifest 1 枚だけ。 成果物の id は前任者のノート PC でビルドしたときのまま、 アプリが起動時に要求する設定もひとつ足りません。 このままリリース基盤 (Shipyard) に投げても、 パイプラインは途中の stage で止まります — ただし、 止まった理由は毎回ちゃんと言ってくれます。\n\n## 最初の一手\n\n**「起動」** のあと、 **「アクセス先 URL」** の `Ops` から **Shipyard** (`/shipyard`) を開いてください。 この 1 枚に、 いまの全部が出ています: ビルド済み成果物の一覧、 リリース記録 (前任者の失敗した 1 件が最初から残っています)、 公開側の判定、 鍵の保管庫、 manifest の書き方、 使える API の一覧。 `Public` (`/site`) が公開側の入口ですが、 いまは 503 — 何も配られていないからです。\n\n操作は板の API コンソール `/docs` からできます。 一覧に無い API は **「任意の API を試す」** にメソッドとパスを入れて **「実行」**。 manifest の変更は `PATCH /api/settings` で送ります (手元の問題ファイルは書き換えません)。 やり直しは `DELETE /api/settings`、 デプロイは `POST /shipyard/releases` です。\n\n## 答えるもの — checkpoint は 5 つ\n\n見つけた値を回答欄に貼って **「提出 (+点数)」**。 値は起動ごとに変わります。 誤答は小さな減点付きで再提出できます。 各 checkpoint の 1 つ目のヒントは減点なしです。\n\n1. **配るものを突き止める** (20 点) — ビルドは終わっているのに、 何も配られていません。 リリース基盤が実際に持っている成果物の id を確かめて提出します。 手元の manifest に書いてある id を信じないこと — 一覧と突き合わせてください。\n2. **リリースを 1 本通す** (40 点) — `POST /shipyard/releases` でデプロイします。 進めなかった最初の stage で止まり、 stage 名と理由が返るので、 その行が言っていることを直してもう一度。 これを繰り返して、 リリースが live になったときに発行される **receipt** を提出します (Shipyard の releases 表の receipt 列と、 ログの promote 行の両方に出ます)。 manifest の書き方と、 鍵の渡し方 2 通りは Shipyard の画面に書いてあります。\n3. **公開側の見出しを差し替える** (40 点) — `/site` に出る見出しは板の設定ファイルからではなく、 live なリリースの env から来ます。 見出しを自分の言葉に変えてデプロイし直し、 公開側が**実際に返している**見出しを確かめて提出します (初期値のままでは通りません)。\n4. **鍵の入れ替えをまたぐ** (60 点) — 本番の鍵は入れ替わります。 `POST /shipyard/secrets/rotate` で自分で入れ替えて、 公開側がどうなるかを見てください。 `GET /posture` の gate `survives_key_rotation` は 「**次の**入れ替えの後でも公開側が生きているか」 を、 何も壊さずに測っています (採点があなたの代わりに鍵を回すことはありません)。 その gate が true のときに `tokens` に出る受領証を提出します。 直後は無事に見えたデプロイが、 なぜ次の入れ替えで死ぬのか — releases 表の keyBinding 列が手がかりです。\n5. **止めずに片付ける** (40 点) — うまくいかなかった試行の記録を片付けます。 `DELETE /shipyard/release?id=...` で 1 件ずつ消せますが、 live のものも消せてしまい、 消せば公開側は落ちます。 `GET /posture` の 3 つの gate (公開側が実際に応えた実績 / 入れ替え耐性 / 記録が live の 1 本だけ) がそろって true になると `readyToken` が出るので、 それを提出します。 Shipyard の画面には 「いまの live release に対して /site が 200 を返した実績」 も出ています — 採点ではなく、 あなた自身のアクセスだけがこれを 「あり」 にします。\n\n## ゴール\n\n公開側の入口が自分の言葉の見出しで開いていて、 鍵を入れ替えても開いたままで、 リリース記録は live の 1 本だけ — その状態で `readyToken` が出たら終わりです。 「デプロイが成功した」 という記録と 「外から 200 が返る」 は別の事実で、 この問題の採点は毎回、 公開側へ実際のリクエストを投げて確かめています。",
  "description": "## 目的\n\nIssue 285。 「ビルド」 と 「デプロイ」 が別物であること、 必須設定を渡すこと、 デプロイログから失敗を切り分けること、 公開側から動作確認すること ── この 4 つを 1 本のリリース経路の上で通す。\n\n## クラウドではなくコンテナで何を模しているか\n\nクラウドアカウントも公開 URL も使わない。 代わりにコンテナ内に最小のリリース基盤を置く。\n\n| Shipyard | 現実の対応 |\n|---|---|\n| artifact registry | ECR / コンテナレジストリ |\n| release + generation | App Runner / ECS のリビジョン |\n| `BOARD_SIGNING_KEY: \"<値>\"` | 環境変数に秘密を貼り付ける |\n| `BOARD_SIGNING_KEY: {\"fromSecret\": ...}` | Secrets Manager 参照 |\n| `/site` | ALB / CloudFront の向こう側 |\n\n`/site` は **同じプロセスの別サーフェス** であって別ネットワークではない。 README にもそう書いてある。 模しているのは 「運用コンソールで見えていることと、 公開側が実際に返すことは別」 という点だけで、 それ以上は主張しない。\n\n## 何を実測しているか\n\n| checkpoint | 実測しているもの |\n|---|---|\n| `built-artifact` | registry にある artifact id (seed 由来、 リポジトリのどこにも無い) |\n| `release-receipt` | promote stage が実際に走ったこと (receipt は promote でしか発行されない) |\n| `published-title` | `/site` に実 HTTP で聞いて、 live release の env が運んだ見出しを返していること |\n| `rotation-survives` | (1) `/site` がいま 200 を返す (2) 次の鍵世代でも受理される (3) その gate の receipt |\n| `clean-cutover` | 3 gate 全緑 + `/site` の実 HTTP プローブ + `readyToken` |\n\n## 危険な回避策と、 それを落とす場所\n\n| 回避策 | 落ちる場所 |\n|---|---|\n| 秘密の値を読んで manifest に直書き | deploy も `published-title` も通る。 `rotation-survives` (60) と `clean-cutover` (40) が落ちる |\n| 先に rotate してから新しい値を直書き | dry-run のプローブ世代は必ず 1 つ先なので `survives_key_rotation` は false のまま |\n| 全部消して 「1 本だけ」 を満たす | `single_release` は 0 で false、 `/site` も 503。 サービスを止めて片付けの体裁を整える経路は不合格 |\n| 失敗した記録を残したまま | `single_release` が false のまま token が出ない |\n| `config/app.json` の見出しを書き換える | `/site` の見出しは live release の env からしか来ない |\n| health gate を manifest から緩める | manifest に health 系のフィールドが無く、 未知キーは `resolve-config` が拒否する |\n\n## 採点が壊さないこと\n\n`rotation-survives` は store を **動かさない**。 判定はプローブ世代 (version + 1) に対する dry-run で行うので、 誤答でも再送でも参加者の環境は変わらない。 鍵が実際に動くのは参加者が `POST /shipyard/secrets/rotate` を打ったときだけ。\n\n## Battle への引き継ぎ (正直な粒度)\n\n`battles/stackstack` は `phased-polling` の CloudFormation 問題なので、 この scenario の JavaScript も in-process のリスナも境界を越えない。 越えるのは形だけ: (1) 採点する事実 = 「外から観測された 200」 (ALB target group が healthy)、 (2) 入れ替え判定 = 「環境変数に貼った値か Secrets Manager 参照か」 (`DescribeTaskDefinition` で何も壊さず答えられる)、 (3) 昇格ゲートの形 = 申告ではなく実測。 「同じデプロイ資産をそのまま再利用する」 とは書かない ── それはこの 2 つの実行基盤の間では成立しない。\n\n## 既知の限界\n\nコンテナ内の判定なので、 参加者が自分のチェックアウトの `runtimes/stackstack/` を書き換えて image を焼き直せば全部無効化できる。 このカタログのコンテナ問題すべてに共通する local-play の正直さの限界で、 採点バグではない。",
  "writeup": "## この 45 分で触ったもの\n\n- **ビルドとデプロイ** — `GET /shipyard/artifacts` にはブート時から artifact が 1 つある。 それでも `/site` は 503 だった。 ビルド成果物が存在することと、 それが配られていることは別の事実で、 本番障害の切り分けはたいていこの区別から始まる。\n- **stage 単位のデプロイログ** — パイプラインは最初に進めなかった stage で止まり、 `read-manifest` / `resolve-artifact` / `resolve-config` / `start` / `health-gate` / `promote` のどれかを名指しした。 「デプロイが失敗した」 では何も分からないが、 「`resolve-config` が `BOARD_SIGNING_KEY` を要求している」 なら次の一手が決まる。\n- **鍵の束ね方** — 値を貼り付けたリリースは、 貼った瞬間は健全で、 次の入れ替えで死ぬ。 参照を持ったリリースはリクエストごとに解決し直すので入れ替えに気づきもしない。 AWS なら task definition に環境変数として秘密を書くか、 Secrets Manager の ARN を書くかの違いで、 症状も同じ ── 「先週から何も変えていないのに落ちた」。\n- **止めずに片付ける** — 失敗した記録を消すのと、 動いているものを消すのは 1 文字しか違わない。 `single_release` を満たす最短経路は全消しで、 それは公開側を落とす。 片付けの評価に可用性が入っていないと、 人は必ず落とすほうを選ぶ。\n\n## なぜ 「デプロイ成功」 で採点しないのか\n\nパイプラインが 201 を返したことと、 外から 200 が返ることは別の事実だ。 この問題の `published-title` と `rotation-survives` と `clean-cutover` は、 採点のたびに `/site` へ実際の HTTP リクエストを 1 本投げている。 「デプロイは成功しました」 という記録だけを見て安心した経験があるなら、 その差がこの問題の本体だ。\n\n## 次にやること\n\n同じ板を、 今度は本番品質まで持っていく `stackstack` Battle が本編。",
  "tags": [
    "stackstack",
    "deploy",
    "release",
    "secrets",
    "health-check",
    "local-play",
    "container",
    "web-app"
  ],
  "exposedPorts": [
    {
      "port": 18080,
      "name": "Web (board / shipyard / site)"
    },
    {
      "port": 18081,
      "name": "/verify (loopback)"
    }
  ],
  "learningGoals": [
    "ビルド済みの成果物が存在することと、 それが配られていることが別の事実だと区別できる",
    "stage 単位のデプロイログから、 止まった原因と次に直す場所を切り分けられる",
    "秘密を値で渡すか参照で渡すかで、 入れ替え時の壊れ方が変わることを挙動で説明できる",
    "デプロイの成功記録ではなく、 公開側の応答で正常性を確認する習慣をつける"
  ],
  "i18n": {
    "en": {
      "name": "As Far As the Outside",
      "shortDescription": "The board works. Only from inside the ops console.\n\nThe first Slack message of the day is one line from the CTO: \"That message board — get it visible from the public entrance today.\" The previous SRE left exactly one deployment file behind: `release.json`. Open it and the artifact id is still the one from their laptop build, and one setting the app demands at startup is missing. Throw it at the release platform (Shipyard) and the pipeline stops partway through. The reason it stopped is written in the deploy log, one line per stage, with the stage named.\n\nThe build already happened. The deploy has never happened once. Feeling the difference between those two is the first forty-five minutes.",
      "instructions": "## Before you start — the world and the words\n\nYou are the operator at TenkaCloud Inc., in charge of the AI-generated company message board, with no predecessor left to ask. The board runs — but only from inside the ops console. Everything runs in a container on your machine; no bill, and mistakes can be redone.\n\n- **Words** (used in this problem):\n  - **Recap** (words shared by this series): a **request / response** is the \"ask\" you send an app and its reply; the doorway is an **API**, called as method + path (e.g. `GET /posture`). **JSON** = data written like `{\"key\": \"value\"}`. **Container** = your own small virtual computer. **Log** = the app's line-by-line record of what it did. **API console** = the screen at `/docs` where APIs run from the browser.\n  - **Build** = packaging a program into a distributable form. The resulting bundle is an **artifact**, and it has an id.\n  - **Deploy** = actually starting an artifact and serving it where users can see it. \"It was built\" and \"it is being served\" are different facts.\n  - **Release** = the record of one deploy. The one currently serving is called **live**.\n  - **Manifest** = the instruction sheet saying what to ship and how (here, one JSON document called `release.json`).\n  - **Environment variables (env)** = named values handed to the app at startup.\n  - **Signing key** = the secret string the app signs its responses with. It lives in the platform's **secret store** and is periodically **rotated** (swapped for a new one).\n  - **Pipeline / stage** = a deploy split into named steps run in order. It stops at the first stage that cannot proceed, and names it, with the reason.\n  - **Gate / receipt** = a gate is a true/false entry on `GET /posture`; while a gate is true, `tokens` carries that gate's receipt string.\n  - **Checkpoint** = a scored sub-question. This problem has 5.\n\n## Getting started\n\nThe first message of the day is one line from the CTO: \"That message board — get it visible from the public entrance today.\"\n\nThe predecessor left exactly one deployment file: the manifest. Its artifact id is still the one from their laptop build, and one setting the app demands at startup is missing. Throw it at the release platform (Shipyard) as-is and the pipeline stops partway — but it always tells you why it stopped.\n\n## Your first move\n\nAfter **Start**, open **Shipyard** (`/shipyard`) from `Ops` under **Access URLs**. That one page shows everything: the built artifacts, the release records (one failed attempt from the predecessor is there from the start), the public side's verdict, the secret store, how to write the manifest, and the list of APIs. `Public` (`/site`) is the public entrance — currently a 503, because nothing is deployed.\n\nOperations run from the board's API console `/docs`: enter method and path under **Try any API** and press **Send**. Change the manifest with `PATCH /api/settings` (never by editing the problem's files on disk); reset with `DELETE /api/settings`; deploy with `POST /shipyard/releases`.\n\n## What you answer — 5 checkpoints\n\nPaste each value into the answer field and press **Submit (+points)**. Values change on every start. Wrong answers can be resubmitted with a small penalty. The first hint on every checkpoint is free.\n\n1. **Identify what gets shipped** (20 pt) — the build already happened, and nothing is serving it. Ask the release platform what artifacts it actually holds, and submit that id. Do not trust the id in the manifest you inherited — check it against the registry.\n2. **Get one release through** (40 pt) — deploy with `POST /shipyard/releases`. It stops at the first stage that cannot proceed, naming the stage and the reason; fix what that line says and run it again. Repeat until a release goes live, then submit the **receipt** issued for it (it appears in the receipt column of Shipyard's releases table and on the promote log line). The manifest's shape, and both ways of passing the key, are documented on the Shipyard page.\n3. **Change what the outside sees** (40 pt) — the heading on `/site` does not come from the board's config file; it comes from the live release's env. Change it to your own words, deploy again, and submit the heading the public side is **actually serving** (the initial value is not credited).\n4. **Survive a key rotation** (60 pt) — production keys get rotated. Rotate it yourself with `POST /shipyard/secrets/rotate` and watch what happens to the public side. The `survives_key_rotation` gate on `GET /posture` measures, without breaking anything, whether the site would still be alive after the **next** rotation (scoring never rotates on your behalf). Submit the receipt that appears under `tokens` while that gate is true. Why does a deploy that looked healthy die at the next rotation? The keyBinding column of the releases table is your clue.\n5. **Clean up without an outage** (40 pt) — tidy away the records of the attempts that failed. `DELETE /shipyard/release?id=...` removes one at a time — including the live one, and deleting that takes the public side down. Once the three gates on `GET /posture` (the public side has really answered / rotation survival / exactly one record, the live one) are all true, `readyToken` appears; submit it. The Shipyard page also shows whether `/site` has actually answered 200 for the current live release — only your own access, never scoring, turns that to \"yes\".\n\n## Goal\n\nThe public entrance is open under your own heading, stays open across a key rotation, and exactly one release record — the live one — remains. When `readyToken` appears in that state, you are done. \"The deploy succeeded\" and \"the outside answers 200\" are different facts, and this problem's scoring checks the public side with a real request every time.",
      "description": "## Purpose\n\nIssue 285. Build versus deploy, passing a required setting, isolating a failure from a deploy log, and confirming from the public side — all four on one release path.\n\n## What the container models instead of a cloud\n\nNo cloud account and no public URL. A minimal release plane inside the container instead.\n\n| Shipyard | Real equivalent |\n|---|---|\n| artifact registry | ECR / a container registry |\n| release + generation | an App Runner / ECS revision |\n| `BOARD_SIGNING_KEY: \"<value>\"` | pasting a secret into an environment variable |\n| `BOARD_SIGNING_KEY: {\"fromSecret\": ...}` | a Secrets Manager reference |\n| `/site` | what sits behind an ALB / CloudFront |\n\n`/site` is **a second surface in the same process**, not a second network, and the READMEs say so. The only thing being modelled is that what the ops console shows and what the public side actually returns are different facts. Nothing beyond that is claimed.\n\n## What is measured\n\n| checkpoint | what is measured |\n|---|---|\n| `built-artifact` | the artifact id in the registry (seed-derived, in no committed file) |\n| `release-receipt` | that a promote stage really ran (a receipt is issued nowhere else) |\n| `published-title` | a real HTTP request to `/site`, serving the heading the live release's env carried |\n| `rotation-survives` | (1) `/site` answers 200 now (2) it would still be accepted at the next key generation (3) that gate's receipt |\n| `clean-cutover` | all three gates green + a real HTTP probe of `/site` + `readyToken` |\n\n## Dangerous shortcuts, and where each one falls over\n\n| Shortcut | Where it falls |\n|---|---|\n| Read the secret's value and paste it into the manifest | the deploy and `published-title` both pass; `rotation-survives` (60) and `clean-cutover` (40) do not |\n| Rotate first, then paste the fresh value | the dry-run probe epoch is always one ahead, so `survives_key_rotation` stays false |\n| Delete everything to satisfy \"exactly one\" | `single_release` is 0, not 1, and `/site` is 503. Taking the service down to look tidy is not a pass |\n| Leave the failed records in place | `single_release` stays false and no token is emitted |\n| Edit the heading in `config/app.json` | `/site`'s heading comes only from the live release's env |\n| Relax the health gate from the manifest | the manifest has no health field, and `resolve-config` refuses an unknown key |\n\n## What scoring does not do\n\n`rotation-survives` does **not** move the store. It answers the rotation question against a dry-run probe epoch (version + 1), so neither a wrong answer nor a retried request changes the participant's environment. The key moves only when the participant posts to the rotate route.\n\n## What carries into the Battle (at the right granularity)\n\n`battles/stackstack` is a `phased-polling` CloudFormation problem, so neither this scenario's JavaScript nor its in-process listener crosses that boundary. Only the shape does: (1) the scored fact — an externally observed 200 (an ALB target group reporting healthy), (2) the rotation predicate — a pasted environment variable versus a Secrets Manager reference, answerable by `DescribeTaskDefinition` without breaking anything, and (3) the promotion gate shape — measured, not claimed. This problem does not claim the deploy assets themselves are reused; between these two execution models they cannot be.\n\n## Known limit\n\nThe verdict comes from inside the container, so a participant who edits `runtimes/stackstack/` in their own checkout and rebuilds the image defeats every check here. That is the local-play honesty limit every container problem in this catalog shares, not a scoring bug.",
      "writeup": "## What you touched in those forty-five minutes\n\n- **Build versus deploy** — `GET /shipyard/artifacts` had an artifact from the moment the container booted, and `/site` was still returning 503. \"The artifact exists\" and \"the artifact is being served\" are two different facts, and most production triage starts by separating them.\n- **A deploy log with stages in it** — the pipeline stopped at the first stage that could not proceed and named it: `read-manifest` / `resolve-artifact` / `resolve-config` / `start` / `health-gate` / `promote`. \"The deploy failed\" tells you nothing; \"`resolve-config` wants `BOARD_SIGNING_KEY`\" decides your next move.\n- **How the key was bound** — a release holding a copy of the value is perfectly healthy the moment you paste it, and dies at the next rotation. A release holding a reference re-resolves per request and never notices. On AWS that is an environment variable in a task definition versus a Secrets Manager reference, and the symptom is the same: \"nothing changed since last week and it fell over.\"\n- **Cleaning up without an outage** — deleting a dead record and deleting the one that is serving differ by one character. The shortest path to \"exactly one release\" is to delete them all, and that takes the public side down. If availability is not part of how tidiness is scored, people will pick the version that drops the service.\n\n## Why \"the deploy succeeded\" is not the scored fact\n\nA pipeline returning 201 and the outside returning 200 are different facts. `published-title`, `rotation-survives` and `clean-cutover` each send a real HTTP request to `/site` at the moment they are answered. If you have ever been reassured by a green deployment record while the service was down, that gap is what this problem is about.\n\n## Next\n\nThe main event is the `stackstack` Battle: the same board, taken to production quality.",
      "learningGoals": [
        "Separate \"the artifact exists\" from \"the artifact is being served\" as two distinct facts",
        "Use a stage-by-stage deploy log to isolate why a release stopped and what to fix next",
        "Explain, from observed behaviour, how passing a secret by value differs from passing it by reference when the secret rotates",
        "Confirm health from what the public side answers rather than from a successful deployment record"
      ],
      "checks": [
        {
          "id": "built-artifact",
          "label": "Identify what gets shipped",
          "hints": [
            {
              "id": "ship-artifact-1",
              "content": "Something was built before you arrived, and nothing is serving it. The release plane keeps a list of what it has on hand — ask it for that list."
            },
            {
              "id": "ship-artifact-2",
              "content": "Run `GET /shipyard/artifacts` from `/docs`; `artifacts` contains exactly one id. Submit it verbatim. The id in the starting manifest is not it — that build came off somebody else's machine."
            }
          ]
        },
        {
          "id": "release-receipt",
          "label": "Get one release through",
          "hints": [
            {
              "id": "ship-receipt-1",
              "content": "`POST /shipyard/releases` runs the pipeline and stops at the first stage that cannot proceed, naming the stage and the reason. Fix what that line says, run it again, and repeat. The log line written when a release finally goes live carries the value this checkpoint wants."
            },
            {
              "id": "ship-receipt-2",
              "content": "Two things block the shipped manifest: the artifact id, and a setting the app will not start without — `resolve-config` names it. The Shipyard console documents the shape of `env` and what that setting accepts, and `GET /shipyard/secrets` shows what the platform's store holds. Supply it, `POST /shipyard/releases` again, and submit the `receipt=` value from the `stage=promote status=ok` line (the same value is the `receipt` column on the console)."
            }
          ]
        },
        {
          "id": "published-title",
          "label": "Change what the outside sees",
          "hints": [
            {
              "id": "ship-title-1",
              "content": "The heading on the public entrance does not come from the board's config file. It comes from the release. Change it where the release says it, deploy again, and look at what the outside is actually serving."
            },
            {
              "id": "ship-title-2",
              "content": "`BOARD_PUBLIC_TITLE` in the manifest's `env` is the heading `/site` renders. Change it through `PATCH /api/settings`, run `POST /shipyard/releases` again, then run `GET /site/healthz` from `/docs` and submit `title`. The starting value is not credited."
            }
          ]
        },
        {
          "id": "rotation-survives",
          "label": "Survive a key rotation",
          "hints": [
            {
              "id": "ship-rotation-1",
              "content": "Production keys get rotated. Rotate the one in `/shipyard/secrets` yourself and then look at what the public entrance does. `GET /posture` has a gate watching the same thing without breaking anything, and scoring never rotates on your behalf."
            },
            {
              "id": "ship-rotation-2",
              "content": "Run `POST /shipyard/secrets/rotate`, then re-check `GET /site/healthz` from `/docs`. If it returns 503, the detail names the release, and `GET /shipyard/releases` shows how each release resolved that setting in `keyBinding`. Fix the runtime manifest, deploy again, and submit `survives_key_rotation` from `tokens` on `GET /posture`."
            }
          ]
        },
        {
          "id": "clean-cutover",
          "label": "Clean up without an outage",
          "hints": [
            {
              "id": "ship-cutover-1",
              "content": "`GET /posture` reports three gates measured from the running plane. The sign-off appears only once all three are true, and a false gate names your next action. One of them is about what is still lying around from the attempts that did not work."
            },
            {
              "id": "ship-cutover-2",
              "content": "`GET /shipyard/releases` lists every record, including the one that was already there when you arrived; running `delete /shipyard/release?id=rel-N` from `/docs` removes one. The plane will let you delete the live one too, and the public side goes down if you do. Leave exactly one, live, ask `/site` for it yourself at least once, then submit `readyToken`."
            }
          ]
        }
      ]
    }
  },
  "runtime": {
    "provider": "docker",
    "engine": "compose",
    "entry": "local/docker-compose.yml",
    "challengeEndpoints": {
      "Ops": "http://127.0.0.1:18080/shipyard",
      "Public": "http://127.0.0.1:18080/site"
    },
    "verifyUrl": "http://127.0.0.1:18081/verify",
    "secretEnv": [
      "FLAG_SEED"
    ]
  },
  "scoring": {
    "kind": "multi-verify",
    "checks": [
      {
        "id": "built-artifact",
        "label": "配るものを突き止める",
        "points": 20,
        "wrongAnswerPenalty": 2,
        "hints": [
          {
            "id": "ship-artifact-1",
            "content": "あなたが来る前に、 何かがビルドされています。 そして、 それを配っているものは 1 つもありません。 リリース基盤は手元にあるものの一覧を持っています。 まずその一覧を出させてください。",
            "penalty": 0
          },
          {
            "id": "ship-artifact-2",
            "content": "`/docs` から `GET /shipyard/artifacts` を実行すると、 `artifacts` に id がちょうど 1 つ返ります。 それをそのまま提出してください。 初期 manifest の id は別のマシンで焼いたビルドなので違います。",
            "penalty": 8
          }
        ]
      },
      {
        "id": "release-receipt",
        "label": "リリースを 1 本通す",
        "points": 40,
        "wrongAnswerPenalty": 2,
        "hints": [
          {
            "id": "ship-receipt-1",
            "content": "`POST /shipyard/releases` はパイプラインを走らせ、 進めなかった最初の stage で止まって、 その stage 名と理由を返します。 その行が言っていることを直して、 もう一度打つ。 これを繰り返してください。 リリースがついに live になったときに書かれるログ行に、 このチェックポイントが求めている値が入っています。",
            "penalty": 0
          },
          {
            "id": "ship-receipt-2",
            "content": "出荷時の manifest を止めているのは 2 つ: artifact の id と、 それが無いとアプリが起動しない設定 (`resolve-config` が名前を言います)。 `env` の書き方とその設定が受け付ける形は Shipyard のコンソールに書いてあり、 `GET /shipyard/secrets` にはプラットフォーム側の store の中身が並んでいます。 渡してから `POST /shipyard/releases` をやり直し、 `stage=promote status=ok` の行の `receipt=` の値を提出してください (コンソールの `receipt` 列にも同じ値が出ます)。",
            "penalty": 16
          }
        ]
      },
      {
        "id": "published-title",
        "label": "公開側の見出しを差し替える",
        "points": 40,
        "wrongAnswerPenalty": 2,
        "hints": [
          {
            "id": "ship-title-1",
            "content": "公開側の入口に出る見出しは、 板の設定ファイルから来ていません。 リリースから来ています。 リリース側で書き換えて、 もう一度デプロイして、 外が実際に何を返しているかを見てください。",
            "penalty": 0
          },
          {
            "id": "ship-title-2",
            "content": "manifest の `env` にある `BOARD_PUBLIC_TITLE` が `/site` の見出しです。 `PATCH /api/settings` で外に見せたい名前に変え、 `POST /shipyard/releases` をやり直します。 `/docs` から `GET /site/healthz` を実行し、 `title` を提出してください。 初期値は credit されません。",
            "penalty": 16
          }
        ]
      },
      {
        "id": "rotation-survives",
        "label": "鍵の入れ替えをまたぐ",
        "points": 60,
        "wrongAnswerPenalty": 2,
        "hints": [
          {
            "id": "ship-rotation-1",
            "content": "本番の鍵は入れ替わります。 `/shipyard/secrets` にあるものを自分で入れ替えて、 そのあと公開側の入口がどうなるかを見てください。 `GET /posture` の gate は、 何も壊さずに同じことを見ています。 採点があなたの代わりに鍵を入れ替えることはありません。",
            "penalty": 0
          },
          {
            "id": "ship-rotation-2",
            "content": "`/docs` から `POST /shipyard/secrets/rotate` を実行し、 `GET /site/healthz` をもう一度確認します。 503 なら detail がリリースを示し、 `GET /shipyard/releases` の `keyBinding` に設定の解決方法が出ます。 runtime manifest を直してデプロイし直し、 `GET /posture` の `tokens` にある `survives_key_rotation` を提出してください。",
            "penalty": 28
          }
        ]
      },
      {
        "id": "clean-cutover",
        "label": "止めずに片付ける",
        "points": 40,
        "wrongAnswerPenalty": 2,
        "hints": [
          {
            "id": "ship-cutover-1",
            "content": "`GET /posture` は走っている plane から実測した 3 つの gate を返します。 合格印は 3 つとも true になって初めて出ます。 false の gate がそのまま次にやることです。 そのうち 1 つは、 うまくいかなかった試行の残骸についてのものです。",
            "penalty": 0
          },
          {
            "id": "ship-cutover-2",
            "content": "`GET /shipyard/releases` は、 あなたが来たときすでにあったものも含めて全部の記録を並べます。 `/docs` から `delete /shipyard/release?id=rel-N` を実行すると 1 件消せます。 live のものも消せてしまい、 消せば公開側は落ちます。 live なものを 1 本だけ残し、 自分で 1 度は `/site` を叩いてから `readyToken` を提出してください。",
            "penalty": 16
          }
        ]
      }
    ]
  },
  "track": {
    "id": "stackstack-route",
    "order": 100,
    "chapter": "4. StackStack 本編"
  }
}
