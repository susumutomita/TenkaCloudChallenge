{
  "id": "ac26-w3-fft-domain",
  "name": "その domain、本当に割り切れますか",
  "category": "Challenge",
  "status": "draft",
  "visibility": "public",
  "track": {
    "id": "advanced-cryptography-2026",
    "order": 317,
    "chapter": "Week 3 / FFT Domain and Interpolation"
  },
  "difficulty": 3,
  "estimatedDuration": "45-60 分",
  "shortDescription": "渡された omega を信じずに evaluation domain を検証し、FFT・逆変換・補間を本物の domain の上だけで動かす。",
  "description": "多項式コミットも arithmetization も、その下では「n 個の相異なる評価点」が支えている。 この問題が扱うのは、omega ** n == 1 という 1 本の等式だけを確かめて evaluation domain を信じた実装である。\n\nその等式は、より小さい部分群のすべての元が満たす。 1 さえ満たす。 公開テストの domain はどれも本物なので、等式だけの実装と位数を確かめる実装は区別が付かず、壊れた starter は public test を全部通る。\n\n参加者は Python 標準ライブラリだけで、渡された (p, n, omega) を判定する validate_domain、その上での変換 fft と逆変換 ifft、そして domain の内外どちらの点にも答える interpolate_and_evaluate を書く。 到達点は「omega の位数がちょうど n」を n の各素因数 q について omega ** (n/q) != 1 まで確かめる形である。\n\n採点は書き方ではなく振る舞いで行う。 hidden phase のパラメータ集合は、教科書式がたまたま正しい素数と、形だけ正しい元を返す素数を半々以上で混ぜてあり、等式 1 本の実装は運ではなく必然で落ちる。",
  "instructions": "## 前提 — 中学の数学から\n\n- **学校で習ったこと**: 2 点が決まれば直線が 1 本に決まり、3 点が決まれば放物線が 1 本に決まります(中 2・高 1)。だから **n 個の点での値** と **n 個の係数** は同じ情報で、行き来できます。ただし条件がひとつ — その n 個の点が **全部ちがう** こと。同じ点を 2 回数えたら、情報は n 個分ありません。\n- **数の世界**: 数はぜんぶ「素数 p で割ったあまり」で考えます。使う数は 0 〜 p−1 の p 個だけ。`≡` は「p で割ったあまりが同じ」という意味です。Python では `% p` がその役です。\n- **多項式と評価**: `f(x) = a0 + a1·x + a2·x² + …` の形の式を **多項式**、x に数を入れて値を出すことを **評価** と呼びます。値も p で割ったあまりにします。\n- **位数**: ある数 ω を掛け続けると、いつか 1 に戻ります。**初めて 1 に戻るまでの回数** が ω の **位数** です。\n- **1 桁の例**(紙で追えます): p = 5 で ω = 2 なら、冪は 2 → 4 → 3 → 1。4 回目に初めて 1 なので位数 4 で、出てくる点 1, 2, 4, 3 は全部ちがいます。ところが ω = 4 でも 4⁴ = 256 ≡ 1 です。しかし冪は 4 → 1 → 4 → 1 で、点は 2 種類しか出ません(位数 2)。**「ω を n 乗すると 1」を確かめただけでは、点が n 個そろう保証にならない** — これがこの問題の全部です。\n- **言葉**: 評価点の集まり(evaluation domain)= 値を取る点の並び {1, ω, ω², …, ω^(n−1)}。原始 n 乗根 = 位数がちょうど n の ω。変換(fft)= 係数の並びから評価値の並びを作ること。逆変換(ifft)= その逆。補間 = n 個の値から、それを通る多項式を決めること。逆元 = 掛けて 1 になる相手(Python なら `pow(x, -1, p)`)。checkpoint = 採点される小問で、この問題には 5 つあります。\n- **この問題の位置づけ**: Week 3 の 4 問目。前の問題 `ac26-w3-ntt-roots` では ω を **自分で選び** ました。ここでは ω が **外から渡されてきます** — 選ぶ側ではなく、受け取った側の話です。\n\n## はじめに\n\n証明の仕組みは、下のほうで「n 個の相異なる評価点」に支えられています。その点の並びは、どこかで作られて、あなたのコードに `(p, n, omega)` の形で **渡されてきます**。渡す側が正しいとはかぎりません。\n\n配られた `fftdomain.py` は動きます。公開テストも全部通ります。ただし、渡された omega を `omega を n 乗すると 1` という **等式 1 本だけ** で信じています。上の p = 5, ω = 4 のように、より小さい輪の中の数もその等式を満たします。1 でさえ満たします。公開テストで使われる domain はどれも本物なので、等式だけの実装と位数まで確かめる実装は、公開テストでは見分けがつきません。\n\nあなたの仕事は、受け取った `(p, n, omega)` が本物かを **自分で判定する** ことと、偽物の上では計算そのものを断ることです。\n\n## 最初の一手\n\nParticipant Portal(この問題文が表示されているサイト)で **「起動」** を押すと、同じ画面に **問題エディタ**(`fftdomain.py` が入っています)が出ます。\n\n問題エディタの **「証拠を確認」** を押してください。表示されるのは次の 5 つです。\n\n1. **deployment** — 環境が動いた確認用の文字列。この問題では提出しません。\n2. **field family** — 採点で使われる素数の一覧。\n3. **素数ごとの「使える n」の一覧** — n が使えるのは、**n が p−1 を割り切るとき** だけです。\n4. **本物の domain の例**(毎回同じ値) — p = 17, n = 4, ω = 13、`f(x) = 1 + 2x + 3x² + 4x³` について、i = 0..3 の ω^i と f(ω^i) の表。点は 1, 13, 16, 4 で全部ちがいます。\n5. **本物に見えるだけの domain の例**(毎回同じ値) — p = 97, n = 8, ω = 75。`ω を 8 乗すると 1`(みんなが確かめる等式)は成り立っています。ところが冪を並べると `1, 75, 96, 22, 1, 75, 96, 22` — **4 個目で 1 に戻り、後半が前半の繰り返し**。8 個ほしい点が 4 個しかありません。\n\n**まず 5 を紙で確かめてください**: 75² を 97 で割ったあまりは 96、96² = 9216 を 97 で割ったあまりは 1。つまり 75 は 4 乗で 1 に戻ります。ここで「等式 1 本では足りない」を自分の目で見ておくと、あとの 5 つの checkpoint はすべて同じ 1 つの判定に帰着します。\n\n## 答えるもの — checkpoint は 5 つ\n\n書き換えるファイルは `fftdomain.py` だけです。入力の検査(`_parse_prime` / `_parse_list`)、1 点での評価(`_evaluate`)、4 つの関数の骨組みは最初から入っています。判定を書く場所も `_domain_ok` として用意してあります。新しくファイルを作る必要はありません。\n\n**関数の形**(starter のとおり。どれも辞書を返します):\n\n- `validate_domain(prime, order, omega)` → `{\"ok\": True, \"valid\": True}` または `{\"ok\": True, \"valid\": False}`。**本物** と言えるのは、omega の位数がちょうど order のときだけです(このとき order は自動的に p−1 を割り切ります)。形がおかしい入力は `{\"ok\": False, \"error\": \"名前\"}` で、名前は `invalid_prime`(素数でない)、`invalid_order`(整数でない、または 1〜4096 の外)、`invalid_omega`(整数でない)です。**order が 1〜4096 の整数なのに p−1 を割り切らない場合は、エラーではなく `valid: False`** です。\n- `fft(coefficients, omega, prime)` → `{\"ok\": True, \"values\": [...]}`。n は `len(coefficients)` です。`values[i]` は `a0 + a1·(ω^i) + a2·(ω^i)² + …` を prime で割ったあまりで、i = 0 から n−1 まで **その順番のまま** 並べます。domain が本物でなければ `{\"ok\": False, \"error\": \"invalid_domain\"}`。係数の形がおかしければ `invalid_coefficients`。\n- `ifft(values, omega, prime)` → `{\"ok\": True, \"coefficients\": [...]}`。作り方は同じ計算を **omega の逆元** で行い、最後に全部へ **n の逆元** を掛けます。値の形がおかしければ `invalid_values`、domain が本物でなければ `invalid_domain`。\n- `interpolate_and_evaluate(values, omega, point, prime)` → `{\"ok\": True, \"value\": f(point)}`。`values` は domain の上での値の並びで、それを通る多項式を `point` で評価した値を返します。`point` が整数でないか 0 以上 p 未満でなければ `invalid_point`。\n\ncheckpoint はこの順です。\n\n1. **domain**(40 点) — `validate_domain` が本物と偽物を分けること。偽物には位数が n より小さいものと **n より大きいもの** の両方が来ます。さらに、偽物を渡された `fft` と `ifft` が計算せずに `invalid_domain` で断ること。\n2. **roundtrip**(40 点) — さらに、本物の domain で `fft` → `ifft` が元の係数をそのまま返すこと。逆元を片方だけ忘れた実装は、長さも範囲も正しい **まちがった係数** を返すので、目で見ても気づけません。\n3. **ordering**(30 点) — さらに、`values[i]` が ω の i 乗での値であること。並べ替えが混じっていないかは、`f(x) = x`(係数 `[0, 1, 0, …]`)を変換すると一目で分かります — 出てくるのは ω の冪そのものの並びのはずです。\n4. **interpolate**(45 点) — さらに、`interpolate_and_evaluate` が domain の上の点にはその位置の値を返し、domain の外の点にも同じ計算で答えること。0 で割る場合分けが要らない書き方があります。\n5. **generalize**(45 点) — 以上ぜんぶを、**公開テストに出てこない素数と n** でもう一度。2 の累乗でない n(3 や 9 など)も来ます。n は p−1 を割り切りさえすればよく、2 の累乗である必要はありません。名前付きのエラーもここでまとめて確かめられます。\n\n後ろの checkpoint は前の性質もまとめて確かめます。位数の判定が入らないうちは 5 つとも通らず、そこが入れば一気に進みます。**位数がちょうど n かの判定** は定義どおりでかまいません — 1 から始めて ω を掛けていき、初めて 1 になるまでの回数を数えて n と比べる。この問題の p はどれも 1 万未満なので、それで十分間に合います。\n\n5 つとも、checkpoint ごとの **「提出 (+N pt)」** を押した時点で、そのときエディタに入っている `fftdomain.py` がそのまま送られて採点されます(入力欄が出ていても、採点に使われるのはエディタの中身です)。5 つは別々に採点されるので 1 つ詰まっても他は取れますし、何度でも出し直せます — ただし 1 回まちがえるごとに −10 〜 −15 点(checkpoint ごとに違います)。\n\n**「公開テストを実行」** は手元での下見です。そこで使われる domain はどれも本物なので、**配られたままの `fftdomain.py` でも全部通ります**。**「初期状態に戻す」** で最初の `fftdomain.py` に戻ります。\n\n## ゴール\n\n渡された `(p, n, omega)` が本物の evaluation domain かを自分で判定し、本物の上では変換・逆変換・補間が正しく往復し、偽物の上では計算せずに断る。それが、見たことのない素数と n でも成り立ったら終わりです。",
  "writeup": "## omega ** n == 1 は「n 乗すると 1」でしかない\n\nその等式は位数が n の約数だと言っているだけで、位数が n だとは言っていない。 位数 4 の元は 8 乗しても 1 になる。 1 はいつでも 1 になる。 等式 1 本で受け入れた omega の冪は繰り返し、n 点あるはずの評価は実際には 4 点や 1 点しかない。\n\n## 位数がちょうど n であることは 2 段で確かめる\n\nomega ** n == 1 に加えて、n の各素因数 q について omega ** (n/q) != 1 を確かめる。 これで「n 乗して初めて 1 になる」が言える。 n が p-1 を割ることは前提条件で、割らなければ位数 n の元はそもそも存在しない。\n\n## 逆変換と補間は domain の上に立つ\n\nifft は omega ** -1 の冪で回して n ** -1 を掛ける。 どちらを忘れても、形の正しい間違った係数が返る。 補間は係数へ戻ってから評価すれば、domain 上の点でも外の点でも同じ算術で答えられ、ゼロ除算の特別扱いが要らない。\n\n## 証明していないこと\n\nFFT を速くしたのではない。 渡された domain が本物かを判定し、本物の上でだけ変換・逆変換・補間が成り立つようにした。 その上に載る commitment や arithmetization の正しさは、それぞれの問題である。",
  "tags": [
    "advanced-cryptography-2026",
    "fft",
    "roots-of-unity",
    "interpolation",
    "finite-field",
    "local-play",
    "container"
  ],
  "exposedPorts": [
    {
      "port": 18131,
      "name": "Local verifier + Portal editor API (loopback only)"
    }
  ],
  "learningGoals": [
    "n | p-1 が evaluation domain の前提条件である理由を乗法群から説明できる",
    "omega ** n == 1 だけでは位数が確かめられない理由を説明できる",
    "n の素因数ごとの検査で位数がちょうど n であることを確かめられる",
    "係数形と評価形を index を崩さずに往復できる",
    "逆変換の omega ** -1 と n ** -1 を正しく置ける",
    "domain 上の点と外の点を同じ算術で補間できる",
    "検証できない domain を計算せずに拒否できる"
  ],
  "courseAlignment": {
    "courseId": "advanced-cryptography-program",
    "edition": "2026",
    "week": 3,
    "role": "mechanism",
    "spoilerPolicy": "independent-reimplementation",
    "sources": [
      {
        "repository": "zk-tokyo/advanced-cryptography-2026",
        "ref": "a3aa4b56fa88fbe803b57d320fbc87c1a203b480",
        "path": "week3/README.md",
        "kind": "lecture"
      },
      {
        "repository": "zk-tokyo/advanced-cryptography-2026",
        "ref": "b1b46669949b5615be7941b2b62da4ea635e9c30",
        "path": "week3/week3_zksnark_slides.pdf",
        "kind": "slide"
      }
    ]
  },
  "runtime": {
    "provider": "docker",
    "engine": "compose",
    "entry": "local/docker-compose.yml",
    "verifyUrl": "http://127.0.0.1:18131/verify",
    "secretEnv": [
      "FLAG_SEED"
    ]
  },
  "scoring": {
    "kind": "multi-verify",
    "checks": [
      {
        "id": "domain",
        "label": "domain — 渡された omega の位数を自分で確かめる",
        "points": 40,
        "wrongAnswerPenalty": 13,
        "hints": [
          {
            "id": "domain-hint-1",
            "content": "F_p の乗法群の要素数は p-1 です。 位数 n の元が存在する条件と、omega ** n == 1 を満たす元の集合を分けて考えてください。 位数が「ちょうど」n であることは、n の素因数 q ごとに omega ** (n/q) != 1 で確かめられます。",
            "penalty": 16
          }
        ],
        "input": "multiline"
      },
      {
        "id": "roundtrip",
        "label": "roundtrip — 逆変換が係数を取り戻す",
        "points": 40,
        "wrongAnswerPenalty": 13,
        "hints": [
          {
            "id": "roundtrip-hint-1",
            "content": "逆変換は forward の根を逆にして回し、最後に n ** -1 を掛けます。 どちらか一方を忘れた実装は、長さも範囲も正しい間違った係数を返します。",
            "penalty": 16
          }
        ],
        "input": "multiline"
      },
      {
        "id": "ordering",
        "label": "ordering — 値を index どおりの冪に置く",
        "points": 30,
        "wrongAnswerPenalty": 10,
        "hints": [
          {
            "id": "ordering-hint-1",
            "content": "values[i] が f(omega ** i) である、という index の意味を先に固定してください。 再帰の都合で生まれる並び替えは、境界の 1 か所へ閉じ込めます。 f(x) = x を変換すると、並びの崩れはそのまま見えます。",
            "penalty": 12
          }
        ],
        "input": "multiline"
      },
      {
        "id": "interpolate",
        "label": "interpolate — domain の内外どちらの点にも答える",
        "points": 45,
        "wrongAnswerPenalty": 15,
        "hints": [
          {
            "id": "interpolate-hint-1",
            "content": "domain 上の点は、その index の値そのものが答えです。 係数へ戻ってから評価すれば、内外どちらの点も同じ算術で通り、ゼロ除算の特別扱いが要りません。",
            "penalty": 18
          }
        ],
        "input": "multiline"
      },
      {
        "id": "generalize",
        "label": "generalize — 見たことのない素数と次数でも成り立たせる",
        "points": 45,
        "wrongAnswerPenalty": 15,
        "hints": [
          {
            "id": "generalize-hint-1",
            "content": "n は 2 冪とは限りません。 p-1 を割るなら奇数の n も合法で、hidden phase は実際に使います。 名前付きの拒否 (invalid_prime / invalid_order / invalid_omega / invalid_coefficients / invalid_values / invalid_point / invalid_domain) も契約の一部です。",
            "penalty": 18
          }
        ],
        "input": "multiline"
      }
    ]
  },
  "i18n": {
    "en": {
      "name": "Does that domain actually divide?",
      "shortDescription": "Validate a handed evaluation domain instead of trusting it, and run the transform, its inverse, and interpolation only over real domains.",
      "description": "Polynomial commitments and arithmetization both stand on one quiet fact: n *distinct* evaluation points. This problem is about the implementation that trusted an evaluation domain after checking a single equation, omega ** n == 1.\n\nThat equation is satisfied by every element of every smaller subgroup — including 1. Every domain in the public tests is real, so an equation-only implementation and an order-checking one are indistinguishable there, and the broken starter passes every public test.\n\nParticipants write, in the standard library only: validate_domain for a handed (p, n, omega); fft and ifft over it; and interpolate_and_evaluate, which answers points on and off the domain. The destination is checking that omega's order is *exactly* n — omega ** (n/q) != 1 for every prime factor q of n.\n\nGrading is behavioural, not stylistic. Every hidden parameter set mixes primes where the textbook rule happens to be right with primes where it returns a plausible fake, at least half the second kind, so trusting the one equation fails by necessity rather than by luck.",
      "instructions": "## Before you start — from school math\n\n- **What you already know**: two points fix one straight line, three points fix one parabola. So **n values at n points** and **n coefficients** carry the same information, and you can move between them. With one condition — the n points must be **all different**. Counting the same point twice does not give you n points' worth of information.\n- **The world of numbers here**: every number is a remainder after dividing by a prime p, so the only numbers are 0 .. p−1. `≡` means \"leaves the same remainder mod p\". In Python, `% p` does the folding.\n- **Polynomials and evaluation**: an expression `f(x) = a0 + a1·x + a2·x² + …` is a **polynomial**, and putting a number in for x is **evaluating** it. Values are folded mod p as well.\n- **Order**: keep multiplying some number ω by itself and it eventually returns to 1. The **number of multiplications until it first reaches 1** is the **order** of ω.\n- **A one-digit example** (follow it on paper): with p = 5 and ω = 2, the powers run 2 → 4 → 3 → 1. It first reaches 1 on the fourth, so the order is 4 and the points 1, 2, 4, 3 are all different. But ω = 4 also satisfies 4⁴ = 256 ≡ 1 — and its powers run 4 → 1 → 4 → 1, only two distinct points (order 2). **Checking \"ω to the n is 1\" does not guarantee n distinct points**, and that is this entire problem.\n- **Words**: evaluation domain = the list of points values are taken at, {1, ω, ω², …, ω^(n−1)}. Primitive n-th root of unity = an ω whose order is exactly n. Transform (fft) = building the list of values from the list of coefficients. Inverse transform (ifft) = the other direction. Interpolation = determining the polynomial that passes through n given values. Inverse = the partner that multiplies to 1 (Python: `pow(x, -1, p)`). checkpoint = one graded question; this problem has five.\n- **Where this problem sits**: the fourth problem of Week 3. In the previous one, `ac26-w3-ntt-roots`, **you chose** ω. Here ω **is handed to you** — this is the receiving side, not the choosing side.\n\n## The situation\n\nDown at the bottom, the proof machinery rests on \"n distinct evaluation points\". That list of points is built somewhere else and arrives at your code as `(p, n, omega)`. Whoever sent it is not guaranteed to be right.\n\nThe `fftdomain.py` you are given runs, and it passes every public test. But it trusts the omega it was handed on the strength of **one equation**: `omega to the n is 1`. As with p = 5, ω = 4 above, elements of a smaller cycle satisfy that equation too. So does 1. Every domain the public tests use is real, so the equation-only implementation and the order-checking one are indistinguishable there.\n\nYour job is to **decide for yourself** whether the `(p, n, omega)` you received is real, and to refuse to compute at all on one that is not.\n\n## First move\n\nIn Participant Portal (the site showing this statement), select **起動 (Start)**. The **problem editor** appears on the same page, carrying `fftdomain.py`.\n\nPress **Inspect evidence**. It shows five things:\n\n1. **deployment** — a string proving the runtime really ran. Nothing here submits it.\n2. **field family** — the list of primes the grading draws from.\n3. **the legal n for each prime** — n is legal exactly when **n divides p−1**.\n4. **a real domain** (same on every deployment) — p = 17, n = 4, ω = 13, with `f(x) = 1 + 2x + 3x² + 4x³` and a table of ω^i and f(ω^i) for i = 0..3. The points are 1, 13, 16, 4 — all different.\n5. **a domain that only looks real** (same on every deployment) — p = 97, n = 8, ω = 75. `ω to the 8 is 1`, the equation everyone checks, does hold. But its powers are `1, 75, 96, 22, 1, 75, 96, 22` — **back to 1 on the fourth, the second half repeating the first**. The 8 points you wanted are only 4.\n\n**Check item 5 on paper first**: 75² mod 97 is 96, and 96² = 9216 mod 97 is 1, so 75 returns to 1 at the fourth power. Once you have seen \"one equation is not enough\" with your own eyes, all five checkpoints reduce to that one decision.\n\n## What you answer — five checkpoints\n\n`fftdomain.py` is the only file you edit. The input validation (`_parse_prime` / `_parse_list`), the single-point evaluation (`_evaluate`) and the skeletons of the four functions are there from the start, and there is a place for the decision to live: `_domain_ok`. You never create a file.\n\n**The function shapes** (exactly as in the starter; all of them return a dict):\n\n- `validate_domain(prime, order, omega)` → `{\"ok\": True, \"valid\": True}` or `{\"ok\": True, \"valid\": False}`. It is **real** only when omega's order is exactly `order` (which then automatically divides p−1). Malformed input is `{\"ok\": False, \"error\": \"<name>\"}`, with `invalid_prime` (not a prime), `invalid_order` (not an integer, or outside 1..4096), `invalid_omega` (not an integer). **An `order` that is a legal integer but does not divide p−1 is not an error — it is `valid: False`.**\n- `fft(coefficients, omega, prime)` → `{\"ok\": True, \"values\": [...]}`, with n = `len(coefficients)`. `values[i]` is `a0 + a1·(ω^i) + a2·(ω^i)² + …` folded mod prime, listed for i = 0 .. n−1 **in that order**. A domain that is not real is `{\"ok\": False, \"error\": \"invalid_domain\"}`; malformed coefficients are `invalid_coefficients`.\n- `ifft(values, omega, prime)` → `{\"ok\": True, \"coefficients\": [...]}`. It is the same computation with **the inverse of omega**, then everything multiplied by **the inverse of n**. Malformed values are `invalid_values`; a domain that is not real is `invalid_domain`.\n- `interpolate_and_evaluate(values, omega, point, prime)` → `{\"ok\": True, \"value\": f(point)}`. `values` are the values over the domain; return the polynomial through them, evaluated at `point`. A `point` that is not an integer in 0 .. p−1 is `invalid_point`.\n\nThe checkpoints, in order:\n\n1. **domain** (40 pts) — `validate_domain` separates the real from the fake. The fakes include elements of order smaller than n **and larger than n**. And `fft` and `ifft`, handed a fake, must refuse with `invalid_domain` instead of computing.\n2. **roundtrip** (40 pts) — and on a real domain `fft` → `ifft` returns the original coefficients. Forgetting either inverse yields **wrong coefficients** of the right length and range, so nothing looks wrong.\n3. **ordering** (30 pts) — and `values[i]` is the value at the i-th power of ω. Whether a reordering crept in shows up at once if you transform `f(x) = x` (coefficients `[0, 1, 0, …]`): what comes back should be the powers of ω themselves, in order.\n4. **interpolate** (45 pts) — and `interpolate_and_evaluate` returns, for a point of the domain, exactly the value listed at that index, and answers points outside the domain with the same arithmetic. There is a way to write it that never needs a divide-by-zero special case.\n5. **generalize** (45 pts) — all of the above again over **primes and n the public tests never use**, including orders that are not powers of two (3, 9, …). n only has to divide p−1. The named errors are all re-checked here too.\n\nEach later checkpoint re-checks the earlier properties: until the order decision is in, none of the five passes, and once it is in, several fall at once. **Deciding whether the order is exactly n** can be done straight from the definition — start at 1, keep multiplying by ω, count how many multiplications until you first reach 1, and compare with n. Every prime here is below ten thousand, so that is fast enough.\n\nFor all five, pressing that checkpoint's **提出 (Submit, +N pt)** sends whatever `fftdomain.py` currently holds in the editor and grades it (even where an input box is shown, what is graded is the editor's contents). The five are graded separately, so getting stuck on one does not cost the others, and you can resubmit freely — each wrong submission costs 10 to 15 points depending on the checkpoint.\n\n**Run public tests** is a local preview. Every domain it uses is real, so **the untouched `fftdomain.py` passes all of them**. **Restore starter** puts the original `fftdomain.py` back.\n\n## Goal\n\nYou are done when you decide for yourself whether the `(p, n, omega)` handed to you is a real evaluation domain, the transform, inverse and interpolation round-trip correctly on the real ones, and the fake ones are refused without being computed on — and when that still holds for primes and n you have not seen.",
      "writeup": "## omega ** n == 1 only says \"n is a multiple of the order\"\n\nThe equation says the order divides n, not that it equals n. An element of order 4 also lands on 1 after 8 steps; the element 1 lands on 1 after any number of steps. Accept an omega on that one equation and its powers repeat: the n-point evaluation is really 4 points, or 1.\n\n## Order exactly n takes both halves\n\nBesides omega ** n == 1, check omega ** (n/q) != 1 for every prime factor q of n. That is what \"reaches 1 for the first time at n\" means. And n must divide p-1 first — otherwise no element of order n exists at all.\n\n## The inverse and the interpolation stand on the domain\n\nThe inverse runs at the powers of omega ** -1 and scales by n ** -1; forgetting either returns well-shaped wrong coefficients. Interpolation that goes back to coefficients answers members and non-members with the same arithmetic, and never needs to special-case a division by zero.\n\n## What this does not prove\n\nIt does not make the FFT fast. It decides whether a handed domain is real, and makes the transform, its inverse, and interpolation hold only over real ones. Whatever is built on top — commitments, arithmetization — is its own problem.",
      "learningGoals": [
        "Explain from the multiplicative group why n | p-1 is a precondition",
        "Explain why omega ** n == 1 alone cannot establish the order",
        "Check order exactly n with a per-prime-factor test",
        "Round-trip coefficient and evaluation form without breaking the indexing",
        "Place both omega ** -1 and n ** -1 correctly in the inverse",
        "Interpolate members and non-members with the same arithmetic",
        "Refuse a domain you cannot verify instead of computing on it"
      ],
      "checks": [
        {
          "id": "domain",
          "label": "domain - check the order of the omega you were handed",
          "hints": [
            {
              "id": "domain-hint-1",
              "content": "The multiplicative group of F_p has p-1 elements. Separate \"an element of order n exists\" from \"this element satisfies omega ** n == 1\". Order exactly n is checked per prime factor q of n: omega ** (n/q) != 1."
            }
          ]
        },
        {
          "id": "roundtrip",
          "label": "roundtrip - turn the values back into the coefficients",
          "hints": [
            {
              "id": "roundtrip-hint-1",
              "content": "The inverse runs at the inverse root and scales by n ** -1 at the end. Forget either and you get wrong coefficients of the right length and range."
            }
          ]
        },
        {
          "id": "ordering",
          "label": "ordering - put each value at the power its index names",
          "hints": [
            {
              "id": "ordering-hint-1",
              "content": "Fix the meaning of the index first: values[i] is f(omega ** i). Confine any recursion-order shuffle to one boundary. Transforming f(x) = x makes a broken order directly visible."
            }
          ]
        },
        {
          "id": "interpolate",
          "label": "interpolate - answer points on and off the domain",
          "hints": [
            {
              "id": "interpolate-hint-1",
              "content": "A domain member's answer is its listed value. Going back to coefficients first answers both cases with the same arithmetic and never divides by zero."
            }
          ]
        },
        {
          "id": "generalize",
          "label": "generalize - hold for primes and orders you have not seen",
          "hints": [
            {
              "id": "generalize-hint-1",
              "content": "n is not always a power of two: any n dividing p-1 is legal, and the hidden phases use odd ones. The named refusals (invalid_prime / invalid_order / invalid_omega / invalid_coefficients / invalid_values / invalid_point / invalid_domain) are part of the contract too."
            }
          ]
        }
      ]
    }
  }
}
