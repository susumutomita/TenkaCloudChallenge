{
  "$schema": "../../SCHEMA.json",
  "id": "stackstack-safe-exposure",
  "name": "誰に見せるかを決める",
  "category": "Challenge",
  "status": "ready",
  "visibility": "public",
  "difficulty": 3,
  "estimatedDuration": "60〜75 分",
  "shortDescription": "入社三日目。 昨日あなたが外から見えるようにした掲示板は、 今日から取引先にも配られる。\n\n出社したら CTO が机の横に立っている。 「星野デザインにも同じ板を出して。 下書き機能ごと。 管理画面と、 各自の下書きは外に出さないで。 それと監視は止めないこと ── 『安全にするために全部閉じました』 は無しね。 それをやった会社を知ってる」\n\n前任の SRE が残したファイルの中に `access.json` がある。 中身は `defaultEffect: \"allow\"` と、 ほぼ空のルール一覧。 引き継ぎメモには 「あとで締めます」 とだけ書いてあって、 その人はもういない。 いま誰が何に手が届く状態なのかは、 誰も書き残していない。",
  "instructions": "## はじめに\n\n取引先にも使わせる板で、 管理画面と他人の下書きまで見えています。 認証だけでなく、 owner・tenant・role・共有範囲を使ってリソース単位に認可する問題です。\n\n## ブラウザだけで進める\n\n1. `/portal` を開き、 rule の評価順、 require の意味、 ステージング鍵、 現在の gate を読む\n2. `/portal/review` で赤い probe と、 どの rule が判定したかを見る\n3. `/docs` の `PATCH /api/settings` で access policy を変更する。 リポジトリのファイルは編集しない\n4. `/portal/review` と `/docs` の `GET /posture` で、 拒否だけでなく取引先・本人・管理者・監視が動くことも確認する\n5. やり直しは `DELETE /api/settings`\n\n## ゴール\n\n5つの gate がすべて緑になり、 `readyToken` が出ること。 `/portal` と `/portal/review` は policy の外なので、 締めすぎても診断は続けられます。",
  "description": "## 目的\n\nIssue 286。 認証と認可を区別する、 公開エンドポイントと保護対象を仕分ける、 未認証アクセスを拒否する、 他ユーザー・他テナントのデータアクセスを止める、 そのうえで正常利用と監視を落とさない ── この 5 つを 1 枚の access ドキュメントの上で通す。\n\n## クラウドではなくコンテナで何を模しているか\n\nクラウドアカウントも ID プロバイダも使わない。 コンテナ内に最小の認可基盤を置く。\n\n| この問題 | 現実の対応 |\n|---|---|\n| `Authorization: Bearer sk_...` の 4 アカウント | Cognito / ALB authenticator が解決する principal |\n| `access.json` の順序つき rule | IAM / OPA / ALB listener rule のような宣言的ポリシー |\n| `owner` / `tenant` / `shared` | リソース属性に基づく認可 (ABAC) |\n| `/portal/admin/*` | 管理コンソール・運用 API |\n| `GET /portal/healthz` | ALB / 外形監視のヘルスチェック経路 |\n\n**認証の発行者だけが違う。** 採点している性質 ── 「有効な principal を持たないリクエストが管理オブジェクトに到達しない」 「オブジェクトの属性が判定に参加する」 ── は同じで、 それを表す access ドキュメントも同じ形。 ここは正直に書いておく。\n\n**板そのものは access.json の管轄外。** `GET /` `api/board` `api/logs` `healthz` `posture` `POST /api/posts` は共有ベースアプリのルートで、 scenario は再宣言できない (再宣言は boot 失敗)。 だからこの問題が締めるのは `/portal` 以下だけで、 README にもそう書いてある。 「ポリシーが実際より遠くまで届く」 とは主張しない。\n\n## 何を実測しているか\n\ngate は 5 つで、 それぞれが probe 群 1 つの判定そのもの。 gate と checkpoint が別経路で測って食い違う、 という事故が起きないように、 どちらも同じ `handle()` を通る ── gate はプロセス内から、 checkpoint はソケット越しに。\n\n| gate | probe 群 |\n|---|---|\n| `service_intact` | 鍵なしの healthz / 4 アカウントの /portal/me / 各自が自分の下書きを読む / 共有された runbook を同僚が読む |\n| `drafts_usable` | 書く → 読み返す → 消す (毎回新しい id) / 一覧が自分のものを返す |\n| `drafts_scoped` | 同一テナントの他人の private / 他テナント / 他テナントの team 共有 / 鍵なし / 一覧の混入 / 他人の下書きの削除 |\n| `admin_available` | 管理者が handover / audit / 全下書き / 管理削除に到達する |\n| `admin_sealed` | 4 つの admin ルート × {鍵なし, 未知の鍵, member 3 人} = 20 通り |\n\n| checkpoint | 実測しているもの |\n|---|---|\n| `exposed-record` | 取引先の private 下書きの本文にある seed 由来の符号 (リポジトリのどこにも無い) |\n| `admin-sealed` | 実 HTTP で admin surface を叩く + `admin_available` **と** `admin_sealed` の両方 + その gate の受領証 |\n| `drafts-sealed` | 実 HTTP で draft surface を叩く + `service_intact` / `drafts_usable` / `drafts_scoped` + 受領証 |\n| `exposure-signoff` | healthz / admin / draft の 3 surface を実 HTTP + 5 gate 全緑 + `readyToken` |\n\n**absence だけの checkpoint は 1 つも無い。** 閉じたことを見る gate (`drafts_scoped` / `admin_sealed`) は、 必ず 「その機能が本当に動いている」 gate と対にして採点する。 全部拒否する stub は片方を必ず落とす。\n\n## 危険な回避策と、 それを落とす場所\n\n| 回避策 | 落ちる場所 |\n|---|---|\n| `defaultEffect: \"deny\"` だけ書いて rule 無し | `service_intact` `drafts_usable` `admin_available` が同時に赤。 3 つの sealing checkpoint 全滅 |\n| 全ルートに `require: [\"authenticated\"]` 1 本 | 落とすべき probe は全員 **有効な鍵を持っている**。 `drafts_scoped` `admin_sealed` は赤のまま |\n| `require: [\"subject:sre-anzu\"]` で自分に固定 | `service_intact` (kenji / mika / cto が自分の下書きに届かない) と `drafts_usable` (取引先が書けない) が赤 |\n| `require: [\"authenticated\",\"tenant\"]` でテナント単位 | `drafts_scoped` が赤。 同一テナントの private を 1 件わざと置いてある |\n| `require: [\"authenticated\",\"owner\"]` だけに締める | `service_intact` が赤。 pm-kenji が team に共有した runbook が読めなくなる |\n| 探索中に見えた id を allowlist | `drafts_usable` の probe は毎回その場で作った id を読み返す |\n| 監視経路を閉じる | `service_intact` が赤 |\n| 送信元アドレスで絞る | `client-ip:` / `ip:` は **理由つきで拒否される**。 公開ポート越しに届くのでこのアプリが見ているのは proxy のアドレスであり、 条件として成立しない。 policy が読めない間 governed ルートは 503 で、 全 gate が赤 |\n| 漏れている下書きを消して隠す | `service_intact` (取引先が自分の下書きを読めない) と `admin_available` (4 件そろっていない) が赤 |\n| 直して token をコピーしてから元に戻す | checkpoint は毎回その場で実 HTTP + gate 再評価。 受領証は証拠であって成績ではない |\n\n## 採点が壊さないこと\n\ncheckpoint が送るリクエストは **全部 GET**。 作成も削除もしない。 誤答でも再送でも参加者の環境は変わらない。 gate 側の probe は書き込みを含むが、 評価は先頭から末尾まで同期実行で、 作った probe 用の下書きは同じ同期ブロックの中で必ず消える ── 他のリクエストがその途中状態を観測できる瞬間は存在しない。 参加者が書いた下書きには一切触れない。\n\n## 答えがリポジトリにあることについて (正直に)\n\nこのカタログは OSS で、 `scripts/stackstack-safe-exposure.test.ts` には **通る access.json が入っている**。 参加者のチェックアウトにも `problems/` として同じものが入っている。 隠せない以上、 隠したふりはしない ── README に明記し、 ヒントもそれ込みで値付けしてある (無料ヒントは 1 つも無い)。 テストが reference policy を持たないと 「この checkpoint は正しい答えを通す」 を証明できず、 それは通らない答えを弾くことより重要な保証なので、 トレードオフとしてこちらを取る。\n\n## Battle への引き継ぎ (正直な粒度)\n\n完了条件の 「Challenge 単体と StackStack の Expose フェーズで検証ロジックを共用できる」 は **構造的にしか満たしていない**。 `battles/stackstack` は今日時点で difficulty 4 / `phased-polling` の CloudFormation 問題で、 phase は production-ramp と incident-response の 2 つだけ。 Expose フェーズは存在せず、 EC2 上で動いているのは別のアプリで、 `access` ブロックを積んだ config も無い。 だからこの scenario の JavaScript も in-process のリスナも境界を越えない。\n\n越えられる形だけを書いておく: (1) probe 表と executor の分離 (`GROUPS` は宣言データ、 `runProbe` が実行器) — Battle 側は同じ形の表を ALB URL に向ける、 (2) 採点する事実 = 「外から観測された 401/403/200」、 (3) gate = 申告ではなく実測。 実際に共用するには battles/stackstack に Expose phase を足し、 EC2 のワークロードを同じ access ドキュメントを読むものに置き換える必要がある ── それは #291 の作業で、 ここでは範囲外。 「同じ検証コードをそのまま再利用する」 とは書かない。\n\n## 既知の限界\n\nコンテナ内の判定なので、 参加者が自分のチェックアウトの `runtimes/stackstack/` を書き換えて image を焼き直せば全部無効化できる。 このカタログのコンテナ問題すべてに共通する local-play の正直さの限界で、 採点バグではない。",
  "writeup": "## この 1 時間で触ったもの\n\n- **認証と認可は別の話** — 全ルートに `require: [\"authenticated\"]` を 1 本置くと、 鍵の無いリクエストは止まる。 それでも `drafts_scoped` と `admin_sealed` は赤のままだった。 落とすべき probe は全員、 正しい鍵を持った本物のアカウントだからだ。 「ログインが要る」 は 「あなたに見せていい」 とは何の関係もない。\n- **判定にオブジェクトの属性が参加する** — 同じ `GET /portal/draft` でも、 対象が自分のものか、 同僚が team に共有したものか、 別テナントのものかで答えが変わる。 caller の属性 (role / tenant) だけを見るポリシーでは、 同一テナントの他人の private が最後まで開いたままになる。\n- **締めすぎも不合格** — pm-kenji は runbook を 「チームで読めるように」 わざと共有している。 `owner` だけに締めるとそれが読めなくなり、 `service_intact` が赤になる。 セキュリティ対応が業務要件を壊したら、 それは対応ではない。\n- **一覧と単体は同じ判定でなければならない** — `/portal/drafts` は 1 件ずつ `/portal/draft` と同じ decide を通して絞っている。 別々に書けば、 単体は 403 なのに一覧には出る、 という定番の漏れ方をする。\n- **存在を漏らさない** — 判定はオブジェクトを読み込んだ後、 応答を返す前。 拒否された相手には 「無い id」 と 「読めない id」 が同じ答えになる。 順序を逆にすると、 404 と 403 の差だけで id を総当たりできる。\n\n## なぜ 「全部閉じた」 で満点にしないのか\n\n`defaultEffect: \"deny\"` の 1 文字は、 AI に 「これを安全にして」 と言ったときに最初に返ってくる答えでもある。 それで漏れは止まる。 止まるのは業務も同じで、 取引先は下書きを書けず、 管理者は管理画面に入れず、 監視は死んだと言い出す。 この問題の gate が 5 つあって 2 つしか 「閉じる」 側でないのは、 そのためだ。\n\n## 次にやること\n\n同じ板を、 今度は本番品質まで持っていく `stackstack` Battle が本編。",
  "tags": [
    "stackstack",
    "authorization",
    "authentication",
    "multi-tenant",
    "access-control",
    "local-play",
    "container",
    "web-app"
  ],
  "exposedPorts": [
    {
      "port": 18080,
      "name": "Web (board / portal review)"
    },
    {
      "port": 18081,
      "name": "/verify (loopback)"
    }
  ],
  "learningGoals": [
    "認証 (誰か) と認可 (何をしてよいか) を、 同じリクエストの別の判定として区別できる",
    "公開したままにするエンドポイントと保護するエンドポイントを、 一覧の上で仕分けられる",
    "オブジェクトの owner / tenant / visibility が判定に参加する認可を、 宣言的なルールとして書ける",
    "一覧ルートと単体ルートを同じ判定で通し、 存在の有無を応答から漏らさない",
    "遮断だけで満足せず、 正常利用と監視経路が生きていることを実測で確かめる"
  ],
  "i18n": {
    "en": {
      "name": "Deciding Who Sees What",
      "shortDescription": "Day three. The board you made visible from the outside yesterday goes to a customer today.\n\nThe CTO is standing by your desk when you arrive. \"Give Hoshino Design the same board. Drafts and all. The admin screens and each person's drafts do not go outside. And do not stop the monitoring — 'we made it safe by closing everything' is not a thing. I have worked at a company that did that.\"\n\nAmong the files the previous SRE left is `access.json`. Inside it: `defaultEffect: \"allow\"` and an almost empty list of rules. The handover note says only \"I will tighten this later,\" and that person is gone. Nobody wrote down who can currently reach what.",
      "instructions": "## Getting started\n\nA customer-facing board exposes admin screens and other people's drafts. Authorize each resource from owner, tenant, role, and sharing attributes—not merely from authentication.\n\n## Work entirely in the browser\n\n1. Open `/portal` for rule order, requirement meanings, staging keys, and current gates\n2. Open `/portal/review` for each failing probe and the rule that decided it\n3. Change the access policy with `PATCH /api/settings` in `/docs`. Do not edit repository files\n4. Use `/portal/review` and `GET /posture` to confirm the customer, owners, admin, and monitoring still work as forbidden access closes\n5. Retry with `DELETE /api/settings`\n\n## Goal\n\nMake all five gates green and emit `readyToken`. `/portal` and `/portal/review` stay outside the policy so diagnostics remain reachable after an over-tightening mistake.",
      "description": "## Purpose\n\nIssue 286. Separate authentication from authorization, sort the public endpoints from the protected ones, refuse unauthenticated access, stop cross-user and cross-tenant data access, and keep normal use and monitoring alive — all five on one access document.\n\n## What the container models instead of a cloud\n\nNo cloud account and no identity provider. A minimal authorization plane inside the container instead.\n\n| Here | Real equivalent |\n|---|---|\n| four `Authorization: Bearer sk_...` accounts | a principal resolved by Cognito or an ALB authenticator |\n| ordered rules in `access.json` | a declarative policy — IAM, OPA, ALB listener rules |\n| `owner` / `tenant` / `shared` | attribute-based authorization over the resource |\n| `/portal/admin/*` | an admin console or an operations API |\n| `GET /portal/healthz` | the ALB / synthetic monitor health path |\n\n**Only the credential issuer differs.** The properties being graded — a request with no valid principal never reaches an admin object; the object's own attributes participate in the decision — are the same ones, and the document that expresses them is the same shape.\n\n**The board itself is outside the document's reach.** `GET /`, `api/board`, `api/logs`, `healthz`, `posture` and `POST /api/posts` belong to the shared base app, and a scenario redeclaring one of them is a boot failure. So what this problem closes is everything under `/portal`, and the READMEs say so rather than implying a policy reaches further than it does.\n\n## What is measured\n\nFive gates, each the verdict of one probe group. Gates and checkpoints go through the same `handle()` — the gates in-process, the checkpoints over the socket — so the panel and the score cannot disagree about the same policy.\n\n| gate | probe group |\n|---|---|\n| `service_intact` | health with no key / four accounts on /portal/me / each account reading their own draft / a colleague reading the shared runbook |\n| `drafts_usable` | write → read back → remove (a new id every time) / the list still lists |\n| `drafts_scoped` | another member's private draft, another tenant, another tenant's team-shared draft, no key, list contamination, deleting somebody else's |\n| `admin_available` | the admin reaches handover / audit / every draft / the admin delete |\n| `admin_sealed` | 4 admin routes × {no key, unknown key, three members} = 20 refusals |\n\n| checkpoint | what is measured |\n|---|---|\n| `exposed-record` | the seed-derived marking inside the customer's private draft (in no committed file) |\n| `admin-sealed` | a real HTTP pass over the admin surface + **both** `admin_available` and `admin_sealed` + that gate's receipt |\n| `drafts-sealed` | a real HTTP pass over the draft surface + `service_intact` / `drafts_usable` / `drafts_scoped` + the receipt |\n| `exposure-signoff` | health / admin / draft surfaces over real HTTP + all five gates + `readyToken` |\n\n**No checkpoint grades an absence alone.** Each closure gate is scored together with a gate proving the same feature still works, so a policy that refuses everything fails rather than passes.\n\n## Dangerous shortcuts, and where each one falls over\n\n| Shortcut | Where it falls |\n|---|---|\n| `defaultEffect: \"deny\"` and no rules | `service_intact`, `drafts_usable` and `admin_available` all go red at once; all three sealing checkpoints fail |\n| one rule, `require: [\"authenticated\"]`, everywhere | every probe that must be refused presents a **valid key**; `drafts_scoped` and `admin_sealed` stay red |\n| `require: [\"subject:sre-anzu\"]` | `service_intact` (kenji / mika / cto cannot reach their own) and `drafts_usable` (the customer cannot write) go red |\n| `require: [\"authenticated\",\"tenant\"]` | `drafts_scoped` stays red: one same-tenant private draft is seeded for exactly this |\n| `require: [\"authenticated\",\"owner\"]` only | `service_intact` goes red: the runbook pm-kenji shared with the team stops being readable |\n| allowlist the ids seen while exploring | `drafts_usable` reads back an id created during the probe itself |\n| close the monitoring path | `service_intact` goes red |\n| filter on source address | `client-ip:` / `ip:` are **refused by name with the reason**: requests arrive through a published port, so the address this app sees is the proxy's. While the document will not load, every governed route answers 503 and every gate is red |\n| delete the leaking draft to hide it | `service_intact` (the customer cannot read their own) and `admin_available` (four documents are not four) go red |\n| fix it, copy the token, revert | every checkpoint re-probes over HTTP and re-evaluates the gates as it is answered; a receipt is evidence, never the grade |\n\n## What scoring does not do\n\nEvery request a checkpoint sends is a **GET**. Nothing is created and nothing is removed, so a wrong answer or a retry cannot change the participant's environment. The gate probes do write, but the whole evaluation is synchronous from first probe to last and the probe drafts are removed inside the same synchronous block — no other request can ever observe that intermediate state, and nothing the participant wrote is touched.\n\n## About the answer being in this repository (plainly)\n\nThis catalog is open source, and `scripts/stackstack-safe-exposure.test.ts` **contains an access document that passes** — the same tree is mounted in the participant's checkout as `problems/`. It cannot be hidden, so it is not pretended away: the READMEs say so, and the hints are priced with that in mind (none of them is free). A suite without a reference policy cannot prove that a checkpoint accepts the right answer, and that guarantee matters more than making the answer inconvenient to find.\n\n## What carries into the Battle (at the right granularity)\n\nThe completion condition \"the Challenge and StackStack's Expose phase can share verification logic\" is met **structurally only**. `battles/stackstack` is today a difficulty-4 `phased-polling` CloudFormation problem with exactly two phases, production-ramp and incident-response. There is no Expose phase, the EC2 workload is a different application, and no config carries an `access` block. So neither this scenario's JavaScript nor its in-process listener crosses that boundary.\n\nWhat can cross is the shape: (1) the probe table is declarative data and `runProbe` is the executor, so the Battle points a table of the same shape at an ALB URL; (2) the scored fact is an externally observed 401 / 403 / 200; (3) a gate measures rather than accepts a claim. Actually sharing it requires adding an Expose phase to `battles/stackstack` and replacing the EC2 workload with one that reads the same access document — that is #291's work and out of scope here. This problem does not claim the verification code itself is reused.\n\n## Known limit\n\nThe verdict is computed inside the container, so a participant who edits `runtimes/stackstack/` in their own checkout and rebuilds the image defeats every check here. That is the local-play honesty limit every container problem in this catalog shares, not a scoring bug.",
      "writeup": "## What you touched in that hour\n\n- **Authentication and authorization are different questions** — one rule, `require: [\"authenticated\"]`, on every route stops the requests with no key. `drafts_scoped` and `admin_sealed` stayed red anyway, because every probe that must be refused is a real account holding a real key. \"You have to log in\" says nothing at all about \"you may see this.\"\n- **The object's attributes participate in the decision** — the same `GET /portal/draft` has to answer differently depending on whether the target is yours, something a colleague shared with the team, or something in another tenant. A policy that only looks at the caller's attributes leaves the same-tenant private draft open to the end.\n- **Over-tightening is also a failure** — pm-kenji shared the runbook with the team on purpose. Locking drafts to `owner` alone makes it unreadable and turns `service_intact` red. A security change that breaks a documented business requirement is not a fix.\n- **A list and a read must be the same decision** — `/portal/drafts` filters element by element through the same `decide` the object route uses. Written separately, they drift into the classic leak: 403 on the object, present in the list.\n- **Do not leak existence** — the decision happens after the object is loaded and before the response is written, so a refused caller gets the same answer for an id that does not exist as for one they may not read. Reverse the order and the 404-versus-403 difference alone enumerates every id.\n\n## Why \"closed everything\" is not full marks\n\n`defaultEffect: \"deny\"` is one word, and it is also the first thing an AI hands back when you ask it to make something safe. It does stop the leak. It stops the business too: the customer cannot write a draft, the admin cannot open the admin screens, and the monitor starts reporting the service as dead. That is why this problem has five gates and only two of them are about closing.\n\n## Next\n\nThe main event is the `stackstack` Battle: the same board, taken to production quality.",
      "learningGoals": [
        "Separate authentication (who is this) from authorization (may they do this) as two decisions about one request",
        "Sort the endpoints that stay public from the ones that must be protected, as a list you can check",
        "Express authorization where the object's owner / tenant / visibility participate in the decision, as declarative rules",
        "Run a collection route and an object route through one decision, and keep existence out of the response",
        "Refuse to stop at blocking: confirm by measurement that normal use and the monitoring path are still alive"
      ],
      "checks": [
        {
          "id": "exposed-record",
          "label": "The reference inside the customer's draft",
          "hints": [
            {
              "id": "exp-record-1",
              "content": "The review console lists a draft surface you have not used yet. Every draft record says who owns it and which tenant it belongs to — list them with one of the staging keys and read those two fields on what comes back."
            },
            {
              "id": "exp-record-2",
              "content": "In `/docs`, run `GET /portal/drafts` with header `{\"Authorization\":\"Bearer <any staging key>\"}` and find the record whose `tenant` is not `tenka`. Its body carries a `TC{exposed_...}` reference; submit only that value. It remains reachable through `GET /portal/admin/drafts` with the admin key after the fix."
            }
          ]
        },
        {
          "id": "admin-sealed",
          "label": "Admin screens, for admins only",
          "hints": [
            {
              "id": "admin-seal-1",
              "content": "Run `GET /portal/review` from `/docs` and inspect the failed entries in `groups.admin_sealed.probes`. Each names the rule that decided it. Two admin gates have to be true at the same time, not one."
            },
            {
              "id": "admin-seal-2",
              "content": "Every probe in the closure half presents a key this app recognises, so requiring a key changes none of them. What separates cto-daichi from the other three is on `/portal/me`. Remember that the admin surface spans four routes, and one of them is not a GET."
            },
            {
              "id": "admin-seal-3",
              "content": "An admin rule is `{\"effect\":\"allow\",\"methods\":[\"*\"],\"path\":\"/portal/admin/*\",\"require\":[\"authenticated\",\"role:admin\"]}`, with `defaultEffect` set to `deny` so anything unmatched falls closed. Once you do that, the rest of `/portal` needs rules of its own or `service_intact` goes red with it. Then submit the `admin_sealed` entry from `tokens` on `GET /posture`."
            }
          ]
        },
        {
          "id": "drafts-sealed",
          "label": "Drafts, for their owner and whoever they were shared with",
          "hints": [
            {
              "id": "drafts-seal-1",
              "content": "A draft record carries `owner`, `tenant` and `visibility`. Of the four seeded ones, exactly one is meant to stay readable by a colleague who did not write it, and one that looks similar is not — the difference is written on the object itself, not on the caller."
            },
            {
              "id": "drafts-seal-2",
              "content": "Two allow rules on the same path are fine: the first one whose requirements all hold decides, and one that matches the path but not the requirements is skipped rather than treated as a refusal. So one rule can describe \"it is mine\" and the next \"my tenant, and it was shared\". The collection route is a different path from the object route, and with no single object `owner` and `tenant` are always false there — the list is filtered element by element through the object rule anyway, so it does not need to repeat them."
            },
            {
              "id": "drafts-seal-3",
              "content": "Use an owner rule for `GET`/`DELETE /portal/draft`, then a tenant-and-shared rule for `GET /portal/draft`, plus `authenticated` on `GET`/`POST /portal/drafts` and `/portal/me`. Run `GET /portal/review` from `/docs` and confirm `groups.drafts_usable` before submitting `drafts_scoped` from `tokens` on `GET /posture` — closing without that group green is not a pass."
            }
          ]
        },
        {
          "id": "exposure-signoff",
          "label": "Sign-off for the exposure review",
          "hints": [
            {
              "id": "signoff-1",
              "content": "`GET /posture` lists five gates and the sign-off appears only while all five are true. While it is null, the red gates are your to-do list and `GET /portal/review` says which probe inside each one is failing and which rule decided it."
            },
            {
              "id": "signoff-2",
              "content": "Run `GET /posture` from `/docs` and submit `readyToken`. If `service_intact` is red you closed too much: the health path must answer without a credential, all four accounts must reach `/portal/me`, and the shared runbook must remain readable by its colleague."
            }
          ]
        }
      ]
    }
  },
  "runtime": {
    "provider": "docker",
    "engine": "compose",
    "entry": "local/docker-compose.yml",
    "challengeEndpoints": {
      "Review": "http://127.0.0.1:18080/portal",
      "Board": "http://127.0.0.1:18080/"
    },
    "verifyUrl": "http://127.0.0.1:18081/verify",
    "secretEnv": [
      "FLAG_SEED"
    ]
  },
  "scoring": {
    "kind": "multi-verify",
    "checks": [
      {
        "id": "exposed-record",
        "label": "取引先の下書きに書かれていた管理番号",
        "points": 30,
        "wrongAnswerPenalty": 2,
        "hints": [
          {
            "id": "exp-record-1",
            "content": "レビュー画面には、 まだ触っていない下書きのルートが並んでいます。 下書きは 1 件ずつ 「誰のものか」 と 「どのテナントのものか」 を持っています。 ステージング用の鍵のどれかで一覧を出して、 返ってきたものの owner と tenant を見てください。",
            "penalty": 4
          },
          {
            "id": "exp-record-2",
            "content": "`/docs` で header に `{\"Authorization\":\"Bearer <ステージング鍵>\"}` を入れ、 `GET /portal/drafts` を実行します。 `tenant` が `tenka` ではない記録の本文にある `TC{exposed_...}` を提出してください。 修正後も管理者の鍵で `GET /portal/admin/drafts` を実行すれば同じ記録に届きます。",
            "penalty": 9
          }
        ]
      },
      {
        "id": "admin-sealed",
        "label": "管理画面を管理者だけに戻す",
        "points": 50,
        "wrongAnswerPenalty": 3,
        "hints": [
          {
            "id": "admin-seal-1",
            "content": "`/docs` から `GET /portal/review` を実行し、 `groups.admin_sealed.probes` の失敗項目を見ます。 どの rule が決めたか (`decidedBy`) も出ます。 admin 側は gate が 2 つあり、 両方同時に true でないと通りません。",
            "penalty": 5
          },
          {
            "id": "admin-seal-2",
            "content": "落とすべき probe は全員、 このアプリが知っている鍵を出しています。 だから 「鍵を要求する」 だけでは 1 本も変わりません。 cto-daichi と他の 3 人を分けているものは `/portal/me` に出ています。 admin の surface は 4 ルートあって、 そのうち 1 本は GET ではないことも忘れずに。",
            "penalty": 7
          },
          {
            "id": "admin-seal-3",
            "content": "admin の rule は `{\"effect\":\"allow\",\"methods\":[\"*\"],\"path\":\"/portal/admin/*\",\"require\":[\"authenticated\",\"role:admin\"]}` で、 `defaultEffect` を `deny` にして合致しないものを閉じます。 そうすると `/portal` の残りにも rule が要ります ── 無いと `service_intact` が一緒に赤くなります。 そのうえで `GET /posture` の `tokens` にある `admin_sealed` を提出してください。",
            "penalty": 11
          }
        ]
      },
      {
        "id": "drafts-sealed",
        "label": "下書きを持ち主と共有先だけに戻す",
        "points": 60,
        "wrongAnswerPenalty": 3,
        "hints": [
          {
            "id": "drafts-seal-1",
            "content": "下書きは `owner` と `tenant` と `visibility` を持っています。 最初からある 4 件のうち、 ちょうど 1 件は 「書いていない同僚が読めたままでよい」 もので、 よく似たもう 1 件は違います。 その差は caller ではなく、 オブジェクト自身に書いてあります。",
            "penalty": 6
          },
          {
            "id": "drafts-seal-2",
            "content": "同じ path に allow を 2 本置いて構いません。 require が全部そろった最初の 1 本が決め、 path は合うが require が足りない rule は 「拒否」 ではなく 「次へ」 です。 だから 1 本目に 「自分のもの」、 2 本目に 「自分のテナントで、 共有されているもの」 と書けます。 一覧ルートは単体ルートとは別の path で、 対象が 1 件に定まらないので `owner` / `tenant` はそこでは必ず false です ── 一覧は単体の rule で 1 件ずつ絞られるので、 同じ条件を書き直す必要はありません。",
            "penalty": 9
          },
          {
            "id": "drafts-seal-3",
            "content": "`GET`/`DELETE /portal/draft` には owner rule、 次の `GET /portal/draft` には tenant-and-shared rule、 `GET`/`POST /portal/drafts` と `/portal/me` には `authenticated` を使います。 `/docs` から `GET /portal/review` を実行し、 `groups.drafts_usable` が緑になってから `GET /posture` の `drafts_scoped` を提出してください。",
            "penalty": 13
          }
        ]
      },
      {
        "id": "exposure-signoff",
        "label": "公開範囲レビューの合格印",
        "points": 60,
        "wrongAnswerPenalty": 2,
        "hints": [
          {
            "id": "signoff-1",
            "content": "`GET /posture` は gate を 5 つ返します。 合格印は 5 つとも true になって初めて出ます。 null の間は、 赤い gate がそのままやることリストです。 どの probe が落ちていて、 どの rule が決めたかは `GET /portal/review` に出ています。",
            "penalty": 8
          },
          {
            "id": "signoff-2",
            "content": "`/docs` から `GET /posture` を実行し、 `readyToken` を提出してください。 `service_intact` が赤なら締めすぎです。 監視経路は鍵なしで答え、 4 アカウントとも `/portal/me` に届き、 共有 runbook は共有先の同僚が読める必要があります。",
            "penalty": 15
          }
        ]
      }
    ]
  },
  "track": {
    "id": "stackstack-route",
    "order": 120,
    "chapter": "4. StackStack 本編"
  }
}
