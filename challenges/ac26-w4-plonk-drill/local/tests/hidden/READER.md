
## 2026-09-08 participant-only reread (Issue #716)

An independent reader used only Japanese/English participant instructions and public starter surfaces, without hidden tests, reference code or verifier implementation. The first pass found two gaps: the Japanese address hint called column tags1,2,3 “column numbers” although actual column indices are0,1,2; and the connection from f(1)=f(6)=0 to divisibility by Y²−1 was unstated. The hint now names address bases. Both languages now explain quotient and remainder, substitute1 and6 into the linear remainder AY+B, and use inverse3 of5 modulo7 to derive A=B=0. The participant-only reread confirmed both gaps resolved. This was text and hand calculation, not UI submission.
