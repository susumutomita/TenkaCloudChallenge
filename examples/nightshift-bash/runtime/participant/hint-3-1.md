# 3 / ヒント1 — 今の状態と作り直される状態

サービスの起動時に設定を作り直すスクリプトがあると、今のファイルを修復しても、次の起動で以前の内容に戻ることがあります。

必要なのは「たった今は安全」だけでなく、「次の起動でも安全」です。`app/startup.d`のファイルと、ログに残る起動時の記録を結び付けてください。この演習では、そこにある`.sh`ファイルがバッチサービスの起動時に順に実行されます。
