# 1 / ヒント1 — 誰に読む権利があるか

ファイルには「所有者」「同じグループ」「それ以外」の3つの相手に対する権限があります。`ls -l`の先頭で確認できます。`r`は読む、`w`は書く、`x`は実行する権限です。ディレクトリの`x`は中へたどり着くための権限です。

今のユーザーは`id`で確認します。ファイルの用途ではなく、そのユーザーから本当に読めるかを確かめます。バックアップも本体と同じように情報を持っています。
