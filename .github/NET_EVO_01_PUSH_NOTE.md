net-evo-01-reachability was pushed to this branch via the GitHub API because the
session's git push proxy was unavailable. The authoritative source is the local
signed commit; this API commit mirrors it. See issues #50 (series spec) and #51
(catalog core). Safe to delete once the full git push lands.
