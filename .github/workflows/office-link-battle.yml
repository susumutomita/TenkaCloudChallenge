name: office-link-battle

on:
  pull_request:
    paths:
      - 'challenges/office-link-battle/**'
      - 'challenges/office-link-gate/**'
      - '.github/workflows/office-link-battle.yml'
  push:
    branches: [main]
    paths:
      - 'challenges/office-link-battle/**'
      - 'challenges/office-link-gate/**'
      - '.github/workflows/office-link-battle.yml'

permissions:
  contents: read

jobs:
  workshop:
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
        with:
          persist-credentials: false
      - uses: oven-sh/setup-bun@735343b667d3e6f658f44d0eca948eb6282f2b76
        with:
          bun-version: '1.3.11'
      - run: bun install --frozen-lockfile
      - run: make -C challenges/office-link-gate test
      - run: node --check challenges/office-link-gate/web.js
      - run: make -C challenges/office-link-battle test
      - run: node --check challenges/office-link-battle/web.js
