name: Nightshift Bash Battle

on:
  pull_request:
    paths:
      - 'examples/nightshift-bash/**'
      - '.github/workflows/nightshift-bash.yml'
  push:
    branches: [main]
    paths:
      - 'examples/nightshift-bash/**'
      - '.github/workflows/nightshift-bash.yml'

permissions:
  contents: read

concurrency:
  group: nightshift-bash-${{ github.ref }}
  cancel-in-progress: true

jobs:
  docker-integration:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    defaults:
      run:
        shell: bash
        working-directory: examples/nightshift-bash
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
        with:
          persist-credentials: false
      - name: Bash syntax
        run: make syntax
      - name: Docker runtime integration
        run: bash tests/run.sh docker
