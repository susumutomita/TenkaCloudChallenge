name: ci

# Catalog-side CI for TenkaCloudChallenge.
#
# Validates every problems/{battles,challenges}/<id>/metadata.json against
# problems/SCHEMA.json + cross-refs the template.yaml Outputs / portal slot
# files referenced from metadata. The validator is a verbatim copy of the
# platform's scripts/validate-problems.ts (= same source of truth).
#
# Phase 2 will additionally check that publish.yml-produced S3 keys exist
# for the new commit, once the AWS IAM Role is bound.
#
# Shape: `suite` runs the test files in parallel shards, `checks` runs the
# generators and static validators, and `validate` is a no-op gate that both
# feed. `validate` exists so there is one stable check name in front of a
# matrix whose job names change whenever the shard count does.

permissions:
  contents: read

# `push:` is scoped to main on purpose. A bare `push:` fires on every branch, so a
# branch that also has a pull request ran this entire workflow TWICE for the same commit
# -- once for the push event and once for the pull_request event. Measured on
# 907bd4b6 (PR #547): 12 jobs and ~41 runner-minutes, charged twice, on 34 of the last
# 100 branch pushes. Scoping to main keeps the post-merge run and drops the duplicate.
on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

# Superseding a pull request's own earlier run is safe and is most of the saving: a
# force-push or a quick follow-up commit used to leave the previous full matrix running
# to completion. Pushes to main are NOT cancelled -- each merge commit's run is the
# record for that commit, and two merges landing close together must both finish.
concurrency:
  group: ci-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  suite:
    name: suite (${{ matrix.shard }}/4)
    runs-on: ubuntu-latest
    # Sharded because the suite's wall time tracks the size of the catalog, not
    # the size of a change: it runs each container problem's starter, reference,
    # mutation suite and /verify contract for real. It was ~5 min at 13 problems,
    # ~8 min at 43, and at 63 test files it reached the old 25-minute single-job
    # cap and was cancelled — which reads as a red X with no failing assertion,
    # the worst signal available.
    #
    # The cap below is a runaway guard, not a budget. When the catalog grows,
    # add a shard rather than minutes; scripts/validate-shard.test.ts checks the
    # matrix and the denominator below stay in agreement, so a half-done bump
    # fails loudly instead of silently skipping files.
    timeout-minutes: 15
    strategy:
      fail-fast: false
      matrix:
        shard: [1, 2, 3, 4]
    env:
      CI: "true"
      npm_config_ignore_scripts: "true"
    steps:
      - name: Checkout
        # actions/checkout v4.3.1 (= pin matches TenkaCloud main repo)
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
        with:
          # Neither job performs an authenticated git operation; it reads the
          # tree and runs repository scripts. Without this the checkout leaves a
          # token in .git/config for the life of the job, readable by everything
          # the suite executes — and the suite executes participant-shaped code.
          persist-credentials: false

      - name: Setup Bun
        # oven-sh/setup-bun v2.0.2 (= pin matches TenkaCloud main repo)
        uses: oven-sh/setup-bun@735343b667d3e6f658f44d0eca948eb6282f2b76
        with:
          bun-version: "1.3.11"

      - name: Install deps
        run: bun install --ignore-scripts

      - name: Run catalog test suite
        run: bun run scripts/validate-shard.ts --shard=${{ matrix.shard }}/4

  solvability:
    runs-on: ubuntu-latest
    # Its own job because sharding by file cannot split it: the gate probes every
    # problem inside a single test, so it lands wholly in whichever shard owns the
    # file and grows with the catalog. It took shard 1 to the 15-minute cap while
    # the other three finished in five to nine minutes.
    #
    # Splitting the gate's own work by suite shard is not the fix either — the other
    # three shards never execute the file, so they would probe nothing and each would
    # still print "no findings". SEPARATELY_SCHEDULED_FILES in
    # scripts/validate-shard.ts keeps it out of the matrix, and
    # scripts/solvability-audit.test.ts fails if this job stops naming it.
    timeout-minutes: 25
    env:
      CI: "true"
      npm_config_ignore_scripts: "true"
    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
        with:
          persist-credentials: false

      - name: Setup Bun
        uses: oven-sh/setup-bun@735343b667d3e6f658f44d0eca948eb6282f2b76
        with:
          bun-version: "1.3.11"

      - name: Install deps
        run: bun install --ignore-scripts

      - name: Run the solvability gate over the whole catalog
        run: bun test scripts/solvability-audit.test.ts

  checks:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    env:
      CI: "true"
      npm_config_ignore_scripts: "true"
    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
        with:
          persist-credentials: false

      - name: Setup Bun
        uses: oven-sh/setup-bun@735343b667d3e6f658f44d0eca948eb6282f2b76
        with:
          bun-version: "1.3.11"

      - name: Install deps
        run: bun install --ignore-scripts

      # Keep both gates explicit in CI: TypeScript proves compiler invariants,
      # while ESLint covers promise handling and other type-aware semantics.
      - name: Typecheck catalog tooling
        run: bun run typecheck

      - name: Lint catalog tooling
        run: bun run lint

      - name: Validate problem metadata
        run: bun run scripts/validate-problems.ts

      # 問題は 1 問ずつ独立して deploy できなければならないので、PortalEditorSupport は
      # 共有 package ではなく各問題への複製で配っている。複製が仕様である以上、揃って
      # いることを機械が保証しないと、1 問だけ直した修正が他へ届かず問題ごとに挙動が
      # 変わる。中身は判定せず「全部同じか」だけを見る。
      - name: Check vendored workbench copies do not drift
        run: bun run scripts/check-workbench-vendoring.ts

      - name: Check CloudFormation templates
        run: |
          bun run scripts/check-template-ascii.ts
          bun run scripts/check-template-security.ts

      # 参加者の画面に配信される HTML / inline script の欠陥 (Issue 395 / 396)。
      # ソースを読むと正しく見える種類なので、目視レビューでは落ちない。
      # stackstack-base/ も走査対象なので、この step は check:problem --all では代替できない。
      - name: Check participant-visible surface
        run: bun run check:participant-surface

      # local play は空いているポートへ再割り当てするので、焼き込んだ 18080 は
      # 「別の問題を起動したまま起動した」瞬間に別問題を指す (Issue 399)。
      - name: Check no participant surface pins a reassignable port
        run: bun run check:local-play-urls

      # 出荷ゲートを 1 問ずつ回す (Issue 382)。第三者の PR / 独自問題という想定ケースで
      # 実際に使われるのと同じ入口を CI でも通し、「1 問だけ緑」と「カタログ全体が緑」が
      # 同じ検出ロジックで決まるようにする。列挙は index.json ではなく filesystem なので、
      # catalog に登録し忘れた新問題も素通りしない。
      - name: Check every problem against the shipping gate
        run: bun run check:problem --all

      # verifier 側の生成物。metadata と生成ブロックがずれたまま merge されると、
      # Portal から見える問題文と実際に採点される内容が食い違う。
      - name: Check generated course workbenches are up to date
        run: python3 scripts/generate-course-workbenches.py --check

      - name: Check simulator workload
        run: bun run validate:simulator-workload

      - name: Check crypto battle game model
        run: bun run validate:crypto-battle

      # Cross-repository capability coverage needs an immutable Simulator
      # checkout and is enforced by simulator-compatibility.yml. Do not replace
      # that pinned proof with a moving branch or a hidden clone in this job.

      # index.json is the platform-read catalog, regenerated from every
      # metadata.json by scripts/build-index.ts. --check fails if it is stale
      # (= a problem was added/edited without rerunning the generator), so a
      # new problem can never merge while invisible to the catalog.
      - name: Check index.json is up to date
        run: |
          bun run scripts/build-index.ts --check \
            || { echo "::error::index.json is stale. Run 'bun run reindex' and commit (or 'bun run setup' once to enable the auto-reindex hook)."; exit 1; }

      # cost-report.json is the static AWS cost / leftover-billing estimate for
      # every problem (#87 Phase A), regenerated from template.yaml + rate table
      # by scripts/estimate-cost.ts. --check fails if it is stale (= a template
      # changed cost-relevant resources without rerunning the generator),
      # so always-on / billing-risk drift is caught before merge.
      - name: Check cost-report.json is up to date
        run: |
          bun run cost:check \
            || { echo "::error::cost-report.json is stale. Run 'bun run reindex' and commit (or 'bun run setup' once to enable the auto-reindex hook)."; exit 1; }

      # course:drift は course-drift.yml (schedule / workflow_dispatch / pin か
      # checker 自体が変わった PR のみ) が独立して担う。ここでも走らせると、
      # upstream 教材が動くたびに無関係な PR 全部が course:drift のためだけに
      # red になる (#488, #499) — validate-problems 等と違い、pin の SHA を
      # 直さない限り docs/curricula/<track>/SYNC.md の判断待ちのまま止まり
      # 続けることがあるチェックなので、他の全ゲートと同じ「全 PR 必須」枠に
      # 同居させない。

  # Which of the five per-problem runtime jobs below this pull request can actually
  # affect. Each of those jobs boots one problem's stack and scores it; none of them
  # installs dependencies, and each verify script imports only node builtins plus files
  # under its own challenges/<problem>/ directory (checked: no shared internal module),
  # so "did anything that job reads change?" is answerable from the diff alone.
  #
  # This job always runs and reports true for everything outside a pull request, so a
  # push to main still verifies all five and `needs:` never skips a dependent job for
  # the wrong reason. Costs about twenty seconds; saves up to five jobs.
  changes:
    name: changed paths
    runs-on: ubuntu-latest
    timeout-minutes: 5
    outputs:
      rls: ${{ steps.filter.outputs.rls }}
      eventbridge: ${{ steps.filter.outputs.eventbridge }}
      oidc: ${{ steps.filter.outputs.oidc }}
      signed_npm: ${{ steps.filter.outputs.signed_npm }}
      asm: ${{ steps.filter.outputs.asm }}
    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
        with:
          fetch-depth: 0
          persist-credentials: false

      - name: Work out which runtime jobs this change can affect
        id: filter
        env:
          EVENT_NAME: ${{ github.event_name }}
          BASE_REF: ${{ github.base_ref }}
        run: |
          set -euo pipefail

          emit() { printf '%s=%s\n' "$1" "$2" >> "$GITHUB_OUTPUT"; }

          # Outside a pull request there is no base to diff against, and a push to main
          # is the post-merge record for that commit: verify everything.
          if [ "$EVENT_NAME" != "pull_request" ]; then
            for key in rls eventbridge oidc signed_npm asm; do emit "$key" true; done
            exit 0
          fi

          git fetch --no-tags origin "+refs/heads/${BASE_REF}:refs/remotes/origin/${BASE_REF}"
          base="$(git merge-base "origin/${BASE_REF}" HEAD)"
          changed="$(git diff --name-only "$base" HEAD)"
          printf 'changed files:\n%s\n' "$changed"

          # A change to this workflow or to the script aliases can alter how any of them
          # runs, so either one turns all five back on.
          if printf '%s\n' "$changed" | grep -qE '^(\.github/workflows/ci\.yml|package\.json)$'; then
            for key in rls eventbridge oidc signed_npm asm; do emit "$key" true; done
            exit 0
          fi

          affected() {
            if printf '%s\n' "$changed" | grep -qE "$1"; then echo true; else echo false; fi
          }

          emit rls         "$(affected '^(challenges/rls-tenant-isolation/|scripts/verify-rls-tenant-isolation\.ts$)')"
          emit eventbridge "$(affected '^(challenges/eventbridge-delivery-discipline/|scripts/verify-eventbridge-delivery-discipline\.ts$)')"
          emit oidc        "$(affected '^(challenges/github-oidc-trust-boundary/|scripts/verify-github-oidc-trust-boundary\.ts$)')"
          emit signed_npm  "$(affected '^(challenges/signed-does-not-mean-safe/|scripts/verify-signed-does-not-mean-safe\.ts$)')"
          emit asm         "$(affected '^challenges/asm-worst-case-latency/')"

  rls-runtime:
    name: rls-tenant-isolation runtime
    needs: changes
    if: needs.changes.outputs.rls == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    env:
      CI: "true"
      npm_config_ignore_scripts: "true"
    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
        with:
          persist-credentials: false

      - name: Setup Bun
        uses: oven-sh/setup-bun@735343b667d3e6f658f44d0eca948eb6282f2b76
        with:
          bun-version: "1.3.11"

      - name: Verify clean RLS startup and scoring
        run: bun run rls:runtime

  eventbridge-runtime:
    name: eventbridge-delivery-discipline runtime
    needs: changes
    if: needs.changes.outputs.eventbridge == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    env:
      CI: "true"
      npm_config_ignore_scripts: "true"
    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
        with:
          persist-credentials: false

      - name: Setup Bun
        uses: oven-sh/setup-bun@735343b667d3e6f658f44d0eca948eb6282f2b76
        with:
          bun-version: "1.3.11"

      - name: Verify isolated local runtime and scoring
        run: bun run eventbridge:runtime

  github-oidc-runtime:
    name: github-oidc-trust-boundary runtime
    needs: changes
    if: needs.changes.outputs.oidc == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    env:
      CI: "true"
      npm_config_ignore_scripts: "true"
    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
        with:
          persist-credentials: false

      - name: Setup Bun
        uses: oven-sh/setup-bun@735343b667d3e6f658f44d0eca948eb6282f2b76
        with:
          bun-version: "1.3.11"

      - name: Verify isolated OIDC trust-policy runtime and cleanup
        run: bun run github-oidc:runtime

  signed-npm-runtime:
    name: signed-does-not-mean-safe runtime
    needs: changes
    if: needs.changes.outputs.signed_npm == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    env:
      CI: "true"
      npm_config_ignore_scripts: "true"
    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
        with:
          persist-credentials: false

      - name: Setup Bun
        uses: oven-sh/setup-bun@735343b667d3e6f658f44d0eca948eb6282f2b76
        with:
          bun-version: "1.3.11"

      - name: Verify isolated npm-provenance triage runtime and cleanup
        run: bun run signed-npm:runtime

  asm-worst-case-latency-runtime:
    name: asm-worst-case-latency native-amd64 Docker runtime
    needs: changes
    if: needs.changes.outputs.asm == 'true'
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    env:
      EXPECTED_SHA: ${{ github.event.pull_request.head.sha || github.sha }}
      IMAGE: tcc-asm-worst-case-latency:${{ github.run_id }}-${{ github.run_attempt }}
      COMPOSE_FILE: challenges/asm-worst-case-latency/local/docker-compose.yml
      COMPOSE_PROJECT_NAME: tcc-asm-latency-${{ github.run_id }}-${{ github.run_attempt }}
      FLAG_SEED: exact-head-http-${{ github.run_id }}-${{ github.run_attempt }}
    steps:
      # Pull-request workflows normally checkout refs/pull/<n>/merge. Timing proof
      # must name and execute the exact candidate head instead of a synthetic merge.
      - name: Checkout exact event head
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
        with:
          persist-credentials: false
          ref: ${{ github.event.pull_request.head.sha || github.sha }}

      - name: Require the exact native-amd64 host contract
        shell: bash
        run: |
          set -euo pipefail
          test "$(git rev-parse HEAD)" = "$EXPECTED_SHA"
          test "$(uname -s)" = Linux
          test "$(uname -m)" = x86_64
          case "$(docker version --format '{{.Server.Os}}/{{.Server.Arch}}')" in
            linux/amd64|linux/x86_64) ;;
            *) echo "Docker server is not native linux/amd64" >&2; exit 1 ;;
          esac
          uname -a
          lscpu

      - name: Build the exact-head author image
        run: |
          docker build --target author -t "$IMAGE" \
            challenges/asm-worst-case-latency/local
          test "$(docker image inspect "$IMAGE" --format '{{.Architecture}}')" = amd64

      - name: Require declared CPU features inside the container
        run: |
          docker run --rm --network none "$IMAGE" python3 -c '
          import json
          from fixtures.generate import host_report
          report = host_report()
          print(json.dumps(report, indent=2, sort_keys=True))
          required = ("rdtscp", "constant_tsc", "nonstop_tsc", "clflush")
          if report["architecture"] != "x86_64":
              raise SystemExit("container is not native x86_64")
          missing = [name for name in required if not report["cpuFlags"].get(name)]
          if missing:
              raise SystemExit(f"missing required CPU flags: {missing}")
          '
          docker run --rm --network none "$IMAGE" sh -c \
            'gcc --version | head -n1; objdump --version | head -n1; python3 --version; dpkg-query -W gcc binutils libc6-dev python3'

      - name: Prove the reference margin across seeds and repeated clean runs
        shell: bash
        run: |
          set -euo pipefail
          scores="$RUNNER_TEMP/asm-worst-case-latency-reference-scores.txt"
          : > "$scores"
          for seed in 7 42 1234 20260816; do
            for repetition in 1 2 3; do
              score="$(docker run --rm --network none "$IMAGE" python3 -c '
          from pathlib import Path
          import sys
          from tests.hidden.check_candidate import grade
          from verifier.server import THRESHOLDS
          expected = {"measure": 0.75, "dependency": 3.0, "miss": 20.0, "generalize": 20.0}
          if THRESHOLDS != expected:
              raise SystemExit(f"checkpoint contract drifted: {THRESHOLDS!r}")
          score = grade(
              Path("reference/candidate.S").read_text(encoding="utf-8"),
              int(sys.argv[1]),
          )["normalizedScore"]
          print(f"{score:.8f}")
          ' "$seed")"
              printf 'seed=%s repetition=%s reference=%.2fx required-margin=40.00x\n' \
                "$seed" "$repetition" "$score"
              printf '%s\n' "$score" >> "$scores"
            done
          done
          python3 - "$scores" <<'PY'
          import sys
          from pathlib import Path
          scores = [float(line) for line in Path(sys.argv[1]).read_text().splitlines()]
          if len(scores) != 12 or min(scores) < 40.0:
              raise SystemExit(f"reference lacks a stable 2x margin over the 20x checkpoint: {scores}")
          print(f"reference summary: runs={len(scores)} min={min(scores):.2f}x max={max(scores):.2f}x")
          PY

      - name: Reject boundary mutations and the former loop-count score bypass
        run: docker run --rm --network none -e FLAG_SEED=exact-head-ci "$IMAGE" python3 mutation.py

      - name: Exercise the real Compose Workbench and verifier HTTP path
        shell: bash
        run: |
          set -euo pipefail
          docker compose -p "$COMPOSE_PROJECT_NAME" -f "$COMPOSE_FILE" up --build --detach
          for service in workbench verifier; do
            container_id="$(docker compose -p "$COMPOSE_PROJECT_NAME" -f "$COMPOSE_FILE" ps -q "$service")"
            test -n "$container_id"
            test "$(docker inspect "$container_id" --format '{{.HostConfig.NanoCpus}}')" = 1000000000
            docker exec "$container_id" python3 -c '
          from pathlib import Path
          mounts = (line.split() for line in Path("/proc/mounts").read_text().splitlines())
          options = next((set(fields[3].split(",")) for fields in mounts if fields[1] == "/tmp"), None)
          if options is None:
              raise SystemExit("/tmp is not a dedicated runtime mount")
          if "noexec" in options or not {"rw", "nosuid", "nodev"}.issubset(options):
              raise SystemExit(f"unsafe or non-executable /tmp mount: {sorted(options)}")
          '
          done
          python3 scripts/asm-worst-case-latency-http-smoke.py

      - name: Remove the exact runtime resources
        if: always()
        run: |
          docker compose -p "$COMPOSE_PROJECT_NAME" -f "$COMPOSE_FILE" \
            down --volumes --remove-orphans --rmi local || true
          docker image rm -f "$IMAGE" >/dev/null 2>&1 || true

  validate:
    # One stable name in front of the matrix. `if: always()` is what makes this
    # run when a shard fails — and it is also what makes `needs:` stop gating, so
    # the result has to be asserted explicitly rather than implied.
    needs: [changes, suite, solvability, checks, rls-runtime, eventbridge-runtime, github-oidc-runtime, signed-npm-runtime, asm-worst-case-latency-runtime]
    if: always()
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Require every job to have succeeded
        env:
          CHANGES: ${{ needs.changes.result }}
          SUITE: ${{ needs.suite.result }}
          SOLVABILITY: ${{ needs.solvability.result }}
          CHECKS: ${{ needs.checks.result }}
          RLS: ${{ needs.rls-runtime.result }}
          EVENTBRIDGE: ${{ needs.eventbridge-runtime.result }}
          OIDC: ${{ needs.github-oidc-runtime.result }}
          SIGNED_NPM: ${{ needs.signed-npm-runtime.result }}
          ASM: ${{ needs.asm-worst-case-latency-runtime.result }}
        run: |
          set -uo pipefail
          echo "changes=$CHANGES suite=$SUITE solvability=$SOLVABILITY checks=$CHECKS rls=$RLS eventbridge=$EVENTBRIDGE oidc=$OIDC signed-npm=$SIGNED_NPM asm=$ASM"

          failed=0
          # These always run, so only success passes.
          for pair in "changes:$CHANGES" "suite:$SUITE" "solvability:$SOLVABILITY" "checks:$CHECKS"; do
            name="${pair%%:*}"; result="${pair#*:}"
            if [ "$result" != "success" ]; then
              echo "::error::$name did not succeed (result: $result)"
              failed=1
            fi
          done

          # The five per-problem runtime jobs are gated on `changes`, so `skipped` here
          # means this pull request cannot affect that problem — a pass, not a gap. It
          # can only mean that: `changes` is asserted to have succeeded above, so a
          # skip caused by a failure further up would already have failed this job.
          for pair in "rls-runtime:$RLS" "eventbridge-runtime:$EVENTBRIDGE" "github-oidc-runtime:$OIDC" "signed-npm-runtime:$SIGNED_NPM" "asm-worst-case-latency-runtime:$ASM"; do
            name="${pair%%:*}"; result="${pair#*:}"
            case "$result" in
              success) ;;
              skipped) echo "$name skipped: nothing this pull request changed can affect it" ;;
              *) echo "::error::$name did not succeed (result: $result)"; failed=1 ;;
            esac
          done

          exit "$failed"
