# AWS Recovery Battle validation

## Local evidence — 2026-09-22

Compiled-controller tests cover four serial faults and mandatory explanation, healthy/unhealthy HTTP and role observations, a fresh Session Manager session/check command, exact mutation/recovery targets, partial apply failure, deadline/retry lease, DynamoDB conditional writes, service-error propagation, stale participant submissions, private operator authority and cleanup ordering. AWS clients are injected observations: these are request/state contracts, not real fault execution.

Before the cooperation update, the family passed 68 Python checks (46 shared, 5 Preparation, 7 optional consumer packaging, 10 Battle). Catalog validation accepted all 126 metadata files. The existing platform Gate suites passed 45 tests, and this event-gate.json was parsed by the real schema: zero score remains locked, 100-point completion opens Battle for that team, and an unfinished second team stays locked. Generated-template parity and CFN lint check syntax and embedded source. W2010 remains documented for capability URL outputs; it is not suppressed or treated as proof of secrecy. The existing platform uptime scorer and progression Gate supply official scoring/unlock behavior; no problem-specific platform code is added.

## Participant evidence

`preview.py` renders the production UI and participant handler with explicit loopback-only fixtures. It never assumes AWS credentials, provisions resources or awards official points. Chromium exercised all four rounds through visible controls, rejected an unrecovered fixture and an incorrect explanation without advancing, and completed the final review. English switching retained completion; the 390×844 completion screenshot was inspected, and browser error collection was empty. The current Lambda consumer’s initial desktop screen was also inspected after moving the AWS action above the explanation.

## Cooperation and self-registration update — 2026-09-22

The Battle now passes 20 Python tests, including private card authorization, all-four confirmation, rotating operator/explainer authority, stale submissions, a round change between authorization and verification, emergency restore after interrupted apply, a finale with different ports/sources, and operator assistance. Six Bun tests cover bilateral exchange, no one-sided points, cancellation, replay, event and viewer isolation, three-partner caps, late arrivals and the UTF-8 state budget for 99 teams with 26-character IDs and 120-character names. The catalog's `make install && make agent-gate` accepts all 126 metadata files. The new Portal component and coordination plugin type-check against the actual platform SDK.

Four separate Chromium sessions exercised the production card UI and compiled participant handler using loopback AWS fixtures: a wrong combined clue was rejected, all four cards confirmed, only the assigned operator could check, an unrecovered state was rejected, and only the assigned explainer could finish the first round. The next round rotated roles. This run verifies the added interaction path; the earlier four-round recovery checks above predate the card update. No check proves that four different humans hold the cards.

The actual exchange component and reducer ran in a local HTTP fixture with separate authenticated team contexts. One submission left scores at zero; reciprocal submission awarded 20 to both; refresh retained completion. Japanese desktop and English 390×844 views were visually inspected; browser errors were empty. Official score persistence uses the existing additive coordination-scoring path and is not claimed from this fixture. The platform's separate self-registration tests and browser run cover invitation, allocation, preparation, resume and login.

The shared runtime now passes 49 Python tests. Cleanup disables EC2 termination protection only on the lab-tagged instance attached to its fixed interface, and removes DynamoDB deletion protection only on the assigned table before deletion. Permission failures remain failed cleanup. Generated IAM limits the EC2 action to that attribute and grants the DynamoDB action only to the private lifecycle role. The problem family totals 81 Python checks (49 shared, 5 Preparation, 7 optional packaging, 20 Battle).

## Optional event rehearsal — not run

Use [OPERATOR.md](OPERATOR.md) and [redteam/README.md](redteam/README.md): two teams, all real faults/recoveries, new Session Manager connection, no automatic points before registration, official score/header/rank consistency, eight-second healthy-probe budget, role isolation, propagation delays, automatic recovery, mid-fault deletion and residual resource/billing checks. Hosting time estimates are not measured participant results.
