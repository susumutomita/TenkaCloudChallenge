# AWS Recovery Battle validation

## Local evidence — 2026-09-22

Compiled-controller tests cover four serial faults and mandatory explanation, healthy/unhealthy HTTP and role observations, a fresh Session Manager session/check command, exact mutation/recovery targets, partial apply failure, deadline/retry lease, DynamoDB conditional writes, service-error propagation, stale participant submissions, private operator authority and cleanup ordering. AWS clients are injected observations: these are request/state contracts, not real fault execution.

The current family passed 68 Python checks (46 shared, 5 Preparation, 7 optional consumer packaging, 10 Battle). Catalog validation accepted all 126 metadata files. The existing platform Gate suites passed 45 tests, and this event-gate.json was parsed by the real schema: zero score remains locked, 100-point completion opens Battle for that team, and an unfinished second team stays locked. Generated-template parity and CFN lint check syntax and embedded source. W2010 remains documented for capability URL outputs; it is not suppressed or treated as proof of secrecy. The existing platform uptime scorer and progression Gate supply official scoring/unlock behavior; no problem-specific platform code is added.

## Participant evidence

`preview.py` renders the production UI and participant handler with explicit loopback-only fixtures. It never assumes AWS credentials, provisions resources or awards official points. Chromium exercised all four rounds through visible controls, rejected an unrecovered fixture and an incorrect explanation without advancing, and completed the final review. English switching retained completion; the 390×844 completion screenshot was inspected, and browser error collection was empty. The current Lambda consumer’s initial desktop screen was also inspected after moving the AWS action above the explanation.

## Optional event rehearsal — not run

Use [OPERATOR.md](OPERATOR.md) and [redteam/README.md](redteam/README.md): two teams, all real faults/recoveries, new Session Manager connection, no automatic points before registration, official score/header/rank consistency, eight-second healthy-probe budget, role isolation, propagation delays, automatic recovery, mid-fault deletion and residual resource/billing checks. Hosting time estimates are not measured participant results.
